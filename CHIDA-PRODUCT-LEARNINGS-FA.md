# دفتر یادگیری محصول CHIDA

این فایل دفتر زندهٔ تجربه‌های ساخت پروتوتایپ، شکاف‌ها و ابهام‌های `CHIDA-Product-Definition-FA.md` و تصمیم‌هایی است که در جریان طراحی می‌گیریم. هدف آن فراهم‌کردن ورودی دقیق برای تکمیل بعدی سند مادر است.

## جایگاه و قاعدهٔ استفاده

- سند مادر تنها مرجع تعریف محصول است؛ این دفتر جای آن را نمی‌گیرد.
- `BUILDER-FEATURE-BACKLOG-FA.md` فقط ترتیب اجرایی پیشنهادی قابلیت‌ها را تعیین می‌کند.
- تصمیم صریح تازهٔ کاربر برای کار جاری لازم‌الاجراست. اگر با سند مادر تعارض دارد، تعارض در اینجا باز می‌ماند تا در یک بازنگری جداگانه وارد سند مادر شود.
- تصمیمی که صرفاً برای تکمیل پروتوتایپ گرفته شده است تا زمان تأیید کاربر «تصمیم کاری پروتوتایپ» می‌ماند و حقیقت قطعی محصول محسوب نمی‌شود.
- سابقهٔ یک تصمیم بی‌صدا حذف یا بازنویسی نمی‌شود؛ اصلاح بعدی با وضعیت «جایگزین‌شده» و ارجاع به تصمیم تازه ثبت می‌شود.
- تغییر این دفتر به‌معنای commit، push، deploy یا تغییر خودکار سند مادر نیست.

## وضعیت‌ها

| وضعیت | معنا |
| --- | --- |
| تأییدشده | کاربر نتیجه یا تصمیم را تأیید کرده است. |
| در انتظار تأیید | در پروتوتایپ اجرا شده، اما هنوز بازخورد نهایی کاربر ثبت نشده است. |
| باز | شکاف یا سؤال شناخته شده است و هنوز تصمیم ندارد. |
| جایگزین‌شده | تصمیم تازه‌ای جای این تصمیم را گرفته و سابقه برای ردیابی حفظ شده است. |

## روال ثبت در هر تسک

۱. پیش از ساخت، بخش مرتبط سند مادر، بک‌لاگ و این دفتر خوانده شود.
۲. هنگام ساخت، تجربهٔ واقعی و شواهد مشاهده‌شده از حدس محصول جدا ثبت شود.
۳. پیش از تحویل، شکاف سند مادر، تصمیم کاری و پیشنهاد اصلاح بعدی سند مادر نوشته شود.
۴. پس از بازخورد کاربر، وضعیت همان تسک و تصمیم‌هایش به‌روزرسانی شود.
۵. انتقال موارد این دفتر به سند مادر فقط در یک بازنگری صریح و یکپارچه انجام شود.

---

## موارد سراسری

### C-001 — نقش حساب

- **وضعیت:** تأییدشده
- **نوع:** تصمیم صریح کاربر + تعارض سند مادر
- **شاهد سند مادر:** راه‌اندازی «سازنده، تأمین‌کننده یا هر دو» را مجاز می‌داند و در مدل مالکیت نیز یک فرد می‌تواند هر دو نقش را داشته باشد.
- **تصمیم جاری:** هر حساب دقیقاً یک نقش تغییرناپذیر دارد؛ کاربر یا سازنده است یا تأمین‌کننده و هرگز هر دو نیست.
- **پیشنهاد برای سند مادر:** بخش راه‌اندازی، مدل نقش و مالکیت، موجودیت‌های داده و سناریوهای چندنقشی با این قاعده یکپارچه شوند.

### C-002 — دامنهٔ نمایش و تم پروتوتایپ

- **وضعیت:** تأییدشده
- **نوع:** تصمیم صریح کاربر + جزئیات تعریف‌نشده در سند مادر
- **تجربهٔ ساخت:** وقتی viewport مرورگر به اندازهٔ دسکتاپ برگشت، اپ صحیح موبایل شبیه ستونی افتاده در وسط صفحه دیده شد. بررسی واقعی در `390 × 844` نشان داد نمای موبایل تمام‌صفحه، بدون قاب، بدون overflow و بدون cursor مصنوعی درست است.
- **تصمیم جاری:** موبایل و دسکتاپ دو طراحی مستقل‌اند؛ دامنهٔ فعلی فقط موبایل `390 × 844` و فقط Dark است. ستون موبایل وسط viewport دسکتاپ طراحی دسکتاپ نیست و انتخاب دستگاه جزو UI محصول نیست.
- **پیشنهاد برای سند مادر:** حالت‌های نمایش، معیار پذیرش موبایل و دسکتاپ، و دامنهٔ موقت Dark-only به‌صورت صریح تعریف شوند.

### C-003 — تمایز مشاهده، ذخیره و انتشار

- **وضعیت:** تأییدشده
- **نوع:** تجربهٔ اجرایی
- **تجربهٔ ساخت:** مشاهدهٔ یک حالت در مرورگر اثبات ذخیره‌شدن داده، commit، push یا deploy نیست؛ هرکدام باید جدا گزارش و راستی‌آزمایی شوند.
- **تصمیم جاری:** در تحویل هر مرحله، وضعیت مشاهده‌شدهٔ رابط، ماندگاری محلی، وضعیت Git و انتشار جداگانه اعلام می‌شوند.
- **پیشنهاد برای سند مادر:** این مورد الزام محصولی نیست؛ در بازنگری بعدی برای راهنمای توسعه و معیار پذیرش نگه‌داری شود.

### C-004 — ریتم توسعهٔ پروتوتایپ

- **وضعیت:** تأییدشده
- **نوع:** تصمیم صریح کاربر
- **تصمیم جاری:** قابلیت‌های سازنده طبق اولویت به تسک‌های کوچک تقسیم می‌شوند؛ هر بار فقط یک تسک ساخته و اصلاح می‌شود و شروع تسک بعدی به تأیید کاربر وابسته است.
- **پیشنهاد برای سند مادر:** وارد سند محصول نشود؛ به‌عنوان روش توسعه و اعتبارسنجی پروتوتایپ حفظ شود.

### C-005 — شناسنامه و مشخصات فیزیکی پروژه

- **وضعیت:** تأییدشده
- **نوع:** شکاف سند مادر + بازخورد کاربر
- **شاهد سند مادر:** بخش «واحد اصلی: پروژه» فقط از «مشخصات پایه و موقعیت در سطح مجاز» و «مرحله و نقاط عطف» نام می‌برد و شِمای مشخصات فیزیکی پروژه را تعریف نمی‌کند.
- **شکاف:** تعداد طبقات روی زمین، طبقات منفی، تعداد واحدها، مساحت زمین و زیربنا در سند مادر به‌صورت صریح نیامده‌اند. کاربری، نوع سازه، وضعیت مجوز و سایر مشخصات احتمالی نیز هنوز اولویت و سطح الزام مشخصی ندارند.
- **برداشت فعلی:** نام پروژه، محدودهٔ تهران و مرحلهٔ ساخت حداقل دادهٔ ورود هستند، نه کل شناسنامهٔ پروژه. کامل‌دانستن فضای پروژه با همین سه فیلد نادرست است.
- **تصمیم کاری اجراشده:** فرم ورود با همان سه فیلد الزامی کوتاه می‌ماند؛ داخل فضای پروژه نوع کاربری، مساحت زمین، زیربنای کل، تعداد طبقات روی زمین، تعداد طبقات منفی و تعداد واحدها به‌صورت تدریجی و اختیاری تکمیل می‌شوند.
- **مرز تصمیم:** افزودن فیلد دیگری مانند نوع سازه، تعداد بلوک یا وضعیت مجوز باید بر اساس استفادهٔ واقعی آن در خرید، اسناد، گزارش یا مدیریت پروژه توجیه شود تا شناسنامه به فرم بی‌هدف تبدیل نشود.
- **تجربهٔ ساخت رابط:** منوی بومی `select` در مرورگر به‌صورت سطح سفید خارج از تم Dark باز شد. مرحلهٔ ساخت و نوع کاربری به منوی داخل‌اپ Dark تبدیل شدند. کنترل بسته داخل `MobileScroll` باید drag عمودی را به صفحه واگذار کند و فقط با tap باز شود؛ مالک‌کردن کامل gesture، اسکرول را از روی همان کنترل از کار می‌اندازد.
- **تجربهٔ داده:** مقادیر اختیاری فیزیکی بدون اعتبارسنجی می‌توانستند متن آزاد، عدد منفی یا تعداد طبقهٔ اعشاری را به‌عنوان واقعیت پروژه نمایش دهند. مساحت‌ها اکنون عدد صفر یا بیشتر و طبقات/واحدها عدد صحیح صفر یا بیشتر می‌پذیرند؛ ارقام فارسی و عربی هنگام ذخیره به مقدار عددی پایدار نرمال می‌شوند.
- **پیشنهاد برای سند مادر:** یک شِمای نسخه‌دار برای `ProjectProfile` تعریف شود که فیلدهای الزامی راه‌اندازی را از مشخصات تکمیلی تدریجی، واحد اندازه‌گیری، مقدار نامعلوم و منشأ داده جدا کند.

### C-006 — رده‌بندی مراحل ساخت

- **وضعیت:** تأییدشده برای فهرست؛ نگاشت دادهٔ قدیمی در انتظار تأیید
- **نوع:** تصمیم صریح کاربر + شکاف سند مادر + تصمیم سازگاری پروتوتایپ
- **شاهد سند مادر:** سند مادر در تعریف پروژه، حافظه، فایل، گزارش و مدیریت پروژه از «مرحله» و «نقطهٔ عطف» نام می‌برد، اما هیچ taxonomy یا ترتیب اجرایی برای مراحل ساخت تعیین نمی‌کند.
- **[تصمیم صریح کاربر، تأییدشده]** ترتیب و املای معتبر مراحل عبارت است از:
  1. طراحی و اخذ مجوز
  2. تخریب و گودبرداری
  3. فونداسیون
  4. اسکلت بندی
  5. دیوارچینی و سفت کاری
  6. گچ و خاک و تاسیسات
  7. نازک کاری و نما
  8. ظریف کاری و نصبیات
  9. پایان کار
- **تجربهٔ داده:** تعویض مستقیم فهرست، پروژه‌های ذخیره‌شده با شش عنوان قبلی را از نظر اعتبارسنجی «ناشناخته» و نیازمند تکمیل می‌کرد. یک migration idempotent هنگام خواندن داده اضافه شد تا دادهٔ محلی قبلی از بین نرود.
- **[تصمیم کاری پروتوتایپ، در انتظار تأیید]** نگاشت سازگاری چنین است: «طراحی و مجوز» به «طراحی و اخذ مجوز»، «گودبرداری» به «تخریب و گودبرداری»، «اسکلت» به «اسکلت بندی»، «سفت‌کاری» به «دیوارچینی و سفت کاری»، «نازک‌کاری» به «نازک کاری و نما» و «تکمیل و تحویل» به «پایان کار». سه مرحلهٔ تازهٔ میانی برای دادهٔ قدیمی حدس زده نمی‌شوند.
- **مرز تصمیم:** این نگاشت فقط برای metadata محلی همین پروتوتایپ است و نباید بدون بازبینی دامنه، به migration دادهٔ واقعی یا تحلیل پیشرفت پروژه تعمیم داده شود.
- **پیشنهاد برای سند مادر:** taxonomy مراحل با شناسهٔ پایدار، برچسب فارسی نسخه‌دار، قواعد عبور/هم‌پوشانی و نسبت آن با milestone تعریف شود؛ به‌ویژه تفاوت «تکمیل و تحویل» با «پایان کار» پیش از backend واقعی تعیین تکلیف شود.

### C-007 — ترتیب قطعی فازهای ادامه و معیار پایان سمت سازنده

- **وضعیت:** تأییدشده
- **نوع:** تصمیم صریح کاربر + تعریف اجرایی پروتوتایپ
- **تجربهٔ ساخت:** ادامهٔ طولانی در یک گفت‌وگو، مرز میان تسک جاری، قابلیت‌های باقی‌مانده و وضعیت انتشار را سنگین و مستعد برداشت نادرست کرد. T6-B2، اصلاحات تجربهٔ پروژه/استعلام/چت، T6-C و T6-D پس از مشاهدهٔ نسخهٔ لوکال تأیید و با release `bd766bc` یکپارچه منتشر شدند؛ T7-A پس از مشاهدهٔ لوکال مناسب ارزیابی و با release `bf59df1` روی هر سه مقصد منتشر شد. کاربر نسخهٔ زنده را دید و به‌جای مجوز T7-B، اصلاحات بازخوردی C-010 و سپس انتقال به گفت‌وگوی تازه را خواست.
- **[تصمیم ترتیب، تأییدشده توسط کاربر]** ترتیب ادامه چنین است: ۱) تکمیل و تأیید سمت سازنده؛ ۲) اتصال مدل محلی برای ارزیابی روی همان تجربه؛ ۳) ساخت مسیر و فضای تأمین‌کننده. هر فاز و هر تسک همچنان فقط پس از تأیید کاربر به مورد بعدی می‌رود.
- **[تعریف کاری پایان فاز، در انتظار تأیید]** پیشنهاد اجرایی هنداف این است که «سمت سازنده تمام است» یعنی تجربهٔ موبایل Dark، مدل داده، جداسازی پروژه، نسخه/تاریخچه، حالت‌های شکست و قراردادهای قابل‌آزمون آن در همین پروتوتایپ کامل شده‌اند. این عبارت به معنی backend، شبکهٔ واقعی، ارسال واقعی، وب واقعی، sync یا آمادگی production نیست.
- **[تصمیم کاری صداقت داده، در انتظار تأیید]** پیشنهاد شده است تا ساخته‌شدن سمت تأمین‌کننده، مسیر ماندگار فقط از ثبت دستی سازنده استفاده کند. fixtureهای `Mock` باید تجربه‌ای جدا، فقط‌خواندنی و درون‌حافظه‌ای باشند که در ورودی، کارت، جزئیات و نام دسترس‌پذیر آشکارند و هیچ داده‌ای در پروژه/چت/storage نمی‌نویسند؛ هیچ فروشنده، قیمت، پاسخ، ارسال یا رضایت دوطرفهٔ واقعی ادعا نمی‌شود.
- **نقطهٔ ادامه در زمان ثبت این تصمیم — تاریخی:** C-010 با commit کد `d750f88fcfbf72f30ff8b58e4331b3d081bf9f4f` روی GitHub `main`، Cloudflare Pages و ChatGPT Sites نسخهٔ ۸ same-source منتشر و راستی‌آزمایی شد. commit مستندیِ receipt، prototype tree را تغییر نداد و baseline نهایی بعدی به `57bbf6560e40a7b2ef9bb0a369af253c1b063d82` رسید. این نقطه با دستور بعدی ماهیار و شروع لوکال T7-B1 منسوخ شده است؛ وضعیت جاری در بخش T7-B1 و هنداف ثبت شده و مدل محلی/تأمین‌کننده همچنان شروع نشده‌اند.
- **پیشنهاد برای سند مادر:** معیار بلوغ پروتوتایپ، پایلوت و production از هم جدا شود؛ برای جریان دوطرفه نیز قرارداد نمایش صادقانهٔ دادهٔ دستی/نمونه پیش از اتصال شبکه تعریف شود. ترتیب توسعه تصمیم اجرایی است و لازم نیست به نقشهٔ محصول تبدیل شود.

### C-008 — اتصال آزمایشی Gemma محلی پس از پایان فاز سازنده

- **وضعیت:** تأییدشده برای فاز آینده؛ هنوز پیاده‌سازی نشده است
- **نوع:** تصمیم صریح کاربر + استثنای توسعه/ارزیابی نسبت به محیط مرجع سند مادر
- **شاهد سند مادر:** مدل‌ها باید پشت یک درگاه قابل‌تعویض باشند و محیط اجرای مرجع، فضای ابری مدیریت‌شده است؛ رایانهٔ کاربر محیط production چیدا محسوب نمی‌شود.
- **تجربهٔ فنی مشاهده‌شده:** روی همین مک Ollama فقط روی loopback `127.0.0.1:11434` فعال است و مدل `gemma4:26b-mlx` با اندازهٔ 26.2B و quantization برابر `nvfp4` در دسترس است. preflight مبدأ `http://127.0.0.1:5173` پذیرفته شد و probe کوچک فارسی با `think:false` پاسخ صحیح داد؛ بارگذاری سرد حدود ۷ ثانیه و کل درخواست حدود ۸.۱ ثانیه بود. هیچ کد یا تنظیم پروتوتایپ برای این probe تغییر نکرد.
- **[تصمیم دامنه، تأییدشده توسط کاربر]** اتصال فقط پس از تأیید صریح پایان سازنده، به‌صورت opt-in و local-development-only و پشت قرارداد provider-neutral ساخته می‌شود. برش نخست فقط متن صریح همان پیام را می‌فرستد و history، حافظه، فایل، وب، ابزار، ذخیره، تغییر وضعیت و اقدام بیرونی ندارد.
- **[تصمیم صداقت و شکست، تأییدشده توسط کاربر]** UI باید «مدل محلی آزمایشی» و «بدون منبع/وب» را بیرون از متن مدل نشان دهد؛ timeout، cancel، پاسخ خراب و unavailable باید fail-closed و دارای retry/fallback محصولی باشند. مدل دوم یا cloud fallback بدون سیاست داده و رضایت صریح فعال نمی‌شود.
- **[تصمیم استقرار، تأییدشده توسط کاربر]** endpoint فقط loopback می‌ماند و در Cloudflare Pages و ChatGPT Sites خاموش است. تست‌های عادی با adapter جعلی و قطعی اجرا می‌شوند و smoke/eval واقعی Ollama اختیاری و خارج از CI است.
- **مرز باز:** تزریق opt-in حافظه، فایل و SourceRecord، پاسخ منبع‌دار، استخراج ساختاریافتهٔ خرید و tool calling برش‌های بعدی‌اند و هرکدام قرارداد و تأیید مستقل می‌خواهند. مدل و پاسخ آن خودکار حافظه نمی‌شوند.
- **پیشنهاد برای سند مادر:** «adapter محلی برای توسعه و ارزیابی» از محیط production جدا تعریف شود و برای محل پردازش، نمایش provider، سیاست context، retention، fallback، بودجه و ارزیابی قرارداد صریح افزوده شود؛ اگر مدل محلی روزی قابلیت محصول شد، بازنگری مستقل سند مادر لازم است.

### C-009 — پروژهٔ جدید در دسترس، استعلام با افشای تدریجی و زمینهٔ چت پروژه

- **وضعیت:** تصمیم صریح کاربر؛ پیاده‌سازی، بازآزمایی و QA محلی کامل؛ در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸ توسط کاربر تأیید شد
- **نوع:** اصلاح تجربهٔ سازنده پس از مشاهدهٔ T6-C
- **شاهد سند مادر:** «فضاها» مقصد اصلی پروژه‌هاست، زمینهٔ فعال باید با اقدام ساده عوض شود و اصل افشای تدریجی می‌گوید پاسخ/پیش‌نمایش ساده ابتدا و تنظیمات پیشرفته فقط در صورت درخواست دیده شوند.
- **[تصمیم دسترسی پروژه، تأییدشده توسط کاربر]** ساخت پروژهٔ تازه باید از بالای خانه، نوار پروژه در پایین خانه و منوی راست در دسترس باشد. هر سه یک فرم واحد و کوتاهِ نام، محدودهٔ تهران و مرحلهٔ ساخت را باز می‌کنند و پروژهٔ موفق بلافاصله زمینهٔ فعال می‌شود.
- **[تصمیم تجربهٔ استعلام، تأییدشده توسط کاربر]** درخواست محصول/خدمت پیش‌فرض در حالت «ساده» باز می‌شود و فقط فیلدهای مهم روزمره را نشان می‌دهد؛ انتخاب «پیشرفته» همان draft و همان schema را با جزئیات تکمیلی باز می‌کند. تغییر حالت نباید مقدارهای پنهان را حذف، نسخهٔ جدا بسازد یا قرارداد unknown/clarification را دور بزند.
- **تجربهٔ مشاهده‌شده:** بازکردن پروژهٔ تازه در حالی که Composer و پیام‌های پروژهٔ قبلی در state مشترک بودند، زمینهٔ فعال را عوض می‌کرد اما متن و گفت‌وگوی قبلی را زیر نام پروژهٔ جدید نشان می‌داد. draft و پیام‌های نشست اکنون با `projectId` جدا می‌شوند؛ پروژهٔ تازه خالی است و بازگشت به پروژهٔ قبلی state همان پروژه را برمی‌گرداند.
- **[تصمیم زمینهٔ چت، تأییدشده توسط کاربر]** جداسازی فعلی فقط داخل نشست React است و persistence، thread واقعی، sync یا حافظهٔ مدل ادعا نمی‌کند. این مرز جلوی نشت دیداری میان پروژه‌ها را می‌گیرد و قرارداد ذخیرهٔ چت باید در تسک مستقل آینده تعریف شود.
- **تجربهٔ شکست:** ساخت پروژه باید write-before-state باشد؛ اگر نوشتن فهرست پروژه‌ها یا زمینهٔ فعال شکست خورد، پروژهٔ جدید و موفقیت در UI نمایش داده نمی‌شوند، sheet باز و پروژهٔ قبلی فعال می‌ماند.
- **تجربهٔ فرم:** mode ساده/پیشرفته presentation state است، نه فیلد دامنه. بازگشت به ساده مقادیر پیشرفته را از draft حذف نمی‌کند؛ ذخیرهٔ فیلد خالی همچنان مقدار canonical unknown و clarification قطعی می‌سازد.
- **مرز تصمیم:** این دو حالت فقط density و افشای رابط‌اند؛ نوع محصول/خدمت، version، Approval، ReviewRevision، payload و حریم T6-B2/T6-C مستقل از حالت UI باقی می‌مانند.
- **پیشنهاد برای سند مادر:** قرارداد تجربهٔ ساخت فضای بعدی، نقاط دسترسی در موبایل و progressive disclosure اشیای پیچیده با حفظ یک مدل دادهٔ واحد به معیار پذیرش تجربهٔ سازنده اضافه شود.
- **شواهد خط مبنای منتشرشده:** regressionهای سه ورودی، شکست write دوم و rollback، ترمیم زمینهٔ فعال، جداسازی draft/پیام، نام ۱۰۰کاراکتری در dock، حفظ مقدار پیشرفته و unknown/clarification پاس شدند. برای release `bd766bc` اپ ۹۳/۹۳، runtime ۱۰۱/۱۰۱ و Sites ۴/۴ پاس بود؛ QA `390 × 844` نیز overflow صفر و console تازهٔ بدون warning/error را نشان داد.
- **بازخورد پس از مشاهده:** کاربر مجموعهٔ اصلاحات را تأیید کرد و صریحاً خواست همان نسخه همه‌جا منتشر و سپس تسک بعدی آغاز شود؛ این مجوز با انتشار release `bd766bc` مصرف شد و شامل commit/push/deploy تغییرهای بعدی T7-A نیست.
- **[وضعیت بعدی]** تصمیم «سه ورودی ساخت پروژه» در ۱۴۰۵/۰۶/۰۷ — ۲۰۲۶/۰۸/۲۹ با بازخورد صریح تازهٔ کاربر منسوخ شد؛ این سطر فقط تاریخچه را نگه می‌دارد و قرارداد جاری در C-010 ثبت شده است.

### C-010 — سخت‌سازی بازخوردی خانه، کار، Brief و تنظیمات سازنده

- **وضعیت:** تصمیم صریح کاربر؛ پیاده‌سازی، QA و انتشار هم‌نسخه کامل
- **نوع:** اصلاح تجربه و قراردادهای قابل‌آزمون سازنده پس از مشاهدهٔ نسخهٔ منتشرشدهٔ T7-A
- **شاهد سند مادر:** Work Engine باید کارها را ایجاد و ویرایش کند و تاریخ شروع/پایان را نگه دارد؛ تنظیمات باید حریم، مدل و داده را صادقانه بازتاب دهد؛ «فضاها» مقصد پروژه‌هاست. سند مادر الزام جاری برای نمایش سه میان‌بر ساخت پروژه، منبع واقعی usage، جزئیات چرخهٔ ویرایش کار یا رفتار بسته‌شدن Brief پس از ذخیره ندارد.
- **تجربهٔ مشاهده‌شده:** کار ذخیره‌شده قابل‌ویرایش نبود و موعد آن از روی کارت دیده نمی‌شد؛ ردیف‌های افقی اقدام سریع و فیلتر کار در موبایل گیر اسکرول می‌ساختند؛ Brief پس از ذخیره باز می‌ماند؛ تنظیمات چند ردیف ظاهراً فعال اما بی‌اثر داشت و usage، مرز داده، مدل و نسخه را روشن نمی‌کرد؛ آواتار فقط fallback حروف بود.
- **[تصمیم دسترسی پروژه، تأییدشده توسط کاربر]** تصمیم سه‌ورودی C-009 لغو شد. ساخت پروژه فقط از مجموعهٔ «پروژه‌ها» باز می‌شود؛ میان‌بر بالای خانه، دکمهٔ پایین چت و اقدام مستقیم داخل منو حذف می‌شوند، اما ردیف ورود به خود مجموعهٔ پروژه‌ها در منو باقی می‌ماند.
- **[تصمیم کار، تأییدشده توسط کاربر]** هر کار یک `dueDate` اختیاری و مستقل از `completedAt` دارد. موعد روی کارت باز دیده می‌شود؛ ویرایش همان رکورد را با `version + 1` و رویداد `updated` نگه می‌دارد، status و زمان تکمیل را بی‌دلیل عوض نمی‌کند، و ویرایش بدون تغییر version یا storage را نمی‌چرخاند. دادهٔ قدیمیِ بدون موعد به `null` مهاجرت می‌کند و شکست نوشتن باید editor را باز، state و bytes قبلی را دست‌نخورده نگه دارد.
- **[تصمیم اسکرول، تأییدشده توسط کاربر]** اقدام‌های سریع و فیلترهای کار در دامنهٔ اپ به grid چندردیفه و بدون overflow تبدیل شدند؛ Carousel محافظت‌شدهٔ runtime دست‌نخورده ماند. معیار پذیرش در `390 × 844`، صفر بودن overflow افقی ریشه، پنل و این دو گروه است.
- **[تصمیم Brief، تأییدشده توسط کاربر]** ذخیره باید write-before-state باشد؛ موفقیت، صفحه را می‌بندد و شکست، همان صفحه را با پیام صریح و دادهٔ قبلی باز نگه می‌دارد. این رفتار هنوز زمان‌بندی محلی همان مرورگر است و backend یا اعلان واقعی ادعا نمی‌کند.
- **[تصمیم تنظیمات و تصویر، تأییدشده توسط کاربر]** تنظیمات به‌جای کنترل‌های بی‌اثر، وضعیت‌های قابل‌راستی‌آزماییِ نقش ثابت، داده و رکورد محلی، حریم، مدل، Brief، Dark-only و نسخه را نشان می‌دهد. چون منبع usage/token/quota/billing وصل نیست، عدد ساختگی نمایش داده نمی‌شود و همان قطع‌بودن منبع صریح است. تصویر پروفایل یک fixture کاملاً فرضی و برچسب‌خورده است، نه هویت واقعی یا دادهٔ حساب.
- **شکاف سند مادر:** schema دقیق Task، semantics موعد و ویرایش، منبع و واحدهای usage، scope حساب/پروژه برای Brief و معیار پذیرش ناوبری پروژه هنوز باید در سند مادر شفاف شوند.
- **پیشنهاد برای سند مادر:** قرارداد نسخه‌بندی و تاریخچهٔ Task، تفکیک `dueDate` از `completedAt`، حالت unavailable برای usage، رفتار transactional ذخیرهٔ Brief و اصل «یک مقصد اصلی، بدون میان‌بر تکراری» به بازنگری بعدی افزوده شود.
- **شواهد محلی تا پیش از release:** QA مرورگر واقعی روی خانه، منو، تنظیمات، مرکز کار و editor در `390 × 844`، تصویر `640 × 640`، صفر بودن overflowهای هدف و console بدون warning/error را نشان داد. `check:runtime` برای ۲۸ فایل، build/TypeScript، اپ ۱۰۳/۱۰۳، runtime ۱۱۱/۱۱۱، Sites چهار از چهار و `git diff --check` روی snapshot نهایی پاس شدند؛ تنها هشدار شناخته‌شده chunk بزرگ‌تر از 500kB است. رسید انتشار در هنداف release ثبت می‌شود.
- **رسید انتشار:** commit کد `d750f88fcfbf72f30ff8b58e4331b3d081bf9f4f` روی GitHub `main`، Cloudflare deployment `a09cde6f-628f-4d94-bc56-0fd8c5915133` و ChatGPT Sites نسخهٔ ۸/deployment `appgdep_6a91fd0cbe4c8191ae0af74919f4411c` منتشر شد. Cloudflare canonical و immutable با build محلی hash یکسان داشتند و Sites source commit همان SHA و وضعیت `succeeded` را گزارش کرد؛ جزئیات کامل در هنداف است.

---

## T1 — دریافت اطلاعات نخستین پروژه و اصلاح اقدام‌های سریع

- **تاریخ:** ۱۴۰۵/۰۶/۰۵ — ۲۰۲۶/۰۸/۲۷
- **وضعیت:** تأییدشده
- **دامنه:** ساخت یا تکمیل نخستین پروژه پیش از ورود به خانهٔ سازنده؛ اسکرول ردیف اقدام‌های سریع.

### تجربهٔ واقعی ساخت

- «حداقل اطلاعات» برای شروع پروژه بدون تعریف فیلدهای الزامی، تجربهٔ ورود قابل‌اجرا و قابل‌آزمون نمی‌ساخت.
- برای دادهٔ قدیمی ناقص، ساخت پروژهٔ دوم رفتار درستی نبود؛ همان پروژه باید با حفظ شناسه تکمیل شود.
- ردیف RTL اقدام‌های سریع با فرض‌های LTR اسکرول افقی سازگار نبود. راه‌حل پایدار، نگه‌داشتن مکانیزم اسکرول در جهت LTR و نمایش محتوای داخلی به‌صورت RTL بود تا ردیف از سمت راست آغاز شود و drag در viewport واقعی کار کند.

### شکاف سند مادر

- سند مادر در راه‌اندازی فقط «ایجاد نخستین پروژه با حداقل اطلاعات» را می‌گوید و فیلدهای حداقلی، محدودهٔ جغرافیایی پایلوت و رفتار پروژهٔ ناقص را مشخص نمی‌کند.

### تصمیم‌ها

- **[تصمیم صریح کاربر، تأییدشده]** نام پروژه، محدودهٔ پروژه در تهران و مرحلهٔ ساخت هر سه الزامی‌اند.
- **[تصمیم صریح کاربر، تأییدشده]** در این مرحله فقط تهران فعال است؛ شهر به‌صورت زمینهٔ ثابت نمایش داده می‌شود و آدرس دقیق دریافت نمی‌شود.
- **[تصمیم کاری پروتوتایپ، تأییدشده]** پروژهٔ قدیمی ناقص در همان رکورد و با حفظ شناسه تکمیل می‌شود.
- **[تصمیم کاری پروتوتایپ، تأییدشده]** دادهٔ پروژه در این نسخه فقط در مرورگر نگه‌داری می‌شود و ذخیرهٔ backend ادعا نمی‌شود.
- **[تصمیم کاری پروتوتایپ، تأییدشده]** اقدام‌های سریع یک ردیف افقی باقی می‌مانند و با drag در موبایل قابل‌پیمایش‌اند.

### پیشنهاد برای تکمیل سند مادر

- در بخش راه‌اندازی، حداقل دادهٔ پروژه، تهران به‌عنوان محدودهٔ پایلوت، عدم دریافت آدرس دقیق در این مرحله و قاعدهٔ تکمیل پروژهٔ ناقص تعریف شوند.
- در معیار پذیرش موبایل، جهت آغاز و رفتار drag ردیف‌های افقی RTL ثبت شود.

### شواهد

- پیاده‌سازی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`
- آزمون جریان: `prototype/tests/chida-flow.spec.ts`
- بازخورد کاربر: تأیید مرحله و اجازهٔ رفتن به تسک بعدی.

---

## T2 — فضای واقعی پروژه

- **تاریخ:** ۱۴۰۵/۰۶/۰۵ — ۲۰۲۶/۰۸/۲۷
- **وضعیت:** تأییدشده
- **دامنه:** صفحهٔ تمام‌صفحهٔ پروژه از نوار پروژهٔ فعال و فهرست پروژه‌ها؛ ویرایش اطلاعات پایه و بازگشت به چت.

### تجربهٔ واقعی ساخت

- سند مادر مفهوم «فضای پروژه» و داده‌های آن را تعریف می‌کند، اما شکل ورود، صفحهٔ اصلی موبایل و حداقل عمل قابل‌انجام در نخستین نسخه را تعیین نمی‌کند.
- نمایش آمار یا ماژول‌های آینده با دادهٔ ساختگی، مرز میان قابلیت واقعی و نمایشی را مخدوش می‌کرد؛ صفحه ابتدا با سه دادهٔ راه‌اندازی محدود شد و پس از آشکارشدن شکاف C-005 فقط با شناسنامهٔ تکمیلیِ قابل‌ثبت گسترش یافت.
- نگه‌داشتن فضای پروژه به‌عنوان نمای داخلی خانهٔ سازنده باعث شد بازگشت به چت، پیش‌نویس Composer و نشست جاری را حفظ کند.

### شکاف سند مادر

- رابطهٔ دقیق خانهٔ Chat-first با صفحهٔ پروژه، نقاط ورود موبایل، رفتار بازگشت و حداقل عملیات نسخهٔ نخست مشخص نشده است.
- مشخص نیست کدام بخش‌های فهرست بلند داده‌های پروژه باید پیش از فایل، حافظه، کارها و خرید قابل‌مشاهده باشند.
- شِمای مشخصات فیزیکی پروژه تعریف نشده است؛ به C-005 مراجعه شود.

### تصمیم‌ها

- **[تصمیم کاری پروتوتایپ، تأییدشده]** خانهٔ پیش‌فرض همچنان Chat-first است و فضای پروژه یک نمای جزئیات تمام‌صفحه است که از نوار پروژهٔ فعال و فهرست پروژه‌ها باز می‌شود.
- **[تصمیم کاری پروتوتایپ، تأییدشده]** صفحه فقط دادهٔ واقعی ثبت‌شدهٔ پروژه را نشان می‌دهد: سه دادهٔ راه‌اندازی و مشخصات تکمیلی C-005؛ داشبورد، آمار یا placeholder قابلیت‌های آینده ندارد.
- **[تصمیم کاری پروتوتایپ، تأییدشده]** کاربر می‌تواند شناسنامهٔ پروژه را ویرایش کند و با «ادامه در چت» یا بازگشت، به زمینهٔ قبلی چت برگردد.
- **[مرز تسک، تأییدشده]** فایل، حافظه و جست‌وجو، مرکز کارها و تأییدها، خرید، گزارش و مالی در T2 ساخته نمی‌شوند و طبق بک‌لاگ در تسک‌های مستقل می‌آیند.
- **[تصمیم کاری پروتوتایپ، تأییدشده]** ویرایش در این مرحله فقط در مرورگر ماندگار می‌شود و backend ادعا نمی‌شود.
- **[نتیجهٔ بازخورد، تأییدشده]** سه فیلد نخست فقط حداقل راه‌اندازی‌اند؛ شناسنامهٔ فیزیکی اکنون با نوع کاربری، مساحت زمین، زیربنای کل، طبقات روی زمین، طبقات منفی و تعداد واحدها قابل تکمیل است.
- **[تصمیم رابط، تأییدشده]** انتخاب مرحلهٔ ساخت و نوع کاربری با منوی RTL و Dark داخل‌اپ انجام می‌شود و دیگر منوی سفید بومی مرورگر باز نمی‌شود.
- **[تصمیم داده، تأییدشده]** فیلدهای فیزیکی اختیاری‌اند، اما مقدار ثبت‌شده باید از نظر عددی معتبر باشد؛ دادهٔ ذخیره‌شدهٔ نسخهٔ قبلی نیز اگر ذخیرهٔ جدید از نظر JSON یا شِما خراب باشد بازیابی و به شِمای تازه مهاجرت می‌کند. آرایهٔ خالیِ معتبر در نسخهٔ جدید عمداً خالی تلقی می‌شود و دادهٔ قدیمی را دوباره زنده نمی‌کند.

### پیشنهاد برای تکمیل سند مادر

- معماری اطلاعات نسخهٔ موبایل، نسبت خانهٔ چت با فضای پروژه و حداقل عملیات صفحهٔ پروژه در افق اول تعریف شوند.
- برای هر قابلیت صفحه، وضعیت «دادهٔ واقعی»، «شبیه‌سازی محلی» یا «خارج از تسک» به معیار پذیرش اضافه شود.

### شواهد

- پیاده‌سازی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`
- آزمون جریان: `prototype/tests/chida-flow.spec.ts`
- بررسی موبایل: viewport واقعی `390 × 844`، بدون overflow افقی و بدون خطای runtime.
- اعتبارسنجی خودکار: `check:runtime`، `build`، `test:app`، `test:runtime` و `test:sites` پاس شده‌اند.
- بازخورد کاربر در ۲۰۲۶/۰۸/۲۷: نبود تعداد طبقات، واحدها، مساحت زمین، زیربنا و طبقات منفی شکاف C-005 را آشکار کرد؛ تکمیل شناسنامه تأیید شد، تصویر منوی سفید مرحلهٔ ساخت اصلاح شد و کاربر اجازهٔ عبور به تسک بعدی را داد.
- بررسی مرورگر: viewport واقعی `390 × 844`، منوی Dark، بدون overflow افقی، بدون خطای console و با ماندگاری مشخصات پس از reload.
- بازبینی مرزی: drag عمودی از روی trigger مرحلهٔ ساخت، fallback از v2 ناخوانا یا schema-invalid به دادهٔ legacy، حفظ معنای `[]` معتبر، ورود با ارقام فارسی، و جلوگیری از مساحت منفی/طبقه یا واحد اعشاری در آزمون رگرسیون پوشش داده شدند.

---

## T3 — فایل‌ها و اسناد پروژه

- **تاریخ:** ۱۴۰۵/۰۶/۰۵ — ۲۰۲۶/۰۸/۲۷
- **وضعیت:** تأییدشده
- **دامنه:** کتابخانهٔ تمام‌صفحهٔ فایل‌های پروژه، انتخاب فایل پشتیبانی‌شده، پیش‌نمایش شناسنامه، ثبت محلی، جزئیات و تغییر نام نمایشی.

### تجربهٔ واقعی ساخت

- سند مادر فایل را «شهروند درجه‌اول» می‌داند، اما صفحه، فیلدهای حداقلی و مرز تجربهٔ نسخهٔ نخست را مشخص نمی‌کند.
- ذخیره‌کردن Blob، base64 یا متن فایل در `localStorage` هم ناپایدار بود و هم به‌غلط حس آپلود واقعی می‌داد؛ بنابراین تصمیم اولیهٔ T3 ثبت صرف شناسنامه بود. پس از درخواست صریح گالری تصاویر، این تصمیم برای اسناد غیرتصویری حفظ و مسیر محتوای تصویر در T3.1 جدا شد.
- «فایل پروژه» با «فایل پیوست‌شده به پیام» یک معنا ندارد. هدایت ردیف Composer به کتابخانه بدون attachment واقعی، وضعیت نادرست می‌ساخت؛ در T3 فقط فضای پروژه و Tools به کتابخانه وصل شدند.
- دستهٔ سند از نام یا نوع فایل پیشنهاد می‌شود، اما پیش از ثبت در پیش‌نمایش قابل‌مشاهده و اصلاح است؛ نتیجهٔ استخراج‌شده یا حافظهٔ خودکار ساخته نمی‌شود.
- ثبت رکورد در state پیش از اطمینان از `localStorage` موفقیت کاذب می‌ساخت: رکورد تا reload دیده می‌شد و بعد ناپدید می‌شد. ثبت و تغییر نام اکنون فقط پس از ذخیرهٔ موفق نهایی می‌شوند و شکست، داخل همان جریان به کاربر اعلام می‌شود.
- دادهٔ ماندگار فقط با درست‌بودن معنایی پذیرفته می‌شود: شناسه و نام خالی، تاریخ نامعتبر، پسوند خارج از فهرست و شناسهٔ تکراری حذف می‌شوند تا ردیف مبهم یا کلید React تکراری ساخته نشود.
- بازشدن نمای تمام‌صفحهٔ فایل‌ها، فضای پروژه را unmount می‌کرد و موقعیت اسکرول را از بین می‌برد؛ همچنین بازگشت به چت ردیف اقدام‌های سریع را از شروع RTL خارج می‌کرد. موقعیت فضای پروژه ذخیره/بازیابی و هم‌ترازی rail پس از هر بازگشت دوباره اجرا می‌شود.
- فرمت HEIC/HEIF برای انتخاب تصویر موبایل به فهرست روز اول افزوده شد؛ متن رابط نیز فرمت‌های تصویر پشتیبانی‌شده را صریح می‌گوید.
- متن متادیتای ردیف و جزئیات فایل در بازبینی موبایل ۸ تا ۹ پیکسل و کم‌خوانا بود؛ اندازهٔ اطلاعات اصلی به حداقل ۱۱ پیکسل افزایش یافت و یک آزمون computed style از بازگشت آن جلوگیری می‌کند.

### شکاف سند مادر

- شِمای حداقلی `File` و `FileVersion`، انواع روز اول، سقف حجم، سیاست فایل تکراری، تغییر نام، حذف، نگهداری و معنای نسخهٔ دوم تعیین نشده‌اند.
- رابطهٔ دقیق کتابخانهٔ پروژه با پیوست Composer، استخراج، جست‌وجو، استناد صفحه‌ای و کار پس‌زمینه به PRD محدود نیاز دارد.

### تصمیم‌ها

- **[تصمیم کاری پروتوتایپ، تأییدشده]** هر رکورد فایل به `projectId` متصل است و فقط در همان پروژه دیده می‌شود.
- **[تصمیم کاری پروتوتایپ، تأییدشده]** شناسنامه شامل نام اصلی و نمایشی، نوع، حجم، دسته، منشأ، وضعیت، دسترسی خصوصی پروژه، مرحلهٔ پروژه هنگام ثبت، نسخهٔ ۱ و تاریخ ثبت است.
- **[تصمیم صداقت محصول، تأییدشده]** محتوای اسناد غیرتصویری ذخیره یا روی سرور ارسال نمی‌شود؛ تصویر گالری فقط طبق T3.1 در همان مرورگر نگه‌داری می‌شود. هیچ ادعای OCR، استخراج، جست‌وجو، استناد یا حافظه‌سازی وجود ندارد.
- **[تصمیم امنیت، تأییدشده]** محتوای فایل بیرونی «داده برای بررسی» است، نه دستور برای چیدا، و ثبت آن هیچ اقدام یا اشتراک بیرونی ایجاد نمی‌کند.
- **[تصمیم صداقت محصول، تأییدشده]** اگر ذخیرهٔ شناسنامه در مرورگر شکست بخورد، هیچ ردیفی ثبت‌شده نشان داده نمی‌شود و کاربر پیام قابل‌مشاهده برای تلاش دوباره می‌گیرد.
- **[تصمیم تجربه، تأییدشده]** ردیف فایل در Composer تا ساخته‌شدن پیوست واقعی غیرفعال و با برچسب «به‌زودی» باقی می‌ماند؛ کتابخانهٔ پروژه جایگزین ضمنی پیوست پیام نیست.
- **[مرز تسک، تأییدشده]** نسخهٔ دوم و تاریخچه، حذف/بازگشت، اشتراک، اتصال به پیام، حافظه و جست‌وجوی منبع‌دار در این تسک ساخته نمی‌شوند.

### پیشنهاد برای تکمیل سند مادر

- یک PRD مشترک فایل، حافظه و جست‌وجو، شِمای `File`/`FileVersion` و حالات ثبت، پردازش، شکست، نسخه، حذف و دسترسی را تعریف کند.
- مسیرهای «ثبت در کتابخانه»، «پیوست به پیام» و «استفاده به‌عنوان منبع» از هم جدا و رابطهٔ آن‌ها صریح شود.
- سیاست امنیتی فایل، اسکن، حجم و فرمت، نگهداری محتوا و منشأ صفحه‌ای پیش از ذخیرهٔ واقعی نهایی شود.

### شواهد

- پیاده‌سازی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`
- آزمون جریان و جداسازی پروژه: `prototype/tests/chida-flow.spec.ts`
- بررسی موبایل: حالت خالی در viewport واقعی `390 × 844`، Dark/RTL، تمام‌صفحه و بدون overflow افقی مشاهده شد.
- محتوای اسناد غیرتصویری ثبت نمی‌شود و هیچ payload یا متنی در `localStorage` قرار نمی‌گیرد؛ مسیر محلی Blob تصویر و شواهد آن در T3.1 ثبت شده است.
- آزمون‌های بازگشتی، شکست `localStorage` بدون موفقیت کاذب، پاک‌سازی رکوردهای نامعتبر و تکراری، HEIC، ثبت واقعی در پروژهٔ دوم، حفظ scroll فضای پروژه و هم‌ترازی مجدد اقدام‌های سریع را پوشش می‌دهند.
- بازخورد کاربر در ۲۰۲۶/۰۸/۲۷: با درخواست اجرای «تسک بعدی از فهرست»، T3 و ادامهٔ تصویری آن T3.1 تأیید و عبور به T4 مجاز شد.

---

## T3.1 — گالری تصاویر پروژه

- **تاریخ:** ۱۴۰۵/۰۶/۰۵ — ۲۰۲۶/۰۸/۲۷
- **وضعیت:** تأییدشده
- **دامنه:** افزودن عکس از دستگاه یا دوربین، پیش‌نمایش پیش از ثبت، گالری پروژه‌محور، مشاهدهٔ جزئیات و ماندگاری محلی تصویر در همان مرورگر.

### تجربهٔ واقعی ساخت

- سند مادر عکس را ورودی درجه‌اول و فایل را شیء پروژه‌محور می‌داند، اما موجودیت یا taxonomy مستقلی با نام «گالری» تعریف نمی‌کند. کم‌اختراع‌ترین مدل این است که گالری نمای فیلترشدهٔ فایل‌های تصویری همان پروژه باشد، نه مخزن دادهٔ جدا.
- نگه‌داشتن صرف `Object URL` فقط در همان نشست کار می‌کند و پس از reload از بین می‌رود؛ این رفتار با انتظار «گذاشتن عکس برای پروژه» سازگار نبود. Blob تصویر در IndexedDB نسخه‌دار همان مرورگر نگه‌داری و شناسنامهٔ کوچک آن همچنان در `localStorage` حفظ شد. هیچ Blob، base64 یا payload تصویری وارد `localStorage` نمی‌شود.
- ثبت تصویر دو مرز ذخیره‌سازی دارد: ابتدا metadata پروژه نوشته می‌شود و فقط پس از موفقیت آن Blob در IndexedDB قرار می‌گیرد. شکست metadata اصلاً دیتابیس تصویر را لمس نمی‌کند؛ در شکست Blob نیز metadata به مقدار قبل برگردانده می‌شود و کارت موفق نمایش داده نمی‌شود. اگر خود rollback هم شکست بخورد، شروع بعدی اپ رکورد `browser-image` بدون Blob را reconcile و حذف می‌کند. این هماهنگی محلی جای تراکنش اتمیک backend را نمی‌گیرد و باید در نسخهٔ واقعی با storage واحد یا سازوکار reconciliation تعریف شود.
- دستهٔ قابل‌ویرایش سند برای تعیین رسانه کافی نیست؛ وگرنه PDF با دستهٔ «عکس» وارد گالری می‌شود. در پروتوتایپ، عضویت گالری از پسوند تصویری معتبر همراه MIME سازگار مشتق می‌شود و category فقط metadata انسانی می‌ماند.
- Chromium نمایش بومی HEIC/HEIF را تضمین نمی‌کند. پسوند HEIC/HEIF حتی با MIME دوربینِ ناسازگار برای preview اولویت قطعی دارد و فقط placeholder نوع فایل نمایش داده می‌شود؛ فایل raster نامعتبر نیز با رویداد decode به همین fallback می‌رود تا تصویر شکسته یا thumbnail ساختگی ساخته نشود. برای PNG/JPEG/WebP نگاشت MIME و پسوند باید سازگار باشد و مثلاً SVG با نام PNG رد شود.
- URLهای موقت نمایش از Blob محلی ساخته و هنگام تغییر فهرست یا unmount آزاد می‌شوند تا گالری با تکرار باز و بسته‌شدن حافظه را نگه ندارد.
- اجرای هم‌زمان دو مجموعهٔ کامل Playwright یک‌بار در تست قدیمی فرم پروژه race فوکوس کیبورد شبیه‌سازی‌شده ساخت؛ همان تست و سپس کل runtime در اجرای مستقل پاس شدند. شواهد نهایی موبایل از اجرای مستقل مجموعه‌ها گرفته می‌شود، نه از retry خاموش.

### شکاف سند مادر

- سند مادر «عکس» و «فایل» را تعریف می‌کند، اما معماری اطلاعات گالری، نسبت عکس با `FileVersion`، عنوان/توضیح، ترتیب، حذف، نگهداری، سقف حجم، quota مرورگر، پشتیبان، همگام‌سازی و fallback فرمت‌های غیرقابل‌نمایش را مشخص نمی‌کند.
- تفاوت «منشأ دوربین» با «انتخاب از گالری دستگاه»، سیاست metadata احتمالی EXIF و سطح مکان مجاز هنوز PRD ندارد.

### تصمیم‌ها

- **[تصمیم صریح کاربر، تأییدشده]** سازنده باید بتواند برای پروژه عکس بگذارد و هر پروژه گالری تصاویر خودش را داشته باشد.
- **[تصمیم مدل محصول، تأییدشده]** عکس همان فایل پروژه با دستهٔ «عکس» است و گالری فقط نمای فیلترشدهٔ تصاویر پروژهٔ فعال است؛ مدل مستقل `Gallery` یا `Album` ساخته نمی‌شود.
- **[تصمیم کاری پروتوتایپ، تأییدشده]** Blob تصویر فقط در IndexedDB همان مرورگر و metadata آن در `localStorage` نگه‌داری می‌شود؛ تصویر بعد از reload همان مرورگر بازمی‌گردد، اما آپلود، sync، backup یا دسترسی دستگاه دیگر ادعا نمی‌شود.
- **[تصمیم تجربه، تأییدشده]** دو ورودی روشن «انتخاب عکس» و «دوربین» وجود دارد؛ پیش از ثبت، تصویر، پروژهٔ مقصد، منشأ، نوع، حجم، مرحله، دسترسی خصوصی و نسخهٔ ۱ بازبینی می‌شوند.
- **[تصمیم امنیت و صداقت، تأییدشده]** ثبت عکس هیچ OCR، تحلیل پیشرفت، تشخیص نقص یا ایمنی، جست‌وجو، حافظه‌سازی، اشتراک یا ارسال بیرونی ایجاد نمی‌کند.
- **[مرز تسک، تأییدشده]** انتخاب دسته‌ای چند عکس، حذف/بازیابی، caption، مرتب‌سازی، آلبوم، اتصال به گزارش یا پیام، و ذخیرهٔ backend در این تسک ساخته نمی‌شوند.

### پیشنهاد برای تکمیل سند مادر

- در PRD فایل و رسانه، `mediaKind`، Blob/object storage، نسخه، thumbnail، caption/alt text، منشأ دوربین یا دستگاه، quota، نگهداری، حذف، بازیابی، sync و fallback فرمت تعریف شوند.
- گالری به‌عنوان یک view پروژه‌محور در معماری اطلاعات موبایل ثبت و تفاوت آن با پیوست Composer، گزارش کارگاه و حافظه صریح شود.

### شواهد

- پیاده‌سازی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`
- آزمون: ثبت PNG واقعی از انتخاب دستگاه و دوربین، نمایش thumbnail و جزئیات، ماندگاری بعد از reload، fallback بدون تصویر شکسته برای HEIC و raster غیرقابل decode، رد MIME/پسوند ناسازگار، جلوگیری از ورود PDF با دستهٔ «عکس»، آزادسازی Object URL، نبود payload در `localStorage`، بازگردانی یا reconciliation شکست هر دو مرز ذخیره‌سازی، جداسازی پروژه و نبود overflow افقی در `390 × 844` در `prototype/tests/chida-flow.spec.ts`.
- اعتبارسنجی نهایی: `check:runtime` برای ۲۸ فایل محافظت‌شده، `build`، تست اپ ۲۶ از ۲۶، runtime در اجرای مستقل ۳۴ از ۳۴ و Sites چهار از چهار پاس شدند. بازبینی مستقل نهایی نیز پس از بازتولید مسیرهای مرزی finding عملی باقی‌مانده‌ای گزارش نکرد.
- بازخورد کاربر در ۲۰۲۶/۰۸/۲۷: درخواست اجرای تسک بعدی، گالری و مرزهای محلی/پروژه‌محور آن را تأیید کرد.

---

## T4-A — حافظهٔ شفاف پروژه («چیدا چه می‌داند»)

- **تاریخ:** ۱۴۰۵/۰۶/۰۵ — ۲۰۲۶/۰۸/۲۷
- **وضعیت:** تأییدشده
- **دامنه:** ثبت دستی حافظه در پروژهٔ فعال، مشاهدهٔ منشأ و تاریخ و نسخه، ویرایش، ترجیح استفاده در زمینه، حذف با تأیید و ماندگاری محلی پروژه‌محور.

### تجربهٔ واقعی ساخت

- ساخت جست‌وجوی «منبع‌دار» پیش از وجود یک منبع واقعیِ قابل‌بازکردن، تاریخ‌دار و پروژه‌محور، خروجی نمایشی و گمراه‌کننده می‌ساخت. اولویت دوم بک‌لاگ به سه برش T4-A حافظه، T4-B بازیابی محلی و T4-C پاسخ منبع‌دار پس از اتصال محتوای واقعی تقسیم شد.
- تاریخچهٔ چت و حافظه دو موجودیت متفاوت‌اند. ارسال پیام آزمایشی نباید رکورد حافظه بسازد؛ فقط عمل صریح «افزودن به حافظه» رکورد ایجاد می‌کند.
- کنترل «استفاده در زمینه» بدون اتصال واقعی مدل فقط می‌تواند یک ترجیح ذخیره‌شده باشد. متن رابط این مرز را در صفحه، فرم و جزئیات تکرار می‌کند تا وضعیت محلی به‌عنوان رفتار AI نمایش داده نشود.
- حذف ظاهری از state کافی نیست؛ حذف موفق باید رکورد را از storage واقعی همان مرورگر بردارد. همچنین اگر `localStorage` در افزودن، ویرایش یا تغییر وضعیت شکست بخورد، sheet باز می‌ماند و هیچ موفقیت کاذبی نمایش داده نمی‌شود.
- ثبت `projectId` و فیلتر همهٔ خواندن‌ها و تغییرها با پروژهٔ فعال برای جلوگیری از نشت حافظه میان پروژه‌ها لازم است؛ یک عنوان مشترک یا فهرست سراسری نمی‌تواند جای این مرز را بگیرد.
- «واقعیت تأییدشده توسط سازنده» فقط به معنی تأیید صاحب پروژه است، نه راستی‌آزمایی بیرونی. منشأ هر دو نوع مورد در این برش «ثبت مستقیم شما» باقی می‌ماند.
- بستن BottomSheet ویرایش درحالی‌که ورودی فوکوس دارد باید کیبورد runtime را هم‌زمان ببندد؛ بستن صرف sheet می‌توانست state کیبورد را برای بازشدن بعدی نگه دارد و ارتفاع اولیه را ناپایدار کند.
- تأیید حذف که در انتهای یک sheet بلند ظاهر می‌شود باید به‌صورت `alertdialog` اعلام و فوکوس را به اقدام امن «انصراف» منتقل کند؛ ظاهرشدن صرف یک بلوک پایین صفحه برای صفحه‌خوان و کاربر کیبورد کافی نیست.
- سقف ۸۰ نویسه بدون `overflow-wrap` هنوز می‌توانست با یک رشتهٔ بدون فاصله اسکرول افقی بسازد؛ کنترل طول ورودی جای مهار layout و آزمون عنوان مرزی را نمی‌گیرد.

### شکاف سند مادر

- سند مادر شرایط اعتبار Memory را تعریف می‌کند، اما شِمای نسخه‌دار رکورد، طول و ساختار محتوا، taxonomy نوع حافظه، وضعیت تأیید، تاریخ منشأ در برابر تاریخ ثبت/ویرایش و معنای دقیق منع استفاده را تعیین نمی‌کند.
- رفتار حذف واقعی روشن نیست: حذف سخت، tombstone، audit trail، retention و امکان بازیابی برای محصول واقعی باید جدا تعریف شوند.
- مرز «واقعیت تأییدشده توسط کاربر» با «واقعیت راستی‌آزمایی‌شده از منبع خارجی» و «برداشت چیدا» به واژگان و وضعیت‌های مستقل نیاز دارد.
- قرارداد وصل‌شدن `useInContext` به retrieval/model harness، سنجش تازگی و ارتباط، و رفتار fallback هنوز PRD اجرایی ندارد.

### تصمیم‌ها

- **[تصمیم ترتیب اجرا، تأییدشده]** T4-A فقط حافظهٔ دستی و شفاف است؛ T4-B جست‌وجوی واقعی در حافظه و شناسنامهٔ فایل‌ها فقط پس از تأیید این مرحله آغاز می‌شود و T4-C به محتوای قابل‌خواندن فایل یا منبع وب واقعی وابسته است.
- **[تصمیم کاری پروتوتایپ، تأییدشده]** رکورد حداقلی شامل `id`، `projectId`، عنوان، متن، نوع، منشأ ثابت «ثبت مستقیم شما»، دسترسی «خصوصی پروژه»، ترجیح استفاده در زمینه، وضعیت «ثبت محلی»، نسخهٔ ۱ و تاریخ ایجاد/ویرایش است.
- **[تصمیم صداقت محصول، تأییدشده]** فقط ثبت دستی سازنده حافظه می‌سازد؛ چت، فایل و عکس خودکار حافظه نمی‌شوند و هیچ برداشت AI در فهرست نمایش داده نمی‌شود.
- **[تصمیم تجربه، تأییدشده]** سازنده می‌تواند مورد را مشاهده و ویرایش کند، استفادهٔ آینده در زمینه را فعال/غیرفعال کند و پس از تأیید صریح آن را از همین مرورگر حذف کند.
- **[تصمیم داده و حریم، تأییدشده]** همهٔ خواندن‌ها و تغییرها با پروژهٔ فعال محدود می‌شوند؛ رکوردهای ذخیره‌شدهٔ نامعتبر یا دارای شناسهٔ تکراری پذیرفته نمی‌شوند و state فقط پس از ذخیرهٔ موفق تغییر می‌کند.
- **[مرز تسک، تأییدشده]** ترجیح زمینه واقعاً به مدل تزریق نمی‌شود؛ جست‌وجوی وب/بازار، OCR، خواندن محتوای PDF یا تصویر، embedding، citation، حافظهٔ خودکار، sync و backend ساخته نشده‌اند. ردیف جست‌وجوی منبع‌دار تا T4-B با «به‌زودی» غیرفعال است.

### پیشنهاد برای تکمیل سند مادر

- یک شِمای نسخه‌دار `MemoryRecord` با دامنه، مالک، نوع ادعا، وضعیت تأیید، منبع، تاریخ منبع، تاریخ ثبت/ویرایش، تازگی، وضعیت استفاده و سیاست حذف/بازگشت تعریف شود.
- واژگان «ثبت مستقیم کاربر»، «تأییدشده توسط صاحب پروژه»، «راستی‌آزمایی‌شده از منبع» و «برداشت چیدا» از هم جدا و نمایش هرکدام در UI و پاسخ مشخص شود.
- قرارداد مرحله‌ای ورود حافظه به زمینهٔ مدل، شامل relevance، permission، freshness، منبع قابل‌بازکردن، منع استفاده و audit تعریف شود.
- ترتیب تجربهٔ موبایل «مشاهدهٔ حافظه → ثبت/اصلاح/منع استفاده → بازیابی محلی → پاسخ منبع‌دار» و fallback هر مرحله به معیار پذیرش محصول اضافه شود.

### شواهد

- پیاده‌سازی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`
- آزمون: چرخهٔ کامل ثبت، اعتبارسنجی، منشأ/دسترسی/نسخه، منع استفاده، ویرایش، reload و حذف واقعی؛ جداسازی دو پروژه؛ نبود حافظه‌سازی خودکار از چت؛ پاک‌سازی رکوردهای خراب و تکراری؛ عنوان مرزی ۸۰ نویسه؛ بستن هم‌زمان کیبورد؛ انتقال فوکوس تأیید حذف؛ و شکست مستقل ایجاد، ویرایش، تغییر وضعیت و حذف بدون موفقیت کاذب در `prototype/tests/chida-flow.spec.ts`.
- اعتبارسنجی نهایی: `check:runtime` برای ۲۸ فایل محافظت‌شده، `build`، تست اپ ۳۲ از ۳۲، runtime در اجرای مستقل ۴۰ از ۴۰، Sites چهار از چهار و `git diff --check` پاس شدند؛ runtime محافظت‌شده diff ندارد.
- بررسی مرورگر داخلی: مسیر واقعی ثبت و مشاهدهٔ حافظه در viewport `390 × 844`، Dark/RTL و تمام‌صفحه دیده شد؛ عرض سند دقیقاً ۳۹۰، overflow افقی صفر و console بدون warning یا error بود. منشأ/نسخه، غیرفعال‌سازی زمینه و تأیید حذف نیز از خود UI بررسی شدند.
- سه بازبینی مستقل محصول، داده و رابط پس از اصلاح چرخهٔ کیبورد، دسترس‌پذیری حذف، عنوان مرزی و هدف‌های لمسی، finding عملی باقی‌مانده‌ای گزارش نکردند.
- بازخورد کاربر در ۲۰۲۶/۰۸/۲۷: با درخواست push، merge و به‌روزرسانی Sites، T4-A و مرزهای آن تأیید و انتشار نسخهٔ یکپارچه مجاز شد.

---

## T4-B — جست‌وجوی محلی در حافظه و شناسنامهٔ فایل‌ها

- **تاریخ:** ۱۴۰۵/۰۶/۰۵ — ۲۰۲۶/۰۸/۲۷
- **وضعیت:** تأییدشده و منتشرشده
- **دامنه:** تطبیق قطعی و محلی در حافظهٔ دستی و شناسنامهٔ فایل‌های پروژهٔ فعال، نمایش مرز و منشأ نتیجه، بازکردن رکورد اصلی و fallback صریح؛ بدون خواندن محتوای فایل، وب یا پاسخ AI.

### تجربهٔ واقعی ساخت

- نام «جست‌وجوی منبع‌دار» برای برشی که هنوز فایل یا وب را نمی‌خواند، ادعای بیش‌ازحد می‌ساخت. ردیف Tools با عنوان محدود «جست‌وجوی محلی پروژه» فعال شد و پاسخ منبع‌دار برای T4-C باقی ماند.
- پیمایش مستقیم رکوردهای معتبر همان مرورگر برای این برش کافی بود و ساخت index، embedding یا storage تازه ارزش واقعی اضافه نمی‌کرد. نتیجه‌ها بر اساس تاریخ رکورد مرتب می‌شوند، نه رتبه‌بندی هوشمند یا ارتباط مدل.
- جست‌وجو باید allowlist فیلد داشته باشد. جست‌وجوی کل شیء با `JSON.stringify` می‌توانست فیلدی نامعتبر مانند `extractedText` را ناخواسته قابل‌بازیابی کند و ادعای خواندن محتوای فایل بسازد.
- یکسان‌سازی صرف نیم‌فاصله به فاصله کافی نبود؛ «پیش‌فاکتور» باید با «پیشفاکتور» هم تطبیق پیدا کند. تطبیق قطعی اکنون شکل‌های `ی/ي`، `ک/ك`، الف‌های همزه‌دار، اعراب، کشیده، نیم‌فاصله/فاصله، نشانه‌گذاری و ارقام فارسی/عربی را فقط در کلید مقایسه عادی می‌کند و دادهٔ ذخیره‌شده را تغییر نمی‌دهد.
- «غیرفعال برای زمینه» منع ورود حافظه به مدل است، نه منع مشاهده توسط مالک. چنین رکوردی در جست‌وجوی دستی دیده می‌شود، اما برچسب غیرفعال دارد و هیچ پاسخ AI از آن ساخته نمی‌شود.
- نتیجه باید به رکورد اصلی برگردد؛ ساخت کارت بی‌مقصد دوباره منبع حقیقت را در یک نمای ثانویه تکثیر می‌کرد. عبارت جست‌وجو پس از دیدن جزئیات حفظ و هنگام تغییر پروژه پاک می‌شود.
- منشأ خراب نباید به یک منشأ معتبر fallback شود. رکورد فایل با منشأ خارج از دو مقدار مجاز رد می‌شود تا UI «انتخاب مستقیم از دستگاه» را جعل نکند.
- خطای خواندن `localStorage` با «هنوز منبعی ثبت نشده» یکی نیست. نمای جست‌وجو بازیابی ناقص را هشدار می‌دهد و نبود نتیجه را قطعی اعلام نمی‌کند.
- هشدارِ read-error به‌تنهایی کافی نیست: اگر آرایهٔ ناخوانده به state خالی تبدیل شود و ثبت تازه همچنان مجاز بماند، اولین ذخیره می‌تواند همهٔ رکوردهای قبلی پروژه‌ها را بازنویسی کند. فایل، گالری و حافظه اکنون در این وضعیت empty state نشان نمی‌دهند و همهٔ mutationهای همان مخزن تا بارگذاری موفق بعدی قفل می‌شوند.
- حذف همهٔ فاصله‌های corpus یک تطبیق کاذب میان مرز دو فیلد می‌ساخت؛ مثلاً پایان عنوان و آغاز متن می‌توانستند یک واژهٔ جعلی بسازند. شکل compact اکنون برای هر فیلد جدا محاسبه می‌شود و همچنان نیم‌فاصله/بی‌فاصله را داخل همان فیلد پوشش می‌دهد.
- دکمهٔ پاک‌کردن جست‌وجو باید عبارت را خالی کند، اما فوکوس و کیبورد را برای عبارت بعدی نگه دارد. اندازهٔ متن ورودی نیز برای جلوگیری از auto-zoom رایج iOS به ۱۶ پیکسل رسید و رنگ placeholder از توکن موجود Dark استفاده می‌کند.
- شکست سراسری خواندن `localStorage` نباید پیش از نمایش fallback برنامه را crash کند؛ خواندن project، ابزار نصب‌شده و Brief اکنون در مرزهای امن انجام می‌شود و خطای مخزن فایل/حافظه همچنان به نمای صریح خودش می‌رسد.
- حذف `aria-label` کوتاه و استفاده از متن کامل کارت باعث شد منشأ، محدوده، نسخه و مرز محتوای فایل در نام دسترس‌پذیر نتیجه نیز خوانده شود.

### شکاف سند مادر

- سند مادر فایل را «قابل جست‌وجو و استناد» و حافظه را قابل ورود به زمینه می‌داند، اما بلوغ مرحله‌ای «بازیابی metadata → خواندن محتوا → استناد صفحه‌ای → پاسخ منبع‌دار» و نام مناسب هر سطح را تعریف نمی‌کند.
- معنای `useInContext=false` برای جست‌وجوی دستی مالک در برابر retrieval خودکار مدل صریح نیست؛ منع مشاهده، منع بازیابی و منع مصرف مدل باید سه مفهوم جدا باشند.
- قرارداد تطبیق فارسی، tokenization، ranking، ترتیب زمانی، حداقل طول query، ثبت تاریخ جست‌وجو و سیاست نگه‌داری query در سند مادر مشخص نشده است.
- fallback خرابی یا عدم دسترسی storage محلی، نتیجهٔ جزئی و رفتار تلاش دوباره برای منابع خصوصی هنوز PRD اجرایی ندارد.
- شِمای فایل منشأهای مجاز و رفتار رکورد با منشأ نامعتبر را به‌صورت enum و migration policy تثبیت نکرده است.

### تصمیم‌ها

- **[تصمیم کاری پروتوتایپ، تأییدشده توسط کاربر]** T4-B فقط تطبیق محلی و قطعی در پروژهٔ فعال است: حافظه روی عنوان، متن، نوع و منشأ؛ فایل روی نام نمایشی/اصلی، دسته، فرمت، مرحله و منشأ جست‌وجو می‌شود.
- **[تصمیم داده و حریم، تأییدشده توسط کاربر]** ورودی جست‌وجو فقط `activeProjectMemories` و `activeProjectFiles` است و در خود نما نیز `projectId` دوباره کنترل می‌شود؛ query هنگام تغییر پروژه پاک می‌شود.
- **[تصمیم حافظه، تأییدشده توسط کاربر]** حافظهٔ `useInContext=false` برای مالک قابل جست‌وجو و بازکردن است، اما با «برای زمینه غیرفعال» نمایش داده می‌شود و در این تسک به مدل وارد نمی‌شود.
- **[تصمیم منشأ، تأییدشده توسط کاربر]** هر نتیجه نوع رکورد، پروژه و دسترسی خصوصی، منشأ، تاریخ، نسخه و وضعیت محلی را نشان می‌دهد و لمس آن جزئیات رکورد اصلی حافظه یا فایل را باز می‌کند. منشأ نامعتبر فایل رد می‌شود.
- **[تصمیم شکست، تأییدشده توسط کاربر]** نبود تطبیق فقط دربارهٔ حافظه و شناسنامهٔ فایل‌های همان پروژه سخن می‌گوید؛ خطای خواندن با هشدار «بازیابی کامل نشد» از empty state جداست و ثبت، ویرایش، تغییر وضعیت و حذف همان مخزن تا بارگذاری موفق بعدی مسدود می‌ماند.
- **[مرز تسک، تأییدشده توسط کاربر]** هیچ index پایدار، embedding، fuzzy/semantic ranking، محتوای PDF/تصویر، OCR، EXIF، وب/بازار، citation، پاسخ یا خلاصهٔ AI، حافظهٔ خودکار، تزریق به مدل، پروژهٔ دیگر، sync، cloud یا backend ساخته نشده است.
- **[تصمیم ترتیب اجرا، تأییدشده توسط کاربر]** T4-C فقط پس از اتصال محتوای واقعاً قابل‌خواندن فایل یا منبع وب واقعی می‌تواند پاسخ منبع‌دار با لینک/صفحه، تاریخ و fallback بسازد.

### پیشنهاد برای تکمیل سند مادر

- برای «جست‌وجو» چهار سطح قابلیت و واژگان صریح تعریف شود: بازیابی شناسنامه، بازیابی محتوا، استناد قابل‌بازکردن و پاسخ ترکیبی منبع‌دار؛ UI حق نداشته باشد سطح بالاتر را پیش از وجود قرارداد آن ادعا کند.
- قرارداد `MemoryUsePolicy` میان مشاهدهٔ مالک، جست‌وجوی دستی، retrieval خودکار، مصرف مدل و اشتراک بیرونی تفکیک شود.
- قرارداد جست‌وجوی فارسی شامل normalization، tokenization، ترتیب نتایج، نمایش علت تطبیق و رفتار query میان تغییر فضاها نوشته شود.
- وضعیت `SourceRead` شامل کامل، جزئی، ناموفق، منبع خراب و نیازمند تلاش دوباره به قرارداد شکست افزوده شود.
- enum منشأ فایل، رفتار migration/rejection و تمایز تاریخ ثبت، تاریخ فایل و تاریخ استخراج در شِمای نسخه‌دار فایل تثبیت شود.

### شواهد

- پیاده‌سازی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`؛ راهنماهای جاری در `AGENTS.md`، `prototype/AGENTS.md`، `README.md` و `prototype/README.md` همگام شدند.
- آزمون: تطبیق فارسی/عربی و ارقام، نیم‌فاصله، جلوگیری از تطبیق کاذب میان مرز فیلدها، حافظهٔ غیرفعال برای زمینه، metadata فایل، عدم جست‌وجوی فیلد محتوایی جعلی، بازکردن رکورد اصلی، حفظ query، حفظ فوکوس هنگام پاک‌کردن، جداسازی حافظه و فایل دو پروژه، پاک‌شدن query هنگام تغییر پروژه، منشأ نامعتبر، read-error سراسری، قفل mutation پس از دادهٔ ناخوانا، targetهای ۴۴ پیکسل، نام دسترس‌پذیر کامل و نبود overflow در `390 × 844`.
- اعتبارسنجی نهایی: `check:runtime` برای ۲۸ فایل محافظت‌شده، `build`، تست اپ ۳۶ از ۳۶، runtime در اجرای مستقل ۴۴ از ۴۴، Sites چهار از چهار و `git diff --check` پاس شدند.
- بررسی مرورگر داخلی: Tools و صفحهٔ جست‌وجوی واقعی در viewport `390 × 844` دیده شدند؛ عرض سند و نمای جست‌وجو دقیقاً ۳۹۰، overflow افقی صفر و متن ورودی ۱۶ پیکسل بود. پاک‌کردن query فوکوس و keyboard state را حفظ کرد و یک حافظهٔ واقعی با منشأ، تاریخ، نسخه و برچسب «برای زمینه غیرفعال» پیدا و جزئیات رکورد اصلی آن باز شد.
- سه بازبینی مستقل محصول، رابط و release، جعل منشأ، fallback و قفل خطای خواندن، تطبیق نیم‌فاصله و مرز فیلد، نام دسترس‌پذیر، برچسب بازگشت، پوشش جداسازی فایل، فوکوس پاک‌کردن و ریسک auto-zoom iOS را یافتند؛ همه پیش از اعتبارسنجی نهایی اصلاح و با تست پوشش داده شدند.
- بازخورد کاربر: T4-B تأیید شد و کاربر در ۲۰۲۶/۰۸/۲۷ درخواست push، merge و انتشار همان نسخه را داد؛ عبور به T4-C یا تسک بعدی پس از پایان انتشار انجام می‌شود.
- انتشار تأییدشده: commit یکپارچهٔ `647a733da68a10fa4ac11a3ed1897983a501f9bd` روی `main` و `origin/main` است؛ Cloudflare Pages در `https://chida-prototype.pages.dev/` پاسخ HTTP 200 می‌دهد و ChatGPT Sites در `https://chida-prototype.mahyarkl.chatgpt.site/` منتشر و owner-only است (درخواست ناشناس HTTP 401).

---

## T5-A — مرکز کارهای پروژه و وظیفهٔ داخلی محلی

- **تاریخ:** ۱۴۰۵/۰۶/۰۵ — ۲۰۲۶/۰۸/۲۷
- **وضعیت:** تأییدشده و منتشرشده
- **دامنه:** مرکز کارهای پروژهٔ فعال، ثبت دستی وظیفهٔ داخلی، وضعیت «در حال انجام/تمام‌شده»، بازگشایی، نسخه و تاریخچهٔ واقعیِ محلی؛ بدون اجرای پس‌زمینه، تأیید تجاری، شکست اجرایی، پایش، اعلان یا ارسال بیرونی.

### تجربهٔ واقعی ساخت

- T4-C بدون محتوای واقعاً قابل‌خواندن فایل یا یک منبع وب واقعی، فقط می‌توانست پاسخ و citation نمایشی بسازد. این برش حذف یا لغو نشد؛ تا فراهم‌شدن منبع قابل‌بازکردن، تاریخ‌دار و دارای قرارداد شکست به تعویق افتاد و اجرای بک‌لاگ از نخستین برش صادقانهٔ اولویت سوم ادامه یافت.
- «کار داخلی» کوچک‌ترین موجودیت واقعی برای شروع مرکز کارها بود: سازنده خودش عنوان و گام بعدی را ثبت می‌کند و تنها گذارهای واقعی فعلی «ثبت → تمام‌شده → بازگشایی» هستند. تب‌های تأیید، شکست و پایش بدون تولیدکنندهٔ واقعی، empty state صریح باقی ماندند.
- نشان‌دادن موفقیت پیش از نوشتن storage می‌توانست وظیفه یا نسخه‌ای بسازد که پس از reload ناپدید شود. ایجاد و تغییر وضعیت اکنون فقط پس از ذخیرهٔ موفق در مرورگر وارد state می‌شوند؛ در شکست نوشتن، رکورد و نسخهٔ قبلی دست‌نخورده می‌ماند.
- دادهٔ ناخوانا یا رکورد تکراری نباید به «صفر کار» تبدیل شود. خطای خواندن از empty state جدا شد، mutation قفل می‌شود و حتی rail فیلترها و شمارنده‌های قطعی پنهان می‌مانند تا دادهٔ ناقص با عدد صفر نمایش داده نشود.
- اعتبار یک وظیفه فقط با وجود چند فیلد کافی نیست؛ ترتیب تاریخچه، شمارهٔ نسخه، نوع رویداد، زمان ایجاد/تغییر/تکمیل و وضعیت جاری باید با هم سازگار باشند. رکوردهای ناسازگار به‌عنوان بازیابی ناقص گزارش می‌شوند.
- متن متشکل فقط از فاصله، نیم‌فاصله یا نویسه‌های نامرئی می‌توانست ظاهراً یک کارت خالی بسازد؛ اعتبارسنجی ورودی و دادهٔ بازیابی‌شده اکنون وجود متن قابل‌دیدن را کنترل می‌کند.
- Bottom Sheet کنترل‌شده بدون trigger داخلی فوکوس را پس از Escape روی `body` رها می‌کرد. دکمهٔ «کار جدید» به‌عنوان مقصد بازگشت فوکوس تثبیت شد؛ textarea نیز برای جلوگیری از auto-zoom رایج iOS، اندازهٔ ۱۶ پیکسل و پس‌زمینهٔ Dark صریح دارد.
- overlay بستهٔ Bottom Sheet با وجود محوشدن بصری، به‌علت style درون‌خطی runtime چندصد میلی‌ثانیه tap اول را می‌گرفت. حالت `closed` در CSS اپ با اولویت صریح از دریافت pointer خارج شد و tap فوری پس از Escape در آزمون واقعی پوشش داده شد.
- rail فیلترهای RTL در بارگذاری نخست می‌توانست گزینهٔ انتخاب‌شده را بیرون قاب بگذارد. گزینهٔ فعال پس از layout و تغییر اندازه در مرکز فضای قابل‌دید قرار می‌گیرد، بدون ایجاد overflow افقی.

### شکاف سند مادر

- سند مادر از پیگیری پروژه، صف اقدام و تأیید انسانی سخن می‌گوید، اما موجودیت نسخه‌دار `Task`، فیلدهای حداقلی، وضعیت‌ها، نقش سازنده/عامل، اولویت، موعد، وابستگی، تکرار، بایگانی و سیاست حذف را تعریف نمی‌کند.
- تفاوت مفهومی «وظیفهٔ داخلی»، «اجرای عامل»، «اقدام منتظر تأیید»، «اجرای ناموفق» و «پایش زمان‌دار» و اینکه هرکدام توسط چه تولیدکنندهٔ واقعی ساخته می‌شوند، در سند مادر صریح نیست.
- قرارداد تاریخچه و audit برای گذار وضعیت، retry، تغییر مسئول، ویرایش محتوا و بازگشایی تعیین نشده است؛ همچنین معلوم نیست نسخهٔ جدید برای کدام تغییرها ساخته می‌شود.
- همگام‌سازی چند دستگاه، حل تعارض، مالکیت/دسترسی تیمی، retention و fallback زمانی که backend یا اعلان در دسترس نیست هنوز PRD اجرایی ندارند.

### تصمیم‌ها

- **[تصمیم ترتیب اجرا، تأییدشده]** T4-C تا وجود منبع واقعیِ قابل‌خواندن و قابل‌بازکردن به تعویق می‌ماند؛ T5-A نخستین برش از «مرکز کارها و تأییدها» است و فقط وظیفهٔ داخلی دستی را فعال می‌کند.
- **[تصمیم کاری پروتوتایپ، تأییدشده]** هر وظیفه به `projectId` پروژهٔ فعال محدود است و شامل شناسه، عنوان، گام جاری/آخرین گام، وضعیت، منشأ «ثبت مستقیم شما»، دسترسی «خصوصی پروژه»، وضعیت «ثبت محلی»، نسخه، زمان‌ها و تاریخچهٔ رویدادهاست.
- **[تصمیم گذار وضعیت، تأییدشده]** وظیفه با نسخهٔ ۱ و رویداد `created` ساخته می‌شود؛ تکمیل و بازگشایی هرکدام دقیقاً یک نسخه و رویداد جدید می‌سازند. وضعیت تکمیل‌شده به‌جای ادعای «گام جاری»، «آخرین گام ثبت‌شده» را نمایش می‌دهد.
- **[تصمیم شکست، تأییدشده]** نوشتن storage پیش از تغییر state انجام می‌شود. شکست خواندن، ثبت و تغییر وضعیت به‌صورت جدا نمایش داده می‌شوند؛ در read-error همهٔ تغییرها و شمارنده‌های قطعی تا reload موفق قفل یا مخفی‌اند.
- **[تصمیم تجربه، تأییدشده]** مرکز کارها از Drawer باز می‌شود، draft چت را از بین نمی‌برد، فیلترهای افقی قابل‌اسکرول دارد و جزئیات وظیفه منشأ، حریم، نسخه، زمان‌ها و تاریخچه را نشان می‌دهد.
- **[مرز تسک، تأییدشده]** هیچ عامل پس‌زمینه، اتصال به مدل، اعلان، زمان‌بندی، approval واقعی، retry، RFQ، مذاکره، ارسال بیرونی، backend یا sync ساخته نشده است. تب‌های مربوط فقط مرز و zero-state صریح دارند.

### پیشنهاد برای تکمیل سند مادر

- یک شِمای نسخه‌دار `Task` و `TaskEvent` با مالک پروژه، سازنده/مسئول، عنوان و شرح، وضعیت، اولویت، موعد، وابستگی، منشأ، visibility، actor، version، timestamps و سیاست حذف/بایگانی تعریف شود.
- یک state machine جدا برای `Task`، `AgentRun`، `ApprovalRequest` و `Monitor` نوشته شود تا «در حال انجام»، «منتظر تأیید»، «ناموفق» و «پایش» صرفاً تب‌های ظاهری یک موجودیت مبهم نباشند.
- تولیدکنندهٔ واقعی هر ردیف مرکز کارها و مرز تأیید انسان مشخص شود؛ اقدام تجاری یا ارسال بیرونی باید پیش‌نمایش، دادهٔ مقصد، هزینه/ریسک و تأیید صریح ثبت‌شده داشته باشد.
- قرارداد backend، sync، تعارض، اعلان، retry و fallback آفلاین پیش از تبدیل ثبت محلی به مدیریت پروژهٔ واقعی نهایی شود.
- جایگاه مرکز کارها نسبت به گفت‌وگو، Brief، درخواست خرید، مالی و حافظهٔ پروژه در معماری اطلاعات نسخهٔ موبایل تثبیت شود.

### شواهد

- پیاده‌سازی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`؛ بدون تغییر runtime محافظت‌شده.
- آزمون متمرکز: هفت سناریوی مرکز کارها شامل چرخهٔ کامل و reload، جداسازی پروژه، read-error، شکست ایجاد، رکورد تکراری، storage خالیِ نامعتبر و شکست تغییر وضعیت پاس شدند.
- پوشش رابط: اعتبارسنجی متن نامرئی، بازگشت فوکوس پس از Escape، متن ۱۶ پیکسل و رنگ Dark فیلد، نبود شمارندهٔ قطعی در read-error، اندازهٔ هدف لمسی و نبود overflow افقی در تست ثبت شد.
- بررسی مرورگر داخلی: مسیر مرکز کارها، ثبت/تکمیل، نسخهٔ ۲ و تاریخچه در viewport واقعی `390 × 844`، Dark/RTL و تمام‌صفحه دیده شد؛ گزینهٔ فعال rail کاملاً قابل‌دیدن، overflow افقی صفر، label کار تمام‌شده «آخرین گام ثبت‌شده» و console بدون warning/error بود.
- اعتبارسنجی نهایی: `check:runtime` برای ۲۸ فایل محافظت‌شده، `build`، تست اپ ۴۳ از ۴۳، runtime در اجرای مستقل ۵۱ از ۵۱، Sites چهار از چهار و `git diff --check` پاس شدند.
- سه بازبینی مستقل قرارداد محصول، مرز داده/ذخیره‌سازی و رابط موبایل پس از اصلاح شمارندهٔ read-error، متن نامرئی، بازگشت فوکوس و tap overlay، finding مادی باقی‌مانده‌ای گزارش نکردند.
- بازخورد کاربر در ۲۰۲۶/۰۸/۲۷: T5-A تأیید شد و کاربر push، merge و انتشار همین نسخه را مجاز کرد؛ ساخت T4-C با دادهٔ نمونهٔ آشکار نیز به‌عنوان تسک بعدی درخواست شد.
- انتشار تأییدشده: PR شمارهٔ ۳ با merge commit `a81574fc30263ee79f1abdc5f60a592fa297e681` روی `main` و `origin/main` یکپارچه شد؛ Cloudflare Pages از source `a81574f` در `https://chida-prototype.pages.dev/` پاسخ HTTP 200 می‌دهد و ChatGPT Sites نسخهٔ ۴ را از همین source در `https://chida-prototype.mahyarkl.chatgpt.site/` به‌صورت owner-only منتشر کرده است (درخواست ناشناس HTTP 401).

---

## T4-C-Demo — نمونهٔ فقط‌خواندنی پاسخ منبع‌دار با Mock Data

- **تاریخ:** ۱۴۰۵/۰۶/۰۵ — ۲۰۲۶/۰۸/۲۷
- **وضعیت:** تأییدشده توسط کاربر و در release پایهٔ `233e279` منتشرشده
- **دامنه:** نمایش قرارداد آیندهٔ پاسخ منبع‌دار با پرسش، جمع‌بندی، ارجاع و دو رکورد منبع کاملاً ساختگی؛ بدون اجرای وب، خواندن فایل، مدل هوش مصنوعی، لینک، ذخیره‌سازی یا تغییر دادهٔ پروژه.

### تجربهٔ واقعی ساخت

- اصطلاح دقیق برای نیاز کاربر «Mock Data / دادهٔ نمونه» است: markup ساختار صفحه را توصیف می‌کند، اما این برش به رکوردهای نمایشی نیاز داشت که شکل پاسخ و منبع را بدون ادعای قابلیت واقعی قابل‌آزمون کنند.
- قراردادن نمونه داخل نتیجهٔ T4-B یا پاسخ Composer مرز صداقت محصول را مبهم می‌کرد. یک ردیف مستقل در Tools و یک صفحهٔ فقط‌خواندنی ساخته شد تا جست‌وجوی واقعی محلی، چت و دادهٔ ساختگی هرگز یک خروجی مشترک نداشته باشند.
- یک برچسب کوچک «نمونه» کافی نبود؛ هشدار باید پیش از پاسخ در نخستین viewport دیده شود و همان برچسب در عنوان، پاسخ، کارت منبع، جزئیات و نام دسترس‌پذیر تکرار شود تا رنگ تنها حامل معنا نباشد.
- دادهٔ نمایشی هم به قرارداد fail-closed نیاز دارد. شناسهٔ تکراری، ارجاع بی‌منبع، حذف `isMock` یا اضافه‌شدن URL/retrieval واقعی، کل پاسخ را به‌جای نمایش ناقص می‌بندد.
- Bottom Sheet جزئیات چون trigger داخلی ندارد، پس از Escape به بازگردانی صریح فوکوس روی همان کارت منبع نیاز داشت؛ پیش‌نویس Composer نیز با رفت‌وبرگشت صفحه حفظ می‌شود.

### شکاف سند مادر

- سند مادر قرارداد تجربهٔ «نمونهٔ غیرعملیاتی» را از قابلیت functional جدا نمی‌کند و واژگان اجباری برای Mock، prototype-only، دادهٔ ساختگی و عدم اعتبار اجرایی ندارد.
- حداقل metadata لازم برای نمایش منبع شامل نوع، عنوان، ناشر، نسخه، تاریخ منبع، locator، محدودهٔ جغرافیایی، وضعیت بازیابی و دامنهٔ اعتبار به‌صورت یک شِمای واحد مشخص نشده است.
- سند مادر الزام نگاشت هر ادعا به منبع، نمایش تعارض، وضعیت fallback و رفتار fail-closed در نبود یا خرابی citation را به معیار پذیرش رابط تبدیل نکرده است.
- معلوم نیست پاسخ نمونه باید به پروژه «متعلق» باشد یا فقط نام پروژه را برای زمینهٔ دیداری نشان دهد؛ بدون قرارداد روشن، دادهٔ دمو ممکن است با حافظه یا اسناد واقعی پروژه اشتباه شود.

### تصمیم‌ها

- **[تصمیم کاربر، تأییدشده]** بنا بر درخواست صریح کاربر، قرارداد ظاهری T4-C پیش از اتصال منبع واقعی با Mock Data ساخته می‌شود؛ این تصمیم فقط اجازهٔ نمونهٔ صریح را می‌دهد و شرط منبع واقعی برای T4-C عملیاتی را تغییر نمی‌دهد.
- **[تصمیم صداقت محصول، تأییدشده]** عنوان ورودی «پاسخ منبع‌دار · نمونه» است و بنر ثابت می‌گوید تمام پاسخ و منابع ساختگی‌اند و هیچ فایل، وب یا مدل هوش مصنوعی اجرا نشده است.
- **[تصمیم داده، تأییدشده]** پرسش، ادعاها و دو منبع فقط constant در حافظهٔ کد هستند؛ `persisted=false` و `belongsToProject=false` دارند، URL و زمان retrieval ندارند و هیچ mutation در `localStorage`، IndexedDB، چت، فایل یا حافظهٔ پروژه ایجاد نمی‌کنند.
- **[تصمیم استناد، تأییدشده]** هر ادعا شناسهٔ منبع موجود دارد و جزئیات هر رکورد نسخه، تاریخ نمونه، locator، محدوده و «فاقد اعتبار اجرایی» را نشان می‌دهد؛ هیچ لینک جعلی ساخته نمی‌شود.
- **[مرز تسک، تأییدشده]** ورودی آزاد، درخواست شبکه، خواندن/OCR فایل، جست‌وجوی وب، مدل، prompt، citation واقعی، ذخیره در پروژه، sync، backend و اقدام تجاری ساخته نشده‌اند. پاسخ منبع‌دار واقعی همچنان تا اتصال منبع قابل‌خواندن و قابل‌بازکردن به تعویق می‌ماند.

### پیشنهاد برای تکمیل سند مادر

- یک بخش `Prototype Truthfulness` اضافه شود که سطح‌های Mock، شبیه‌سازی محلی و قابلیت واقعی، برچسب اجباری هر سطح و منع اختلاط دادهٔ نمونه با رکورد محصول را تعریف کند.
- شِمای نسخه‌دار `SourceRecord` و `Citation` با منشأ، ناشر، نسخه، تاریخ منبع/بازیابی، locator، جغرافیا، اعتبار، URL قابل‌بازکردن، وضعیت خواندن و پیوند claim-to-source تدوین شود.
- معیار پذیرش پاسخ منبع‌دار شامل مشاهدهٔ هشدار/fallback، بازکردن منبع، تاریخ و نسخه، تعارض، عدم‌قطعیت، invalid-source fail-closed و جداسازی پروژه شود.
- مشخص شود نمونهٔ رابط فقط active-project context را نشان می‌دهد و تا ثبت واقعی، مالکیت پروژه، حافظه، فایل یا تاریخچه ایجاد نمی‌کند.

### شواهد

- پیاده‌سازی محلی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`؛ راهنمای صداقت نمونه در `AGENTS.md` و `prototype/AGENTS.md` ثبت شد و runtime محافظت‌شده تغییر نکرد.
- آزمون متمرکز ابتدا به‌علت نبود ورودی نمونه شکست خورد و پس از پیاده‌سازی پاس شد؛ صراحت برچسب‌ها، metadata، نبود لینک، نبود درخواست شبکه یا mutation ذخیره‌سازی، بازگشت فوکوس، حفظ draft، target بازگشت و نبود overflow افقی در `390 × 844` را پوشش می‌دهد.
- بررسی مرورگر داخلی در viewport واقعی `390 × 844`: ورودی مستقل Tools، هشدار نمایشی در نخستین viewport، پرسش و جمع‌بندی، دو کارت منبع، Bottom Sheet جزئیات منبع [۱] و آخرین fallback دیده شدند؛ نمایش Dark/RTL تمام‌صفحه بود و هیچ overflow افقی یا اختلال قابل‌مشاهده‌ای وجود نداشت.
- اعتبارسنجی نهایی: `check:runtime` برای ۲۸ فایل محافظت‌شده، `build`، تست اپ ۴۴ از ۴۴، runtime در اجرای مستقل ۵۲ از ۵۲، Sites چهار از چهار و `git diff --check` پاس شدند.
- بازبینی مستقل یک oracle ناپایدار در تست یافت: بارگذاری `blob:` محلی گالری به‌اشتباه request شبکه شمرده می‌شد. معیار به schemeهای واقعی `http/https` محدود و دوباره تست شد؛ پس از اصلاح، finding مادی در رابط یا مرز داده باقی نماند.
- بازخورد کاربر در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸: با درخواست «بریم تسک بعدی»، نمونهٔ T4-C و مرزهای صداقت/عدم‌ذخیرهٔ آن تأیید و عبور به برش بعدی مجاز شد.
- بازخورد نهایی کاربر در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸: انتشار این برش همراه T6-A و T6-B با commit، push، merge و publish صریحاً مجاز شد.

---

## T6-A — پیش‌نویس محلی تک‌قلمی درخواست خرید

- **تاریخ:** ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸
- **وضعیت:** تأییدشده توسط کاربر و در release پایهٔ `233e279` منتشرشده
- **دامنه:** ساخت، تکمیل و نسخه‌بندی یک درخواست خرید محصول برای پروژهٔ فعال، از متن نیاز خام تا وضعیت «آمادهٔ بازبینی»؛ بدون تأمین‌کننده، قیمت، تأیید، ارسال بیرونی یا تعهد خرید.

### تجربهٔ واقعی ساخت

- پرکردن تب «منتظر تأیید» پیش از وجود یک شیء واقعی و نسخه‌دار، فقط یک صف نمایشی می‌ساخت. نخست باید خود درخواست خرید، نسخهٔ قابل‌بازبینی و مرز دادهٔ قابل‌اشتراک وجود داشته باشد؛ بنابراین ترتیب صادقانهٔ جریان به «پیش‌نویس درخواست → نسخهٔ آمادهٔ بازبینی → درخواست تأیید همان نسخه → انتخاب مقصد → تأیید مستقل ارسال بیرونی» تفکیک شد.
- اقدام سریع «درخواست قیمت» تا این برش فقط متن Composer را پر می‌کرد. اکنون ورودی ساخت یک درخواست واقعیِ محلی است و همان رکورد از فضای پروژه نیز با شمارندهٔ واقعی بازیابی می‌شود؛ پیش‌نویس Composer و دادهٔ کارها، فایل‌ها و حافظه تغییر نمی‌کنند.
- نیاز خام می‌تواند با یک متن دستی و بدون شناسنامهٔ کامل ذخیره شود. تبدیل متن به نام قلم، مقدار یا موعد در این نسخه با AI یا parser حدسی انجام نمی‌شود؛ فیلدهای ساختاری فقط با ثبت مستقیم سازنده تکمیل می‌شوند و `confidence=null` می‌ماند.
- آماده‌شدن برای بازبینی فقط پس از وجود نام قلم، مقدار مثبت canonical، واحد و محدودهٔ پروژه ممکن است. برند، مشخصات، موعد، حمل، مالیات و شرایط پرداخت می‌توانند نامشخص بمانند، اما ابهام آن‌ها به‌صراحت در جزئیات دیده می‌شود.
- وضعیت «آمادهٔ بازبینی» اگرچه نسخهٔ جدید و snapshot فقط‌خواندنی می‌سازد، نه `ApprovalRequest` است و نه مجوز ارسال. در بررسی واقعی، شمارندهٔ تب «منتظر تأیید» پس از آماده‌شدن درخواست همچنان صفر ماند.
- lifecycle یک Bottom Sheet که بلافاصله پس از ساخت رکورد بسته و از صفحهٔ جزئیات دوباره برای ویرایش باز می‌شد، می‌توانست callback بسته‌شدنِ انیمیشن قبلی را به نمونهٔ تازه منتقل کند و شیت را خودکار ببندد. کلید مستقل برای نشست ساخت و ویرایش، instance قبلی را از ویرایش تازه جدا کرد و مسیر در مرورگر و Playwright پایدار شد.
- مثل فایل، حافظه و کارها، موفقیت نمایشی پیش از ذخیره قابل‌اعتماد نبود. هر ایجاد، ویرایش و گذار وضعیت ابتدا در کلید نسخه‌دار `localStorage` نوشته و فقط پس از موفقیت وارد state می‌شود؛ read-error کل مخزن را قفل می‌کند و دادهٔ ناقص با empty state اشتباه نمی‌شود.
- parser فقط شکل JSON را بررسی نمی‌کند: شناسهٔ تکراری، متن نامرئی، مقدار غیرمثبت یا غیرcanonical، تاریخ و ترتیب نسخهٔ ناسازگار، گذار نامعتبر و رکورد «آمادهٔ بازبینی» فاقد نام/مقدار/واحد همگی بازیابی ناقص محسوب می‌شوند.
- پاک‌سازی فقط هنگام خواندن کافی نبود؛ ورودی اختیاریِ متشکل از نویسه‌های نامرئی باید پیش از نوشتن نیز به `null` تبدیل شود تا رکوردی که همین اپ ساخته پس از reload توسط parser خودش رد نشود.
- تغییر وضعیت و بازگشت میان فهرست، جزئیات و مبدأ ورود باید مقصد فوکوس صریح داشته باشد؛ پس از ذخیره یا گذار وضعیت، عنوان جزئیات و پس از بازگشت، همان کارت درخواست یا ورودی فضای پروژه/اقدام سریع فوکوس می‌گیرد.

### شکاف سند مادر

- سند مادر مسیر درخواست خرید و استعلام را توضیح می‌دهد، اما شِمای حداقلی و نسخه‌دار `PurchaseRequest`، تفاوت نیاز خام با قلم ساختاریافته، واحدهای روز اول، تک‌قلمی/چندقلمی بودن و سیاست فیلدهای ناقص را تعیین نمی‌کند.
- مرزهای `draft`، `ready-for-review`، `pending-approval`، `approved-for-review` و `approved-to-send` و اینکه هر گذار دقیقاً کدام نسخه، مقصد و payload را قفل می‌کند صریح نشده است.
- دادهٔ قابل‌اشتراک با تأمین‌کننده و دادهٔ خصوصی پروژه، سطح دقت مجاز موقعیت تحویل، زمان آشکارکردن آدرس دقیق و snapshot حریم هنگام تأیید ارسال قرارداد اجرایی ندارند.
- حمل، مالیات، شرایط پرداخت، جایگزین قابل‌قبول، موعد و وابستگی آن‌ها به مقایسهٔ پیشنهادها در شِمای منبع‌دار تعریف نشده‌اند؛ «نامشخص» بودن باید وضعیت معتبر باشد، نه متن placeholder مبهم.
- رابطهٔ درخواست خرید با Task، ApprovalRequest، RFQ، SupplierMatch، Quote، NegotiationThread و گزارش پروژه و همچنین idempotency و audit ارسال هنوز به state machine مشترک نیاز دارد.

### تصمیم‌ها

- **[تصمیم ترتیب اجرا، تأییدشده توسط کاربر]** T6-A پیش از صف تأیید ساخته می‌شود، چون Approval واقعی باید به `requestId` و `requestVersion` موجود اشاره کند؛ صف عمومی بدون producer واقعی پر نمی‌شود.
- **[تصمیم کاری پروتوتایپ، تأییدشده توسط کاربر]** نسخهٔ نخست فقط درخواست خرید «محصول» و فقط یک قلم دارد؛ خدمت، چند قلم، پیوست، صدا و استخراج هوشمند در این برش ساخته نمی‌شوند.
- **[تصمیم داده، تأییدشده توسط کاربر]** رکورد به `projectId` محدود و شامل متن نیاز خام، قلم ساختاریافته، محدودهٔ تهران، موعد اختیاری، شرایط نامشخص، وضعیت اشتراک، نسخه، زمان‌ها و تاریخچهٔ رویدادهای واقعی است. مقدار عددی در storage به شکل canonical لاتین ذخیره و در رابط فارسی نمایش داده می‌شود.
- **[تصمیم گذار وضعیت، تأییدشده توسط کاربر]** ساخت با نسخهٔ ۱ و `created` آغاز می‌شود؛ هر ویرایش موفق `updated` و هر آماده‌سازی `marked-ready-for-review` با یک نسخهٔ تازه می‌سازد. نسخهٔ آماده مستقیم ویرایش نمی‌شود و ابتدا باید با `returned-to-draft` به ویرایش برگردد.
- **[تصمیم حریم، تأییدشده توسط کاربر]** محدودهٔ تقریبی تهران همراه قلم و مشخصات ثبت‌شده می‌تواند در آینده وارد پیش‌نمایش اشتراک شود؛ نام پروژه، حافظه، فایل‌ها، بودجه و آدرس دقیق خصوصی می‌مانند. در T6-A هیچ‌کدام واقعاً ارسال نمی‌شوند.
- **[تصمیم شکست، تأییدشده توسط کاربر]** نوشتن storage پیش از state، parser سخت‌گیر، تشخیص شناسهٔ تکراری، تفکیک read-error از empty state، قفل mutation در بازیابی ناقص و جداسازی پروژه الزامی‌اند.
- **[مرز تسک، تأییدشده توسط کاربر]** هیچ مدل یا AI، تأمین‌کننده، تطبیق، قیمت، پیشنهاد، مقایسه، ApprovalRequest، RFQ، شبکه، اعلان، backend، sync، سفارش، پرداخت، قرارداد، تضمین، حمل، تحویل یا حل اختلاف ساخته نشده است.

### پیشنهاد برای تکمیل سند مادر

- شِمای نسخه‌دار `PurchaseRequest`، `PurchaseRequestItem` و `PurchaseRequestEvent` با منشأ هر فیلد، confidence فقط در صورت استخراج واقعی، وضعیت‌های ناقص و قرارداد نسخه/تاریخچه تعریف شود.
- state machine جدا و متصل برای `PurchaseRequest`، `ApprovalRequest` و `ExternalSend` نوشته شود؛ تأیید نسخهٔ درخواست با تأیید مقصد و payload ارسال بیرونی یکی نباشد.
- ماتریس دادهٔ قابل‌اشتراک/خصوصی و سطح دقت موقعیت بر اساس مرحلهٔ جریان، همراه با پیش‌نمایش حریم و ثبت نسخهٔ تأییدشده، به معیار پذیرش خرید اضافه شود.
- taxonomy واحد، نوع درخواست محصول/خدمت، چندقلمی‌شدن، شرایط حمل/مالیات/پرداخت و سیاست جایگزین و موعد پیش از T6-B/T6-C نهایی شود.
- رابطهٔ درخواست با مرکز کارها، فایل و حافظه، استعلام، تطبیق تأمین‌کننده، پیشنهاد، مذاکره، اعلان و گزارش با شناسه‌های مرجع و audit trail صریح شود.

### شواهد

- پیاده‌سازی محلی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`؛ runtime محافظت‌شده تغییر نکرد.
- آزمون متمرکز ابتدا با نبود UI قرمز شد و پس از پیاده‌سازی نه سناریو پاس شدند: چرخهٔ ساخت/ویرایش/آماده‌سازی و reload، جداسازی پروژه، read-error، شکست نوشتن، مقدار صفر، پاک‌سازی متن اختیاری نامرئی، بازگشت نسخه‌دار به پیش‌نویس همراه شکست نوشتن، شناسهٔ تکراری و رد رکورد آمادهٔ ناقص.
- آزمون کامل: `check:runtime` برای ۲۸ فایل محافظت‌شده، `build`، تست اپ ۵۳ از ۵۳، runtime در اجرای مستقل ۶۱ از ۶۱، Sites چهار از چهار و `git diff --check` پاس شدند.
- بررسی مرورگر داخلی در viewport واقعی `390 × 844`: فرم، حالت ناقص، نسخهٔ ۲ تکمیل‌شده، نسخهٔ ۳ آمادهٔ بازبینی، فهرست و شمارندهٔ فضای پروژه دیده شدند؛ overflow افقی document و root صفر، کنترل‌های اصلی ۴۸ تا ۵۰ پیکسل و console بدون warning/error بود.
- در مشاهدهٔ واقعی، شرایط حمل/مالیات/پرداخت «نامشخص»، وضعیت اشتراک «ارسال نشده»، مرز «این وضعیت تأیید یا مجوز ارسال نیست» و صفرماندن تب «منتظر تأیید» هم‌زمان قابل‌مشاهده بودند.
- دو بازبینی مستقل کد و رابط پس از اصلاح هم‌ترازی پاک‌سازی با parser، گذارهای نسخه‌دار، خطاهای ذخیره و بازگردانی فوکوس، finding مادی باقی‌مانده‌ای گزارش نکردند.
- بازخورد کاربر در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸: با درخواست «بریم تسک بعد»، T6-A و مرزهای محلی/تک‌قلمی/بدون‌ارسال آن تأیید و عبور به برش نسخه‌محور تأیید درخواست مجاز شد.
- بازخورد نهایی کاربر در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸: انتشار این برش همراه T4-C و T6-B با commit، push، merge و publish صریحاً مجاز شد.

---

## T6-B — تأیید داخلی نسخهٔ مشخص درخواست خرید

- **تاریخ:** ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸
- **وضعیت:** تأییدشده توسط کاربر و در release پایهٔ `233e279` منتشرشده
- **دامنه:** ثبت صریح یک نسخهٔ کامل و آمادهٔ بازبینی در صف تأیید پروژه، مشاهدهٔ snapshot همان نسخه و تصمیم انسانی «تأیید نسخه/نیاز به اصلاح»؛ بدون انتخاب تأمین‌کننده یا مقصد، اشتراک داده، RFQ، قیمت، پیام، مجوز ارسال یا اثر بیرونی.

### تجربهٔ واقعی ساخت

- وضعیت `ready-for-review` به‌تنهایی نباید صف «منتظر تأیید» را پر کند. فقط اقدام صریح سازنده، یک رکورد تأیید جدا برای همان `requestId + requestVersion + purpose` می‌سازد؛ کلیک یا reload تکراری رکورد دوم نمی‌سازد.
- تاریخچهٔ رویدادهای T6-A مقادیر فیلدهای نسخه‌های قدیمی را نگه نمی‌دارد. بنابراین Approval فقط با اشاره به شمارهٔ نسخه قابل‌حسابرسی نبود و snapshot تغییرناپذیر نیاز خام، قلم، تحویل، شرایط نامشخص، وضعیت اشتراک و پیش‌نمایش حریم را در مخزن مستقل خود ثبت می‌کند.
- «تأیید نسخه» فقط محتوای همان snapshot را برای ادامهٔ فرایند می‌پذیرد. رکورد همواره `externalEffect=none`، مقصد `null` و `sendAuthorized=false` دارد؛ درخواست همچنان «ارسال نشده» می‌ماند و هیچ Task، شبکه، تأمین‌کننده، قیمت یا تعهدی تغییر نمی‌کند.
- Approval منتظر تصمیم، بازگشت همان نسخه به draft را قفل می‌کند تا snapshot در حال بازبینی بی‌صدا عوض نشود. پس از «نیاز به اصلاح» یا «تأیید»، بازگشت به ویرایش یک اقدام جدا و نسخه‌دار است؛ نسخهٔ تازه Approval تازه می‌خواهد و تصمیم نسخهٔ قبلی به‌عنوان سابقه باقی می‌ماند.
- تصمیم و تغییر درخواست در یک mutation چندمخزنی ادغام نشدند. نوشتن هر مخزن پیش از state انجام می‌شود تا شکست storage نه تصمیم موفق کاذب بسازد و نه درخواست را نیمه‌کاره تغییر دهد.
- parser باید علاوه بر شکل JSON، lineage نسخه را اثبات کند: تاریخچه همیشه با `created` آغاز می‌شود و نسخهٔ هدف باید در تاریخچهٔ همان درخواست واقعاً رویداد `marked-ready-for-review` با زمان برابر `target.updatedAt` داشته باشد. شناسه، tuple، event، snapshot یا تاریخچهٔ تکراری/ناسازگار fail-closed است.
- مخزن Task و Approval مستقل‌اند. خرابی یکی، mutation وابسته را قفل و شمارنده را `!` می‌کند، اما رکورد سالم مخزن دیگر در فیلتر «تمام‌شده» همچنان همراه هشدار بازیابی ناقص دیده می‌شود؛ دادهٔ معتبرِ جزئی همان مخزن خراب نمایش داده نمی‌شود.
- مسیر ساخت Approval عمداً پس از Back کارت صف را نشان می‌دهد تا producer و queue به هم وصل باشند. بازکردن دوبارهٔ Approval موجود از جزئیات درخواست، به همان درخواست و CTA مبدأ برمی‌گردد؛ فوکوس نیز در هر دو مسیر بازیابی می‌شود.
- متن «تأیید داخلی و ارسال نشده» فقط بصری نیست: مرز عدم مجوز ارسال با `aria-describedby` به هر دو CTA تصمیم وصل و در نام دسترس‌پذیر کارت نیز تکرار شد.
- در QA مرورگر یک Approval نمونهٔ pending ساخته شد. سیاست امنیتی مرورگر اجرای مستقیم اسکریپت حذف localStorage را مسدود کرد و UI فعلی نیز لغو/پس‌گرفتن Approval ندارد؛ رکورد محلی بدون اثر بیرونی در پیش‌نمایش باقی ماند و نبود قرارداد cancellation به‌عنوان شکاف ثبت شد.

### شکاف سند مادر

- سند مادر شِمای نسخه‌دار `ApprovalRequest`، purposeهای مختلف، snapshot اجباری، کلید dedupe و state machine `pending / approved / changes-requested` را تعریف نمی‌کند.
- «تأیید داخلی محتوای نسخه» از «تأیید مقصد و payload ارسال بیرونی» به‌صورت رسمی جدا نشده است؛ بدون این تفکیک، واژهٔ تأیید می‌تواند به‌اشتباه مجوز ارسال یا اقدام تجاری برداشت شود.
- رفتار لغو/پس‌گرفتن درخواست تأیید pending، تغییر تصمیم نهایی، بایگانی، retention، orphan شدن target و نمایش تصمیم‌های قدیمی تعیین نشده است.
- نقش `requestedBy/decidedBy` در حساب تک‌کاربرهٔ امروز و تیم چندنقشی آینده، سطح دسترسی، جانشینی، تعارض تصمیم و audit actor روشن نیست.
- سازگاری دو مخزن PurchaseRequest و Approval در چند تب/دستگاه، idempotency هم‌زمان، sync، retry و fallback backend قرارداد اجرایی ندارد.
- سیاست شمارنده‌ها و جایگاه تصمیم‌های نهایی در مرکز کارها مشخص نیست؛ در این پروتوتایپ pendingها در «منتظر تأیید»، تصمیم‌ها در «تمام‌شده» و badge Drawer فقط برای Taskهای فعال باقی مانده است.

### تصمیم‌ها

- **[تصمیم کاری پروتوتایپ، تأییدشده توسط کاربر]** Approval یک شیء مستقل و پروژه‌محور با purpose ثابت `review-purchase-request-version`، target نسخه‌دار، dedupeKey و snapshot غیرقابل‌تغییر است.
- **[تصمیم مرز انسان، تأییدشده توسط کاربر]** `approved` فقط پذیرش داخلی محتوای همان نسخه برای رفتن به مرحلهٔ بعد است و هرگز انتخاب مقصد، اشتراک داده یا مجوز ارسال آینده محسوب نمی‌شود.
- **[تصمیم گذار وضعیت، تأییدشده توسط کاربر]** تنها گذارهای این برش `pending → approved` و `pending → changes-requested` هستند؛ تصمیم نهایی در همان رکورد دوباره باز نمی‌شود.
- **[تصمیم نسخه، تأییدشده توسط کاربر]** pending همان نسخه را از بازگشت به draft بازمی‌دارد؛ پس از تصمیم، ویرایش درخواست اقدام جداست و نسخهٔ جدید باید Approval جدید بگیرد.
- **[تصمیم تجربه، تأییدشده توسط کاربر]** pendingها در صف «منتظر تأیید» و تصمیم‌های نهایی در «تمام‌شده» دیده می‌شوند؛ ساخت اولیه به کارت صف و بازکردن مجدد از درخواست به همان درخواست برمی‌گردد.
- **[تصمیم شکست، تأییدشده توسط کاربر]** مخزن Approval جدا، write-before-state، parser سخت‌گیر، جداسازی پروژه، قفل mutation وابسته و نمایش رکورد سالم مخزن مستقل در بازیابی ناقص الزامی‌اند.
- **[مرز تسک، تأییدشده توسط کاربر]** مقصد، تأمین‌کننده، تطبیق، RFQ، قیمت، پیشنهاد، مقایسه، مذاکره، اعلان، backend، sync، سفارش و هر ارسال بیرونی ساخته نشده‌اند.

### پیشنهاد برای تکمیل سند مادر

- شِمای `ApprovalRequest` و `ApprovalEvent` با purpose، target type/id/version، snapshot، privacy snapshot، requested/decided actor، status، timestamps، version، dedupe و external-effect contract افزوده شود.
- state machine مستقل و متصل برای `PurchaseRequestVersionApproval` و `ExternalSendApproval` نوشته شود؛ دومی فقط با مقصد، گیرندگان، payload دقیق، نسخه، پیش‌نمایش حریم و کلید جلوگیری از ارسال تکراری معتبر باشد.
- الزام lineage نسخه، snapshot غیرقابل‌تغییر، رفتار نسخهٔ تازه، لغو pending، اصلاح تصمیم، retention/orphan و سیاست audit در معیار پذیرش رسمی شود.
- قرارداد تیمی نقش درخواست‌کننده/تأییدکننده، سطح دسترسی و تعارض، همراه با backend/sync/idempotency/fallback پیش از عملیاتی‌شدن تعریف شود.
- سیاست معماری اطلاعات و شمارندهٔ صف تأیید در Drawer، مرکز کارها، جزئیات درخواست و گزارش پروژه به‌صورت محصولی نهایی شود.

### شواهد

- پیاده‌سازی محلی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`؛ runtime محافظت‌شده تغییر نکرد.
- آزمون‌های T6-B ساخت/تصمیم نسخهٔ دقیق، snapshot و عدم اثر بیرونی، نیاز به اصلاح و نسخهٔ تازه، dedupe، شکست ایجاد/تصمیم، read-error مستقل، نمایش سالم در بازیابی ناقص، فساد snapshot/tuple/history/lineage و جداسازی پروژه را پوشش می‌دهند.
- بررسی مرورگر داخلی در viewport واقعی `390 × 844`: جزئیات نسخهٔ ۵، مرز عدم مجوز ارسال، snapshot، حریم، تاریخچه و دو CTA دیده شدند؛ view دقیقاً ۳۹۰×۸۴۴، overflow افقی صفر، دکمه‌ها ۵۰ پیکسل و عنوان جزئیات فوکوس‌شده بود.
- دو دور بازبینی مستقل محصول، کد و رابط، دو نقص P1 parser، کپی قدیمی «بدون تأیید»، مرز دسترس‌پذیری، بازگشت مبدأ و پنهان‌شدن دادهٔ سالم در read-error جزئی را یافتند؛ همه با تست regression اصلاح شدند.
- اعتبارسنجی نهایی: `check:runtime` برای ۲۸ فایل محافظت‌شده، `build`، تست اپ ۶۴ از ۶۴، runtime در اجرای مستقل ۷۲ از ۷۲، Sites چهار از چهار و `git diff --check` پاس شدند؛ بازبینی نهایی مستقل محصول، کد و رابط finding مادی باقی‌مانده‌ای گزارش نکرد.
- بازخورد کاربر در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸: T6-B و مرز «تأیید داخلیِ نسخه، نه مجوز ارسال» تأیید شد و انتشار آن همراه T4-C و T6-A با commit، push، merge و publish صریحاً مجاز شد.

---

## T6-B2 — شِمای چندقلمی محصول، خدمت مستقل و روشن‌سازی قطعی

- **تاریخ:** ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸
- **وضعیت:** پیاده‌سازی و آزمون کامل؛ در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸ توسط کاربر تأیید و در release `bd766bc` منتشر شد
- **دامنه:** محصول یک تا هشت قلمی و خدمت با شِمای مستقل، unknown صریح، پرسش روشن‌سازی فقط برای فیلد اثرگذارِ ناقص، lineage هر قلم/خدمت/پاسخ و snapshot بازبینی هر revision؛ بدون attachment، استخراج خودکار، AI، مقصد یا اثر بیرونی.

### تجربهٔ واقعی ساخت

- ساخت رکورد سؤال برای همهٔ فیلدها، حتی فیلدهای از ابتدا تکمیل‌شده، با قرارداد «سؤال حداقلی از کمبود واقعی» تعارض داشت. سؤال اکنون فقط نخستین‌بار هنگام `null/unknown` بودن فیلد اثرگذار ساخته می‌شود؛ اگر کاربر بعداً همان فیلد را تکمیل کند، همان پاسخ با نسخه و رویداد `updated` برای تاریخچه باقی می‌ماند.
- fallback خودکار محدودهٔ تحویل از location پروژه، unknown صریح را ناممکن و منشأ «ثبت مستقیم شما» را نادرست می‌کرد. فرم تازه محدوده را خالی آغاز می‌کند؛ خالی‌ماندن به «نامشخص» معتبر تبدیل و سؤال قطعی آن با پاسخ `null` ثبت می‌شود، نه اینکه مقدار پروژه حدس زده شود.
- چندقلمی‌کردن بدون حذف قلم ذخیره‌شده، کاربر را پس از یک ثبت اشتباه در draft ناقص دائمی حبس می‌کرد. قلم فعال اکنون در ویرایش قابل حذف است، نسخهٔ درخواست جلو می‌رود و snapshot revision قدیمی اقلام همان زمان را نگه می‌دارد.
- نسخه و تاریخچه فقط با شمارنده معتبر نمی‌شوند. parser اکنون یکتایی شناسهٔ قلم و clarification را در snapshot جاری و تاریخی، گذار `created → updated`، رابطهٔ قابل‌دستیابی source/actor، پوشش دقیق ready revision، allowlist حریم و تطابق snapshot Approval با revision را fail-closed کنترل می‌کند.
- fingerprint بازبینی برای تشخیص ناسازگاری داخلی از serialization مرتب و FNV-1a ساخته می‌شود؛ این مقدار امضای امنیتی یا شاهد اصالت بیرونی نیست و فقط همراه parser و snapshot دقیق، consistency محلی را کنترل می‌کند.
- مهاجرت v1 باید در هر reload قطعی باشد. clarificationها و revision مهاجرتی شناسهٔ پایدار می‌گیرند؛ Approval دقیقِ نسخهٔ جاری و Approval تصمیم‌گرفتهٔ تاریخی نیز از snapshot قدیمی خودش به revision مهاجرتی تبدیل می‌شود تا دادهٔ سالم پس از جلو رفتن درخواست، مخزن را قفل نکند.
- خدمت همان پوستهٔ درخواست را استفاده می‌کند اما `items=[]` و شناسنامهٔ مستقل ده‌فیلدی دارد. سطح مکان با metadata ثابت `area-or-project-section` و متن روشن «بدون آدرس دقیق» حمل می‌شود؛ محصول و خدمت نمی‌توانند هم‌زمان در یک رکورد معتبر حاضر باشند.
- save بدون تغییر، نسخه یا history درخواست، قلم و clarification را جلو نمی‌برد. نوع محصول/خدمت پس از ساخت ثابت می‌ماند و confidence تا نبود استخراج واقعی همیشه `null/not-applicable` است.

### شکاف سند مادر

- سند مادر نوع `PurchaseRequest` مشترک و تفاوت رسمی `ProductRequestItem[]` با `ServiceRequestSpec`، سقف قلم، حذف قلم و نسبت snapshotهای تاریخی با اقلام فعال را تعریف نمی‌کند.
- فهرست فیلدهای «اثرگذار»، ترتیب سؤال‌ها، چرخهٔ `explicitly-unknown / needs-confirmation / answered` و اینکه پاسخ روشن‌سازی چه زمانی ساخته یا تاریخی می‌شود، قرارداد محصولی ندارد.
- unknown در لایهٔ فیلد، نمایش، payload و مقایسهٔ آینده یک نمایش canonical واحد ندارد؛ تفاوت «ثبت نشده»، «نامشخص صریح» و «نیازمند تأیید پس از migration» باید رسمی شود.
- سطح مجاز مکان خدمت و تحویل، کنترل آدرس دقیق، منشأ هر فیلد و allowlist قابل‌اشتراک در هر مرحلهٔ جریان هنوز به schema و معیار پذیرش رسمی نیاز دارد.
- migration snapshotهای قدیمی، اعتبار fingerprint غیرامنیتی، retention revision، حذف/بایگانی قلم و رفتار Approval تاریخی در سند مادر مشخص نشده‌اند.

### تصمیم‌ها

- **[تصمیم کاری پروتوتایپ، تأییدشده توسط کاربر]** محصول یک تا هشت قلم فعال دارد؛ هر قلم منشأ، completion، confidence تهی، نسخه، زمان و history مستقل دارد و حذف قلم یک تغییر نسخه‌دار درخواست است.
- **[تصمیم کاری پروتوتایپ، تأییدشده توسط کاربر]** خدمت ده فیلد مستقل دامنه، موقعیت مجاز، اندازه/حجم، صلاحیت، زمان، روش، داخل/خارج دامنه، ضمانت اعلامی و پرداخت دارد؛ سطح location فقط محدوده یا بخش پروژه است.
- **[تصمیم روشن‌سازی، تأییدشده توسط کاربر]** مجموعه و ترتیب سؤال‌ها deterministic است، اما رکورد سؤال فقط برای فیلد اثرگذارِ ناقص ساخته می‌شود؛ پاسخ بعدی همان lineage را به نسخهٔ تازه می‌برد و هیچ مقدار ساختگی یا confidence عددی تولید نمی‌شود.
- **[تصمیم نسخه و حریم، تأییدشده توسط کاربر]** هر ready version یک `reviewRevision` با snapshot، allowlist دقیق و fingerprint consistency دارد؛ Approval فقط با همان revision معتبر است و تغییر نسخه، Approval قدیمی را برای نسخهٔ جاری معتبر نمی‌کند.
- **[تصمیم سازگاری، تأییدشده توسط کاربر]** store key قدیمی برای سازگاری باقی مانده اما رکورد تازه `schemaVersion=2` است؛ داده و Approval سالم v1 جاری یا تاریخی در حافظه به v2 مهاجرت می‌کنند و دادهٔ ناسازگار همچنان read-error و قفل mutation می‌سازد.
- **[مرز تسک، تأییدشده توسط کاربر]** attachment، عکس/فایل، OCR یا استخراج، مدل، تأمین‌کننده، مقصد، تطبیق، قیمت، RFQ، ارسال، backend و sync در T6-B2 ساخته نشده‌اند.

### بازخورد و عبور از T6-B2

- کاربر در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸ پس از مشاهدهٔ نسخهٔ لوکال در `390 × 844` گفت «خوبه ادامه بده»؛ این بازخورد تأیید T6-B2 و اجازهٔ شروع فقط T6-C محسوب می‌شود.
- T6-C باید گیرندهٔ محلی، دعوتِ بدون ارسال، دلیل تطبیق و پیش‌نمایش دقیق payload را بسازد و همچنان هیچ حساب تأمین‌کننده، شبکه، احراز، تضمین، ارسال یا اثر بیرونی ادعا نکند.

### پیشنهاد برای تکمیل سند مادر

- union نسخه‌دار `ProductPurchaseRequest | ServiceRequest`، lifecycle قلم فعال/حذف‌شده، فیلدهای اجباری و optional و canonical unknown برای هر نوع تعریف شود.
- `ClarificationQuestion/Answer` با trigger فیلد ناقص، اولویت اثر، status، source، confidence applicability، history و رابطهٔ دقیق با field version رسمی شود.
- قرارداد `ReviewRevision` و `ApprovalRequest` شامل snapshot تاریخی، allowlist حریم، invalidation نسخه، migration، retention و تأکید بر غیرامنیتی‌بودن checksum نوشته شود.
- taxonomy سطح مکان `city / district-or-area / project-section / exact-address` و gate رضایت برای آدرس دقیق پیش از T6-C/T6-D به معیار پذیرش اضافه شود.

### شواهد

- پیاده‌سازی محلی: `prototype/src/Prototype.tsx` و `prototype/src/prototype.css`؛ تست‌های regression در `prototype/tests/chida-flow.spec.ts`؛ ۲۸ فایل runtime محافظت‌شده تغییر نکردند.
- آزمون هدفمند T6-B2 هفت از هفت و مجموعهٔ گستردهٔ purchase request/Approval بیست‌وهفت از بیست‌وهفت پاس شدند؛ سناریوها محصول دو‌قلمی، خدمت مستقل، unknown و پاسخ نسخه‌دار، حذف قلم، no-op، مهاجرت v1 جاری/تاریخی، دو reload، دست‌کاری هماهنگ revision/Approval، شناسه و history ناممکن و جداسازی پروژه را پوشش می‌دهند.
- اعتبارسنجی نهایی: `check:runtime` برای ۲۸ فایل، `build`، تست اپ ۷۱ از ۷۱، runtime ۷۹ از ۷۹، Sites چهار از چهار و `git diff --check` پاس شدند؛ فقط هشدار شناخته‌شدهٔ chunk بزرگ‌تر از 500kB در build باقی است.
- بررسی مرورگر داخلی روی مبدأ تازه و viewport واقعی `390 × 844`: محصول دو‌قلمی با ۹ clarification واقعاً ناقص و تحویل «نامشخص»، خدمت مستقل با ۷ clarification ناقص و metadata سطح مکان دیده شدند؛ عرض document و هر دو detail برابر ۳۹۰، overflow افقی صفر و console بدون warning/error بود.
- سه بازبینی مستقل schema، invariant و seam، نقص‌های سؤال اضافی، fallback حدسی تحویل، snapshot تاریخی، allowlist، migration قطعی، حذف قلم، touch target و lineage ناممکن را یافتند؛ همه پیش از اجرای نهایی اصلاح شدند و بازبینی پایانی finding مادی باقی‌مانده‌ای گزارش نکرد.
- وضعیت این شاهد در زمان تحویل برش: HEAD محلی `233e2797582895c3ec4901fe3f17900bb5373196` بود و T6-B2 هنوز فقط در working tree قرار داشت. پس از تأیید کاربر، همین برش همراه T6-C/T6-D در release `bd766bc` منتشر شد.

---

## T6-C — گیرندهٔ محلی و پیش‌نمایش دقیق DispatchDraft

- **تاریخ:** ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸
- **وضعیت:** پیاده‌سازی کامل و با اصلاحات تجربه و T6-D یکپارچه؛ بازآزمایی و QA نهایی پاس؛ در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸ توسط کاربر تأیید و در release `bd766bc` منتشر شد
- **دامنه:** `SupplierContact` خصوصی پروژه، انتخاب دستی از revision جاریِ دقیقاً تأییدشده، payload allowlist، `InviteDraft` و `DispatchDraft` نسخه‌دار؛ بدون حساب تأمین‌کننده، شبکه، تطبیق هوشمند، ارسال، قیمت یا اثر بیرونی.

### تجربهٔ واقعی ساخت

- دسته و محدودهٔ اعلامی برای توضیح انتخاب مفیدند، اما معیار قطعی سازگاری نیستند. در این برش فقط توان پاسخ `product / service / both` gate قطعی است و انتخاب نهایی را سازنده دستی انجام می‌دهد؛ رابط صریحاً نبود match و رتبه‌بندی را نشان می‌دهد.
- اگر رکورد گیرنده بعد از ذخیرهٔ Draft سخت حذف یا فیلدهای هویتی‌اش ویرایش شود، snapshot تاریخی مقصد یا unreadable می‌شود یا معنای گذشته تغییر می‌کند. چرخهٔ T6-C بنابراین فقط ساخت و آرشیو/بازیابی دارد؛ snapshot هر `InviteDraft` مقصد همان revision را نگه می‌دارد و UI وضعیت آرشیو فعلی را جدا نشان می‌دهد.
- حذف نام پروژه و «فیلد آدرس دقیق» برای ادعای privacy کافی نیست؛ `delivery.area` و `service.location` متن آزادند و می‌توانند عملاً نشانی دقیق داشته باشند. پیش‌نمایش اکنون این مقادیر را آشکار می‌کند و هشدار می‌دهد بازبینی دستی لازم است و پاک‌سازی معنایی ادعا نمی‌شود.
- payload نباید از متن خام یا state جاری فرم بازسازی شود. projector فقط snapshot دقیق revision تأییدشده را به شِمای canonical محصول/خدمت تبدیل می‌کند؛ allowlist نمایش‌داده‌شده دقیقاً با مسیرهای payload ذخیره‌شده برابر است.
- version کردن Dispatch بدون دسترسی به revision تاریخی ارزش حسابرسی را کم می‌کرد. selector فقط‌خواندنی revision اضافه شد تا مقصدهای گذشته، snapshot دعوت و وضعیت آرشیو فعلی قابل‌دیدن باشند؛ save بدون تغییر مجموعهٔ گیرندگان no-op است.
- حذف بی‌صدای event یا revision خراب در parser می‌تواند mutation بعدی را روی تاریخچهٔ ناقص بنویسد. parser اکنون طول raw و parsed را دقیقاً مقایسه می‌کند؛ هر عضو نامعتبر کل مخزن وابسته را read-error و write-locked می‌کند.
- FNV-1a برای تشخیص ناسازگاری تصادفی مفید است، اما tamper-proof نیست. اعتبار محلی از بازسازی canonical payload، تطابق Approval/revision و کنترل تمام dependencyها می‌آید؛ fingerprint فقط checksum consistency است.

### شکاف سند مادر

- سند مادر قرارداد دادهٔ `SupplierContact` خارج از شبکه، مرز حساب واقعی تأمین‌کننده و سیاست edit/archive/delete آن را تعریف نمی‌کند.
- نسبت «دسته/پوشش اعلامی» با capability قطعی، انتخاب دستی، match هوشمند آینده و ممنوعیت رتبه‌بندی پولی به معیار پذیرش شِمای رسمی نیاز دارد.
- allowlist اشتراک برای محصول و خدمت، نمایش pathهای دقیق، taxonomy مکان آزاد و gate آدرس دقیق در سند مادر schema نشده‌اند.
- lifecycle و retention `InviteDraft/DispatchDraft`، snapshot مقصد، revision تاریخی، no-op، checksum غیرامنیتی و رفتار read-error وابسته قرارداد رسمی ندارند.
- مرز دقیق میان Approval محتوای درخواست، ذخیرهٔ Draft مقصد و Approval مستقل برنامهٔ ارسال T6-D باید در مدل دامنه رسمی شود.

### تصمیم‌ها

- **[تصمیم کاری T6-C، تأییدشده توسط کاربر]** فقط exact current revision با Approval `approved` و snapshot یکسان وارد انتخاب گیرنده می‌شود؛ pending یا stale approval مسیر را باز نمی‌کند.
- **[تصمیم گیرنده، تأییدشده توسط کاربر]** `SupplierContact` project-scoped، خصوصی و صرفاً «ثبت مستقیم سازنده» است؛ حساب، عضویت شبکه، احراز یا تضمین کیفیت نیست. در T6-C ویرایش/حذف سخت ندارد و فقط آرشیو/بازیابی می‌شود.
- **[تصمیم انتخاب، تأییدشده توسط کاربر]** capability gate قطعی است؛ دسته و پوشش دلیل شفاف دادهٔ اعلامی‌اند و انتخاب دستی باقی می‌ماند. هیچ بهترین‌بودن، match هوشمند یا اولویت پولی ادعا نمی‌شود.
- **[تصمیم payload و حریم، تأییدشده توسط کاربر]** payload فقط از projector allowlist روی snapshot تأییدشده می‌آید؛ نام پروژه، بودجه، فایل، حافظه، متن خام و clarification حذف‌اند. فیلد آزاد مکان دیده می‌شود و تا ساخت policy معنایی نیازمند بازبینی دستی است.
- **[تصمیم نسخه و حسابرسی، تأییدشده توسط کاربر]** `DispatchDraft` و revisionهای مقصد/payload/invite پروژه‌محور و فقط‌خواندنیِ تاریخی‌اند؛ no-op نسخه نمی‌سازد و هر read-error وابسته mutation را قفل می‌کند.
- **[مرز اثر بیرونی، تأییدشده توسط کاربر]** همهٔ Invite/Dispatchها `externalEffect=none` و `sendAuthorized=false` هستند؛ هیچ درخواست شبکه، حساب تأمین‌کننده، پیام، قیمت، RFQ، status ارسال یا mutation بیرونی در T6-C وجود ندارد.

### پیشنهاد برای تکمیل سند مادر

- schema رسمی `SupplierContact` با source/networkStatus/capability، immutable fields، archive/restore، privacy scope و مرز تبدیل احتمالی به هویت واقعی تأمین‌کننده تعریف شود.
- قرارداد `DispatchPayload` برای product/service با مسیرهای allowlist، canonical unknown، location policy، رضایت آدرس دقیق و privacy omissions رسمی شود.
- `InviteDraft` و `DispatchDraftRevision` با snapshot مقصد، نسخه، history، retention، dependency validation، no-op و معنای صرفاً consistency برای checksum تعریف شوند.
- Approval محتوای درخواست از Approval محلی برنامهٔ ارسال و از مجوز/اقدام بیرونی آینده سه شیء جدا باشد؛ T6-D فقط دومین مورد را به‌صورت شبیه‌سازی محلی می‌سازد.

### شواهد

- پیاده‌سازی محلی در `prototype/src/Prototype.tsx` و `prototype/src/prototype.css` و regressionها در `prototype/tests/chida-flow.spec.ts` انجام شد؛ runtime محافظت‌شده و سند مادر تغییر نکردند.
- دوازده تست هدفمند T6-C محصول و خدمت، exact approval، payload/allowlist و privacy، جداسازی پروژه، نسخه/no-op، آرشیو و تاریخچه، دست‌کاری هماهنگ، عضو خراب history/revision، duplicate، rollback و خطای `getItem` را پوشش می‌دهند و همگی پاس‌اند.
- اعتبارسنجی نهایی: `check:runtime` برای ۲۸ فایل، `build`، تست اپ ۸۳ از ۸۳، runtime ۹۱ از ۹۱، Sites چهار از چهار، `npx tsc --noEmit` و `git diff --check` پاس شدند؛ فقط هشدار شناخته‌شدهٔ chunk بزرگ‌تر از 500kB باقی است.
- مرورگر داخلی در `390 × 844` مسیر exact request version 2، گیرندهٔ محلی، ۱۷ path payload، هشدار مکان آزاد و Draft نسخهٔ ۱ با `none/false` را پس از reload نشان داد؛ overflow افقی و خطای console صفر بود.
- بازبینی مستقل نهایی نقص fail-closed روی عضو خراب history/revision را یافت؛ مقایسهٔ طول raw/parsed و سه تست regression اضافه شد و در بازبینی بعد از اصلاح blocker دیگری باقی نماند.
- وضعیت این شاهد در زمان تحویل برش: HEAD محلی هنوز روی commit پایه بود و T6-B2/T6-C در working tree قرار داشتند. پس از تأیید کاربر، T6-B2/T6-C همراه اصلاحات تجربه و T6-D در release `bd766bc` منتشر شدند.

---

## T6-D — تأیید مستقل محلی برنامهٔ ارسال

- **تاریخ:** ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸
- **وضعیت:** پیاده‌سازی محلی کامل؛ بازآزمایی و QA نهایی یکپارچه پاس؛ در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸ توسط کاربر تأیید و در release `bd766bc` روی GitHub، Cloudflare Pages و ChatGPT Sites نسخهٔ ۶ منتشر شد
- **دامنه:** Approval مستقل و خصوصی برای برنامهٔ مقصد/payload یک `DispatchDraftRevision` جاری، چرخهٔ تصمیم محلی، snapshot و invalidation مشتق‌شده؛ بدون ارسال، شبکه، حساب تأمین‌کننده، قیمت، پیام یا اثر بیرونی.

### تجربهٔ واقعی ساخت

- Approval محتوای درخواست فقط می‌گوید همان revision برای ادامهٔ فرایند پذیرفته شده است؛ استفاده از همان شیء برای مقصد و payload، دو تصمیم انسانی متفاوت را مخلوط و audit را مبهم می‌کرد. `DispatchPlanApproval` بنابراین store، purpose، target، dedupe/idempotency و history مستقل دارد.
- اعتبار تاریخی با «جاری‌بودن» یکی نیست. parser باید exact بودن dependency تاریخی، snapshot و fingerprint را fail-closed کنترل کند، اما تغییر revision جاری نباید رکورد سالم قبلی را به corruption تبدیل کند. وضعیت مؤثر `invalidated` از رابطهٔ رکورد با request، Content Approval، Dispatch revision و نسخه/فعال‌بودن گیرنده‌های جاری مشتق می‌شود.
- snapshot برنامه از فرم یا state چت بازسازی نمی‌شود. مقصدهای `InviteDraft`، شناسه/نسخهٔ گیرنده، payload و privacy همان revision کپی می‌شوند؛ fingerprint برنامه checksum سازگاری محلی است و امضای امنیتی یا مجوز اقدام نیست.
- یک pending بدون امکان پس‌گرفتن، کاربر را در تصمیم نیمه‌کاره حبس می‌کرد. چرخهٔ صریح `pending → withdrawn → reopened → pending → approved` با نسخه و history ساخته شد؛ رکورد invalidated تاریخی فقط‌خواندنی است و هیچ تصمیم تازه‌ای روی آن اعمال نمی‌شود.
- بازکردن جزئیات تأیید، مقصدها، تعداد، payload، حذف‌های حریم و هشدار بازبینی location را پیش از درخواست Approval نشان می‌دهد. acknowledgement فقط بازبینی محلی همین snapshot است و رضایت ارسال یا تأیید طرف دوم نیست.
- خطای خواندن یک dependency، snapshot یا history ناسازگار و duplicate شناسه/dedupe/idempotency مخزن Approval برنامه را write-locked می‌کند. شکست نوشتن در ایجاد یا تصمیم، state قبلی را حفظ می‌کند و موفقیت کاذب نشان نمی‌دهد.
- زمان درخواست Approval باید پس از ساخت Dispatch revision، Review revision، تصمیم Content Approval و نسخهٔ snapshotشدهٔ هر تماس باشد. این chronology مانع آن است که دست‌کاری هماهنگ نسخه و checksum یک Approval قدیمی را برای dependency آینده معتبر جلوه دهد.
- ثبت موفق فقط یک `actionRecord` محلی با عنوان «تأیید محلی برنامهٔ ارسال» می‌سازد. `simulationOnly=true`، `externalEffect=none`، `sendAuthorized=false` و `externalActionAttempted=false` ثابت‌اند؛ کلید یا وضعیت `sent`، `sentAt` و receipt در قرارداد وجود ندارد.

### شکاف سند مادر

- سند مادر مرز سه شیء مستقل «Approval محتوای درخواست»، «Approval محلی برنامهٔ مقصد/payload» و «مجوز/اقدام ارسال واقعی آینده» را schema نکرده است.
- lifecycle رسمی pending/withdraw/reopen/approve، invalidation ناشی از تغییر dependency، retention سابقه و امکان تصمیم دوباره روی revision تازه تعریف نشده‌اند.
- snapshot مقصد، نسخهٔ هویت محلی، allowlist payload/privacy، acknowledgement location، dedupe/idempotency و معنای checksum معیار پذیرش رسمی ندارند.
- قرارداد ثبت نتیجه/خطای اقدام محلی و تفاوت آن با delivery result بیرونی یا receipt آینده در سند مادر تصریح نشده است.

### تصمیم‌ها

- **[تصمیم استقلال، تأییدشده توسط کاربر]** `DispatchPlanApproval` از `ProjectApproval` محتوای درخواست جدا و project-scoped است؛ ساخت یکی دیگری را تغییر نمی‌دهد و هیچ‌کدام مجوز ارسال واقعی نیستند.
- **[تصمیم exact/current، تأییدشده توسط کاربر]** فقط revision جاری با request و Content Approval exact و گیرنده‌های فعال/سازگار می‌تواند Approval تازه یا تصمیم تازه بگیرد؛ رکورد تاریخی exact حفظ و وضعیت نامعتبر آن مشتق می‌شود.
- **[تصمیم snapshot، تأییدشده توسط کاربر]** مقصد، نسخهٔ گیرنده، payload و privacy فقط از `DispatchDraftRevision` همان target snapshot می‌شوند؛ فرم، متن خام، چت، فایل، حافظه و نام پروژه وارد برنامه نمی‌شوند.
- **[تصمیم چرخه، تأییدشده توسط کاربر]** pending قابل پس‌گرفتن و withdrawn قابل بازگشایی است؛ approved فقط ثبت محلی است و invalidated تاریخی terminal و فقط‌خواندنی می‌ماند.
- **[مرز اثر بیرونی، تأییدشده توسط کاربر]** نتیجهٔ T6-D فقط شبیه‌سازی محلی است؛ هیچ شبکه، پیام، RFQ، وضعیت ارسال، رسید، زمان ارسال یا mutation بیرونی وجود ندارد. اقدام واقعی آینده Approval و idempotency مستقل تازه می‌خواهد.

### پیشنهاد برای تکمیل سند مادر

- سه لایهٔ Approval با purpose، target tuple، snapshot، lifecycle، invalidation، actor، retention و مرز اثر بیرونی به‌صورت رسمی تعریف شوند.
- `DispatchPlanSnapshot` و allowlist حریم/مکان همراه سیاست تغییر نسخهٔ گیرنده و revision جدید schema شوند.
- نتیجهٔ local simulation از command/result/error و delivery receipt بیرونی جدا شود و ممنوعیت استفاده از واژه‌ها یا stateهای «ارسال‌شده» پیش از شبکهٔ واقعی به معیار پذیرش افزوده شود.

### شواهد جاری

- پیاده‌سازی محلی در `prototype/src/Prototype.tsx` و `prototype/src/prototype.css` و سناریوهای regression در `prototype/tests/chida-flow.spec.ts` قرار دارد؛ سند مادر و runtime محافظت‌شده تغییر نکرده‌اند.
- پنج سناریوی اختصاصی T6-D چرخهٔ create/withdraw/reopen/approve، snapshot و boundary بدون شبکه، invalidation پس از revision تازه، تاریخچهٔ فقط‌خواندنی، rollback شکست نوشتن، فساد fail-closed و Approval زمانی ناممکن را پوشش دادند و پاس شدند.
- اعتبارسنجی نهایی: `check:runtime` برای ۲۸ فایل، build شامل TypeScript، اپ ۹۳/۹۳، runtime ۱۰۱/۱۰۱، Sites ۴/۴ و `git diff --check` پاس شدند؛ فقط هشدار شناخته‌شدهٔ chunk بزرگ‌تر از 500kB باقی است.
- QA مرورگر داخلی در `390 × 844` درخواست ساده، payload هفده‌فیلدی، target دقیق و چرخهٔ `pending → withdrawn → reopened → approved` را نشان داد؛ پس از هر تصمیم focus روی status برگشت، عرض سند ۳۹۰، overflow سند و status صفر و warning/error تازهٔ console صفر بود. reload و ماندگاری در regression خودکار پوشش داده شد.
- بازبینی مستقل پس از اصلاح جداسازی chat، commit پروژه، chronology، focus و overflow هیچ finding باقی‌ماندهٔ P0 تا P2 گزارش نکرد.
- وضعیت انتشار: release یکپارچه با commit `bd766bc91f25a082cf4fe544613855b1ee22cc5c` روی GitHub `main`، Cloudflare Pages deployment `c24d07c7-1a5a-4fe1-b1fb-6b7374dc7faf` و ChatGPT Sites نسخهٔ ۶/deployment `appgdep_6a91cf323ea0819195175f7ed1ceb286` با موفقیت منتشر و same-source بودن آن تأیید شد.
- بازخورد پس از QA: کاربر نتیجهٔ یکپارچه را تأیید کرد، انتشار همان نسخه روی GitHub/Cloudflare/Sites را مجاز دانست و شروع T7-A را به پس از موفقیت انتشار موکول کرد.

---

## T7-A — صندوق خصوصی پیشنهادهای ثبت‌شده توسط سازنده

- **تاریخ:** ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸
- **وضعیت:** پیاده‌سازی و QA لوکال کامل و توسط کاربر تأیید شد؛ release `bf59df1b82f3886794115647920b834c5e9c74dd` روی GitHub `main`، Cloudflare Pages و ChatGPT Sites نسخهٔ ۷ منتشر و same-source راستی‌آزمایی شد؛ کاربر نسخه را مشاهده کرد و اصلاحات C-010 را خواست، اما شروع T7-B را صریحاً تأیید نکرده است
- **دامنه:** ثبت دستی و پروژه‌محور پیشنهاد تأمین‌کنندهٔ فعلی توسط سازنده، با اصل/مرجع جدا از رونویسی ساختاریافته و unknown صریح؛ بدون پاسخ واقعی تأمین‌کننده، شبکه، استخراج، مقایسه، نرمال‌سازی، توصیه، انتخاب برنده، سفارش یا اثر بیرونی.

### سابقهٔ قرارداد ثبت‌شده پیش از ساخت

- شیء این برش «پیشنهاد ثبت‌شده توسط سازنده» است، نه `Proposal` رسمی پروندهٔ مشترک آینده. منشأ ثابت «ثبت دستی سازنده»، حریم «خصوصی پروژه»، `supplierAuthenticated=false`، `receivedThroughChida=false` و `externalEffect=none` باید در داده و UI صریح بمانند.
- target باید به snapshot دقیق درخواست تأییدشده و قلم‌های همان revision متصل شود. محصول و خدمت مسیرهای مستقل دارند؛ پیشنهاد ماندگار با T6-D شرط نمی‌شود، چون T6-D ارسال یا دریافت واقعی را اثبات نمی‌کند.
- اصل/مرجع فقط یکی از «شناسنامهٔ فایل محلی همین پروژه» یا «بدون فایل» است. برای فایل غیرتصویری، محتوای فایل نگه‌داری یا خوانده نشده و هیچ extraction ادعا نمی‌شود. متن رونویسی‌شدهٔ سازنده از فیلدهای ساختاریافته جداست.
- قیمت، مقدار، واحد، مالیات، حمل، حداقل سفارش، موعد، اعتبار و شرایط پرداخت باید unknown معتبر داشته باشند؛ رشتهٔ خالی یا صفر نباید به مقدار اعلامی ساختگی تبدیل شود.
- هر تغییر معنایی revision تازه و history می‌سازد؛ no-op نسخه نمی‌سازد. تغییر بعدی درخواست یا آرشیو تماس، رکورد را تاریخی/نیازمند بازبینی می‌کند و target آن را بازنویسی نمی‌کند. اصلاح رونویسی همان رکورد تاریخی مجاز است و revision تازه را همچنان روی target قدیمی می‌افزاید؛ این ویرایش هرگز آن را دوباره «جاری» یا به dependency تازه rebind نمی‌کند.
- fixture چندپیشنهادی در این برش ساخته نمی‌شود. T7-B نرمال‌سازی/مقایسه و هر ادعای «بهترین/برنده» را جداگانه و فقط پس از تأیید T7-A بررسی می‌کند.

### سابقهٔ شکاف‌های سند مادر شناسایی‌شده پیش از ساخت

- سند مادر شیء رسمی `Proposal` پروندهٔ مشترک را تعریف می‌کند، اما قرارداد موقتِ ثبت دستی سازنده پیش از وجود مسیر تأمین‌کننده، تفاوت آن با پاسخ احرازشده و سیاست ارتقای احتمالی آن در آینده را schema نکرده است.
- نسبت اصل فایل، metadata محلی، رونویسی کاربر و استخراج آینده، همراه با immutable بودن اصل و version کردن دادهٔ ساختاریافته، معیار پذیرش رسمی ندارد.
- unknownهای مالی/زمانی پیشنهاد و رفتار رکورد تاریخی پس از تغییر request/contact در سند مادر جزئیات اجرایی کافی ندارند.

### تصمیم‌های کاری ثبت‌شده در آغاز T7-A

- **[تصمیم صداقت داده، تأییدشده برای انتشار T7-A]** نام محصولی «پیشنهاد ثبت‌شده توسط شما» است؛ UI و storage هیچ واژه یا وضعیت «دریافت‌شده از تأمین‌کننده»، «ارسال‌شده» یا «پاسخ داخل چیدا» نمی‌سازند.
- **[تصمیم target، تأییدشده برای انتشار T7-A]** فقط درخواست exact و تأییدشدهٔ همین پروژه و تماس محلی فعال/سازگار برای رکورد تازه قابل انتخاب‌اند؛ snapshot تاریخی پس از تغییر dependency قابل‌خواندن و رونویسی آن versionable می‌ماند، اما status همچنان «نیازمند بازبینی» است و rebinding خاموش ممنوع می‌ماند.
- **[تصمیم اصل/رونویسی، تأییدشده برای انتشار T7-A]** reference، متن رونویسی و structured values سه لایهٔ جدا هستند؛ `contentPersisted=false` و `extractionPerformed=false` برای مرجع فایل ثابت می‌مانند.
- **[تصمیم نسخه و شکست، تأییدشده برای انتشار T7-A]** storage مستقل، parser fail-closed، write-before-state، rollback شکست و جداسازی پروژه الزامی‌اند؛ read-error mutation همان صندوق را قفل می‌کند.
- **[مرز دامنه، تأییدشده برای انتشار T7-A]** T7-A هیچ مقایسه، تعدیل، محاسبه، رتبه‌بندی، توصیه، پیام، network request یا تغییر در request/dispatch/chat نمی‌سازد.

### تجربهٔ مشاهده‌شده پس از ساخت

- ورودی «بررسی پیشنهادها» وقتی فقط draft متن می‌ساخت با معنای واقعی صندوق تعارض داشت؛ اکنون Quick Action و ردیف فضای پروژه هر دو همان صندوق خصوصی را باز می‌کنند و مسیر بازگشت focus را به مبدأ مناسب برمی‌گرداند.
- سه لایهٔ «شناسنامهٔ مرجع»، «رونویسی خام سازنده» و «مقادیر ساختاریافتهٔ دستی» در UI و schema جدا شدند. فایل غیرتصویری همچنان فقط metadata است و `contentPersisted=false` و `extractionPerformed=false` باقی می‌مانند.
- مقدارهای تجاری خالی به `null` صریح تبدیل می‌شوند. وضعیت «ذکر نشده» نمی‌تواند هم‌زمان مقدار تجاری غیرتهی داشته باشد؛ اعداد بزرگ و اعشاری با canonicalization رشته‌ای ذخیره می‌شوند تا گردکردن یا notation نمایی، reload را خراب نکند.
- proposal تازه فقط از dependencyهای exact و معتبر ساخته می‌شود. parser eligibility را در زمان ایجاد بازسازی می‌کند؛ رکورد سالم ساخته‌شده پیش از archive/return-to-draft تاریخی و خوانا می‌ماند، اما تاریخ ساخت جابه‌جا‌شده به پس از آن event رد می‌شود.
- producer و parser با round-trip پیش از persist هم‌تراز شدند. ساعت سیستم عقب‌رفته به آخرین dependency/نسخه clamp می‌شود، no-op هم در writer و هم parser رد است و سقف ۱۰۰ در پروژه/۱۰۰۰ در store از self-corruption جلوگیری می‌کند.
- `SupplierContact` فعلی revision هویتی ندارد؛ بنابراین event ویرایش تماس تا تعریف `ContactRevision` معتبر پشتیبانی نمی‌شود و به‌جای حدس snapshot تاریخی fail-closed می‌ماند. archive/restore همچنان معتبر و رکورد پیشنهاد قبلی پس از reload تاریخی است.
- بازگشت از جزئیات دیگر id ذخیره‌شده را داخل CSS selector نمی‌گذارد؛ شناسهٔ نامتعارف localStorage بدون exception به کارت درست focus برمی‌گردد.

### شواهد تحویل لوکال T7-A

- هشت regression اختصاصی محصول دو‌قلمی، خدمت مستقل، unknown/status، جداسازی پروژه و مرجع، focus و id نامتعارف، no-op/version/history، archive تاریخی، chronology تماس/درخواست، numeric دقیق و ساعت عقب‌رفته، unsupported contact update، rollback create/update و دست‌کاری هماهنگ fingerprint/snapshot را پوشش می‌دهند و پاس‌اند.
- اعتبارسنجی نهایی روی snapshot پس از همهٔ اصلاحات: `check:runtime` برای ۲۸ فایل، `build`، تست اپ ۱۰۱ از ۱۰۱، runtime ۱۰۹ از ۱۰۹، Sites چهار از چهار، `npx tsc --noEmit` و `git diff --check` پاس شدند؛ فقط هشدار شناخته‌شدهٔ chunk بزرگ‌تر از 500kB باقی است.
- QA مرورگر داخلی در viewport واقعی `390 × 844`: صندوق خالی و فرم دو‌قلمی با مرز صداقت دیده شدند؛ overflow افقی root/body/editor صفر، focus ورود فرم روی عنوان و بازگشت روی دکمهٔ افزودن، و error overlay صفر بود.
- سه بازبینی مستقل روی مرز release، قرارداد/داده، parser و تجربهٔ موبایل انجام شد. نقص‌های نام فایل، snapshot/chronology تماس، focus، no-op parser، numeric، سقف store، ساعت عقب‌رفته، round-trip و selector شناسه پیش از اجرای نهایی اصلاح شدند و بازبینی آخر blocker مادی باقی‌مانده‌ای گزارش نکرد.
- بازخورد و مجوز انتشار در ۱۴۰۵/۰۶/۰۶ — ۲۰۲۶/۰۸/۲۸: کاربر نتیجه را «خوب» دانست، انتشار کامل همین T7-A در همهٔ مقصدها را صریحاً مجاز کرد و خواست ابتدا لینک ChatGPT Sites را بررسی کند؛ این مجوز فقط همین snapshot را پوشش می‌دهد و آغاز T7-B به تأیید بعدی او وابسته است.
- وضعیت انتشار: commit `bf59df1b82f3886794115647920b834c5e9c74dd` روی local/GitHub `main` هم‌تراز شد. Cloudflare Pages deployment `4801dd57-17f3-4b6c-aafc-5b73fe17b375` با `github:push`، شاخهٔ `main`، `commit_dirty=false` و همین SHA موفق شد؛ HTML/JS/CSS canonical و immutable با build محلی hash یکسان و پاسخ ۲۰۰ داشتند. ChatGPT Sites نسخهٔ ۷ با ۱۷ فایل و همین source commit ذخیره و deployment خصوصی `appgdep_6a91e6f150f4819185379101b1c951ce` با وضعیت `succeeded` منتشر شد؛ دسترسی owner-only/custom حفظ شد.

---

## T7-B1 — هم‌سطح‌سازی و مقایسهٔ قطعی پیشنهادهای محصول

- **تاریخ:** ۱۴۰۵/۰۶/۰۷ — ۲۰۲۶/۰۸/۲۹
- **وضعیت:** پیاده‌سازی، بازبینی مستقل، regression و QA موبایل کامل؛ انتشار نهایی با SHA `92dcc14b911ff7840c3816ce5345c472604bff98` روی GitHub، Cloudflare Pages و ChatGPT Sites same-source بسته شده است
- **دامنه:** مقایسهٔ حداقل دو پیشنهاد محصولِ جاری برای همان `projectId` و همان نسخه/review revision دقیق درخواست؛ بدون مقایسهٔ خدمت، AI، استخراج متن، شبکه، انتخاب «بهترین»، مذاکره، خرید، سفارش، پیام یا اثر بیرونی.

### تجربهٔ مشاهده‌شده

- قرارداد مشترک برای محصول و خدمت، تفاوت صریح منطق مقایسهٔ این دو در سند مادر را پنهان می‌کرد. شِمای T7-A خدمت نیز مقدار/واحد لازم برای هم‌سطح‌سازی عددی صادقانه را ندارد؛ بنابراین این تسک به T7-B1 محصول محدود شد و مقایسهٔ خدمت به T7-B2 مستقل موکول شد.
- قیمت کل فقط وقتی مبنای معتبر است که مقدار و واحد اعلامی با snapshot درخواست دقیقاً برابر باشند. در حالت دیگر، سازنده باید «مقدار لازم در همان واحد اعلامی پیشنهاد» و دلیل تبدیل را صریح ثبت کند؛ واحد قابل‌ویرایش یا متن فرض به‌تنهایی نباید ضرب بُعدی نادرست را معتبر کند.
- متن آزاد مالیات و حمل در پیشنهاد T7-A قابل‌تفسیر خودکار نیست. رابط متن خام را در لایهٔ اعلامی نشان می‌دهد و treatment ساختاریافتهٔ مستقل `included / fixed / rate / unknown` برای مالیات و `included / fixed / unknown` برای حمل فقط با فرض صریح سازنده ثبت می‌شود.
- نمایش «پوشش کامل» برای نتیجهٔ عددی می‌توانست به‌اشتباه کامل‌بودن موعد، اعتبار، حداقل سفارش و پرداخت را القا کند؛ عبارت محصولی به «فرمول مبلغ کامل» محدود شد و این شروط اعلامی بدون امتیازدهی کنار نتیجه باقی ماندند.
- ممیزی revision تاریخی فقط با نمایش مبلغ نهایی کافی نبود؛ mode، مقدار ورودی، فرمول و فرض مالیات/حمل برای هر پیشنهاد در جزئیات همان نسخه آشکار شد. تاریخچهٔ تصمیم نیز نام و شناسهٔ پیشنهاد منتخب هر revision را نشان می‌دهد.
- خطای خواندن اگر هم‌زمان empty state نشان دهد، به‌صورت ضمنی نبود داده را ادعا می‌کند. نمای خطای مقایسه اکنون error-only است، شمارش را نامشخص می‌گذارد و ساخت را قفل می‌کند.

### شکاف‌ها و تصمیم‌های کاری

- **[تصمیم برش محصول، تأییدشده توسط کاربر برای T7-B1]** T7-B1 فقط محصول است. T7-B2 باید قرارداد مستقل خدمت را برای دامنه، مدت، روش، صلاحیت، داخل/خارج کار، ضمانت و پرداخت تعریف کند؛ کپی‌کردن فرمول محصول ممنوع است.
- **[تصمیم حفظ اصل، تأییدشده توسط کاربر برای T7-B1]** store و bytes پیشنهاد T7-A هرگز با ساخت/ویرایش مقایسه یا تصمیم تغییر نمی‌کنند. `BuilderProposalComparisonRecord` و `BuilderProposalComparisonDecisionRecord` دو شیء و دو store مستقل، خصوصی و پروژه‌محورند.
- **[تصمیم منشأ، تأییدشده توسط کاربر برای T7-B1]** سه لایهٔ `دادهٔ اعلامی رونویسی‌شده توسط سازنده`، `فرض ثبت‌شده توسط سازنده` و `محاسبهٔ قطعی محلی چیدا` در schema و UI جدا هستند. هیچ فرضی supplier-declared نمایش داده نمی‌شود.
- **[تصمیم محاسبه، تأییدشده توسط کاربر برای T7-B1]** arithmetic روی decimal canonical و `BigInt` انجام می‌شود؛ تبدیل به `Number/parseFloat` و گردکردن پنهان وجود ندارد. خروج از سقف عددی نتیجه را incomplete و بدون توصیه می‌کند.
- **[تصمیم جمع‌بندی، تأییدشده توسط کاربر برای T7-B1]** تنها معیار این برش `lowest-complete-normalized-total` است. نتیجه فقط `conditional`، `tie` یا `insufficient-data` می‌شود؛ alternative، unavailable، unknown یا پوشش ناقص هیچ نامزد عددی نمی‌سازند و واژهٔ «بهترین/برنده» استفاده نمی‌شود.
- **[تصمیم مستقل سازنده، تأییدشده توسط کاربر برای T7-B1]** تصمیم کاربر از recommendation جدا، به revision دقیق مقایسه pin و نسخه‌دار است؛ سه outcome محدود «ادامهٔ بررسی با یک پیشنهاد»، «نیازمند روشن‌سازی» و «فعلاً هیچ‌کدام» دلیل اجباری دارند و همواره `externalEffect=none`، `sendAuthorized=false`، `purchaseAuthorized=false` و `supplierNotified=false` می‌مانند.
- **[تصمیم نسخه و شکست، تأییدشده توسط کاربر برای T7-B1]** parser خروجی محاسبه را از input دوباره می‌سازد، ref و fingerprint revision تاریخی را بررسی می‌کند، no-op نسخه نمی‌سازد، write-before-state و rollback را حفظ می‌کند و read-error را از empty جدا نشان می‌دهد. تغییر proposal/request/contact فقط status را تاریخی/نیازمند بازبینی می‌کند و rebind خاموش ندارد.

### پیشنهاد برای تکمیل سند مادر

- schema رسمی سه لایهٔ declared/adjustment/calculation، مجموعهٔ unit conversionهای مجاز، قاعدهٔ rounding، سقف عددی، tax/transport treatment و semantics «مبلغ هم‌سطح» تعریف شود.
- معیارهای غیرقیمتی، رفتار tie، alternative equivalence، دادهٔ ناقص و مرز recommendation قاعده‌محور از تصمیم انسانی رسمی جدا شوند.
- `ProposalComparisonDecision` با target revision، outcome، reason، actor، history، invalidation و منع اثر بیرونی به‌عنوان شیء پایدار تعریف شود.
- قرارداد مقایسهٔ خدمت جدا از محصول و پیش از T7-B2 تکمیل شود.

### شواهد تحویل محلی پیش از بازخورد

- پنج regression متمرکز T7-B1 محاسبهٔ hard-coded، استقلال byte-for-byte proposal/request/approval/dispatch، reload، نبود network، unknown بدون candidate، rollback شکست نوشتن، version/no-op/history، invalidation و fail-closed را پوشش می‌دهند. سناریوی عددی مرزی، `۱۹۹ رقم ۹ + ۰٫۵ + ۰٫۵` را به خروجی canonical دقیقاً ۲۰۰رقمی و کامل می‌رساند و از رد کاذب coefficient موقت ۲۰۱رقمی جلوگیری می‌کند.
- اعتبارسنجی نهایی همین snapshot: `check:runtime` برای ۲۸ فایل محافظت‌شده، `build` و TypeScript، اپ ۱۰۸/۱۰۸، runtime ۱۱۶/۱۱۶، Sites چهار از چهار و `git diff --check` پاس شدند؛ فقط هشدار شناخته‌شدهٔ chunk بزرگ‌تر از 500kB باقی است.
- QA مرورگر داخلی در viewport واقعی `390 × 844` ارقام `۲۴٬۴۳۵٬۰۰۰` و `۲۳٬۰۰۰٬۰۰۰` تومان، نامزد شرطی درست، focus روی hero پس از ذخیره و overflow صفر در editor، detail، کارت‌های نتیجه و تصمیم را نشان داد.
- بازبینی‌های مستقل نقص‌های fingerprint ویرایش، واحد قابل‌تغییر/فاقد واحد، بازشدن keyboard روی واحد ثابت، سقف store تصمیم، خواندن decision، بستن و canonicalization arithmetic، نمایش متن خام و فرمول مالیات/حمل، تاریخچهٔ انتخاب تصمیم، عبارت coverage، پیام success و selector شناسه را پیدا کردند و پیش از تحویل اصلاح شدند. بازبینی نهایی finding باز P0/P1/P2 نداشت.
- baseline پیش از انتشار T7-B1 روی SHA `57bbf6560e40a7b2ef9bb0a369af253c1b063d82`، Cloudflare deployment `d2162e8b-8ff3-4fa6-9b38-6f5bd4d38bc7` و ChatGPT Sites نسخهٔ ۹/deployment `appgdep_6a91fdd4e5108191a463c45298665152` بود. ماهیار سپس همین T7-B1 را برای commit/push/deploy همه‌جا تأیید کرد و خواست فقط پس از موفقیت انتشار، T7-B2 آغاز شود.
- commit کد T7-B1 برابر `59405e2b2ffc501aef8287ae6139972b1a142963` روی local/GitHub `main` هم‌تراز شد. Cloudflare Pages deployment `23abfa5d-d88c-4405-becc-742509ce1ced` با `github:push`، شاخهٔ `main`، `commit_dirty=false` و همین SHA به وضعیت `success` رسید؛ HTML، JavaScript و CSS در canonical/immutable/local به‌ترتیب hashهای یکسان `f946d6231fde96219277c83888113d8251287dbef03000fa885c7b35ae32c890`، `64f52f2032eae8783109cc9fa5c08d3cd500e549e28080f62cddde09064987d8` و `83c9bbc44b7f4e2de6aea67986afb2e955c684c6859e03a170a170f3c14ab2e3` داشتند.
- ChatGPT Sites نسخهٔ ۱۰ با ۱۸ فایل، source commit همین SHA و archive hash `sha256:b1d683bb5d8f2d1a84eb43ddd5f4b70471deecc5f5d23cf93ebc792cefe7a327` ذخیره شد؛ version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_8cb2973796a88191ad66b7fa6894e975` و deployment خصوصی `appgdep_6a926918d91c81919d2ca7f2b012de8b` با وضعیت `succeeded` روی `https://chida-prototype.mahyarkl.chatgpt.site` قرار گرفت. دسترسی owner-only/custom با یک مالک و بدون گروه یا مهمان بیرونی حفظ شد.
- receipt نهایی T7-B1 با SHA `92dcc14b911ff7840c3816ce5345c472604bff98` روی local/GitHub `main` هم‌تراز شد. Cloudflare Pages deployment `c5c067b2-a623-4476-a024-6f6c81be8c4a` با همین source به وضعیت `success` رسید. ChatGPT Sites نسخهٔ ۱۱ با version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_791562f862008191889941b682ef13b5` و deployment خصوصی `appgdep_6a926a6591608191995a04aaeab7da6` در وضعیت `succeeded` قرار گرفت. T7-B1 با این receipt بسته شد؛ این مجوز به commit یا انتشار T7-B2 تعمیم پیدا نمی‌کند.

---

## T7-B2 — مقایسهٔ کیفی پیشنهادهای خدمت و تصمیم مستقل سازنده

- **تاریخ:** ۱۴۰۵/۰۶/۰۷ — ۲۰۲۶/۰۸/۲۹
- **وضعیت:** پیاده‌سازی، regression و QA موبایل کامل و توسط ماهیار تأیید شد؛ receipt نهایی same-source با SHA `ef21c022ae28a772bbd710bc09c8f61a18db74ad` در GitHub، Cloudflare Pages و ChatGPT Sites منتشر و بسته شده است
- **دامنه:** مقایسهٔ کیفیِ معیارمحورِ حداقل دو پیشنهاد خدمتِ جاری برای همان `projectId` و همان target/revision دقیق، همراه با تصمیم انسانی جدا؛ بدون امتیازدهی، رتبه‌بندی، نرمال‌سازی قیمت، AI، شبکه، پیام، ارسال، خرید یا هر اثر بیرونی.

### تجربهٔ مشاهده‌شده

- پیشنهاد خدمت T7-A یک اصل ثبت‌شده و immutable است و دادهٔ ساختاریافتهٔ لازم برای فرمول unit-price یا نرمال‌سازی عددی محصول را ندارد. تعمیم مستقیم machinery عددی T7-B1 به خدمت، قطعیت و قابلیت‌مقایسه‌ای را القا می‌کند که منبع ثبت‌شده پشتیبانی نمی‌کند.
- برای خدمت، مفیدترین نمای محلی کنارهم‌گذاری معیارهای صریحِ دامنه، محل، حجم، صلاحیت، زمان‌بندی، روش اجرا، داخل/خارج کار، ضمانت و پرداخت است. unknown باید دیده شود و به‌جای امتیاز یا نامزد خودکار، نیاز به روشن‌سازی بسازد.
- رونویسی تکمیلی و ارزیابی سازنده ممکن است برای بازبینی لازم باشند، اما اگر با اصل پیشنهاد ادغام شوند منشأ داده گم می‌شود. اصل پیشنهاد، رونویسی/تعدیل سازنده و ارزیابی محلی باید جدا و شفاف بمانند.
- کامل‌بودن خانه‌های ماتریس فقط «آمادگی بازبینی» را نشان می‌دهد و معادل مناسب‌بودن خدمت، صلاحیت تأمین‌کننده یا انتخاب تجاری نیست. تصمیم سازنده باید شیء مستقل و pin‌شده به revision دقیق مقایسه باقی بماند.

### شکاف سند مادر

- سند مادر تفاوت قرارداد مقایسهٔ محصول و خدمت را به‌رسمیت می‌شناسد و معیارهای خدمت را نام می‌برد، اما schema دقیق رکورد مقایسهٔ خدمت، منشأ هر خانه، statusهای کیفی، fingerprint، version/invalidation و رفتار شکست/rollback/read-error را تعریف نمی‌کند.
- مرز رسمی میان «بازبینی برای تصمیم آماده است»، «معیار با نیاز هم‌راستاست» و «خدمت یا تأمین‌کننده انتخاب شده است» تعریف نشده و منع scoring/ranking/normalization برای داده‌ای که قرارداد عددی معتبر ندارد نیز باید صریح شود.
- شیء پایدار تصمیم خدمت، نسبت آن با revision تاریخی مقایسه و اثر تغییر proposal/request/contact هنوز در سند مادر قرارداد کامل ندارد.

### تصمیم‌های تأییدشده پس از اعتبارسنجی کاربر

- **[تصمیم کاری؛ تأییدشده توسط کاربر برای T7-B2]** مقایسه فقط پیشنهادهای خدمتِ جاریِ همان target دقیق را می‌پذیرد و هر معیار با یکی از وضعیت‌های کیفیِ آشکار مانند هم‌راستا، نسبی، متفاوت، نامشخص یا نامرتبط ثبت می‌شود. جمع‌بندی فقط «آمادهٔ بازبینی» یا «نیازمند روشن‌سازی» است و candidate، winner، score یا rank نمی‌سازد.
- **[تصمیم کاری؛ تأییدشده توسط کاربر برای T7-B2]** اصل proposal دست‌نخورده می‌ماند؛ رونویسی تکمیلی سازنده و ارزیابی او در لایه‌های جدا، با منشأ صریح و بدون بازنویسی دادهٔ اعلامی ذخیره می‌شوند. مقایسه و تصمیم دو رکورد و store مستقل، خصوصی، پروژه‌محور و نسخه‌دارند.
- **[تصمیم کاری؛ تأییدشده توسط کاربر برای T7-B2]** تصمیم انسانی به revision/fingerprint دقیق مقایسه pin می‌شود، دلیل اجباری دارد و صرفاً ثبت محلی است. تغییر dependency فقط وضعیت تاریخی/نیازمند بازبینی می‌سازد و rebind خاموش ندارد؛ هیچ شبکه، پیام، ارسال، خرید، مجوز یا اثر بیرونی ایجاد نمی‌شود.
- **[تصمیم کاری؛ تأییدشده توسط کاربر برای T7-B2]** قیمت و شروط تجاری فقط در شکل خام/اعلامی نمایش داده می‌شوند؛ فرمول محصول، unit-price، تبدیل واحد، نرمال‌سازی، امتیازدهی و رتبه‌بندی در T7-B2 وجود ندارند.

### بازخورد پس از مشاهده

- ماهیار پیش‌نمایش T7-B2 را مناسب ارزیابی کرد و صریحاً خواست تمام انتشارها انجام شود و سپس تسک بعدی آغاز گردد. این مجوز فقط snapshot تأییدشدهٔ T7-B2 و receipt مستندی همان انتشار را پوشش می‌دهد؛ T8-A پس از موفقیت انتشار آغاز می‌شود و برای commit/push/deploy آیندهٔ خودش به تأیید تازه نیاز دارد.

### پیشنهاد برای تکمیل سند مادر

- schema رسمی و مستقل `ServiceProposalComparison` و `ServiceProposalComparisonDecision` با `projectId`، target/request revision، proposal revisionهای pin‌شده، نسخهٔ معیارها، provenance هر رونویسی/ارزیابی، fingerprint پایدار، revision/history و زمان/actor تعریف شود.
- statusهای کیفی هر معیار و semantics جمع‌بندی «آمادهٔ بازبینی/نیازمند روشن‌سازی» از suitability، qualification و انتخاب تجاری جدا شوند؛ منع صریح scoring، ranking، winner و normalization بدون قرارداد عددیِ جدا و بعداً تأییدشده ثبت شود.
- immutable بودن اصل proposal و جدایی amendment/transcription سازنده از دادهٔ اعلامی، همراه با invalidation dependencyها، no-op/version، write-before-state، rollback، read-error جدا از empty و قفل mutation در خطای خواندن، به invariant رسمی تبدیل شود.
- تصمیم انسانی خدمت به‌عنوان رکورد مستقل و pin‌شده تعریف شود و همواره `externalEffect=none`، بدون ارسال/خرید/اطلاع‌رسانی تأمین‌کننده باقی بماند تا مجوز و مسیر بیرونی جداگانه‌ای در آینده تصویب شود.

### وضعیت شواهد و انتشار

- چهار regression متمرکز T7-B2 ماتریس کیفی ۱۰×۲ با oracle/countهای مستقل، lineage و provenance، decision جدا، reload/no-network/source-byte invariants، declared value همراه unknown، rollback شکست نوشتن، no-op/version/history/invalidation و tamper/read-error fail-closed را پوشش می‌دهند. اجرای متمرکز نهایی ۴/۴، `test:app` کامل ۱۱۲/۱۱۲ و `test:runtime` نهایی ۱۲۰/۱۲۰ پاس شدند.
- `check:runtime` برای ۲۸ فایل محافظت‌شده، build شامل TypeScript و آماده‌سازی Sites، `test:sites` چهار از چهار، `npx tsc --noEmit` و `git diff --check` پاس شدند؛ فقط هشدار شناخته‌شدهٔ chunk جاوااسکریپت بزرگ‌تر از 500kB باقی است.
- QA مرورگر داخلی در viewport واقعی `390 × 844` مسیر صندوق پیشنهادها ← ماتریس خدمت ← تصمیم مستقل را با دو پیشنهاد و ده معیار طی کرد. نام‌های screen reader بر پایهٔ «کنترل + معیار + مجری» قابل تشخیص بودند، جمع‌بندی همیشه بدون نامزد خودکار ماند، تصمیم محلی ثبت شد، console خطا/هشدار مرتبط نداشت و overflow افقی editor، detail و document صفر بود.
- commit کد T7-B2 برابر `0d633e0dbf3d9291d6bba963f64855143b5cf1ef` روی local/GitHub `main` هم‌تراز شد. Cloudflare Pages deployment `20cfc7ac-0984-4102-a394-6d2605eb7158` با `github:push`، شاخهٔ `main`، `commit_dirty=false` و همین SHA به وضعیت `success` رسید؛ hashهای local/canonical/immutable برای HTML، JavaScript و CSS به‌ترتیب `5fa8b8a4ad0ce83907303ca74c086115117208ee20b62bdcf94991815a23a2cd`، `80dc42150e9e44b4d4eb46924f2458c2b959065c414f674ddc71ab81aa768093` و `01be34119cc0b87cb2f85c0af2942f0f0d9a7e87ddae1ca8c64ebaf730badfee` یکسان بودند.
- ChatGPT Sites نسخهٔ ۱۲ با ۱۸ فایل، source commit همین SHA و archive hash `sha256:86daac1e62c11b0f26fbfe4ae6020d3cafecf21c3cd45a068fee4453fec17e3d` ذخیره شد؛ version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_31ac1fcd9cec8191b00f8f2096f2f7e9` و deployment خصوصی `appgdep_6a927bee77808191812616e41360858c` در وضعیت `succeeded` روی `https://chida-prototype.mahyarkl.chatgpt.site` قرار گرفت. دسترسی owner-only/custom با یک مالک و بدون گروه یا مهمان بیرونی حفظ شد.
- commit مستندی نهایی با SHA `ef21c022ae28a772bbd710bc09c8f61a18db74ad` روی local/GitHub `main` هم‌تراز شد و prototype tree آن با commit کد `0d633e0dbf3d9291d6bba963f64855143b5cf1ef` یکسان ماند. Cloudflare Pages deployment نهایی `4396280e-95e7-4ceb-bdf2-da720de51170` با `github:push`، شاخهٔ `main`، `commit_dirty=false` و source commit نهایی به وضعیت `success` رسید؛ canonical و immutable همچنان همان hashهای HTML، JavaScript و CSS ثبت‌شده در بالا را داشتند.
- ChatGPT Sites نسخهٔ ۱۳ با همان ۱۸ فایل و archive hash، source commit نهایی را ذخیره کرد؛ version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_5dd91d43d590819199143a7fa48ff09d` و deployment خصوصی `appgdep_6a927e3aaec881918dc255bde7e18ba4` در وضعیت `succeeded` روی `https://chida-prototype.mahyarkl.chatgpt.site` قرار گرفت. دسترسی owner-only/custom با یک مالک و بدون گروه یا مهمان بیرونی حفظ شد؛ T7-B2 با این receipt بسته و T8-A1 فقط در لوکال آغاز شد.

## T8-A1 — پیش‌نویس خصوصی و نسخه‌دار سؤال مذاکره پس از پیشنهاد

- **تاریخ:** ۱۴۰۵/۰۶/۰۷ — ۲۰۲۶/۰۸/۲۹
- **وضعیت:** پیاده‌سازی، regression، QA موبایل و بازبینی مستقل کامل شد؛ ماهیار T8-A1 را تأیید کرد و receipt same-source نهایی SHA `2d72f5bbe86eab6c975a7beb2bd72ffc38c7e9ba` در GitHub، Cloudflare Pages و ChatGPT Sites منتشر و بسته شد
- **دامنه:** فقط ثبت دستیِ هدف و متن یک سؤال خصوصی و ارسال‌نشده، pin‌شده به یک قلم محصول یا معیار واجدشرایط خدمت در revision دقیق مقایسه و پیشنهاد؛ بدون پاسخ تأمین‌کننده، پیام، شبکه، AI، زمان‌بندی، تبادل تماس، مجوز ارسال یا اثر بیرونی.

### تجربهٔ مشاهده‌شده

- پس از مقایسه، سازنده برای روشن‌کردن یک ابهام به مسیری نیاز دارد که همان پیشنهاد، revision، قلم/معیار و snapshot تأمین‌کننده را نگه دارد؛ یک یادداشت آزاد یا پیام چت این lineage را از بین می‌برد.
- ثبت هدف و متن با بیان مستقیم سازنده، بدون ادعای ارسال، یک برش ماندگار و صادقانه برای فاز قبل از وجود سمت تأمین‌کننده است. پیش‌نویس موجود باید از همان CTA دوباره باز شود و کاربر نباید پس از نوشتن با خطای duplicate غافلگیر شود.
- بازگشت از ثبت یا جزئیات باید کاربر را به همان revision و معیار منشأ برگرداند. دادهٔ stale تاریخی و فقط‌خواندنی می‌ماند و به dependency تازه rebind نمی‌شود.

### شکاف سند مادر

- سند مادر «پیش‌نویس سؤال و پاسخ» و تأیید پیش از ارسال را می‌خواهد، اما شیء مستقل پیش‌نویس خصوصی، target دقیق، version/history/fingerprint، رفتار duplicate و invalidation وابستگی را تعریف نمی‌کند.
- مرز رسمی میان یادداشت خصوصی سازنده، پیام مشترک آینده و پاسخ دستی/واقعی تأمین‌کننده مشخص نیست. همچنین قرارداد read-error، rollback، no-op و retention این شیء هنوز در سند مادر وجود ندارد.
- وضعیت‌های معیار خدمت که واقعاً سؤال تکمیلی می‌طلبند و نسبت آن‌ها با معیارهای هم‌راستا یا نامرتبط باید صریح شود تا UI و parser یک قرارداد واحد داشته باشند.

### تصمیم‌های تأییدشده پس از مشاهدهٔ ماهیار

- **[تصمیم کاری T8-A1؛ تأییدشده توسط کاربر]** `PrivateNegotiationDraft` یک رکورد مستقل، پروژه‌محور، خصوصی و نسخه‌دار است که فقط با ثبت مستقیم سازنده ساخته می‌شود و به revision/fingerprint دقیق مقایسه، درخواست، پیشنهاد، قلم/معیار و snapshot تماس pin می‌شود.
- **[تصمیم کاری T8-A1؛ تأییدشده توسط کاربر]** هدف و متن هر دو اجباری‌اند؛ no-op بایت و نسخه را تغییر نمی‌دهد، تغییر معنادار revision/history تازه می‌سازد و شکست نوشتن نسخهٔ قبلی را حفظ می‌کند. هر target فقط یک پیش‌نویس دارد و CTA هدف موجود همان رکورد را باز می‌کند.
- **[تصمیم کاری T8-A1؛ تأییدشده توسط کاربر]** در خدمت فقط وضعیت‌های نسبی، متفاوت یا نامشخص واجد شروع سؤال‌اند؛ معیار هم‌راستا یا نامرتبط هدف سؤال تکمیلی نیست. قلم محصول می‌تواند یک سؤال اختیاری سازنده داشته باشد و کامل‌بودن محاسبه به‌تنهایی «نیاز به روشن‌سازی» ادعا نمی‌کند.
- **[مرز اثر بیرونی T8-A1؛ تأییدشده توسط کاربر]** رکورد همواره `status=draft`، `externalEffect=none`، `networkUsed=false`، `aiUsed=false`، `sendAuthorized=false`، `supplierNotified=false`، `sharedWithSupplier=false` و `externalActionAttempted=false` دارد؛ در این قرارداد `supplierNotified=false` فقط یعنی چیدا هیچ اطلاع‌رسانی‌ای انجام نداده است و دربارهٔ تماس احتمالی بیرون از چیدا ادعا نمی‌کند. هیچ دکمه یا رسید ارسال ساخته نمی‌شود.

### بازخورد پس از مشاهده

- **[تصمیم تاریخی هنگام تأیید T8-A1]** ماهیار T8-A1 را مناسب ارزیابی کرد و صریحاً خواست به تسک بعدی برویم. مطابق ریتم ثابت پروژه، این تأیید مجوز commit، push و انتشار همان snapshot آزموده در هر سه مقصد بود؛ T8-A2 فقط پس از موفقیت و راستی‌آزمایی same-source آن آغاز شد. مجوز انتشار T8-A1 به commit یا انتشار آیندهٔ T8-A2 تعمیم ندارد.

### پیشنهاد برای تکمیل سند مادر

- `PrivateNegotiationDraft` از `SharedNegotiationMessage` و پاسخ طرف دوم جدا تعریف شود؛ target/version/fingerprint، یک‌به‌یک‌بودن target، history تغییرناپذیر، no-op، invalidation بدون rebind و وضعیت خصوصی/ارسال‌نشده قرارداد رسمی داشته باشند.
- تبدیل احتمالی پیش‌نویس خصوصی به پیام مشترک آینده باید یک اقدام جدا با snapshot مقصد/محتوا/نسخه و Approval مستقل باشد؛ این تبدیل نباید تاریخچهٔ پیش‌نویس را بازنویسی کند.
- سقف رکورد/نسخه، write-before-state، rollback، parser fail-closed، تمایز read-error از empty و قفل mutation تا load موفق برای این شیء نیز invariant رسمی شوند.

### وضعیت شواهد و تحویل محلی

- شش regression متمرکز مسیر قلم محصول و معیار خدمت، target و source-byteهای دقیق، بازگشت/focus به منشأ، duplicate target، no-op/version/history، rollback/read-error، canonical tamper و invalidation/fingerprint را پوشش می‌دهند. اجرای متمرکز نهایی ۶/۶، `test:app` کامل ۱۱۸/۱۱۸ و `test:runtime` کامل ۱۲۶/۱۲۶ پاس شدند.
- `check:runtime` برای ۲۸ فایل محافظت‌شده، build شامل TypeScript و آماده‌سازی Sites، `test:sites` چهار از چهار، `npx tsc --noEmit` و `git diff --check` پاس شدند؛ فقط هشدار شناخته‌شدهٔ chunk جاوااسکریپت بزرگ‌تر از 500kB باقی است.
- QA مرورگر داخلی در viewport واقعی `390 × 844` مسیر ماتریس خدمت ← CTA معیار «مدت و زمان اجرا» ← editor ← detail ← بازگشت به همان معیار ← بازکردن رکورد موجود ← فهرست پیش‌نویس‌ها را طی کرد. focus ورود روی عنوان و خطای هدف روی همان field بود، نام دسترس‌پذیر «معیار + مجری» حفظ شد، CTA معیار هم‌راستا غیرفعال و partial/different فعال بود، editor/detail/list و document overflow افقی صفر داشتند و console warning/error و error overlay صفر بودند.
- بازبینی مستقل نهایی هیچ P0/P1/P2 یا blocker پیدا نکرد؛ دامنه و flags بدون اثر بیرونی، project isolation، exact lineage/fingerprint، canonical parser، no-op/version/rollback/read-lock، duplicate/open-existing، origin focus، eligibility مشترک، a11y و اسناد هم‌خوان گزارش شدند.
- store مستقل `chida-prototype-builder-negotiation-drafts:v1` با سقف ۱۰۰ رکورد در پروژه، ۱۰۰ revision در رکورد و ۱۰۰۰ رکورد کل ساخته شده است. read-error از empty جداست، mutation را قفل می‌کند و دادهٔ منبعِ درخواست/پیشنهاد/مقایسه/تماس را تغییر نمی‌دهد.
- **[وضعیت تاریخی هنگام تحویل T8-A1]** در آن snapshot پاسخ دستی، کشف تناقض، اثر تغییر شرایط، مقایسهٔ نسخهٔ تازهٔ پیشنهاد، پیگیری موعد و هر مسیر مشترک/ارسالی هنوز ساخته نشده بودند؛ T8-A2 بعداً فقط برش رونویسی خصوصی پاسخ را در لوکال پوشش داد و کل T8-A همچنان تکمیل اعلام نمی‌شود.
- commit کد `1d6721da9bfbadc04f4ecdd4c5bbb94de4c6c8e1` روی local/GitHub `main` هم‌تراز شد. Cloudflare Pages deployment `fd8a2414-20c4-48d3-8100-fdbabf5db5df` با `github:push`، شاخهٔ `main`، `commit_dirty=false` و همین source به وضعیت `success` رسید؛ canonical و immutable با build محلی یکسان بودند: HTML برابر `4c352a2abd9d6a6cfd9637204b5e3999302e7c5f4e81003f51b5ba53c82f8f4d`، JavaScript برابر `90638efafed40660cb7ea90f2682abab4d88ba82fba1576c3bd9a882e3b5785f` و CSS برابر `0451b6a6e27e74bc3ef941262c0b921d43e96c35c745e314e65ea2e8b4583838`.
- ChatGPT Sites نسخهٔ ۱۴ با ۱۸ فایل، source commit همین SHA و archive hash `sha256:20e03c011543ef61b61b34ccda06ddb55c9acd091e70e69d61110d2a8b1176c0` ذخیره شد؛ version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_d87834b9652881919ce7e421b183f3d0` و deployment خصوصی `appgdep_6a9294c72bf88191ac00717221609da7` در وضعیت `succeeded` روی `https://chida-prototype.mahyarkl.chatgpt.site` قرار گرفت. دسترسی owner-only/custom با یک مالک و بدون گروه یا مهمان بیرونی حفظ شد.
- **[وضعیت تاریخی پیش از receipt نهایی]** commit مستندی بعدی فقط receipt و وضعیت را ثبت می‌کرد و prototype tree آن با `1d6721d` یکسان می‌ماند؛ نتیجهٔ نهایی همان SHA `2d72f5b` و receiptهای زیر شد و پس از آن T8-A2 فقط در لوکال آغاز شد.

### receipt نهایی انتشار T8-A1

- commit مستندی نهایی `2d72f5bbe86eab6c975a7beb2bd72ffc38c7e9ba` روی local/GitHub `main` هم‌تراز شد و prototype tree آن با commit کد `1d6721da9bfbadc04f4ecdd4c5bbb94de4c6c8e1` یکسان ماند. Cloudflare Pages deployment `806f7378-facb-411a-b9fa-f8febdaac62b` با `github:push`، شاخهٔ `main`، `commit_dirty=false` و همین source به وضعیت `success` رسید؛ canonical و immutable همان hashهای HTML، JavaScript و CSS ثبت‌شده برای release کد را داشتند.
- ChatGPT Sites نسخهٔ ۱۵ با ۱۸ فایل و archive hash `sha256:20e03c011543ef61b61b34ccda06ddb55c9acd091e70e69d61110d2a8b1176c0`، source commit نهایی را ذخیره کرد؛ version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_0b4f39214db48191a6982355aff562b1` و deployment خصوصی `appgdep_6a9295dd273c819195e5bde8cf2886bc` در وضعیت `succeeded` روی `https://chida-prototype.mahyarkl.chatgpt.site` قرار گرفت. access owner-only/custom با یک مالک و بدون گروه یا مهمان بیرونی حفظ شد؛ T8-A1 بسته و T8-A2 فقط در لوکال آغاز شد.

## T8-A2 — رونویسی خصوصی پاسخ مرتبط توسط سازنده

- **تاریخ:** ۱۴۰۵/۰۶/۰۷ — ۲۰۲۶/۰۸/۲۹
- **وضعیت:** قرارداد، پیاده‌سازی، regression، suite کامل، QA موبایل و بازبینی مستقل کامل و توسط ماهیار تأیید شد؛ release کد با commit `f5d68b6e757c0763a6770a46c5c3a211825713d3` و receipt مستندی نهایی با SHA `5357fc503df57f51bd4659401399f1b3b76a0caa` در GitHub، Cloudflare و ChatGPT Sites نسخهٔ ۱۷ موفق و هم‌منبع شدند؛ T8-A2 منتشر و بسته شد
- **دامنه:** یک متن پاسخ که خود سازنده دستی رونویسی می‌کند و به revision دقیق سؤال T8-A1 متصل است؛ بدون ارسال سؤال از چیدا، دریافت پاسخ در چیدا، هویت/اصالت/زمان/کانال تأییدشده، پیام مشترک، شبکه، AI، اعلان یا اثر بیرونی.

### تجربهٔ مشاهده‌شده

- تا پیش از وجود مسیر تأمین‌کننده، سازنده ممکن است پاسخ را بیرون از چیدا شنیده یا دیده باشد و فقط برای رجوع خصوصی رونویسی کند. نام‌گذاری «پاسخ تأمین‌کننده» یا «دریافت شد» بدون شاهد شبکه و هویت، اعتماد کاذب می‌سازد.
- مسیر واقعی موبایل از معیار واجدشرایط خدمت به سؤال ارسال‌نشده و سپس رونویسی پاسخ، lineage را برای سازنده قابل‌دیدن نگه داشت. پس از ذخیره و reload، کارت سؤال «پاسخ دستی ثبت شده» را نشان داد و بازکردن پاسخ همان revision را برگرداند؛ بدون تبدیل آن به گفتگو یا دریافت شبکه‌ای.
- متن مختلط فارسی/LTR به `dir=auto` نیاز داشت و خطای ثبت نباید textarea سالم را برای screen reader نامعتبر اعلام کند. همچنین read-error مخزن پاسخ نباید در کارت سؤال به‌عنوان «بدون پاسخ» تعبیر شود؛ هر سه مورد پس از بازبینی اصلاح شدند.

### شکاف سند مادر

- سند مادر پیش‌نویس سؤال/پاسخ و نسخه‌بندی مذاکره را می‌خواهد، اما شیء مستقل رونویسی دستی، pin شدن به revision سؤال، وضعیت اصالت، رفتار ویرایش/no-op، invalidation و read-error آن را تعریف نمی‌کند.

### تصمیم‌های کاری پیاده‌شده و تأییدشدهٔ T8-A2

- `BuilderManualNegotiationResponseRecord` در store مستقل `chida-prototype-builder-manual-negotiation-responses:v1` نگه‌داری می‌شود و برای هر tuple دقیق `projectId + negotiationDraftId + negotiationDraftRevisionId` فقط یک رکورد دارد. snapshot کامل سؤال و target T8-A1 در آن تغییرناپذیر است.
- فیلدهای صداقت همواره `source=ثبت دستی سازنده`، `status=local-transcription`، `networkStatus=خارج از شبکه چیدا`، `supplierAuthenticated=false`، `authenticityVerified=false`، `questionSentThroughChida=false`، `receivedThroughChida=false`، `externalEffect=none`، `networkUsed=false`، `aiUsed=false`، `sendAuthorized=false`، `supplierNotified=false`، `sharedWithSupplier=false` و `externalActionAttempted=false` می‌مانند. `supplierNotified=false` فقط نبود اطلاع‌رسانی توسط چیدا را ثبت می‌کند و دربارهٔ اینکه سازنده بیرون از چیدا با طرف مقابل تماس داشته یا او را مطلع کرده است ادعایی ندارد.
- تنها `responseText` اجباری و نسخه‌دار است. ویرایش یعنی اصلاح همان رونویسی، نه پاسخ تازه؛ no-op بایت و نسخه را تغییر نمی‌دهد، write failure rollback می‌شود و parser/history/fingerprint fail-closed هستند.
- ساخت/ویرایش فقط برای revision جاری و معتبر سؤال مجاز است. با ویرایش سؤال یا dependency بالادستی، پاسخ قبلی سالم اما تاریخی و فقط‌خواندنی می‌ماند، به نسخهٔ تازه rebind نمی‌شود و revision تازهٔ سؤال می‌تواند پاسخ مستقل خودش را بگیرد.
- خطای خواندن store پاسخ از empty state جداست و فقط mutation پاسخ را قفل می‌کند؛ T8-A1 سالم همچنان خوانا/قابل‌ویرایش می‌ماند. خطای خواندن سؤال یا dependency لازم نیز بخش پاسخ را قفل می‌کند چون lineage قابل اثبات نیست.

### بازخورد پس از مشاهده و مجوز انتشار

- ماهیار پس از مشاهدهٔ T8-A2 گفت «بریم تسک بعدی». مطابق روال صریح پروژه، این بازخورد تأیید همین برش و مجوز commit، push و انتشار همه‌جای snapshot دقیق آزموده و receipt مستندی آن است؛ ابتدا باید GitHub، Cloudflare Pages و ChatGPT Sites هم‌منبع شوند و سپس فقط یک برش کوچک بعدی در لوکال آغاز شود.
- این مجوز به انتشار برش بعدی، پاسخ واقعی یا مشترک طرف دوم، شبکه/ارسال/اعلان، اثر بیرونی، مدل محلی یا مسیر تأمین‌کننده گسترش نمی‌یابد.
- ممیزی release پس از تأیید نشان داد عبارت مطلق «تأمین‌کننده مطلع نیست» با امکان دریافت پاسخ بیرون از چیدا قابل اثبات نیست. متن به «چیدا تأمین‌کننده را مطلع نکرده» محدود و regression آن اضافه شد؛ معنای `supplierNotified=false` نیز صریحاً فقط notification توسط چیدا تعریف شد، بدون تغییر schema یا ادعای تماس بیرونی.

### پیشنهاد برای تکمیل سند مادر

- `PrivateManualNegotiationResponse` از پاسخ واقعی/مشترک طرف دوم جدا تعریف شود و exact question revision، snapshot سؤال/target، وضعیت اصالت، source دستی و ممنوعیت rebind قرارداد رسمی داشته باشند.
- no-op، write-before-state، rollback، parser fail-closed، سقف رکورد/نسخه، project isolation، تمایز read-error از empty و تاریخی‌شدن پس از تغییر سؤال یا dependency بالادستی به invariant رسمی مذاکره اضافه شوند.
- هر پاسخ واقعی آینده باید یک رویداد و schema تازه با هویت/کانال/زمان قابل‌اثبات، مقصد، نسخه و Approval مستقل باشد و تاریخچهٔ رونویسی خصوصی سازنده را بازنویسی نکند.

### وضعیت شواهد و تحویل محلی

- هفت regression متمرکز، قرارداد exact برای خدمت و قلم محصول، project isolation/reload، no-op و revision/history، rollback و read-error مستقل، tamper fail-closed، ویرایش سؤال بدون rebind و invalidation پس از تغییر dependency بالادستی را پوشش دادند و ۷/۷ پاس شدند.
- suite کامل `test:app` برابر ۱۲۵/۱۲۵، `test:runtime` برابر ۱۳۳/۱۳۳ و `test:sites` برابر ۴/۴ پاس شد. `npm run check:runtime` برای ۲۸ فایل محافظت‌شده، build شامل TypeScript و Sites packaging، `npx tsc --noEmit` و `git diff --check` نیز موفق بودند؛ فقط هشدار شناخته‌شدهٔ chunk جاوااسکریپت بزرگ‌تر از 500kB باقی است.
- QA مرورگر داخلی در viewport واقعی `390 × 844` مسیر ماتریس خدمت ← revision نیازمند روشن‌سازی ← سؤال ارسال‌نشده ← پاسخ دستی را طی کرد. validation خالی، متن مختلط RTL/LTR، focus بازگشت/بازکردن، persistence پس از reload، label کارت، `dir=auto`، overflow افقی صفر در document/view، overlay صفر و console warning/error صفر تأیید شدند.
- بازبینی مستقل ابتدا نمایش نادرست empty در read-error، معنای `aria-invalid`، جهت متن و نبود regression dependency بالادستی را یافت؛ همه اصلاح و دوباره بازبینی شدند و finding باز P0/P1/P2 باقی نماند.
- **[وضعیت تاریخی پیش از انتشار]** snapshot تا لحظهٔ این تأیید فقط در working tree اصلی بود و هیچ commit، push، Cloudflare deploy یا ChatGPT Sites version برای T8-A2 نداشت؛ مجوز تازهٔ ماهیار اکنون فقط برای انتشار همین snapshot آزموده و receipt آن مصرف می‌شود.

### release کد T8-A2 و receipt پیش از commit مستندی نهایی

- commit کد `f5d68b6e757c0763a6770a46c5c3a211825713d3` روی local/GitHub `main` هم‌تراز شد. Cloudflare Pages deployment `11eb968e-8348-455a-abcf-6187221f897b` با `github:push`، شاخهٔ `main`، `commit_dirty=false` و همین source به وضعیت `success` رسید؛ canonical `https://chida-prototype.pages.dev` و immutable `https://11eb968e.chida-prototype.pages.dev` با build محلی hash یکسان داشتند: HTML برابر `e1d80ca062a1736bb6c1c1814b6920160f255c80b4362db4a80736fb4561f616`، JavaScript برابر `d9a5af99b514e38fe7106d9bc13d70a4b786d51e14ffa0f59fa20f8b6470246e` و CSS برابر `a8cc77e688cfdf669d53be72c248434f4f4122b2ddf6ace2f8f56bd141a5d444`.
- ChatGPT Sites نسخهٔ ۱۶ با ۱۸ فایل، source commit همین SHA و archive hash `sha256:9790a1b4054451e1192669dfea8ecb8a2a9f4e2086d54cfa13d8452d4bdce76e` ذخیره شد؛ version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_1c2eb22ab4c48191823c95adc6f00677` و deployment خصوصی `appgdep_6a92b4ea95748191be4cd9f8eed39bf8` در وضعیت `succeeded` روی `https://chida-prototype.mahyarkl.chatgpt.site` قرار گرفت. access owner-only/custom با یک مالک و بدون گروه یا مهمان بیرونی حفظ شد.
- **[وضعیت تاریخی پیش از receipt نهایی]** commit مستندی جاری فقط receipt و وضعیت را ثبت می‌کرد و prototype tree آن با `f5d68b6` یکسان می‌ماند؛ نتیجهٔ نهایی همان SHA `5357fc503df57f51bd4659401399f1b3b76a0caa` و receiptهای زیر شد و پس از آن T8-A3 فقط در working tree اصلی آغاز شد.

### receipt نهایی انتشار T8-A2

- commit مستندی نهایی `5357fc503df57f51bd4659401399f1b3b76a0caa` روی local/GitHub `main` هم‌تراز شد. prototype tree آن با commit کد `f5d68b6e757c0763a6770a46c5c3a211825713d3` یکسان و برابر `30440070ef4bf6b63633fccd0a9999953a36a16c` ماند؛ اختلاف این دو commit فقط سه سند وضعیت و receipt بود.
- Cloudflare Pages deployment نهایی `4ae8e8ee-bc2d-4b44-b542-7bffe7332181` با `github:push`، شاخهٔ `main`، `commit_dirty=false` و source commit نهایی به وضعیت `success` رسید. canonical `https://chida-prototype.pages.dev` همان asset hashهای release کد را حفظ کرد: HTML برابر `e1d80ca062a1736bb6c1c1814b6920160f255c80b4362db4a80736fb4561f616`، JavaScript برابر `d9a5af99b514e38fe7106d9bc13d70a4b786d51e14ffa0f59fa20f8b6470246e` و CSS برابر `a8cc77e688cfdf669d53be72c248434f4f4122b2ddf6ace2f8f56bd141a5d444`.
- ChatGPT Sites نسخهٔ ۱۷ با source commit نهایی، ۱۸ فایل و archive hash `sha256:9790a1b4054451e1192669dfea8ecb8a2a9f4e2086d54cfa13d8452d4bdce76e` ذخیره شد؛ version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_7be8bd1b07208191a90be04771891562` و deployment خصوصی `appgdep_6a92b60a7f848191bc8de2ac805a2934` در وضعیت `succeeded` روی `https://chida-prototype.mahyarkl.chatgpt.site` قرار گرفت. دسترسی owner-only/custom حفظ شد؛ T8-A2 منتشر و بسته شد.

### خارج از T8-A2

- ارسال/دریافت واقعی، حساب یا احراز تأمین‌کننده، delivery/read state، پیام مشترک، پاسخ AI، کشف تناقض، محاسبهٔ اثر تغییر شرایط، amendment و مقایسهٔ پیشنهاد تازه، deadline/follow-up، فایل، sync/backend و هر اقدام بیرونی ساخته نمی‌شوند.

## T8-A3 — ارزیابی خصوصی تناقض و ابهام سؤال/پاسخ

- **تاریخ شروع:** ۱۴۰۵/۰۶/۰۷ — ۲۰۲۶/۰۸/۲۹
- **وضعیت:** قرارداد، پیاده‌سازی، هفت regression متمرکز، suite کامل، QA مرورگر داخلی `390 × 844` و بازبینی مستقل کامل و توسط ماهیار تأیید شد؛ release کد با commit `8b7b85a2c6aa5a388df0a87d09f18ff0f1776e6d` و receipt مستندی نهایی `0b8810683dd5c35819397cea95c7ff01e3ce9c95` در GitHub، Cloudflare Pages و ChatGPT Sites نسخهٔ ۱۹ هم‌منبع منتشر و بسته شدند.
- **تجربهٔ مشاهده‌شده:** در جزئیات revision دقیق پاسخ T8-A2، سازنده یک child-flow تمام‌صفحه برای ثبت یا بازکردن بازبینی دستی دارد. سه قضاوت «از نظر شما پاسخ به پرسش می‌پردازد»، «از نظر شما نیازمند روشن‌سازی است» و «از نظر شما تعارض احتمالی دارد» همراه دلیل اجباری، منشأ و تاریخچه نمایش داده می‌شوند. editor/detail در viewport واقعی بدون overflow افقی بودند، focus روی heading/خطای مرتبط/رکورد/CTA بازگشت درست جابه‌جا شد و console خطا یا هشدار مرتبط نداشت.
- **بازخورد و تصمیم ماهیار:** نسخهٔ مشاهده‌شده تأیید شد؛ مجوز فقط انتشار same-source همین برش و ثبت receipt آن است. درخواست ساخت گفت‌وگوی تازه با هنداف کامل پس از پایان انتشار نیز ثبت شد؛ این تأیید مجوز شروع برش بعدی، مدل محلی یا مسیر تأمین‌کننده نیست.
- **قرارداد داده و شکست:** `BuilderManualNegotiationResponseReviewRecord` به `id/version/fingerprint` دقیق revision پاسخ pin می‌شود؛ lineage سؤال T8-A1 از snapshot تغییرناپذیر همان پاسخ به ارث می‌رسد و pin موازی دوم ساخته نمی‌شود. هر response revision رکورد مستقل دارد؛ تغییر پاسخ یا dependency بالادستی رکورد قبلی را تاریخی و فقط‌خواندنی می‌کند، نه اینکه آن را rebind کند. exact-key parser، fingerprint محلی، سقف رکورد/نسخه، project isolation، no-op byte stability، write-before-state/rollback و read-error lock مستقل پوشش داده شده‌اند؛ خرابی store بازبینی، پاسخ سالم T8-A2 را قفل یا پنهان نمی‌کند.
- **مرز صداقت:** این قضاوت فقط ثبت مستقیم سازنده است. `automatedDetectionUsed=false`، `aiUsed=false`، `networkUsed=false`، `authenticityVerified=false` و `externalEffect=none` ثابت‌اند؛ هیچ کشف AI، هویت یا اصالت طرف مقابل، پیام مشترک، شبکه، اعلان، اصلاح خودکار پیشنهاد، مجوز ارسال یا اثر بیرونی ادعا نمی‌شود.
- **شکاف/ابهام سند مادر:** عبارت‌های «پیداکردن ابهام یا تناقض» در مسیر مذاکره روشن نمی‌کنند که این نتیجه در برش پیش از مدل باید تشخیص چیدا باشد یا قضاوت صریح سازنده؛ نسبت آن با revision دقیق رونویسی پاسخ و رفتار historical/no-rebind نیز صریح نیست.
- **تصمیم پروتوتایپ تأییدشده:** تا اتصال مدل و منابع واقعی، فقط قضاوت دستی و برچسب‌خوردهٔ سازنده روی revision دقیق پاسخ مجاز است. هیچ outcome به‌تنهایی سؤال تازه، اعلان، تغییر پیشنهاد یا اقدام تجاری نمی‌سازد.
- **پیشنهاد اصلاح سند مادر:** در بازنگری بعدی، «بازبینی دستی سازنده» از «تشخیص مدل/منبع» جدا تعریف شود و target دقیق پاسخ، reason اجباری، version/history، currentness مشتق‌شده، historical/no-rebind و نبود اثر خودکار برای هر outcome تصریح شوند.
- **شواهد کیفیت محلی:** `T8-A3` متمرکز ۷/۷، `test:app` برابر ۱۳۲/۱۳۲، `test:runtime` برابر ۱۴۰/۱۴۰، `test:sites` برابر ۴/۴، build/TypeScript، `check:runtime` برای ۲۸ فایل، `npx tsc --noEmit` و `git diff --check` پاس شدند. دو finding دسترس‌پذیری P2 دربارهٔ focus خنثیِ outcome و `aria-describedby` نامرتبط پیش از تحویل اصلاح و با regression بسته شدند؛ بازبینی نهایی finding باز P0/P1/P2 ندارد.

### release کد و receipt نهایی T8-A3

- commit کد `8b7b85a2c6aa5a388df0a87d09f18ff0f1776e6d` روی local/GitHub `main` هم‌تراز شد و prototype tree آن `f341b95df9dcaf4b6e8bcdffd3300fbbf18750c8` است.
- Cloudflare Pages deployment `c26edc36-769d-4a13-9a3d-599401b58223` پس از push شاخهٔ `main` منتشر شد. canonical `https://chida-prototype.pages.dev` و immutable `https://c26edc36.chida-prototype.pages.dev` هر دو پاسخ ۲۰۰ و hash دقیق build محلی داشتند: HTML `c3f1bdc00d2c4d3b72955d693a4567538f191833ae149683b91983ac3acc5ea2`، JavaScript `d71326d6ff6640f3af7e4fbd7fb19bbb6c0a311b27809bf357eb959e8afafcd8` و CSS `74d8c2c111a5fff44b77278ea6109d298bc7b7b5ebcdc5e6bd680a20deebaddd`.
- ChatGPT Sites نسخهٔ ۱۸ با source commit همین SHA، ۱۸ فایل و archive hash `sha256:518b2be3a9fa2895965e6ed827a9f6d5f3db2a602d8c5155a888c08616992c42` ذخیره شد؛ version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_32761ebf98c08191b06b180887273315` و deployment خصوصی `appgdep_6a92c98b5f6c81919151a8a2a1962975` در وضعیت `succeeded` روی `https://chida-prototype.mahyarkl.chatgpt.site` قرار گرفت. دسترسی owner-only/custom با یک مالک و بدون گروه یا مهمان بیرونی حفظ شد.
- commit مستندی receipt نهایی `0b8810683dd5c35819397cea95c7ff01e3ce9c95` روی local/GitHub `main` هم‌تراز شد؛ prototype tree آن با commit کد `8b7b85a2c6aa5a388df0a87d09f18ff0f1776e6d` یکسان و برابر `f341b95df9dcaf4b6e8bcdffd3300fbbf18750c8` ماند.
- Cloudflare Pages deployment نهایی `e2fb901e-bc10-49b7-b041-5d25606af0ed` از `main` و source commit نهایی به وضعیت `success` رسید. canonical و immutable هر دو ۲۰۰ و با build محلی یکسان ماندند: HTML `c3f1bdc00d2c4d3b72955d693a4567538f191833ae149683b91983ac3acc5ea2`، JavaScript `d71326d6ff6640f3af7e4fbd7fb19bbb6c0a311b27809bf357eb959e8afafcd8` و CSS `74d8c2c111a5fff44b77278ea6109d298bc7b7b5ebcdc5e6bd680a20deebaddd`.
- ChatGPT Sites نسخهٔ ۱۹ با version ID `appgprj_6a90313e390c81918572fc1b45269dac~appgver_d1ee6e9c0774819196ded43a64197ff0`، source commit نهایی، ۱۸ فایل و archive hash `sha256:518b2be3a9fa2895965e6ed827a9f6d5f3db2a602d8c5155a888c08616992c42` ذخیره و با deployment `appgdep_6a92cb1191ac8191b655a8b73774b039` در وضعیت `succeeded` منتشر شد. دسترسی owner-only/custom با یک مالک، بدون گروه و بدون مهمان بیرونی حفظ شد.

## T8-A4 — ثبت دستی و کیفی اثر تغییر شرایط مذاکره

- **تاریخ شروع:** ۱۴۰۵/۰۶/۰۷ — ۲۰۲۶/۰۸/۲۹
- **وضعیت تاریخی پیش از انتشار:** قرارداد، پیاده‌سازی، هفت regression متمرکز، suite کامل، build/TypeScript، integrity runtime، QA مرورگر داخلی `390 × 844` و بازبینی مستقل کامل شدند و ماهیار پس از مشاهدهٔ نسخهٔ لوکال آن را با پیام «ادامه بده تسک بعدی خوبه» تأیید کرد. در همان لحظه snapshot فقط در working tree اصلی بود و آن بازخورد مجوز commit، push یا deploy نبود؛ ماهیار بعداً انتشار snapshot یکپارچه را جداگانه مجاز کرد و T8-A4 با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` در هر سه مقصد منتشر شد.
- **دامنه:** ثبت یک ارزیابی کیفی و دستی سازنده دربارهٔ تغییر یا روشن‌شدن شرط در revision دقیق پاسخ T8-A2؛ شامل خلاصهٔ تغییر، حوزهٔ اثر، جهت اثر از دید سازنده و دلیل. این برش محاسبهٔ عددی، تحلیل پاسخ، amendment پیشنهاد یا مقایسهٔ revision تازه نیست.

### تجربهٔ مشاهده‌شده

- entry مستقل T8-A4 در جزئیات هر revision پاسخ، بدون وابستگی به وجود یا نسخهٔ بازبینی T8-A3، سازنده را به editor/detail تمام‌صفحه می‌برد. متن‌های honesty، روش `manual-qualitative` و اتصال دقیق به پاسخ در context و detail آشکارند.
- در QA واقعی `390 × 844`، editor و detail بدون overflow افقی بودند؛ focus ورود روی heading، پس از ذخیره روی detail و پس از بازگشت روی CTA همان revision نشست. accessible name معیار و تماس را نگه داشت و console هیچ warning/error مرتبطی نداشت.
- تغییر صرفِ outcome یا دلیل بازبینی T8-A3 نباید ارزیابی اثر را stale کند؛ منشأ اثر خود revision پاسخ است. اصلاح پاسخ یا هر dependency بالادستی، رکورد قبلی را تاریخی و فقط‌خواندنی می‌کند و برای response revision تازه رکورد مستقل می‌سازد.

### شکاف/ابهام سند مادر

- سند مادر «محاسبهٔ اثر تغییر شرایط» را می‌خواهد، اما قرارداد داده، تفاوت محاسبهٔ عددی محصول با ارزیابی کیفی خدمت، baseline دقیق، فرمول، واحد، unknown، provenance و رفتار historical/no-rebind را تعریف نمی‌کند.
- روشن نیست «روشن‌شدن مقدار قبلاً نامشخص» همان amendment قطعی است یا فقط برداشت سازنده از پاسخ. همچنین معلوم نیست چند تغییر در یک پاسخ باید `items[]` مستقل داشته باشند یا یک خلاصهٔ چندحوزه‌ای برای برش پایه کافی است.

### بازخورد و تصمیم تأییدشدهٔ ماهیار

- تجربهٔ لوکال T8-A4 تأیید شد و ماهیار شروع یک برش کوچک بعدی سمت سازنده را خواست. دامنهٔ همین تأیید به انتشار T8-A4، مدل محلی، مسیر تأمین‌کننده، پاسخ واقعی طرف دوم، شبکه یا اثر بیرونی گسترش ندارد.
- قرارداد دستی و کیفی زیر به‌عنوان تصمیم پروتوتایپ T8-A4 حفظ می‌شود؛ محاسبهٔ عددی/فرمول‌دار همچنان یک شکاف جدا و حل‌نشدهٔ سند مادر است.

- `BuilderManualNegotiationConditionImpactRecord` یک رکورد مستقل، خصوصی، پروژه‌محور و one-per-exact-response-revision است. target فقط `id/version/fingerprint` همان response revision را pin می‌کند و lineage سؤال/مقایسه/پیشنهاد را از snapshot تغییرناپذیر داخل response T8-A2 به ارث می‌برد؛ snapshot موازی و rebind خاموش ساخته نمی‌شود.
- revision شامل `changeSummary`، `impactDomain`، `impactDirection` و `reason` اجباری است. دامنه و جهت صرفاً برچسب‌های ثبت‌شدهٔ سازنده‌اند؛ `automatedCalculationUsed=false`، `automatedDetectionUsed=false`، `aiUsed=false`، `networkUsed=false`، `authenticityVerified=false`، `proposalMutated=false`، `comparisonMutated=false` و `externalEffect=none` ثابت‌اند.
- no-op بایت/version/history را تغییر نمی‌دهد؛ تغییر معنادار revision/event تازه می‌سازد. exact-key parser، fingerprint، chronology، سقف ۱۰۰ revision، ۱۰۰ رکورد در پروژه و ۱۰۰۰ رکورد کل، write-before-state، rollback، tamper/read-error fail-close و project isolation اجرا می‌شوند. خرابی store اثر، پاسخ و بازبینی سالم را پنهان یا قفل نمی‌کند.
- این برش هیچ proposal/comparison/request/contact را تغییر نمی‌دهد، سؤال یا task پیگیری نمی‌سازد، پیام/اعلان/ارسال/مجوز/تبادل تماس ندارد و نتیجهٔ آن تأیید طرف مقابل یا محاسبهٔ چیدا نیست.

### پیشنهاد برای تکمیل سند مادر

- «ارزیابی کیفی دستی سازنده» از «محاسبهٔ deterministic چیدا» و «تحلیل مدل/منبع» جدا تعریف شود. برای هرکدام target، baseline، واحد/فرمول، provenance، unknown، history، currentness و اثر مجاز صریح شود.
- نسبت تغییر شرط با response revision، review sibling، amendment/fresh proposal revision و چند تغییر هم‌زمان مشخص شود؛ هیچ outcome بدون اقدام جدا و تأیید انسانی نباید proposal، comparison، task یا پیام را mutate کند.

### شواهد کیفیت محلی

- regression متمرکز T8-A4 برابر ۷/۷، `test:app` برابر ۱۳۹/۱۳۹، `test:runtime` برابر ۱۴۷/۱۴۷ و `test:sites` برابر ۴/۴ پاس شدند. build شامل TypeScript و آماده‌سازی Sites، `npx tsc --noEmit`، `check:runtime` برای ۲۸ فایل و `git diff --check` نیز پاس شدند؛ فقط هشدار شناخته‌شدهٔ chunk جاوااسکریپت بزرگ‌تر از 500kB باقی است.
- هفت regression مسیر خدمت و محصول، exact response pin، source-byte/no-network، project isolation، no-op/version/history، استقلال از تغییر T8-A3، validation/focus/overflow، rollback/read-error، tamper fingerprint، response-revision historical/no-rebind و invalidation بالادستی را پوشش می‌دهند.
- بازبینی مستقل برای source/tests/CSS finding باز P0/P1/P2 نداشت. finding مستندی P2 دربارهٔ عقب‌بودن Learnings/Handoff/Backlog با همین ثبت و هم‌ترازی اسناد بسته شد.

### خارج از T8-A4

- محاسبهٔ عددی یا فرمول اثر، تشخیص خودکار تغییر، تحلیل AI، مقایسهٔ revision تازهٔ پیشنهاد، amendment ساختاریافته، چند آیتم تفصیلی، deadline/follow-up، پاسخ واقعی/مشترک طرف دوم، پیام، شبکه، اعلان، sync/backend و هر اثر بیرونی همچنان ساخته نشده‌اند.

## T8-A5a — مقایسهٔ فقط‌خواندنی revisionهای رونویسی محلی پیشنهاد محصول

- **تاریخ شروع:** ۱۴۰۵/۰۶/۰۷ — ۲۰۲۶/۰۸/۲۹
- **وضعیت تاریخی پیش از انتشار:** ماهیار پس از تأیید لوکال T8-A4 شروع برش بعدی را خواست. T8-A5a در working tree اصلی پیاده‌سازی، آزمون متمرکز و QA موبایل شد و ماهیار پس از مشاهدهٔ نسخهٔ لوکال آن را با پیام «برو تسک بعدی» تأیید کرد. شکست زمان‌بندی runtime زیر بار سنگین CPU محیط نیز پس از آزادشدن CPU با بازاجرای isolated همان تست و نتیجهٔ ۱/۱ بسته شد، بدون تغییر runtime. در آن لحظه commit، push یا deploy مجاز نبود؛ ماهیار بعداً انتشار snapshot یکپارچه را جداگانه مجاز کرد و T8-A5a با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` در هر سه مقصد منتشر شد.
- **دامنه:** نمایش یک diff مشتق‌شده و فقط‌خواندنی میان دو revision دقیق و متفاوت از همان `BuilderRecordedProposalRecord` محصول. baseline باید قدیمی‌تر از candidate باشد؛ پیش‌فرض، revision جاری و نسخهٔ بلافاصله قبل از آن است.

### شکاف و مرز صداقت

- سند مادر «مقایسهٔ نسخه‌های جدید پیشنهاد» را می‌خواهد، اما proposal revision فعلی پروتوتایپ فقط ویرایش نسخه‌دار رونویسی دستی سازنده است؛ دریافت amendment رسمی، هویت/اصالت تأمین‌کننده، نوع تغییر و قرارداد shared-case هنوز وجود ندارند.
- بنابراین T8-A5a «پیشنهاد تازهٔ تأمین‌کننده» یا amendment را ادعا نمی‌کند. فقط مقادیر declared در دو snapshot تغییرناپذیر محلی را با id/version/fingerprint دقیق کنار هم می‌گذارد.

### تصمیم کاری و معیار پذیرش

- entry فقط برای پیشنهاد محصول دارای حداقل دو revision معتبر ظاهر می‌شود. هر دو revision باید از همان proposal و پروژه باشند و `baseline.version < candidate.version`؛ انتخاب نامعتبر نباید به revision دیگری fallback خاموش کند.
- `declaredAt`، `transcript`، `notes` و همهٔ فیلدهای هر قلم با هویت دقیق line/request item مقایسه می‌شوند. خروجی فقط `changed / unchanged` و مقدار قبل/بعد است؛ `null` صریحاً نامشخص می‌ماند و رشته‌های عددی بدون تبدیل به `Number` حفظ می‌شوند.
- view هیچ store، رکورد، version یا history تازه نمی‌سازد. `derivedReadOnly=true`، `persisted=false`، `aiUsed=false`، `networkUsed=false` و `externalEffect=none` مرز ثابت‌اند؛ arithmetic، delta درصدی، score، recommendation، mutation پیشنهاد/مقایسه و اقدام تجاری وجود ندارند.
- parser و project isolation موجود منبع حقیقت revisionهاست؛ tamper یا read-error پیشنهاد fail-close می‌شود و empty state دروغین نمی‌سازد. proposal تاریخی برای مالک قابل مشاهده است، اما diff آن باید «تاریخی · فقط‌خواندنی» برچسب بخورد.

### تجربهٔ مشاهده‌شده در پیاده‌سازی

- CTA فقط در جزئیات پیشنهاد محصول دارای دست‌کم دو revision معتبر ظاهر می‌شود. نما به‌طور پیش‌فرض revision جاری و نزدیک‌ترین revision پیشین را باز می‌کند و برای هر دو، id/version/fingerprint دقیق را پیش از delta نشان می‌دهد.
- diff بدون storage تازه از snapshotهای موجود مشتق می‌شود: سه فیلد سطح پیشنهاد و سیزده فیلد هر قلم، از جمله `currency`، با مقدار قبل/بعد و وضعیت تغییرکرده/بدون‌تغییر دیده می‌شوند. رشتهٔ عددی همان متن ثبت‌شده باقی می‌ماند و هیچ arithmetic انجام نمی‌شود.
- مقدار `null` با متن «نامشخص · ثبت نشده» و `data-value-kind=null` از literal آزاد کاربر با `data-value-kind=literal` جداست. جفت نامعتبر به‌جای برچسب جاری/تاریخی، صریحاً «جفت نامعتبر» و alert fail-close نشان می‌دهد و هیچ fallback ندارد.
- در QA واقعی `390 × 844`، منبع دقیق، selectorهای دسترس‌پذیر، مقدارهای قبل/بعد، badge جاری، focus آغاز و بازگشت، overflow صفر، نبود dialog/overlay باز و console بدون error تأیید شدند. تب محلی روی همین نمای مقایسه برای مشاهدهٔ ماهیار باز مانده است.

### بازخورد و تصمیم تأییدشدهٔ ماهیار

- ماهیار نسخهٔ لوکال T8-A5a را پذیرفت و شروع یک برش کوچک بعدی سمت سازنده را خواست. این تأیید، قرارداد diff فقط‌خواندنی محصول را تثبیت می‌کند اما به انتشار snapshot، amendment رسمی یا shared، محاسبهٔ عددی، مدل محلی، شبکه یا مسیر تأمین‌کننده مجوز نمی‌دهد.
- تنها gate باز قبلی، تست زمان‌بندی محافظت‌شدهٔ Carousel بود. پس از پایان فشار CPU بیرونی، همان تست با فرمان متمرکز و بدون تغییر هیچ فایل runtime با ۱/۱ پاس شد؛ اجرای کامل runtime برای snapshot یکپارچهٔ برش بعدی دوباره انجام می‌شود.

### آزمون و بازبینی تا این snapshot

- red baseline متمرکز پیش از wiring روی نبود `proposal-revision-diff-open` شکست خورد. پس از پیاده‌سازی و اصلاح findings، هفت regression T8-A5a همگی پاس شدند؛ پوشش شامل lineage دقیق، ۲۹ فیلد fixture دو‌قلمی، تمایز null/literal، انتخاب جفت تاریخی، invalid-pair fail-close، بدون‌ذخیره/بدون‌شبکه، project isolation، tamper/read-error، historical/no-rebind، focus/a11y و overflow است.
- `test:app` روی کد ثابت نهایی با ۱۴۶/۱۴۶ پاس شد؛ به‌علت اجرای هم‌زمان یک پردازش بیرونی با مصرف حدود ۴۰۰ تا ۵۰۰ درصد CPU، timeout فقط برای این اجرای شاهد به ۶۰ ثانیه افزایش یافت و تست‌های بدون تغییرِ قدیمی که در ۲۰ ثانیه timeout می‌شدند تا ۲۶ ثانیه زمان بردند و سالم پاس شدند. `test:sites` برابر ۴/۴، build شامل TypeScript و آماده‌سازی Sites، `npx tsc --noEmit`، `check:runtime` برای ۲۸ فایل و `git diff --check` نیز پاس شدند.
- اجرای کامل `test:runtime` در snapshot اولیهٔ T8-A5a، زیر بار CPU بیرونی ۱۵۳/۱۵۴ پاس داشت. تنها تست محافظت‌شدهٔ `Carousel preserves momentum and edge rubber-banding` روی انتظارهای wall-clock ثابت ۱۲۰/۹۰۰ میلی‌ثانیه شکست خورده بود؛ پس از آزادشدن CPU، همان تست با `npx playwright test tests/mobile-runtime.spec.ts -g "Carousel preserves momentum and edge rubber-banding" --workers=1` و نتیجهٔ ۱/۱ پاس شد. هیچ فایل runtime تغییر نکرد و integrity هر ۲۸ فایل پاس ماند؛ سپس روی snapshot یکپارچهٔ T8-A5b اجرای کامل runtime با ۱۶۱/۱۶۱ پاس شد و این gate محیطی بسته شد.
- بازبینی مستقل اولیه دو finding P1 دربارهٔ تمایز null/literal و حذف currency و یک P2 دربارهٔ جفت تاریخی یافت؛ هر سه رفع و بازبینی مجدد شدند. یک P2 بعدی دربارهٔ badge متناقض جفت نامعتبر نیز با وضعیت مستقل `unavailable` و regression صریح بسته شد.

### خارج از T8-A5a

- مقایسهٔ revision خدمت، amendment رسمی یا shared، محاسبهٔ عددی اثر، ساخت یا ویرایش proposal، ذخیرهٔ نتیجهٔ diff، تصمیم/رتبه‌بندی، deadline/follow-up، پیام، مدل، شبکه، اعلان، sync/backend و هر اثر بیرونی ساخته نمی‌شوند.

## T8-A5b — مقایسهٔ فقط‌خواندنی revisionهای رونویسی محلی پیشنهاد خدمت

- **تاریخ شروع:** ۱۴۰۵/۰۶/۰۷ — ۲۰۲۶/۰۸/۲۹
- **وضعیت:** ماهیار پس از تأیید لوکال T8-A5a با پیام «برو تسک بعدی» شروع یک برش کوچک بعدی را مجاز کرد. T8-A5b در همان working tree اصلی پیاده‌سازی، آزمون و در viewport واقعی `390 × 844` QA شد. پیام بعدی ماهیار در ۱۴۰۵/۰۶/۰۸ — ۲۰۲۶/۰۸/۳۰ انتشار کل snapshot تمام‌شده را برای مشاهده از خانه صریحاً مجاز کرد و T8-A5b با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` در هر سه مقصد منتشر شد؛ این انتشار به معنی تأیید تجربهٔ T8-A5b نیست و بازبینی آن همچنان باز می‌ماند.
- **دامنه:** نمایش یک diff مشتق‌شده و فقط‌خواندنی میان دو revision دقیق و متفاوت از همان `BuilderRecordedProposalRecord` خدمت. پیش‌فرض candidate جاری و نزدیک‌ترین baseline قبلی است؛ هر جفت تاریخی فقط با شرط `baseline.version < candidate.version` مجاز است و جفت نامعتبر بدون fallback fail-close می‌شود.

### شکاف سند مادر و مرز صداقت

- سند مادر مقایسهٔ «نسخه‌های جدید پیشنهاد» را می‌خواهد، اما revision proposal خدمت در پروتوتایپ فعلی فقط رونویسی خصوصی و نسخه‌دار سازنده است. amendment رسمی/shared، دریافت شبکه‌ای پاسخ، هویت/اصالت طرف دوم یا پیشنهاد تازهٔ احرازشده هنوز قرارداد و مسیر اجرایی ندارند.
- proposal خدمت یک line خام مشترک با proposal محصول دارد؛ schema ده‌معیارهٔ دامنه، محل، حجم، صلاحیت، زمان، روش، داخل/خارج کار، ضمانت و پرداخت متعلق به لایهٔ مستقل مقایسهٔ T7-B2 و شامل رونویسی تکمیلی و ارزیابی سازنده است. این معیارها نباید به‌عنوان مقادیر اعلامی proposal revision وارد diff T8-A5b شوند.

### تصمیم کاری و معیار پذیرش

- entry فقط برای proposal معتبر خدمت با حداقل دو revision ظاهر می‌شود. `requestSnapshot.requestKind` و target باید خدمت باشند؛ هر revision دقیقاً یک line با id ثابت، `requestItemId=null`، `serviceSpecId` غیرnull و برابر id خدمت در snapshot درخواست، label ثابت و currency ثابت دارد.
- سه فیلد سطح revision یعنی `declaredAt`، `transcript` و `notes` و سیزده فیلد line یعنی status، quantity، unit، unitPrice، totalPrice، currency، tax، transport، minimumOrder، leadTime، validity، paymentTerms و line notes مقایسه می‌شوند. هویت‌ها جداگانه نمایش و اعتبارسنجی می‌شوند و در شمار delta نیستند.
- مقایسه فقط exact string و تمایز صریح null/literal است؛ `Number`، محاسبهٔ اختلاف/درصد، normalization، score، ranking یا recommendation ندارد. ثابت‌های مرزی شامل `derivedReadOnly=true`، `persisted=false`، `arithmeticUsed=false`، `aiUsed=false`، `networkUsed=false`، `proposalMutated=false`، `comparisonMutated=false` و `externalEffect=none` هستند.
- بازکردن، انتخاب جفت و بازگشت هیچ byte از storage، نسخه/history proposal یا مقایسهٔ خدمت را تغییر نمی‌دهد. project isolation، historical/no-rebind و tamper/read-error fail-close حفظ می‌شوند.

### تجربهٔ مشاهده‌شده پیش از بازآرایی T8-UX1

- در snapshot اولیهٔ پیش از T8-UX1، CTA در جزئیات proposal خدمت دو نسخه‌ای با برچسب صریح T8-A5b دیده می‌شد. نما proposal/request، revision id/version/fingerprint و `serviceSpecId` دقیق را پیش از ۱۶ فیلد diff نشان می‌داد؛ T8-UX1 بعداً این نام داخلی و اولویت فنی را از نمای پیش‌فرض به disclosure حسابرسی منتقل کرد، بدون تغییر قرارداد زیرین.
- null و literal حتی اگر متن نمایشی یکسان «نامشخص · ثبت نشده» داشته باشند، با badge دیداری و قابل‌دسترسیِ «ثبت‌نشده در این نسخه» و «متن ثبت‌شدهٔ سازنده» از هم جدا می‌مانند. اگر proposal store بخشی سالم و بخشی مخدوش باشد، inbox هیچ فهرست ناقص، count، CTA یا empty state دروغین نشان نمی‌دهد و مسیرهای وابسته تا خواندن موفق قفل می‌شوند.
- در QA مرورگر داخلی `390 × 844` روی build نهایی، نسخهٔ جاری و قبلی، تغییر قیمت کل/اعتبار/متن و line note، lineage دقیق، ۱۶ فیلد، یک line خدمت، ۳۲ badge origin قابل‌دیدن، title focus، نام‌های دسترس‌پذیر، نبود dialog/overlay، overflow افقی صفر و console پاک تأیید شدند. تب پیش‌نمایش روی همین نمای خدمت برای مشاهدهٔ ماهیار باز مانده است.

### پیشنهاد اصلاح سند مادر

- در بازنگری بعدی، «revision رونویسی محلی سازنده» از «amendment رسمی/shared یا proposal تازهٔ احرازشده» جدا تعریف شود و identity، provenance، currentness، baseline/candidate، historical/no-rebind و رفتار invalid pair برای هرکدام صریح شود.
- فیلدهای خام مشترک proposal خدمت از معیارهای تکمیلی مقایسهٔ خدمت جدا بمانند؛ ورود هر معیار تازه به proposal revision به schema، منشأ و migration صریح نیاز دارد و نباید از UI فعلی T7-B2 استنباط شود.

### شواهد کیفیت محلی

- red baseline متمرکز پیش از wiring، نبود CTA خدمت را ثابت کرد. پس از پیاده‌سازی، هفت regression T8-A5b و مجموعهٔ ترکیبی T8-A5a/T8-A5b با ۷/۷ و ۱۴/۱۴ پاس شدند؛ پوشش شامل lineage و line identity دقیق، همهٔ ۱۶ فیلد، null/literal، بدون arithmetic، جفت تاریخی/نامعتبر، no-storage/no-network، project isolation، tamper/read-error، historical/no-rebind، focus/a11y و overflow است.
- `test:app` برابر ۱۵۳/۱۵۳، `test:runtime` برابر ۱۶۱/۱۶۱ و `test:sites` برابر ۴/۴ پاس شدند. build شامل TypeScript و آماده‌سازی Sites، `npx tsc --noEmit`، `check:runtime` برای ۲۸ فایل و `git diff --check` نیز پاس شدند؛ هشدار شناخته‌شدهٔ chunk جاوااسکریپت بزرگ‌تر از 500kB باقی است.
- ممیزی مستقل قرارداد و برنامهٔ آزمون، جدایی line خام proposal خدمت از schema ده‌معیارهٔ T7-B2 و همین مرز ۱۶ فیلدی را تأیید کردند. بازبینی مستقل نهایی دو P2 مربوط به نمایش فهرست ناقص در read-error و برخورد دیداری null/literal یافت؛ هر دو در source و regression رفع شدند و بازبینی دوباره هیچ finding باز P0/P1/P2 نداشت.

### خارج از T8-A5b

- amendment رسمی یا shared، پاسخ واقعی/احرازشدهٔ طرف دوم، mutation مقایسهٔ خدمت، محاسبه یا normalization عددی، تصمیم/رتبه‌بندی، deadline/follow-up، پیام، AI، مدل محلی، شبکه، اعلان، sync/backend، مسیر تأمین‌کننده و هر اثر بیرونی ساخته نشده‌اند.

## T8-UX1 — ساده‌سازی سراسری تجربهٔ پیشنهاد سازنده

- **تاریخ بازخورد:** ۱۴۰۵/۰۶/۰۷ — ۲۰۲۶/۰۸/۲۹
- **وضعیت:** بازخورد صریح ماهیار ثبت و بازطراحی UI-only در working tree اصلی پیاده‌سازی، آزمون و در viewport واقعی `390 × 844` QA شد. ماهیار در ۱۴۰۵/۰۶/۰۸ — ۲۰۲۶/۰۸/۳۰ گفت «وقتی تموم شد همه جا منتشر کن من از خونه میبینم»؛ snapshot آزموده با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` در GitHub، Cloudflare Pages و ChatGPT Sites منتشر شد، اما تأیید تجربهٔ UX تا مشاهدهٔ نسخهٔ منتشرشده باز می‌ماند.
- **دامنه:** ساده‌سازی صندوق پیشنهاد، ثبت/ویرایش، خلاصهٔ جزئیات و diff نسخه با افشای تدریجی؛ بدون تغییر schema، parser، fingerprint، version/history، lineage، project isolation، fail-close، AI/شبکه یا اثر بیرونی.

### تجربهٔ مشاهده‌شده و بازخورد کاربر

- ممیزی واقعی `390 × 844` نشان داد صندوق پیش از خود پیشنهادها سه ورودی بزرگ هم‌وزن برای مقایسهٔ محصول، مقایسهٔ خدمت و پیش‌نویس/پاسخ، یک بنر بلند، شمارنده و CTA ثبت را هم‌زمان نشان می‌دهد.
- جزئیات پیشنهاد پیش از مبلغ، زمان و اعتبار، شناسهٔ revision، snapshot، منشأ، تاریخچه و همهٔ شروط را نمایش می‌دهد. diff نیز نام داخلی برش، flagهای خام، id/fingerprint و همهٔ فیلدهای تغییرکرده و بدون تغییر را یک‌جا باز می‌کند.
- ماهیار صریحاً گفت این پیچیدگی برای سازنده ترسناک است و خواست کل فرایند پیشنهاد ساده، قابل‌فهم، کم‌متن و کم‌دکمه شود تا کاربر از محصول فرار نکند.

### شکاف سند مادر

- سند مادر اصل افشای تدریجی، یک اقدام روشن، پنهان‌ماندن پیچیدگی پشت صحنه و نمایش منبع/مجوز/تاریخچه در «سطح مناسب» را مقرر می‌کند، اما برای پیشنهاد مشخص نکرده کدام اطلاعات تصمیم‌ساز باید در نمای اول باشد و lineage فنی در کدام سطح باز شود.
- ثبت‌های T7/T8 برای اثبات قرارداد، نمایش دقیق id/version/fingerprint و flagها را در UI معیار پذیرش کرده بودند؛ این معیار با بازخورد تازهٔ کاربر باید از «نمای پیش‌فرض» به «جزئیات فنی قابل‌بازکردن» منتقل شود، نه اینکه از داده یا اعتبارسنجی حذف شود.

### تصمیم صریح و معیار کاری

- **[تصمیم کاربر، منتشرشده برای مشاهده؛ در انتظار تأیید تجربه]** صندوق در حالت عادی فقط یک CTA اصلی «ثبت پیشنهاد» دارد؛ مقایسه و پیگیری به سطح ثانویه می‌روند و نوع محصول/خدمت در لحظهٔ نیاز انتخاب می‌شود.
- **[تصمیم کاربر، منتشرشده برای مشاهده؛ در انتظار تأیید تجربه]** کارت و جزئیات با نام تأمین‌کننده، درخواست، وضعیت، مبلغ/زمان/اعتبار و اقدام بعدی آغاز می‌شوند. شروط کامل، مرجع و رونویسی، تاریخچه و lineage فنی بسته‌اند.
- **[تصمیم کاربر، منتشرشده برای مشاهده؛ در انتظار تأیید تجربه]** فرم در حالت ساده فقط فیلدهای روزمره را نشان می‌دهد و جزئیات تکمیلی بدون پاک‌کردن draft باز می‌شوند.
- **[تصمیم کاربر، منتشرشده برای مشاهده؛ در انتظار تأیید تجربه]** diff با «چه چیزی عوض شده؟»، «قبل/حالا» و فقط موارد تغییرکرده آغاز می‌شود؛ موارد بدون تغییر و id/fingerprint/flagها در disclosure جدا می‌مانند.
- خطای خواندن، وضعیت تاریخی/نیازمند بازبینی و مرز کوتاه «ثبت دستی شما · خصوصی پروژه · بدون ارسال» در سطح اصلی باقی می‌مانند. هیچ قرارداد داده، storage، mutation یا اعتماد به‌خاطر سادگی حذف نمی‌شود.

### نتیجهٔ پیاده‌سازی و مشاهده

- صندوق پیشنهاد با یک اقدام اصلی، کارت‌های کوتاه و مسیرهای ثانویهٔ جمع‌شده شروع می‌شود. فرم ثبت در حالت ساده فقط درخواست/تأمین‌کننده، وضعیت، مبلغ کل و زمان را نشان می‌دهد؛ حالت پیشرفته تمام داده‌های قبلی را بدون پاک‌شدن حفظ می‌کند و برای محصول، مبنای «همین مبلغ برای کل مقدار درخواست است» در همان حالت ساده قابل‌ثبت است.
- جزئیات پیشنهاد با خلاصهٔ قیمت، زمان، اعتبار و پرداخت آغاز می‌شود؛ شرط‌ها، مرجع، تاریخچه و رسید فنی بسته اما قابل‌بازیابی‌اند. revision تاریخی صریحاً «فقط مشاهده» است و ویرایش ندارد. diff موارد تغییرکرده را پیش از موارد ثابت نشان می‌دهد و مقایسهٔ محصول/خدمت، سؤال، پاسخ، بازبینی و اثر شرط با زبان طبیعی و افشای تدریجی بازآرایی شده‌اند.
- بازگشت از سؤال یا پاسخ به همان قلم محصول یا معیار خدمت، disclosure درست را باز و focus را به منشأ برمی‌گرداند. ممیزی مستقل پس از بازگرداندن همهٔ اطلاعات حسابرسی در سطح فنی، finding باز P0/P1/P2 ندارد.
- دو regression تازهٔ T8-UX1، app ۱۵۵/۱۵۵، runtime ۱۶۳/۱۶۳، Sites ۴/۴، build/TypeScript، integrity بیست‌وهشت فایل و `git diff --check` پاس شدند. QA مرورگر داخلی در `390 × 844` روی صندوق، فرم ساده/پیشرفته، جزئیات، diff، مقایسه و مذاکره overflow افقی صفر، نمای پیش‌فرض آرام، نبود نام‌های داخلی T7/T8 و حفظ دسترسی به audit detail را تأیید کرد. یک oracle قدیمی gesture که drag را از روی خود input آغاز می‌کرد، فقط در تست اصلاح شد؛ نقطهٔ شروع غیرتعاملی جدید در اجرای پرفشار موازی ۵۰/۵۰ پاس شد و هیچ فایل runtime محافظت‌شده تغییر نکرد.

### مجوز انتشار و مرز آن

- مجوز تازه فقط برای commit/push و انتشار same-source همین snapshot آزموده و receipt مستندی آن مصرف می‌شود. اتصال مدل محلی، مسیر تأمین‌کننده، پاسخ واقعی طرف دوم، ارسال بیرونی یا برش محصولی بعدی مجاز نشده است.
- پیش از commit، `HEAD`، `main` و `origin/main` روی baseline نهایی T8-A3 برابر `0b8810683dd5c35819397cea95c7ff01e3ce9c95` هم‌تراز بودند؛ working tree فقط نه فایل مستند/پروتوتایپ/تست مربوط به T8-A4/T8-A5a/T8-A5b/T8-UX1 داشت و `git diff --check` پاس بود. build نهایی همان assetهای HTML/JavaScript/CSS با هش‌های `64fbb6549ce602f8897203b2715d2da29fc00c1e71e1fb0fab4854177fc7d640`، `c40ea8cacfac4cbb37f6a62157db45b88b7c604c188746d955567d439b2d05f8` و `0399cb629d6ade85a55c300812929100a0013a5ee5503b28b9dc0d84ae47415a` را ساخت.
- ChatGPT Sites پیش از انتشار همچنان `custom`/owner-only با دقیقاً یک مالک، بدون گروه و بدون مهمان بیرونی بود؛ همین دسترسی باید حفظ شود. شناسه‌های commit/deployment/version/archive در commit مستندی receipt پس از موفقیت مقصدها backfill می‌شوند تا حلقهٔ self-reference ساخته نشود.

### release کد snapshot یکپارچه و receipt پیش از commit مستندی نهایی

- commit کد یکپارچهٔ T8-A4/T8-A5a/T8-A5b/T8-UX1 برابر `00a97a116a0115f6ffdae06579ded8fa64eb8c60` روی local/GitHub `main` هم‌تراز شد. working tree پس از commit و push تمیز بود.
- Cloudflare Pages deployment تولید `907c1060-7b24-4e0a-b625-b459462be282` از شاخهٔ `main` و source `00a97a1` منتشر شد. canonical `https://chida-prototype.pages.dev` و immutable `https://907c1060.chida-prototype.pages.dev` هر دو ۲۰۰ بودند و HTML/JavaScript/CSS دقیقاً هش‌های build محلی `64fbb6549ce602f8897203b2715d2da29fc00c1e71e1fb0fab4854177fc7d640`، `c40ea8cacfac4cbb37f6a62157db45b88b7c604c188746d955567d439b2d05f8` و `0399cb629d6ade85a55c300812929100a0013a5ee5503b28b9dc0d84ae47415a` را داشتند.
- ChatGPT Sites نسخهٔ ۲۰ با version ID `appgprj_6a90313e390c81918572fc1b45269dac~appgver_8654f68344588191acae2ac8f40b2b2d`، source commit دقیق `00a97a116a0115f6ffdae06579ded8fa64eb8c60`، هجده فایل و archive hash `sha256:dea74132d83b196ea3faabebc4581d25ad443ee9d9fbf5b987fd96627b0c294b` ذخیره شد. deployment خصوصی `appgdep_6a934e8632448191b455031830d69f1e` در وضعیت `succeeded` روی `https://chida-prototype.mahyarkl.chatgpt.site` منتشر شد؛ دسترسی `custom`/owner-only با یک مالک، صفر گروه و صفر مهمان بیرونی حفظ شد.
- commit مستندی receipt نهایی `d6224bbc392fda4b07910b49c110ada4859505f0` روی local/GitHub `main` هم‌تراز شد و prototype tree آن با commit کد یکسان و برابر `b1143dda693d72666ef71f4db5f93b421b8e061f` ماند. Cloudflare deployment نهایی `384cfcda-0c93-4902-aab0-0c6b36c69893` با source همین receipt، `github:push`، شاخهٔ `main` و `commit_dirty=false` به وضعیت `success` رسید.
- ChatGPT Sites نسخهٔ ۲۱ با version ID `appgprj_6a90313e390c81918572fc1b45269dac~appgver_6be5d1e18d308191832f820e195c613e`، source commit نهایی، ۱۸ فایل و archive hash `sha256:dea74132d83b196ea3faabebc4581d25ad443ee9d9fbf5b987fd96627b0c294b` ذخیره و با deployment خصوصی `appgdep_6a9351ec56688191a660aadfc1c05bcc` در وضعیت `succeeded` منتشر شد. دسترسی `custom`/owner-only با یک مالک، بدون گروه و بدون مهمان بیرونی حفظ شد.

### پیشنهاد برای تکمیل سند مادر

- برای اشیای پیشنهاد/مقایسه/مذاکره یک قرارداد ارائهٔ دو‌سطحی تعریف شود: سطح تصمیم‌ساز با زبان طبیعی و یک اقدام اصلی؛ سطح حسابرسی با provenance، نسخه، history، فرض، فرمول و شناسه‌های دقیق.
- معیار تجربه اضافه شود که مسیر عادی هیچ نام برش داخلی، fingerprint یا boolean خام نشان ندهد و diff در حالت پیش‌فرض changed-first باشد، در حالی که همهٔ جزئیات برای ممیزی قابل‌بازیابی می‌مانند.

## T8-UX2 — خلوت‌سازی رسانه، حافظه و جریان سریع درخواست خرید

- **تاریخ بازخورد:** ۱۴۰۵/۰۶/۰۸ — ۲۰۲۶/۰۸/۳۰
- **وضعیت:** بازخورد صریح ماهیار پیاده‌سازی، آزموده و پس از درخواست انتشار همه‌جا با commit کد `a08c93edefad6cc79bc7bb1f83a023730a2ef06f` و receipt نهایی `8c7e5d9a46f2da1acb02456b6b55010e6037db94` در GitHub، Cloudflare Pages و ChatGPT Sites نسخهٔ ۲۳ منتشر شده است؛ تأیید نهایی تجربه پس از مشاهده باز می‌ماند.
- **دامنه:** خلوت‌سازی ثبت و جزئیات عکس، فایل و حافظه؛ جداسازی عکس از «فایل‌ها»؛ بازشدن سند منتخب از محتوای محلی همان مرورگر؛ و کوتاه‌کردن درخواست خرید به «ثبت درخواست → انتخاب تأمین‌کننده → تأیید نهایی در کارها»، بدون ارسال، شبکه یا مسیر واقعی تأمین‌کننده.

### تجربهٔ مشاهده‌شده و بازخورد کاربر

- ماهیار در پانزده تصویر موبایل نشان داد که منشأ، نسخه، وضعیت، دسترسی و توضیح‌های تکراری، صفحه‌های عکس، فایل و حافظه را شلوغ و کم‌خوانا کرده‌اند؛ حذف متن‌های نام‌برده و افزایش اندازهٔ متن جزئیات را خواست.
- نمایش عکس‌ها هم در گالری و هم در «فایل‌ها» مرز دو فضای محصول را نامفهوم کرده بود. تصمیم کاربر این است که عکس فقط در گالری و سند فقط در «فایل‌ها» باشد و لمس ردیف سند، خود فایل را باز کند.
- جریان درخواست خرید میان پیش‌نویس، بازبینی محتوا، انتخاب گیرنده، Draft و تأیید برنامه رفت‌وبرگشت زیادی داشت. کاربر مسیر سریع و انتقال فقط تصمیم نهایی به «کارها» را خواست.

### شکاف سند مادر و تعارض قرارداد قبلی

- سند مادر ارسال پس از تأیید برای تأمین‌کنندگان مرتبط را تعریف می‌کند، اما تعداد تأییدهای داخلی و محل تأیید نهایی را مشخص نکرده است.
- در فاز فعلی فقط رکوردهای تأمین‌کنندهٔ ثبت‌شده توسط سازنده وجود دارند؛ ارتباط یا تطبیق هوشمند آن‌ها احراز نشده و نباید با عنوان «تأمین‌کنندهٔ مرتبط» ادعا شود.
- قرارداد قبلی فایل غیرتصویری را `metadata-only` نگه می‌داشت. تصمیم تازه فقط نگه‌داری و بازکردن محتوای سند منتخب در همان مرورگر را مجاز می‌کند؛ استخراج، جست‌وجوی محتوا، `SourceRecord`، آپلود یا ورود به زمینهٔ مدل همچنان مجاز نیستند.

### تصمیم صریح و معیار کاری

- **[تصمیم کاربر؛ پیاده‌سازی لوکال]** منشأ، نسخه، وضعیت و دسترسی از نمای عادی عکس، فایل و حافظه حذف می‌شوند. فیلدهای لازم برای جداسازی پروژه، rollback، migration و fail-close در قرارداد داده حفظ می‌شوند، اما به کاربر عادی تحمیل نمی‌شوند.
- **[تصمیم کاربر؛ پیاده‌سازی لوکال]** عکس فقط در گالری و سند فقط در «فایل‌ها» نمایش داده می‌شود. محتوای عکس یا سندی که کاربر مستقیم انتخاب کرده فقط به‌صورت Blob در IndexedDB همان مرورگر نگه‌داری می‌شود؛ Blob یا base64 وارد `localStorage` نمی‌شود. سند قدیمی `metadata-only` فقط پس از انتخاب فایل پشتیبانی‌شده‌ای با نام و اندازهٔ ثبت‌شده قابل‌بازشدن است و این تطبیق، اصالت یا برابری محتوای فایل قدیمی را تأیید نمی‌کند.
- **[تصمیم کاربر؛ پیاده‌سازی لوکال]** متن‌های توضیحی تکراری دربارهٔ مدرک پروژه، محتوای بیرونی، حافظه‌نبودن گفتگو، دسترسی خصوصی، زمینهٔ پروژه و «فقط در مرورگر» از مسیر عادی حذف می‌شوند؛ خطاهای واقعی و مرزهای لازم فقط هنگام خطر یا شکست باقی می‌مانند.
- **[تصمیم کاربر؛ پیاده‌سازی لوکال]** اندازهٔ متن در جزئیات عکس، فایل و حافظه افزایش می‌یابد؛ اندازهٔ فرم درخواست خرید که کاربر مناسب دانست حفظ می‌شود.
- **[تصمیم کاربر؛ پیاده‌سازی لوکال]** مسیر اصلی خرید سه گام دارد: ثبت نیاز، انتخاب یک یا چند تأمین‌کنندهٔ ثبت‌شده و ثبت برنامه در «کارها» برای تأیید نهایی. Approval محتوا، snapshot دقیق، DispatchDraft و Approval برنامه در داده حفظ می‌شوند، اما مراحل فنی در نمای اصلی افشا نمی‌شوند.

### مرز صداقت و اثر بیرونی

- تأمین‌کننده‌ها در این برش رکوردهای خصوصی و دستی همان پروژه‌اند؛ شبکه، حساب تأمین‌کننده، matching، ranking یا اثبات ارتباط ساخته نشده است.
- تأیید نهایی در «کارها» همان Approval محلی T6-D است و همچنان `simulationOnly=true`، `externalEffect=none`، `sendAuthorized=false` و `externalActionAttempted=false` می‌ماند.
- بازشدن محلی سند به معنی آپلود، sync، backup، parser، OCR، استخراج، جست‌وجوی محتوا، citation یا ورود فایل به حافظه/مدل نیست.

### وضعیت پیاده‌سازی و شواهد

- عکس‌ها از شمارش و فهرست اسناد جدا شدند؛ سند تازه در IndexedDB ذخیره و پس از reload با URL محلی باز می‌شود. برای شناسنامه‌های قدیمی مسیر انتخاب فایل هم‌نام و هم‌اندازه و تبدیل امن به `browser-file` ساخته شد؛ شکست metadata یا Blob rollback می‌شود.
- بازبینی امنیتی پیش از انتشار نشان داد اتکای صرف به پسوند می‌تواند Blob با MIME فعال مانند `text/html` را زیر نام `.pdf` بپذیرد. پذیرش سند اکنون پسوند و MIME سازگار می‌خواهد، MIME شناسنامه canonical می‌شود، Blob در write/read با MIME امن همان پسوند بازسازی می‌شود، رکورد ناسازگار fail-close به `metadata-only` برمی‌گردد و restore قدیمی نام/اندازه/نوع را دوباره در لایهٔ دامنه بررسی می‌کند.
- مسیر پیش‌فرض خرید اکنون از تأیید اطلاعات مستقیماً به انتخاب تأمین‌کننده‌ها و سپس جزئیات تأیید نهایی در «کارها» می‌رود. payload، revision، fingerprint و flagهای فنی فقط در disclosure بسته باقی مانده‌اند.
- build/TypeScript و integrity هر ۲۸ فایل، شش regression متمرکز T8-UX2 برابر ۶/۶، مجموعهٔ گستردهٔ رسانه/فایل/حافظه برابر ۲۰/۲۰، `test:app` مستقل برابر ۱۵۸/۱۵۸، `test:runtime` مستقل برابر ۱۶۶/۱۶۶ و `test:sites` برابر ۴/۴ پاس شدند. اجرای مستقل مجموعه‌های کامل، artifact اجرای هم‌زمان اولیه را بست و شکست باز باقی نماند.
- QA واقعی `390 × 844` روی گالری، فایل، حافظه، ثبت درخواست خرید، انتخاب تأمین‌کننده و تأیید نهایی در «کارها» overflow افقی صفر و console error صفر داشت. مقایسهٔ دیداری قبل/بعد و بازبینی مستقل نیز finding باز P0/P1/P2 نداشتند.
- Cloudflare deployment `c25eaefe-e43a-466f-9e7b-d268fe63bb7e` از `main`/SHA دقیق کد با `github:push`، `commit_dirty=false` و وضعیت `success` منتشر شد. canonical و immutable هر دو ۲۰۰ و با build محلی یکسان بودند: HTML `9410a3cd7e2560cb1ad7a9a5ebe46ee8fa60181e9f7440aa17a03ef7cf0a24c8`، JavaScript `2b4fe095c8f83a7b6648360636853eae8c9b0554a23fc6b04527e8a801323416` و CSS `bf5cf9314d94b54ec47684748229175a7c864a59230bfce63830bc95516c2754`.
- ChatGPT Sites نسخهٔ ۲۲ با version ID `appgprj_6a90313e390c81918572fc1b45269dac~appgver_e69bfaa67138819184f741aedce28725`، source commit دقیق کد، ۱۸ فایل و archive hash `sha256:39511f6d9e5641347e371ee981aef4999be97dce4dd52d2b50564a438162eb9f` ذخیره و با deployment `appgdep_6a93f1adf4788191999f921e914d39c7` در وضعیت `succeeded` منتشر شد؛ access `custom`/owner-only با یک مالک، بدون گروه و بدون مهمان بیرونی حفظ شد.
- receipt نهایی `8c7e5d9a46f2da1acb02456b6b55010e6037db94` همان prototype tree کد یعنی `0fe7bc7f77640e78091254e76d8d88670f002aa6` را حفظ کرد. Cloudflare deployment نهایی `e24010be-d6bd-427d-95f5-77f2c05ec9a5` از source همین receipt منتشر شد؛ ChatGPT Sites نسخهٔ ۲۳ با version ID `appgprj_6a90313e390c81918572fc1b45269dac~appgver_2660535ab57c81918d9f301eb74c8116`، source receipt، ۱۸ فایل و همان archive hash `sha256:39511f6d9e5641347e371ee981aef4999be97dce4dd52d2b50564a438162eb9f` ذخیره و با deployment `appgdep_6a93f27f77c48191adc895a833c41695` در وضعیت `succeeded` منتشر شد. access `custom`/owner-only با یک مالک، بدون گروه و بدون مهمان بیرونی حفظ شد.

### پیشنهاد برای تکمیل سند مادر

- برای فایل، حافظه و اشیای تجاری قرارداد ارائهٔ دو‌سطحی افزوده شود: سطح روزمره فقط محتوا و اقدام بعدی؛ سطح فنی اختیاری برای provenance، نسخه، تاریخچه و وضعیت.
- عکس و سند دو فضای روشن داشته باشند و نگه‌داری محتوای محلی سند از استخراج یا استفادهٔ مدل مستقل تعریف شود.
- اصل «تأیید متناسب با ریسک» تصریح کند که مراحل داخلی و برگشت‌پذیر نباید Approval جداگانه به کاربر تحمیل کنند؛ آخرین تصمیم پیش از اثر بیرونی در «کارها» متمرکز می‌شود.
- عنوان «مرتبط» فقط پس از وجود شبکه، دادهٔ پوشش و قرارداد تطبیق قابل‌آزمون استفاده شود.

## T8-UX3 — پولیش خانهٔ سازنده، نشان CHIDA و Quick Action کامل

- **تاریخ بازخورد:** ۱۴۰۵/۰۶/۰۸ — ۲۰۲۶/۰۸/۳۰
- **وضعیت:** بازخورد صریح ماهیار در checkout اصلی پیاده‌سازی، در viewport واقعی `390 × 844` QA و با commit کد `3e4b35fe08b02918e4be814d5d796da14bfa54ca` و commit مستندی receipt برابر `3bbd870ca021339d89abc09528f57fc690deec50` در GitHub، Cloudflare Pages و ChatGPT Sites نسخهٔ ۲۵ same-source منتشر شده است؛ تأیید تجربه پس از مشاهده باز می‌ماند.
- **دامنه:** خلوت‌سازی خانهٔ Chat-first، روشن‌ترکردن affordance پروژه، تبدیل Quick Action از grid به rail افقی dragپذیر، کامل‌کردن میان‌برهای صادقانهٔ قابلیت‌های ساخته‌شده، افزایش خوانایی و جایگزینی نشان عمومی با دارایی تصویری اختصاصی CHIDA؛ بدون تغییر schema، storage، runtime محافظت‌شده، مدل، شبکه یا اثر بیرونی.

### تجربهٔ مشاهده‌شده و بازخورد کاربر

- در نمای موبایل، عبارت تکراری «پروژه فعال» در کنترل بالای صفحه و «فضای پروژه» همراه فلش در نوار پایین، نام پروژه را در دو کنترل شلوغ و affordance کنترل پایین را مبهم کرده بود.
- چهار Quick Action در grid دو‌ستونه هم با مرجع Grok و هم با فهرست قابلیت‌های ساخته‌شده هم‌خوان نبود؛ ماهیار یک ردیف chip کوچکِ آیکون‌دار و dragپذیر خواست و کامل‌بودن اکشن‌ها را به سند ارجاع داد.
- نشان Sparkles وسط صفحه عمومی و قابل‌تعویض با محصولات دیگر بود. متن‌های کلیدی خانه نیز در موبایل کمی کوچک دیده می‌شدند.

### شکاف سند مادر و تصمیم صریح

- سند مادر خانوادهٔ قابلیت‌ها و مقصدهای اصلی را مشخص می‌کند، اما فهرست بسته و رسمی Quick Action ندارد. بنابراین «کامل» در این snapshot به معنی دسترسی مستقیم به قابلیت‌های واقعاً ساخته‌شده است، نه نمایش قابلیت‌های بک‌لاگ یا affordance بی‌عمل.
- **[تصمیم کاربر؛ جایگزین قاعدهٔ تاریخی C-010 دربارهٔ grid]** Quick Action خانه یک rail افقی dragپذیر با محتوای RTL است؛ overflow فقط داخل خود rail مجاز است و صفحه نباید overflow افقی داشته باشد. این تصمیم، الگوی grid بدون overflow قبلی را برای خانه منسوخ می‌کند و Carousel محافظت‌شده بدون تغییر استفاده می‌شود.
- **[تصمیم کاربر]** کنترل پروژه در بالا و پایین فقط نام پروژه را نشان می‌دهد؛ متن کمکی و فلش حذف می‌شوند، اما هر دو button با نام دسترس‌پذیر و affordance دیداری روشن باقی می‌مانند.
- **[تصمیم کاربر]** نشان وسط خانه یک فایل تصویری اختصاصی CHIDA است و متن‌های خانه به‌صورت scoped درشت‌تر می‌شوند تا صفحه‌های پیچیدهٔ دیگر ناخواسته تغییر نکنند.

### نتیجهٔ پیاده‌سازی و مرز صداقت

- rail ده اقدام دارد: «درخواست قیمت»، «پیشنهادها»، «کار جدید»، «افزودن فایل»، «افزودن عکس»، «ثبت حافظه»، «جست‌وجوی پروژه»، «برایم بساز»، «شروع صورت‌جلسه» و «چیدن برنامه خرید». هشت مورد نخست مقصد ساخته‌شده را باز می‌کنند؛ دو مورد آخر فقط شروع‌کنندهٔ صریح متن در Composer هستند و workflow یا شیء ماندگار تازه ادعا نمی‌کنند.
- rail در ورود و بازگشت به ابتدای RTL هم‌تراز می‌شود، با drag جابه‌جا می‌شود، بخشی از chip بعدی را برای discoverability نشان می‌دهد و focus پس از بازگشت به منشأ برمی‌گردد. overflow صفحه صفر می‌ماند.
- نشان اختصاصی با ImageGen به‌صورت PNG شفاف و بدون متن ساخته و در `prototype/public/chida/chida-assistant-mark-v1.png` نگه‌داری شد. این دارایی فقط هویت بصری است و هیچ ادعای هویت تأییدشده، قابلیت AI متصل یا اثر بیرونی ندارد.
- هشت اکشن واقعی همان قراردادهای موجود را باز می‌کنند؛ Quick Action تازه matching، حساب تأمین‌کننده، ارسال، شبکه، مدل محلی، parser یا mutation خارج از مسیرهای فعلی نمی‌سازد. Build همچنان کنترل‌شده و بدون نصب واقعی یا اجرای کد آزاد است.

### شواهد کیفیت محلی

- چهار regression متمرکز برای تعداد/ترتیب rail، drag و offset RTL، همهٔ مقصدها/شروع‌کننده‌ها، کنترل‌های فقط‌نام، affordance، بارگذاری نشان و حداقل اندازهٔ متن ۴/۴ پاس شدند. assertion تاریخی متن حذف‌شده نیز اصلاح و مجموعهٔ متمرکز نهایی ۵/۵ پاس شد.
- build/TypeScript، integrity هر ۲۸ فایل، `test:app` برابر ۱۵۹/۱۵۹، `test:runtime` برابر ۱۶۷/۱۶۷، `test:sites` برابر ۴/۴ و `git diff --check` پاس شدند. هشدار شناخته‌شدهٔ chunk جاوااسکریپت بزرگ‌تر از 500kB باقی است.
- QA مرورگر داخلی در `390 × 844` مقدار `rootOverflow=0`، overflow داخلی rail برابر ۹۱۸ پیکسل، شروع RTL در offset نهایی، فونت ۱۵px نام پروژه، ۱۴px متن راهنما، ۱۲px chip، border روشن کنترل پروژه، بارگذاری کامل نشان و console بدون warning/error را تأیید کرد. نشان نهایی برای slot کوچک خانه از ۱۲۵۴px/حدود ۴۸۶KB به ۲۵۶px/حدود ۳۶KB بهینه شد و alpha حفظ شد.
- مقایسهٔ مرجع و پیاده‌سازی در یک ورودی دیداری انجام و نتیجه در `design-qa.md` با وضعیت `passed` ثبت شد. بازبینی مستقل پس از رفع وزن دارایی و دو تعارض مستندی، هیچ finding باز P0/P1/P2 نداشت.

### release کد و receipt مستندی نهایی

- commit کد `3e4b35fe08b02918e4be814d5d796da14bfa54ca` روی local/GitHub `main` هم‌تراز شد و prototype tree آن `4297309252e2e6f9d80c184be7aad1e795c80279` است.
- Cloudflare Pages deployment تولید `9b78e089-e0f5-427b-b788-9221b908ef7d` از شاخهٔ `main` و source `3e4b35f` فعال شد. canonical `https://chida-prototype.pages.dev` و immutable `https://9b78e089.chida-prototype.pages.dev` هر دو برای HTML، JavaScript، CSS و نشان ۲۰۰ بودند و هش‌های build محلی را داشتند: HTML `2872325a9536c3c30933c0bf4e05c921c5c57841b2050565d4b4c0e4456ca2ce`، JavaScript `5b5b5cef51c427d108b02983213d14e6e27e403ec99fd27e7c7a75cd5e15ef1b`، CSS `94283e31ad8830896c0e44498a248d96f392b1de83912d9783a2e02867be81df` و نشان `44cc69c64df0f51d8603ce8157e3f3d7eb753ac4908eddf80b9e773ef1231cad`.
- ChatGPT Sites نسخهٔ ۲۴ با version ID `appgprj_6a90313e390c81918572fc1b45269dac~appgver_86ea0b7d394c8191aef34c83a2ae0e2f`، source commit دقیق کد، ۱۹ فایل و archive hash `sha256:316dbe635d0483a16eb3480c288898d8c14a1af97f2b1882187c6baf05da18f5` ذخیره و با deployment خصوصی `appgdep_6a940cbe06d48191912661b9e4ec4615` در وضعیت `succeeded` روی `https://chida-prototype.mahyarkl.chatgpt.site` منتشر شد. access `custom`/owner-only با یک مالک، بدون گروه و بدون مهمان بیرونی حفظ شد.
- commit مستندی receipt نهایی `3bbd870ca021339d89abc09528f57fc690deec50` روی local/GitHub `main` هم‌تراز شد و prototype tree آن با commit کد یکسان و برابر `4297309252e2e6f9d80c184be7aad1e795c80279` ماند. Cloudflare deployment `f665580f-26c9-4363-9793-d4baaae5e644` از همین source موفق شد؛ canonical و immutable hashهای release کد را حفظ کردند. ChatGPT Sites نسخهٔ ۲۵ با version ID `appgprj_6a90313e390c81918572fc1b45269dac~appgver_4fd3dab34a8481918949ad5a947d6942` و deployment `appgdep_6a941bdda88881918951b8508e2c2eec` در وضعیت `succeeded` منتشر و owner-only/custom حفظ شد. شناسه‌های انتشار خود هنداف Sol به‌دلیل حلقهٔ self-reference در پیام تحویل ثبت می‌شوند.

### پیشنهاد برای تکمیل سند مادر

- برای خانهٔ Chat-first، قرارداد Quick Action به‌صورت rail افقی با فهرست نسخه‌دارِ «قابلیت واقعی» و تمایز صریح «مقصد» از «شروع‌کنندهٔ گفتگو» ثبت شود.
- قاعدهٔ نمایش زمینهٔ پروژه اجازه دهد نام پروژه به‌تنهایی، در صورت affordance و accessible name روشن، جای متن‌های تکراری «پروژه فعال/فضای پروژه» را بگیرد.
- برای هویت بصری CHIDA یک دارایی مرجع، اندازه‌های کاربرد و معیار کنتراست/خوانایی تعریف شود تا نشان موقت یا عمومی دوباره وارد صفحهٔ اصلی نشود.

## H1 — توقف برای بازبینی Sol و قرارداد Memory/Harness

- **تاریخ تصمیم:** ۱۴۰۵/۰۶/۰۸ — ۲۰۲۶/۰۸/۳۰
- **وضعیت:** snapshot فعلی کامل آزموده و منتشر شد؛ هنداف مستقل `CHIDA-SOL-HANDOFF-FA.md` تهیه شد و ادامهٔ پیاده‌سازی تا بازبینی Sol و انتخاب صریح ماهیار متوقف است.
- **دامنه:** جمع‌بندی آنچه واقعاً در prototype سازنده ساخته یا شبیه‌سازی شده، مسیر باقی‌ماندهٔ سازنده و نظر معماری پیشنهادی دربارهٔ Memory، Harness و Build؛ بدون ساخت سند معماری مرجع، تغییر سند مادر، اتصال مدل، backend، شبکه یا مسیر تأمین‌کننده.

### تجربه و پرسش کاربر

- ماهیار پرسید حافظهٔ CHIDA چگونه باید یاد بگیرد و به یاد بیاورد و ارزش افزودهٔ Harness همراه مدل چیست. سپس تأکید کرد هنوز زمان ساخت اسناد معماری مرجع نیست و ابتدا باید prototype سازنده کامل شود.
- درخواست نهایی این مرحله این بود که snapshot موجود کامل منتشر، یک هنداف مفصل شامل نظر معماری تهیه و برای Sol در گفت‌وگوی موجود CHIDA ارسال شود؛ سپس پروژه تا بررسی Sol متوقف بماند.

### شکاف و مشاهده

- سند مادر دامنه‌های حافظه، لزوم منبع/تاریخ/کنترل و حلقهٔ عمومی Harness را تعریف می‌کند، اما `MemoryRecord` نسخه‌دار، taxonomy اعتبار، retention، tombstone/hard-delete، `ContextManifest`، interface اجرایی Harness، failure union، idempotency، بودجهٔ اجرا و permission envelope هنوز قرارداد اجرایی ندارند.
- prototype فعلی فقط حافظهٔ دستی project-scoped و retrieval قطعی metadata دارد. `useInContext` ترجیح محلی است و هیچ حافظه، فایل یا تاریخچه‌ای به مدل وارد نمی‌شود.
- ادامهٔ shared negotiation پیش از وجود طرف دوم و backend خطر ساخت تجربهٔ نمایشی و اعتماد کاذب دارد؛ در مقابل، عمق مدیریت پروژه، Brief/Report، دامنه‌های حافظه و lifecycle Build پایهٔ ارزش سازنده و Harness آینده‌اند.

### تصمیم و مرز جاری

- **[تصمیم کاربر]** ابتدا snapshot موجود در GitHub، Cloudflare و ChatGPT Sites کامل منتشر شود، سپس هنداف Sol تهیه و پروژه متوقف شود.
- **[تصمیم کاربر]** فعلاً سند معماری مرجع Memory/Harness ساخته یا سند مادر بازنویسی نمی‌شود؛ Sol ابتدا مسیر را بازبینی می‌کند.
- **[مرز صریح]** این مرحله مجوز شروع برش بعدی، مدل محلی، provider، backend، shared case، شبکه یا مسیر تأمین‌کننده نیست.
- **[صداقت محصول]** مدل نباید صاحب حافظه تلقی شود؛ در پیشنهاد معماری هنداف، داده/حافظه متعلق به محصول است و Harness فقط context محدود، مرتبط، تازه و مجاز را به مدل قابل‌تعویض می‌دهد.

### نظر معماری پیشنهادی برای بازبینی Sol

- منبع حقیقت از اشیای دامنهٔ ساختاریافته و نسخه‌دار آغاز شود؛ فایل/SourceRecord و حافظهٔ صریح یا تأییدشده در رتبه‌های بعد باشند و chat history منبع حقیقت نباشد.
- دامنه‌های `personal / project / case-private / case-shared / procedural` مستقل و دارای owner، source، date، truth/inference، version، permission، freshness، retention و audit باشند.
- خروجی استنباطی مدل فقط `MemoryCandidate` باشد و بدون تأیید کاربر به حافظهٔ پایدار تبدیل نشود؛ دادهٔ حساس یا اختلافی هرگز بی‌صدا ذخیره نشود.
- Harness به‌صورت orchestrator مستقل از UI و مدل، با `ContextManifest`، ModelGateway provider-neutral، registry ابزار محدود، permission envelope، state machine، validation، approval، idempotency، cancel/timeout/retry/recovery، audit و eval تعریف شود.
- برش اثبات معماری پس از پایان رسمی سازنده باید ثبت صریح حافظه در پروژهٔ A، retrieval دلیل‌دار، مشاهدهٔ context، منع استفاده/حذف مؤثر، جداسازی پروژهٔ B، نمایش تعارض و تعویض‌پذیری مدل را در یک vertical slice پوشش دهد.
- پیشنهاد اجرایی برای نظر Sol این است که shared negotiation وابسته به طرف دوم defer و برش کوچک بعدی، در صورت تأیید ماهیار، از T9-A و اتصال نقطهٔ عطف/تصمیم/کار پروژه انتخاب شود.

### رسید کیفیت و انتشار مبنا

- commit کد T8-UX3 `3e4b35fe08b02918e4be814d5d796da14bfa54ca` و receipt مستندی `3bbd870ca021339d89abc09528f57fc690deec50` با prototype tree یکسان `4297309252e2e6f9d80c184be7aad1e795c80279` در GitHub منتشر شدند.
- Cloudflare deployment نهایی `f665580f-26c9-4363-9793-d4baaae5e644` از source commit مستندی فعال است و canonical/immutable برای HTML، JavaScript، CSS و نشان دقیقاً hashهای build محلی را دارند.
- ChatGPT Sites نسخهٔ ۲۵ با version ID `appgprj_6a90313e390c81918572fc1b45269dac~appgver_4fd3dab34a8481918949ad5a947d6942` و deployment `appgdep_6a941bdda88881918951b8508e2c2eec` در وضعیت `succeeded` منتشر شد؛ owner-only/custom حفظ است.
- اجرای تازهٔ `check:runtime`، build، app `159/159`، runtime `167/167` و Sites `4/4` همگی پاس شدند. commit و شناسه‌های انتشار خود هنداف به‌دلیل self-reference در پیام تحویل ثبت می‌شوند.
