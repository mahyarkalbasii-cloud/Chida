# چیدا — تعریف و راهبرد محصول

**پیش‌نویس ۰.۱ · ۱۴۰۵/۰۵/۲۲**

## جایگاه این سند

این سند تعریف، مخاطب و منطق راهبردی چیدا را تثبیت می‌کند. قابلیت نسخهٔ یک، تجربهٔ کار، بازار، رفتار و فازها به‌ترتیب در اسناد ۰۲ تا ۰۶ آمده‌اند. تغییری که اصول این سند را عوض کند، تصمیم بنیادین است و باید در `DECISIONS.md` ثبت شود.

## ۱. تعریف محصول

چیدا یک همکار هوشمند فارسی‌زبان برای بازار ساختمان ایران است که به سازنده کمک می‌کند اطلاعات، تصمیم‌ها و کارهای هر پروژه را در یک فضای منظم پیش ببرد و به تأمین‌کنندهٔ محصولات و خدمات کمک می‌کند عرضه‌اش را مرتب کند، فرصت‌های مرتبط را ببیند و پاسخ بهتری آماده کند.

در نسخهٔ نخست، کاربر با یک شخصیت واحد به نام «چیدا» روبه‌رو است، نه عامل‌های قابل انتخاب. چیدا محیط کار و واسط گفت‌وگویی آن است: ورودی‌های مجاز را به خروجی قابل بررسی و ادامه تبدیل می‌کند. دامنهٔ دقیق در سند ۰۲ است.

شعارهای «ساخت‌وساز، چیده‌شده»، «چیدا پروژه‌تان را می‌چیند» و «چیدا فروشگاه‌تان را می‌چیند» در اسناد قبلی مطرح شده‌اند. این عبارت‌ها گزینه‌های تاریخی برندند، نه کانن قطعی محصول. انتخاب یا کنارگذاشتن آن‌ها یک تصمیم باز برند است و باید جداگانه در `DECISIONS.md` نهایی شود.

## ۲. مسئله‌ای که چیدا حل می‌کند

### مسئلهٔ مشترک بازار

اطلاعات بازار ساختمان میان پیام‌رسان‌ها، تماس‌ها، فایل‌ها، افراد و حافظهٔ شخصی پخش است. هم‌زمان میان عرضه‌کننده و خریدار **عدم تقارن اطلاعات** وجود دارد: سازنده نمی‌داند چه عرضه‌ای متناسب و به‌روز است و تأمین‌کننده نمی‌داند کدام نیاز واقعی و مرتبط است. پیام بیشتر می‌تواند بر شلوغی و بی‌اعتمادی بیفزاید.

چیدا باید اطلاعات پراکنده را به زمینهٔ قابل استفاده تبدیل کند و ارتباط دو سمت را در لحظه‌ای معنادار شکل دهد، بدون آنکه خود وارد معامله شود یا دادهٔ خصوصی یک سمت را بی‌اجازه به سمت دیگر بدهد.

### مسئلهٔ سازنده

سازنده تصمیم‌ها، اسناد، خریدها، پرداخت‌ها، سررسیدها و پیگیری‌ها را هم‌زمان در ذهن نگه می‌دارد. ابزارهای عمومی یا بخشی‌اند یا به ثبت دستی پایداری نیاز دارند. در نتیجه حافظهٔ فرد مرکز پروژه می‌شود، تصمیم‌ها زمینه را از دست می‌دهند و موارد مهم دیر دیده می‌شوند.

هوک سازنده نباید به استعلام وابسته باشد. حتی اگر هیچ تأمین‌کننده‌ای در چیدا حضور نداشته باشد، فضای پروژه باید به او کمک کند افکار و اطلاعاتش را مرتب کند، فایل و سابقه را در جای درست بگذارد، وضعیت را بفهمد و کار بعدی را ببیند. استعلام و دسترسی به عرضه، ارزش افزودهٔ همین محیط است، نه شرط ارزشمندشدن آن.

### مسئلهٔ تأمین‌کننده

تأمین‌کننده یا مجری خدماتی معمولاً اطلاعات عرضه‌اش را در چند کانال و با کیفیت نامنظم نگه می‌دارد. معرفی محصول و خدمت، مشخصات، تصویر و قیمت ممکن است ناقص یا قدیمی باشد و فرصت‌های فروش نیز از میان ارتباطات پراکنده عبور کنند. از سوی دیگر، دسترسی بی‌قید به فهرست سازندگان هم مزاحمت ایجاد می‌کند و هم اعتماد بازار را از بین می‌برد.

چیدا باید به تأمین‌کننده کمک کند یک عرضهٔ مرتب و قابل مراجعه بسازد، فرصت‌های مجاز را در یک محل ببیند و برای پاسخ از زمینهٔ فروشگاهش استفاده کند. ارتباط با سازنده در چهارچوب‌های سند ۰۴ محدود می‌ماند؛ حضور در چیدا مجوز دسترسی مستقیم به سازندگان نیست.

## ۳. مخاطب نخست

### سازنده

مخاطب نخست، فردی است که یک یا چند پروژهٔ ساختمانی در حال ساخت در تهران دارد و خودش در تصمیم‌گیری و پیگیری روزمره نقش مستقیم دارد. ممکن است مالک‌سازنده، سازندهٔ حرفه‌ای یا مدیر پروژه باشد؛ وجه مشترک او عنوان شغلی نیست، بلکه بار ذهنی بالای پروژه و نیاز به نظم قابل اتکاست.

چیدا در آغاز برای سازمان‌های بزرگ با فرایندهای تثبیت‌شده، تیم‌های چندلایه و یکپارچگی‌های سنگین طراحی نمی‌شود. نسخهٔ نخست «برای خودِ کاربر» است. نقش اعضای تیم و همکاری چندنفره به فازهای بعد تعلق دارد؛ زمان و شکل آن در سند ۰۶ و `DECISIONS.md` تعیین می‌شود.

### تأمین‌کننده

مخاطب نخست این سمت، فروشندهٔ محصول یا ارائه‌دهندهٔ خدمت ساختمانی است که **تهران را پوشش می‌دهد**؛ لازم نیست در تهران مستقر باشد. او می‌خواهد فروشگاهش را مرتب کند، فرصت مرتبط بگیرد و بهتر پاسخ دهد.

دامنه از ابتدا هم محصول و هم خدمت را دربرمی‌گیرد. به‌دلیل تنوع زیاد بازار، دسته‌بندی نباید به یک فهرست بسته و طولانی تبدیل شود. منطق دقیق کشف و برچسب‌گذاری عرضه در سندهای ۰۲ و ۰۴ می‌آید.

## ۴. کاری که هر سمت چیدا را برای آن به‌کار می‌گیرد

کار اصلی سازنده این است: «کمکم کن پروژه‌هایم را از ذهن و پیام‌های پراکنده بیرون بیاورم، بدانم چه دارم و چه چیزی توجه می‌خواهد و بدون گم‌کردن زمینه، کار بعدی را پیش ببرم.»

کار اصلی تأمین‌کننده این است: «کمکم کن محصولات و خدماتم را منظم و قابل پیدا شدن عرضه کنم، فرصت‌های مرتبط را از دست ندهم و با اتکا به اطلاعات خودم پاسخ قابل قبول آماده کنم.»

برای سازنده، وعدهٔ ورود «نظم فوری در یک پروژهٔ واقعی» است. آنبورد باید با گفت‌وگو از همان اطلاعاتی که اکنون در ذهن یا فایل‌های اوست یک نتیجهٔ قابل مشاهده بسازد؛ نه اینکه ابتدا فرم‌ها و ساختار محصول را آموزش دهد. دلیل ماندگاری، بازگشت به زمینهٔ جمع‌شدهٔ پروژه، دیدن موارد نیازمند توجه و ادامهٔ طبیعی کار است.

برای تأمین‌کننده، وعدهٔ ورود «یک عرضهٔ تمیز و آماده» است. او فروشگاه محصولات و خدماتش را می‌سازد و فرصت‌های مجاز را پاسخ می‌دهد. ماندگاری از ترکیب فروشگاه، فرصت‌ها و سابقهٔ پاسخ‌ها می‌آید؛ پیش از شکل‌گیری فرصت نیز فروشگاه باید ارزش مستقل داشته باشد.

## ۵. تز محصول

### ۵.۱. هر سمت پیش از اثر شبکه‌ای باید ارزش مستقل بگیرد

بازار دوطرفه در روز نخست متوازن نیست. اگر سازنده فقط برای دریافت پیشنهاد و تأمین‌کننده فقط برای وجود تقاضا وارد شود، محصول در مسئلهٔ مرغ و تخم‌مرغ گیر می‌کند. بنابراین فضای پروژه برای سازنده و فروشگاه مرتب برای تأمین‌کننده باید بدون تراکم سمت مقابل نیز مفید باشد. اثر شبکه‌ای باید این ارزش را تقویت کند، نه آن را ایجاد کند.

### ۵.۲. واحد اصلی زمینه، پروژه یا فروشگاه است

اطلاعات سازنده باید به پروژهٔ مشخص وابسته باشد؛ اطلاعات بیرون پروژه نیز جای مستقل و روشن دارد. اطلاعات تأمین‌کننده باید حول فروشگاه، محصول، خدمت و فرصت فروش سازمان پیدا کند. چت یک راه ورود و ادامهٔ کار است، اما مالکیت زمینه با این فضاهای پایدار است؛ نه با یک رشته پیام بی‌انتها.

### ۵.۳. چت ورودی است و خروجی ساختاریافته نتیجه

گفت‌وگو باید کار را ساده کند، نه اینکه ساختار را پنهان یا نابود کند. نتیجهٔ معنادار گفت‌وگو باید به چیزی قابل مشاهده و قابل بازگشت تبدیل شود: یک رکورد، فهرست، خلاصه یا کارت پیشنهادی ساختاریافته (آرتیفکت). نوع دقیق این خروجی‌ها در سند ۰۲ است. این اصل مانع آن می‌شود که اطلاعات مهم فقط در تاریخچهٔ چت دفن شوند.

### ۵.۴. کنترل با کاربر می‌ماند

چرخهٔ پایه چنین است: نیت کاربر ← برنامه و پیش‌نویس چیدا ← بازبینی کاربر ← تأیید کاربر ← اجرای سیستم. چیدا می‌تواند کار آماده‌سازی را جلو ببرد، اما اقدامی که داده، تعهد، ارتباط بیرونی یا وضعیت مهمی را تغییر می‌دهد بدون تأیید روشن انجام نمی‌دهد. قواعد رفتاری و موارد باز این چرخه در سند ۰۵ آمده است.

### ۵.۵. حلقهٔ دوطرفه باید محافظت‌شده باشد

نیاز سازنده می‌تواند به فرصت مناسب برای تأمین‌کننده و پاسخ تأمین‌کننده به گزینه‌ای برای سازنده تبدیل شود. این حلقه فقط وقتی ارزشمند است که مرتبط، قابل کنترل و کم‌مزاحمت بماند. سازنده به فروشگاه‌ها دسترسی دارد؛ دسترسی تأمین‌کننده به سازنده محدود به مسیرهای مجاز تعریف‌شده در سند ۰۴ است.

## ۶. جایگاه چیدا در برابر گزینه‌های موجود

| گزینه | چیزی که خوب انجام می‌دهد | تفاوت مورد انتظار چیدا |
|---|---|---|
| چت‌بات عمومی | پاسخ و تولید متن | زمینهٔ پایدار پروژه یا فروشگاه و ادامهٔ کار روی آرتیفکت |
| ابزار مدیریت پروژه | ثبت و پیگیری ساختاریافته | ورود طبیعی با گفت‌وگو و سازگاری با عادت‌های کم‌ساختار بازار |
| ابزار مالی یا حسابداری | ثبت دقیق عملیات مالی | قرار دادن نشانه‌های مالی در زمینهٔ کار پروژه، بدون ادعای جایگزینی حسابداری |
| بازار آنلاین یا فهرست عرضه‌کنندگان | نمایش عرضه و تقاضا | ارزش مستقل برای هر سمت و اتصال کنترل‌شده در دل کار واقعی |
| پیام‌رسان و فایل ابری | ارتباط و نگهداری فایل | تبدیل دادهٔ پراکنده به زمینه، خروجی و مورد قابل پیگیری |

جایگاه چیدا اتصال «فهم زمینه، نظم، تصمیم و اقدام تأییدشده» برای کاربر ساختمان است، نه رقابت در کامل‌بودن با هر ابزار تخصصی. قابلیت تازه باید همین جایگاه را تقویت کند.

## ۷. اصول راهبردی

- **ارزش مستقل دو سمت:** هیچ سمت نباید صرفاً برای خوراک‌رسانی به سمت دیگر ساخته شود.
- **اولویت جذب سازنده:** جذب سازنده سخت‌تر و برای سلامت بازار حیاتی‌تر است؛ بنابراین نظم پروژه و بازگشت روزمره بر توسعهٔ زودهنگام ابزارهای فروش اولویت دارد، بدون اینکه تجربهٔ تأمین‌کننده ناقص رها شود.
- **سادگی به‌عنوان تصمیم دامنه:** نسخهٔ نخست باید چند کار پرتکرار را کامل و قابل فهم انجام دهد. «بعداً» باید معنای روشن در سند ۰۶ داشته باشد، نه حضور مبهم در رابط.
- **یک چیدا، یک رابطه:** کاربر نباید برای هر کار شخصیت یا مدل انتخاب کند. تخصص در رفتار و زمینهٔ چیدا ظاهر می‌شود، نه در تیمی از چهره‌ها.
- **کنترل و قابلیت برگشت:** پیش‌نویس، تأیید، ثبت اقدام و امکان اصلاح بخشی از اعتماد محصول‌اند، نه جزئیات رابط.
- **آنبورد فعال، ادامهٔ آرام:** چیدا در شروع برای ساختن اولین نتیجه پیش‌قدم می‌شود؛ بعد از آن بیشتر در پاسخ به کاربر و از راه بریف‌های محدود و مرتبط حضور دارد. دامنهٔ بریف در اسناد ۰۲ و ۰۵ است.
- **حقیقت پیش از روانی:** چیدا باید تفاوت دادهٔ ثبت‌شده، ادعا، تخمین و نادانسته را روشن کند. قاعدهٔ منبع در `DECISIONS.md` و سند ۰۵ الزام‌آور است.
- **جداسازی داده:** دادهٔ پروژه، اطلاعات خصوصی سازنده، محتوای فروشگاه و ارتباطات بازار مالک و مجوزهای متفاوت دارند. معماری دسترسی در سند ۰۴ تعریف می‌شود.

## ۸. محدودهٔ شروع و استقلال محصول

شروع عملیاتی چیدا شهر تهران و زبان اصلی آن فارسی است. «تهران» در سمت سازنده یعنی پروژهٔ واقع در تهران و در سمت تأمین‌کننده یعنی توان پوشش تهران. گسترش جغرافیایی و زبان دیگر پیش‌فرض نیست و باید پس از یادگیری این بازار تصمیم‌گیری شود.

چیدا محصولی مستقل است و نباید هویت، دسترسی یا رابطهٔ تجاری آن با «پرشین‌سازه» فرض گرفته شود. هر نوع پیوند احتمالی میان این دو، تا زمانی که صریحاً تصویب و ثبت نشده، خارج از تعریف محصول است.

## ۹. چیدا چه هست و چه نیست

چیدا محیط هوشمند نظم پروژه، نگهداری زمینه، عرضهٔ مرتب و اتصال کنترل‌شدهٔ دو سمت است. تجربه چت‌محور است، اما داده و نتیجه در جای قابل مراجعه می‌ماند.

چیدا در نسخهٔ نخست تیم انسانی متخصص، نرم‌افزار جامع سازمانی، سیستم حسابداری، موتور پیش‌بینی زمان خرید، مشاور قطعی حقوقی یا فنی، یا بازار انجام معامله نیست. چیدا وجه معامله را جابه‌جا نمی‌کند، قرارداد را منعقد نمی‌کند و مسئول ارسال، تحویل، کیفیت یا ضمانت کالای طرفین نیست. درآمد محصول از اشتراک است، نه کارمزد معامله؛ مدل دقیق پلن و آزمون رایگان در سند تجاری و `DECISIONS.md` نگهداری می‌شود.

## ۱۰. فرضیه‌هایی که باید آزموده شوند

این موارد تصمیم قطعی نیستند و با پژوهش و رفتار واقعی کاربر آزموده می‌شوند:

| فرضیه | نشانهٔ اولیهٔ درست‌بودن |
|---|---|
| سازنده حاضر است اطلاعات یک پروژهٔ واقعی را در آنبورد وارد کند | بخش معناداری از کاربران اولین پروژه و یک آرتیفکت را کامل می‌کنند |
| نظم پروژه به‌تنهایی هوک ماندگار است | کاربران پیش از ساخت استعلام نیز برای کارهای پروژه بازمی‌گردند |
| بریف کوتاه بهتر از داشبورد شلوغ عمل می‌کند | موارد بریف دیده و به اقدام یا گفت‌وگو تبدیل می‌شوند |
| فروشگاه مرتب ارزش مستقل برای تأمین‌کننده دارد | عرضه پیش از دریافت اولین فرصت تکمیل و به‌روزرسانی می‌شود |
| برچسب‌گذاری انعطاف‌پذیر تنوع بازار را پوشش می‌دهد | عرضه قابل کشف می‌ماند و برچسب‌های تکراری یا مبهم کنترل می‌شوند |
| محدودیت دسترسی تأمین‌کننده اعتماد سازنده را حفظ می‌کند | نرخ مزاحمت و گزارش پایین می‌ماند و پاسخ به فرصت افت نمی‌کند |
| متن، ویس‌نوت، تصویر و فایل اصطکاک ثبت را کم می‌کنند | کاربران با ترکیبی از ورودی‌ها کار واقعی را کامل می‌کنند |
| پیش‌نویس و تأیید اعتماد لازم برای اقدام را می‌سازد | کاربران پیشنهادها را بازبینی و سپس تأیید می‌کنند، نه اینکه از قابلیت کنار بکشند |

تعریف دقیق سنجه‌ها و روش آزمون به سند سنجش محصول تعلق دارد و نباید در این سند تثبیت شود.

## ۱۱. تصمیم‌های باز مرتبط

مواردی مانند شعار نهایی برند، تعریف عملیاتی سازندهٔ هدف، میزان کنترل واژگان فروشگاه، سیاست بریف، مرز حافظه، سطح تأیید اقدام، شکل قیمت‌گذاری و ترتیب گسترش بازار باید در `DECISIONS.md` وضعیت روشن داشته باشند. این سند جهت انتخاب را مشخص می‌کند، اما جای ثبت تصمیم نهایی یا جزئیات نسخهٔ یک نیست.
