# Mobile Prototype Agent Guide

## CHIDA Product Decisions

- CHIDA is Persian, true RTL, and mobile-first; the primary visual target is 390 x 844.
- Mobile and desktop are separate intentional layout modes. The current design scope is mobile only: review it at 390 x 844, keep it edge-to-edge, and do not treat a centered mobile-width column in a desktop viewport as the desktop design. Design the desktop mode separately when the user resumes it.
- Use IRANYekanX, warm neutrals, and a restrained copper accent. The current prototype is intentionally Dark-only; do not expose a theme switch until the user asks to resume Light.
- One account has exactly one immutable role. Do not add a second role or a role switcher.
- Builder flow is role -> invite -> phone -> six-digit OTP -> builder chat home.
- Before the first builder chat, require the project name, its area within Tehran, and the current construction stage. Tehran is the only active city in the current prototype; show it as fixed context and do not request an exact address.
- Keep the approved construction-stage taxonomy in this exact order and spelling: `طراحی و اخذ مجوز`, `تخریب و گودبرداری`, `فونداسیون`, `اسکلت بندی`, `دیوارچینی و سفت کاری`, `گچ و خاک و تاسیسات`, `نازک کاری و نما`, `ظریف کاری و نصبیات`, `پایان کار`. Normalize only known legacy labels when reading local project/file metadata so an existing prototype project does not become incomplete after the taxonomy change.
- Keep first-project onboarding short. Treat those three fields as the minimum entry data, then let the builder progressively complete the project workspace with usage, land area, total built area, above-ground floors, basement floors, and unit count.
- Use app-owned Dark dropdowns for project stage and usage. Native browser select popups can escape the product theme; keep these controls RTL and keyboard accessible. A closed trigger inside `MobileScroll` must still yield a vertical drag to the parent and open only on a tap; only the portaled menu surface may own its interaction gesture.
- Treat project files as project-scoped first-class records. Keep photos only in Gallery and non-image documents only in Files. Persist directly selected image and document Blobs in separate versioned IndexedDB stores so real previews or the selected document can open after reload in the same browser; `localStorage` stays metadata-only and must never contain a Blob, base64 payload, extracted text, or file contents. Accept a document only when its supported extension and MIME are compatible, and rebuild its Blob with the extension's safe canonical MIME on both write and read. A legacy `metadata-only` document can become openable only after the user reselects a supported file with the recorded name and size; never claim that this verifies content identity or authenticity. Never imply cloud upload, sync, backup, OCR, content search/indexing, citation, analysis, sharing, or memory write. Hide provenance, privacy, version, local status, and browser-storage explanations from default photo/file/memory surfaces while retaining the internal fields needed for isolation and fail-close behavior. Derive gallery membership from a supported image extension plus compatible MIME, never from an editable category; do not offer the photo category for document registration. Show an honest fallback for HEIC/HEIF or decode failures. Validate persisted records semantically, reject duplicate IDs, scope every query to the active project, revoke object URLs, surface or roll back storage failures, and reconcile persisted Blob metadata whose content is missing instead of displaying a partial record as successfully saved.
- Memory Core currently supports only `account_private` (labelled Personal) and `project_private` (labelled This project). Direct Remember is the only user-facing producer; never turn chat, files, or model output into memory automatically. Keep `MemoryCandidate` structurally and visually separate from `MemoryRecord`; only exact consent bound to candidate id/version, payload hash, exact payload, and scope may atomically promote a still-pending and unexpired candidate, and no candidate producer exists in this slice. Persist the canonical exact envelope only at `chida-prototype-memory-core:v2`, with SHA-256 fingerprints, owner/scope, source refs, immutable revisions, history, current/superseded/disputed/disabled lifecycle, reciprocal supersede/conflict links, and content-free deletion tombstones carrying only delete metadata plus an exact lineage-event proof. Validate the whole envelope, including historical live/tombstone graph reciprocity and proof lifecycle/report binding; reject duplicate, non-canonical, cross-scope, dishonest-delta, retargeted, weak-hash, or impossible-chronology data, distinguish valid empty from unreadable, and never fall back from malformed v2. Migrate only explicitly classified v1 snapshot/lineage/full or exact legacy-array shapes; reconstruct snapshot lineage only from a unique compatible pivot/peer or resolution proof and fail closed on ambiguity. Never resurrect legacy bytes after a valid canonical marker exists. Keep the v2 cutover marker compact and free of memory content; a temporary prior-intent bridge may retain raw delete evidence only until every target store is applied and verified, then it must be removed last. Serialize every mutation with Web Locks, commit-time reread, expectedVersion, no-op byte stability, write-before-state, and fail-close errors. Keep visibility, manual searchability, automatic retrieval eligibility, model eligibility, shareability, and context preference independent; automatic/model/share stay off because no consumer exists. Project search may use only visible, manually searchable current/disputed records from the active project, never personal/disabled/superseded records. UI must remain content-first, expose technical metadata progressively, and state that the model is not connected.
- Project-local search is a deterministic, browser-only lookup over an explicit visible-field allowlist: memory title/content/kind and document display/original name/category/format/stage. Normalize Persian/Arabic character variants, ZWNJ/spacing, Unicode punctuation/symbols, and Persian/Arabic digits only for matching; do not mutate stored records or claim fuzzy, semantic, AI, or relevance ranking. Hidden provenance, privacy, version, local status, or context flags must not create a result whose match reason is invisible. Keep every result inside the active `projectId` and open the original memory or file record. A local read failure is not an empty collection: surface it and lock mutations for that store until a later load reads it successfully, so an incomplete in-memory array can never overwrite unread records. File contents, OCR, embeddings, web/market sources, citations, AI answers, cross-project data, sync, and backend remain unavailable; real source-backed answers stay coming soon until readable content or real web sources are connected. A separate read-only mock may demonstrate that future answer contract only when the entry, persistent warning, answer, source cards, details, and accessible names all say sample/fabricated; it must use fixed in-memory records, expose no links, perform no retrieval, and never write into chat, project records, `localStorage`, or IndexedDB.
- Keep the project document library separate from Composer attachments. Composer may attach one supported image or document to the same draft, preview/remove it before send, and persist exact text plus the whole selected asset as project-private local SourceRecords; it must never redirect to the library as a substitute. Store only metadata in `localStorage` and Blob bytes in the existing versioned IndexedDB stores. Serialize every Source/file metadata writer with one shared Web Lock and commit-time reread. Use the durable multi-store intent with rollback/resume specifically for Composer intake; validate every Source-to-file project/version/kind link and the SHA-256 of each Source-linked Blob before and after mutations. Independent file registration/restoration writes the new Blob first and verifies the selected bytes before committing metadata, but standalone file records still have no persistent content hash. Fail closed without overwriting unreadable bytes. Reopening a Source must show exact text or a verified original asset and an explicit unavailable state for missing/tampered content. This slice does not add OCR, extraction, model use, retrieval, search indexing, cloud upload, backend, sync, sharing, or a full persisted conversation/thread.
- Until the user explicitly approves the start of phase three, Supplier remains selectable but must stop at a clear coming-soon explanation. Phase three may replace that stop with the supplier's own immutable-role flow; never add a role switch.
- The fixed delivery order is builder completion, then a local-model evaluation phase, then the supplier experience. Keep work one task at a time and do not start the next task or phase before the user's approval.
- Keep builder home chat-first and quiet: collapsible right drawer, a centered project-name-only button in the top bar, no duplicate profile control in the header, one horizontally draggable RTL quick-action chip rail, and a compact composer. The attached bottom project button also shows only the project name; both project controls remain visibly clickable without helper labels or arrows. Profile/settings live only at the bottom of the drawer.
- Keep one shared new-project flow only inside the Projects collection reached from the centered project switcher or the drawer's Projects row. The collection owns the single add action; do not restore direct add controls in the header, bottom project strip, or drawer. The flow asks only for project name, Tehran area, and construction stage; on a successful local write the new project becomes active.
- Keep composer drafts keyed by `projectId`; activating or creating a project must never render another project's draft or committed intake. Reconstruct only committed Composer intake cards from the project-scoped Source envelope after reload; do not call this a complete persisted conversation/thread or persist canned assistant text as source truth. The canonical project foundation is the exact `chida-prototype-builder-projects:v3` envelope: separate Project and ProjectProfile records, the active project in the same atomic envelope, versioned SHA-256 revision/history and idempotency receipts. Resolve AccountSide and authorization only from the exact compiled Identity/Policy mirror with its versioned AccountIdentity, Membership, RoleAssignment and AuthorizationContext template. Treat v2/legacy arrays and the old active pointer only as one-time migration inputs; use a durable pending/verified/committed cutover and make v3 the sole authority after committed. A present malformed newer generation, changed pre-commit cutover preimage, invalid marker, identity mismatch, or unreadable canonical must fail closed without fallback or rewrite. Serialize create/select/profile update/rollback under the foundation Web Lock with commit-time reread, expectedVersion, a strict command validator before idempotency, deterministic ids, write-before-state, exact readback, candidate-owned rollback and durable cutover recovery. Parser acceptance must replay command/snapshot preconditions, deterministic revision/event ids, receipt chronology, Project/Profile order and active-project transitions; a home tab whose active profile becomes incomplete must render the completion flow instead of blank content.
- Purchase-request creation and editing use progressive disclosure: open in `simple` mode, show the `advanced` choice explicitly, and reveal extra product/service/commercial fields only when requested. Both modes edit the same versioned request draft; switching modes must preserve values, and omitted advanced values continue through the existing explicit-unknown and clarification contract. Keep the shared optional “More details” field after every product- or service-specific field in both modes and immediately before the primary Continue/Save action; mode changes must preserve its value too.
- An unreadable purchase-request store must remain fail-closed, but it must not trap the builder permanently. Offer an explicit two-step recovery that captures and validates one exact current snapshot; if that snapshot is still unreadable, copy its raw string to a unique versioned local recovery key and verify both that copy and a durable recovery-intent marker before removing only the primary purchase-request key. A retry that sees a valid current snapshot reloads it without backup or reset. Any read, backup, verification, source-change, or reset failure keeps registration locked; failures after removal attempt to restore the primary bytes and retain the verified backup plus recovery intent so a reload cannot mistake an interrupted reset for a valid empty store. Never clear or rewrite files, tasks, approvals, contacts, dispatch records, proposals, or their independent lock state through this recovery.
- Keep the builder proposal journey calm and task-first. The default inbox has one primary create action; cards and proposal details lead with supplier, request, currentness, price/time/validity, and the next useful action. Put product/service comparison choices, follow-up records, full terms, source lineage, revision history, identifiers, fingerprints, technical flags, and internal T7/T8 labels behind progressive disclosure. Revision diff is changed-first and keeps unchanged fields and technical lineage collapsed by default. Storage/read failures, historical or needs-review status, and a short human-readable manual/local/no-external-effect boundary remain immediately visible. This is a presentation rule only: do not weaken schemas, versioning, fingerprints, fail-close behavior, project isolation, or no-network/no-mutation contracts.
- Keep the default purchase-request journey to three human-facing steps: capture the need, select one or more builder-registered suppliers for the same project, and record the final plan decision in Tasks. Preserve content approval, exact snapshots, DispatchDraft, plan approval, versioning, lineage, and failure contracts behind this path, with raw payloads and identifiers collapsed under optional technical details. Do not call a supplier “matched” or “related” until a real network and testable matching contract exist. The Tasks decision remains simulation-only with no network, send authorization, attempted external action, or external effect.
- Manual project tasks remain project-scoped, private, local, and versioned. Let the builder edit title, next step, and an optional local due-date label after creation; persist an `updated` event for each semantic edit, preserve the previous bytes on write failure, skip version churn for no-op saves, and keep legacy records without a due date readable as `null`. Active cards show the due date and completed cards show the actual completion time; this legacy task type is not a Monitor origin and does not imply reminders or background execution.
- Project Backbone is exposed only as the «برنامهٔ فعلی» detail inside the single top-level «کارها» center: one Milestone, one Decision with a mandatory visible reason, and one Task linked to both for each project in this first slice. Home Quick Actions, the Drawer, and Project Workspace must all enter the same Tasks center; do not restore a separate top-level «برنامه/تصمیم‌ها» destination. Keep the three Backbone objects independent but persist them through one atomic versioned envelope with exact owner/scope, immutable revision snapshots, fingerprints, history, commit-time expectedVersion checks, no-op byte stability, read-error fail-close, and rollback as a new revision. Never duplicate the linked Task into the legacy manual-task store; surface it in Tasks as a card that returns to the Backbone source. Its optional deadline now feeds Task/Monitor Core; full Task status transitions remain deferred. Do not imply model, backend, network, supplier, or external effects.
- Project Backbone create/edit sheets must establish their own true RTL context because `BottomSheet` is portaled outside `.chida-app`. Keep the sheet title, description, section copy, and labels right-aligned. Keep the five builder-entered text inputs/textareas explicitly `direction: rtl` and `text-align: right`, while the sixth `datetime-local` deadline control remains explicitly LTR/left-aligned in both create and edit states; do not rely on inherited page direction.
- Task/Monitor Core may create only one project-private deadline Monitor for the current Project Backbone Task. Interpret the `datetime-local` value in `Asia/Tehran`, store canonical UTC ISO in the Task snapshot, and bind every Monitor/Run revision to the exact Task id/version/revision id/fingerprint. Keep Monitor/Run in its own exact versioned envelope with append-only Runs, semantic replay, contiguous attempts, bound schedules, immutable history, fixed 24-hour post-deadline cadence, expected versions, and fail-close parsing; reject non-canonical raw strings or identifiers instead of trimming and later rewriting them. Serialize every Monitor mutation by taking the Monitor lock before the Project Backbone lock; reread both stores at commit, verify the expected Monitor preimage and Backbone postimage, and restore only the candidate bytes owned by a failed write. Stale Task revisions and removed deadlines may produce recoverable failed Runs; a missing/deleted Task or unreadable dependency stays store-level fail-close because this slice has no Task tombstone/delete contract. Run automatic checks only while the Tasks center is mounted and visible, on mount/focus/visibility and every 60 seconds, plus explicit Run now. An automatic request queued behind either Web Lock must recheck the same mounted-and-visible authority inside the critical section before any read or mutation. Never claim a service worker, closed/hidden-tab execution, OS notification, backend, model, web/price/regulation monitor, network action, or external effect.
- The local dispatch-plan approval is an independent, versioned store and never mutates purchase-request approval or dispatch-draft records. Snapshot the exact current dispatch revision, request revision, content approval, recipient contact versions, payload, privacy contract, and explicit review acknowledgement. Validate dependency chronology and derive invalidation when any pinned dependency changes while preserving historical bytes. Support pending withdrawal, reopening, and one final local approval with an announced/focused status; always keep `simulationOnly=true`, `externalEffect=none`, `sendAuthorized=false`, and `externalActionAttempted=false`, with no send control, sent state, receipt, or external request.
- Composer controls are plus and gauge together on the RTL right side, then microphone and send on the left. Do not print the active mode name inside the composer.
- Attach the prominent active-project strip directly below the composer with zero gap. Capability icons open the active-tools list.
- In the Dark-only chat home, visually interlock the composer and its narrower project strip; keep the tool cluster subtle but visibly clickable with a short «ابزارها» label.
- The shipped route is a real full-screen responsive app. Keep the protected device runtime available for isolated runtime fixtures, but hide its picker, bezel, status bar, home indicator, camera cutout, and synthetic keyboard on CHIDA product routes.
- Build lives under Tools as a local, project-scoped `BuiltArtifact`, not as a Plugin, RuntimeTool, Connector, or globally installed string. Build only from the closed `project-followup-view` catalog entry; preview its exact data bindings, permissions, safe components, allowed action, activation location, and relationship-only capability/skill metadata before explicit activation. Persist exact revisions/history/fingerprints under Web Locks, distinguish read failure from empty, preserve prior bytes on write failure, and support disable/reactivate, a fresh draft revision, rollback as a new draft, and tombstone removal. Before waiting for the main-store lock, record an exact hashed dependency-invalidation intent under the journal lock and mirror it into local and same-tab session storage. Every BuiltArtifact writer must keep the fixed journal-lock → main-lock order. Keep `active@N + intent@N` effectively blocked across dependency repair, reload, and project switches, clear the intent only after reread proves the direct `blocked@N+1` descendant, fail closed if either mirror cannot be read, and never spin an automatic cleanup retry loop. Never treat React state as the incident authority or claim plugin/skill generation, free-code execution, network use, external install, backend, model, or external effect.
- Brief lives in the right drawer and supports one persisted daily or weekly schedule. Its summary is action-oriented: decisions, overdue/upcoming work, open procurement, unresolved documents, and changes since the last visit. Write storage before state; close the sheet only after a successful save, and keep it open with an explicit error after failure.
- Settings must describe only observable prototype state: immutable builder role, current response mode, Brief schedule, Dark-only appearance, privacy boundary, local project/record counts, and prototype version. Do not fabricate model-token usage, billing, quota, sync, or backup. A generated builder portrait is a clearly labelled local fixture, not verified identity or uploaded user data.
- At `390 x 844`, the page root and task-status filters must have zero horizontal overflow. Quick actions intentionally overflow only inside the protected `Carousel`, initialize at the RTL start through an LTR scroll mechanism with RTL content, remain draggable without firing a chip, and realign after returning home. Keep the protected Carousel runtime unchanged.
- The current builder-completion phase is local/synthetic only: no real OTP, AI provider, external data, or commercial side effect. After the user explicitly accepts the builder phase as complete, a separate opt-in, local-development-only, provider-neutral adapter may connect to loopback Ollama for evaluation. Its first slice is text-only and must send no project memory, file, web data, tool call, mutation, or external action; it must identify local/ungrounded output honestly, fail closed, and remain disabled in Cloudflare/Sites builds. Do not treat this evaluation adapter as the managed production environment described by the master document.

## Prototype Instructions

In ChatGPT Work Mode, run `sites-preview start "$PWD"`, open `http://terminal.local:4173/` in the cloud browser, and verify the rendered app and its primary interactions. Keep that preview open and tell the user to inspect it in the cloud browser; do not present the local URL as a user-facing chat link. In Codex Desktop, run the local server yourself, open the preview in the in-app browser, and provide the clickable local URL. Do not deploy to Sites unless the user explicitly asks to share, publish, or deploy. Do not give the user server-start instructions when you can run it.

Before planning or implementing any mobile-app change, read this `AGENTS.md` in full. It is the source of truth for the template's runtime and component guidance.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

Before handing off each prototype task, and again after the user's feedback or approval, update `../CHIDA-PRODUCT-LEARNINGS-FA.md` with the observed build experience, any master-document gap, the decision and its status, and the proposed master-document amendment. This working log never overrides `CHIDA-Product-Definition-FA.md`; mark unapproved prototype choices as awaiting validation instead of presenting them as settled product truth.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

## Editing Boundary

- Build app-specific UI in `src/Prototype.tsx` and `src/prototype.css`.
- Treat `src/App.tsx`, `src/main.tsx`, `src/styles.css`, `src/mobile/`, `public/assets/iphone/`, `public/assets/android/`, `public/assets/status/`, `vite.config.ts`, `worker/index.js`, and `scripts/prepare-sites-build.mjs` as protected runtime files. Do not edit, replace, remove, or recreate them unless the user explicitly asks to change the mobile runtime itself. For an explicit runtime change, update the affected lock hashes only after verifying the new runtime behavior.
- Run `npm run check:runtime` before preview or handoff. If it fails, restore the protected runtime instead of weakening or bypassing the check.
- `npm run build` preserves the mobile runtime and prepares the static Cloudflare Worker output required by Sites. Before a Sites handoff, confirm `dist/client/index.html`, `dist/server/index.js`, `dist/.openai/hosting.json`, and source `.openai/hosting.json` exist, then run `npm run test:sites`. Do not replace this project with a Vinext starter.

## Fast Publish Contract

- Treat verification as three tiers. For micro UX changes that do not touch schema, storage, protected runtime, or release tooling, run only the related `npm run test:app -- --grep "..."` regressions, `npm run check:runtime`, real `390 × 844` route QA including console/overflow, and `git diff --check`; if TSX changed, run `npm run build` once after the feedback batch rather than after every tiny edit. For a normal feature slice, run focused domain regressions, `npm run build`, and mobile QA; run `npm run test:sites` only when worker, hosting, or `dist` packaging is in scope.
- Use `npm run gate:release` exactly once on a frozen explicit-publish candidate or at the end of a genuinely high-risk architecture slice involving schema/migration, concurrency, recovery, security, protected runtime, or release tooling. Never run it after each small user-feedback change. It runs `build` (including protected-runtime integrity and TypeScript), `test:all` once, `test:sites`, and `git diff --check`, then stores source and `dist` artifact fingerprints outside the worktree under `.git`. The fingerprints bind paths, canonical mode/type, bytes or symlink targets and must remain stable across the gate.
- Keep `npm run test:app` and `npm run test:runtime` for focused development. `test:runtime` is intentionally limited to `tests/mobile-runtime.spec.ts`; do not make it rerun the application suite. `test:all` is the only full Playwright pass in the release gate.
- On a later explicit publish request, commit the exact validated bytes and run `npm run gate:publish`. It must fail if the repository is dirty, source/artifact changed, branch changed, or HEAD is neither the validated commit nor its single direct non-merge child. A changed candidate requires a new `gate:release`; an unchanged one does not.
- Push the validated SHA once and use that same SHA for GitHub, Cloudflare Pages, and ChatGPT Sites. Report terminal deployment/version identifiers in the task handoff only; never create a receipt-only commit, second push, or second deployment.

## Runtime Contract

- Preserve the mobile device runtime unless the user's task explicitly asks otherwise. Do not replace it with a standalone page. Visual fidelity applies to app-owned content inside the device screen, not to template-owned device chrome.
- Keep `App` composed around `PhoneFrame` -> `KeyboardProvider`, with `StatusBar`, app content, `HomeIndicator`, and `KeyboardDock` mounted inside the phone frame. `StatusBar` and the iOS home indicator are overlaid device chrome. When the Android keyboard is closed, the app viewport reserves the protected navigation-bar region instead of painting behind it. When the Android keyboard is open, preserve the current full-screen keyboard layout: its asset includes the IME navigation strip and the separate black navigation bar is hidden. iOS screens continue to paint behind the home-indicator area and own their safe-area content padding.
- Preserve the `iPhone` / `Pixel 10` device picker and both calibrated device presets. The Pixel screen is `427 x 952`; its `32 x 32` camera circle and `public/assets/android/navigation-bar.svg` bottom navigation bar are protected device chrome, not app content.
- Preserve the device picker's intentionally lightweight Codex styling in the top-right corner: its trigger wrapper is borderless and transparent, its trigger sizes to content, and its right-aligned menu uses the compact 3px inset plus the specified hairline and elevation shadow layers. Keep the prototype root and default app screen white.
- Preserve `StatusBar` as live device chrome, including its platform-specific typography, source status-icon assets, and spacing. Pixel 10 uses Roboto, Android indicators, and 32px top, left, and right padding. iPhone uses its iOS indicators, system typography, and calibrated spacing. Do not hardcode screenshot times like `9:41` into the status bar, replace its real-time clock, or move status bar content into app markup unless the user explicitly asks for a fixed/mock device time.
- `PhoneFrame` owns the calibrated device frame, screen portal, device picker, camera cutout, and custom cursor. Keep device assets in `public/assets/iphone/` and `public/assets/android/`; if an asset fails to load, repair the asset path or restore the asset instead of removing the frame, keyboard, or image render.
- Use `MobileScroll` directly for simple single-screen prototypes. Use `FlowStack` for conventional multi-screen flows whose routes can own their fixed header and footer; when using it, define each route as a `FlowScreen`: `{ id, header?, headerHeight?, footer?, footerHeight?, render }`, and use `flow.push(screen)`, `flow.pop()`, and `flow.replace(screen)` from `FlowStack` render callbacks or `useFlow()` instead of introducing another router.
- Use `Carousel` for a carousel, horizontal rail, swipeable cards, image or media strip, horizontally scrollable cards, chip rail, or other horizontal collection.
- For a layered app shell—such as a persistent composer, independently presented sheet, pushed/peek sidebar, or app-wide transition—compose directly in `Prototype.tsx` rather than forcing it through `FlowStack`. Keep app-owned fixed chrome as sibling layers outside `MobileScroll`.
- When using `FlowScreen`, put route-owned fixed headers or footers in `FlowScreen.header` or `FlowScreen.footer`. Set `headerHeight` to the visible app-toolbar height; `FlowStack` adds the device's top safe-area/status-bar inset automatically. Do not include `StatusBar` or its height in the header. Set `footerHeight` to the full app-footer height. `FlowScreen.footer` is an overlay, not reserved layout space; screens using it must add their own bottom content padding such as `padding-bottom: calc(var(--flow-footer-height) + var(--mobile-safe-area-height) + 24px)` so final content can scroll above the footer while still painting behind it.
- Render only scrollable content inside `MobileScroll`; it is for content that should move with scroll and rubber-band overscroll. Keep app-owned headers, nav bars, tabs, composers, and overlays outside it. This keeps scroll physics, safe areas, keyboard insets, scrollbars, and drag click suppression active without letting content paint under fixed chrome.
- Buttons, links, cards, and images inside `MobileScroll` should still allow drag scrolling when the pointer moves beyond tap slop. Use `data-scroll-drag="ignore"` only for rare controls that must own the drag gesture themselves.
- Do not add `var(--keyboard-height)` to ordinary screen/content padding inside `MobileScroll`; the scroll viewport already shrinks above the simulated keyboard. For custom fixed composers, search bars, or toast chrome, use `useKeyboardInsets().bottomInset`. It is relative to the app viewport: Android returns `0` while the closed-keyboard viewport already reserves navigation, then returns the keyboard height while open; iOS continues to clear the home indicator while closed and ride directly above the keyboard while open. Do not pin custom bottom chrome to `bottom: 0` or only `keyboardHeight`.
- Use `KeyboardInput`, `KeyboardTextarea`, or `MobileTextField` for every text-entry control. A raw `input` or `textarea` disconnects focus, keyboard animation, safe-area insets, and attached surfaces.
- Use `BottomSheet` for phone-scoped sheets. Its props are `open`, `onOpenChange`, `title`, optional `description`, optional `snap`, and `children`; it renders through the phone screen portal and dismisses the keyboard before opening.

## Horizontal Carousels

- Use `Carousel` for horizontally draggable cards, images, media, chips, or other horizontal collections. Do not recreate these with `overflow-x`, custom pointer handlers, or a generic div.
- `Carousel` can be nested directly inside `MobileScroll`. It owns horizontal gestures and automatically yields vertical gestures to the parent.
- Never put `data-scroll-drag="ignore"` on or around a `Carousel`; doing so prevents vertical parent scrolling when a gesture begins inside it.
- Do not add CSS scroll snapping to `Carousel`; its runtime owns momentum and release motion.
- Use `data-scroll-drag="ignore"` only when a control must prevent parent scrolling in every drag direction.

See `src/mobile/COMPONENTS.md` for the full component and gesture contract.

## Keyboard Rule

The simulated keyboard is a separate top-layer component. Before presenting anything that behaves like iOS navigation or modal UI, dismiss it first.

Call `keyboard.hide()` before:

- pushing, popping, or replacing FlowStack routes
- opening bottom sheets, action sheets, dialogs, menus, or navigation sheets
- starting transitions where the destination should not inherit text-input focus

`FlowStack` already hides the keyboard for `push`, `pop`, and `replace`. `BottomSheet` already hides it before opening. If you add new modal/sheet/navigation primitives, follow the same rule.

When a composer, search surface, or other keyboard-attached component closes, call `keyboard.hide()` in the same event before changing that component's open state. Position attached surfaces from `useKeyboardInsets()` rather than a separate timer or visibility flag so both dismiss together.

When any text-entry control loses focus, dismiss the simulated keyboard. If the control is custom or does not use the runtime's keyboard-aware fields, handle its blur event and call `keyboard.hide()` explicitly. Keep the keyboard open only when focus is moving directly to another text-entry control that should share the same keyboard session.

## Interaction Rules

- Do not trigger buttons or inputs after a pointer has become a drag. Preserve the drag suppression behavior in `MobileScroll`.
- Do not allow native browser image/file dragging inside the phone frame. Preserve the phone-level `dragstart` suppression and non-draggable image styles so scroll drags that begin on images still scroll the prototype.
- Use `KeyboardInput`, `KeyboardTextarea`, or `MobileTextField` for text entry so the simulated keyboard and safe-area insets stay connected.
- Fixed phone chrome should not animate with pushed screens. Screen content can animate; the status bar, camera cutout, and preview chrome should stay put.
- Keep the keyboard below the home indicator/safe area layer in z-index, and above ordinary app UI while visible.
- Keep the home indicator as the topmost safe-area layer in the z-index above everything else in the prototype.
