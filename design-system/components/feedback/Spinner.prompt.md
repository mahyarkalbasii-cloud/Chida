A quiet ring spinner for waits under a few seconds — inside buttons, fields, or centered on a screen.

```jsx
<Spinner size={20} label="در حال بررسی کد" />
```

Reduced-motion users see a static ring (global rule). For waits longer than ~3s pair it with one short line of Persian text saying what is happening.
