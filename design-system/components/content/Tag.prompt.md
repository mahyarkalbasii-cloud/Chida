A small status label on cards and rows — state in words, tinted quietly.

```jsx
<Tag tone="accent">در انتظار پاسخ</Tag>
<Tag>پیش‌نویس</Tag>
<Tag tone="danger">منقضی شده</Tag>
```

Always readable as text on its own; the tint is reinforcement, not the message. Maximum two tags per row.
