# هنداف جامع CHIDA برای بازبینی Sol

**تاریخ:** ۱۴۰۵/۰۶/۰۸ — ۲۰۲۶/۰۸/۳۰
**مخزن اصلی:** `/Users/mahyarkl/Desktop/ChatGPT/CHIDA`
**شاخه:** `main` در checkout اصلی
**وضعیت تصمیم:** توقف پس از انتشار و هنداف؛ هیچ برش بعدی، مدل محلی یا مسیر تأمین‌کننده تا بازبینی Sol و انتخاب صریح ماهیار آغاز نشود.

---

## ۱. چرا این هنداف تهیه شده است

این فایل برای پاسخ به سه پرسش آماده شده است:

1. پروتوتایپ سازنده تا امروز واقعاً چه چیزهایی دارد و چه چیزهایی فقط شبیه‌سازی یا نمایش است؟
2. برای کامل‌شدن فاز سازنده چه مسیر اجرایی باقی مانده و کدام بخش‌ها بهتر است به فاز مدل یا تأمین‌کننده موکول شوند؟
3. حافظه و Harness نهایی CHIDA باید چه قراردادی داشته باشند تا ارزش افزودهٔ محصول وابسته به یک مدل خاص نباشد؟

این فایل سند مادر محصول نیست. منبع حقیقت همچنان `CHIDA-Product-Definition-FA.md` است. این هنداف، جمع‌بندی وضعیت اجرا و نظر معماری برای بازبینی Sol است؛ هر اصلاح سند مادر یا تغییر ترتیب محصول فقط پس از تصمیم صریح ماهیار انجام می‌شود.

## ۲. خلاصهٔ مدیریتی

- فاز فعال هنوز «تکمیل و تأیید پروتوتایپ موبایل سمت سازنده» است.
- پروتوتایپ سازنده گسترده، کلیک‌پذیر، پروژه‌محور و دارای قراردادهای آزمودهٔ نسخه، lineage، rollback، fail-close و جداسازی پروژه است؛ اما هنوز «کامل» اعلام نشده است.
- منظور از کامل‌شدن این فاز، کامل‌شدن تجربهٔ کلیک‌پذیر، مدل داده، حالت شکست، حریم و قراردادهای قابل‌آزمون محلی است؛ نه backend، شبکهٔ واقعی، ارسال واقعی یا آمادگی production.
- هیچ مدل محلی یا ابری به پروتوتایپ وصل نیست. مسیر تأمین‌کننده نیز شروع نشده است.
- نسخهٔ منتشرشدهٔ فعلی Mobile-first، فارسی، RTL، فقط Dark و برای viewport واقعی `390 × 844` است.
- تازه‌ترین بازآرایی‌ها، تجربهٔ پیشنهاد، رسانه/حافظه/خرید و خانه را خلوت‌تر، خواناتر و قابل‌فهم‌تر کرده‌اند؛ جزئیات فنی از مسیر روزمره پنهان شده‌اند ولی قرارداد داده حذف نشده است.
- تصمیم فوری پیشنهادی از Sol این نیست که «کد بعدی را بسازد»؛ ابتدا باید تعریف دروازهٔ پایان سازنده و ترتیب T8/T9/T10/T11/T13 را بازبینی و یک برش کوچک بعدی پیشنهاد کند.

## ۳. رسید انتشار snapshot پیش از این هنداف

این snapshot پیش از ساخت فایل حاضر، کامل آزموده و در سه مقصد منتشر شد:

### Git

- commit کد T8-UX3: `3e4b35fe08b02918e4be814d5d796da14bfa54ca`
- commit مستندی receipt: `3bbd870ca021339d89abc09528f57fc690deec50`
- prototype tree هر دو commit: `4297309252e2e6f9d80c184be7aad1e795c80279`
- `HEAD`، `main`، `origin/main` و `git ls-remote origin main` روی commit مستندی هم‌تراز بودند.
- ahead/behind: `0/0`
- working tree پیش از تهیهٔ هنداف: تمیز

### Cloudflare Pages

- deployment نهایی snapshot مستندی: `f665580f-26c9-4363-9793-d4baaae5e644`
- source: شاخهٔ `main` و commit دقیق `3bbd870ca021339d89abc09528f57fc690deec50`
- canonical: <https://chida-prototype.pages.dev>
- immutable: <https://f665580f.chida-prototype.pages.dev>
- HTML: `2872325a9536c3c30933c0bf4e05c921c5c57841b2050565d4b4c0e4456ca2ce`
- JavaScript: `5b5b5cef51c427d108b02983213d14e6e27e403ec99fd27e7c7a75cd5e15ef1b`
- CSS: `94283e31ad8830896c0e44498a248d96f392b1de83912d9783a2e02867be81df`
- نشان CHIDA: `44cc69c64df0f51d8603ce8157e3f3d7eb753ac4908eddf80b9e773ef1231cad`
- هر چهار hash در build محلی، canonical و immutable یکسان تطبیق داده شدند.

### ChatGPT Sites

- نسخهٔ ۲۵
- version ID: `appgprj_6a90313e390c81918572fc1b45269dac~appgver_4fd3dab34a8481918949ad5a947d6942`
- source commit: `3bbd870ca021339d89abc09528f57fc690deec50`
- ۱۹ فایل
- archive hash: `sha256:316dbe635d0483a16eb3480c288898d8c14a1af97f2b1882187c6baf05da18f5`
- deployment نهایی بررسی حاضر: `appgdep_6a941bdda88881918951b8508e2c2eec`
- وضعیت: `succeeded`
- URL: <https://chida-prototype.mahyarkl.chatgpt.site>
- دسترسی `custom` و owner-only با یک مالک، بدون گروه و بدون مهمان بیرونی حفظ شد.

### کیفیت snapshot

- focused نهایی T8-UX3: `5/5`
- app: `159/159`
- runtime: `167/167`
- Sites: `4/4`
- build و TypeScript: پاس
- integrity: هر ۲۸ فایل محافظت‌شده پاس
- QA واقعی موبایل `390 × 844`: بدون overflow افقی صفحه و بدون console error
- بازبینی مستقل: هیچ finding باز P0/P1/P2
- هشدار غیرمسدودکنندهٔ شناخته‌شده: اندازهٔ chunk اصلی JavaScript بیشتر از 500kB است.

commit و deploymentهای خود این فایل به‌علت حلقهٔ self-reference داخل آن ثبت نمی‌شوند؛ رسید نهایی آن‌ها در پیام تحویل به Sol و Git history موجود خواهد بود.

## ۴. سلسله‌مراتب اسناد و تصمیم

1. `CHIDA-Product-Definition-FA.md`: سند مادر و منبع حقیقت محصول.
2. `CHIDA-PRODUCT-LEARNINGS-FA.md`: دفتر تجربه، شکاف، تصمیم کاری و پیشنهاد اصلاح سند مادر؛ مرجع مستقل محصول نیست.
3. `BUILDER-FEATURE-BACKLOG-FA.md`: ترتیب اجرایی و مرز نسخه‌های سمت سازنده.
4. `CHIDA-CONTINUATION-HANDOFF-FA.md`: هنداف زندهٔ ادامهٔ ساخت و receiptهای انتشار.
5. `AGENTS.md` و `prototype/AGENTS.md`: قواعد لازم‌الاجرای کار، کیفیت، انتشار و فایل‌های محافظت‌شده.
6. تصمیم صریح تازهٔ ماهیار بر تصمیم کاری قدیمی مقدم است؛ تعارض باید در دفتر یادگیری ثبت شود و سند مادر نباید بی‌صدا تغییر کند.

## ۵. ترتیب فازها که نباید شکسته شود

1. تکمیل و تأیید صریح پروتوتایپ سازنده؛
2. اتصال آزمایشی و provider-neutral مدل محلی فقط برای ارزیابی تجربهٔ سازنده؛
3. ساخت مسیر مستقل تأمین‌کننده؛
4. هر توسعهٔ مشترک واقعی پس از وجود دو طرف، backend و قرارداد پروندهٔ مشترک.

هر بار فقط یک برش کوچک باید انتخاب، پیاده‌سازی، مشاهده و تأیید شود. تأیید یک برش مجوز خودکار برای برش بعدی نیست.

## ۶. آنچه واقعاً در سمت سازنده ساخته شده است

### ۶.۱. ورود، پروژه و خانه

- onboarding سازنده با نقش ثابت؛ مسیر تأمین‌کننده جدا و شروع‌نشده است.
- ساخت پروژه از مجموعهٔ «پروژه‌ها» با حداقل نام، محدودهٔ تهران و مرحلهٔ ساخت.
- تعویض پروژه و فعال‌شدن زمینهٔ پروژهٔ تازه.
- شناسنامهٔ تدریجی پروژه برای نوع کاربری، مساحت، زیربنا، طبقات و واحدها.
- خانهٔ Chat-first با drawer راست، Composer، نوار پروژه/ابزار و زمینهٔ فعال پروژه.
- کنترل بالا و پایین فقط نام پروژه را نشان می‌دهند، اما button، accessible name و affordance روشن دارند.
- نشان تصویری اختصاصی CHIDA جای آیکون عمومی را گرفته است.
- Quick Action به rail افقی dragپذیر ده‌تایی تبدیل شده است:
  - مقصد واقعی: درخواست قیمت، پیشنهادها، کار جدید، افزودن فایل، افزودن عکس، ثبت حافظه، جست‌وجوی پروژه و Build؛
  - شروع‌کنندهٔ صریح Composer: شروع صورت‌جلسه و چیدن برنامه خرید.
- متن‌های خانه به‌صورت scoped درشت‌تر شده‌اند و صفحه overflow افقی ندارد.

### ۶.۲. فایل، عکس، حافظه و جست‌وجو

- گالری عکس پروژه با نگه‌داری Blob فقط در IndexedDB همان مرورگر.
- سند غیرتصویری تازه، پس از تطبیق پسوند/MIME، با Blob canonical در IndexedDB قابل‌بازشدن است.
- رکورد قدیمی metadata-only برای بازشدن نیاز به انتخاب دوبارهٔ فایل هم‌نام/هم‌اندازه دارد و اصالت محتوا ادعا نمی‌شود.
- ثبت دستی حافظهٔ پروژه، ویرایش، حذف و ترجیح محلی عدم استفاده در زمینه.
- جست‌وجوی قطعی محلی روی حافظهٔ دستی و metadata فایل‌های همان پروژه.
- خطای خواندن از empty state جداست و mutation مخزن ناخوانده تا load موفق قفل می‌شود.
- Demo پاسخ منبع‌دار فقط‌خواندنی با Mock Data جدا، آشکار و بدون mutation وجود دارد.
- در UX روزمره، منشأ/نسخه/وضعیت/دسترسی و متن‌های توضیحی تکراری پنهان شده‌اند؛ این اطلاعات در قرارداد داده یا سطح فنی حذف نشده‌اند.

### ۶.۳. مرکز کارها

- وظیفهٔ دستی project-scoped با عنوان، گام بعدی و موعد اختیاری.
- ویرایش نسخه‌دار، history رویداد `updated`، no-op، rollback و نمایش موعد/تکمیل.
- بعضی تصمیم‌های خرید به‌صورت task برای تأیید نهایی در مرکز کارها قرار می‌گیرند.

### ۶.۴. درخواست خرید

- درخواست محصول، خدمت و محصول چندقلمی در یک schema مشترک و حالت ساده/پیشرفته.
- حفظ داده هنگام جابه‌جایی ساده/پیشرفته.
- روشن‌سازی deterministic برای داده‌های unknown؛ بدون استخراج هوشمند.
- revision، fingerprint، history، no-op، rollback، project isolation و fail-close.
- Approval داخلیِ محتوای revision دقیق درخواست.
- ثبت دستی تأمین‌کننده/گیرندهٔ محلی در همان پروژه.
- DispatchDraft دقیق از درخواست تأییدشده و گیرنده‌های انتخاب‌شده.
- Approval محلی برنامهٔ ارسال با چرخهٔ pending/withdrawn/reopened/approved.
- مسیر اصلی UX کوتاه شده است: ثبت نیاز → انتخاب تأمین‌کننده‌های ثبت‌شده → تأیید نهایی در «کارها».

### ۶.۵. پیشنهاد و مقایسه

- صندوق خصوصی و ثبت دستی پیشنهاد محصول یا خدمت.
- نسخه، history، provenance جداگانه، unknownهای صریح و جداسازی پروژه.
- مقایسهٔ حسابیِ دقیق پیشنهادهای محصول بدون تغییر منبع و با تصمیم مستقل سازنده.
- مقایسهٔ کیفی معیارمحور پیشنهادهای خدمت بدون رتبه‌بندی یا انتخاب «بهترین».
- UX پیشنهاد با یک CTA اصلی، کارت کوتاه، خلاصهٔ تصمیم‌ساز و افشای تدریجی جزئیات فنی ساده شده است.

### ۶.۶. مذاکره تا T8-A5b

- T8-A1: پیش‌نویس خصوصی و نسخه‌دار سؤال مذاکره، pin‌شده به revision دقیق پیشنهاد/مقایسه.
- T8-A2: رونویسی خصوصی و دستی پاسخ مرتبط توسط سازنده، بدون ادعای دریافت شبکه‌ای یا هویت طرف مقابل.
- T8-A3: قضاوت صریح سازنده دربارهٔ پاسخ؛ «می‌پردازد / نیازمند روشن‌سازی / تعارض احتمالی» با دلیل.
- T8-A4: ثبت کیفی تغییر شرایط، حوزه و جهت اثر؛ بدون محاسبهٔ خودکار.
- T8-A5a: diff فقط‌خواندنی revisionهای رونویسی محلی پیشنهاد محصول.
- T8-A5b: diff فقط‌خواندنی revisionهای رونویسی محلی پیشنهاد خدمت.
- همهٔ موارد فوق private/local، نسخه‌دار، historical/no-rebind و بدون اثر بیرونی‌اند.

### ۶.۷. Build، Brief و Settings

- Build فقط اجزای امن و کنترل‌شدهٔ نمایشی را انتخاب می‌کند، مجوز و محل نصب را پیش‌نمایش می‌کند و نصب واقعی یا اجرای کد آزاد ندارد.
- Brief زمان‌بندی مرورگرمحورِ شبیه‌سازی‌شده دارد؛ worker/backend دائمی ندارد و هنوز Report ماندگار نیست.
- Settings فقط وضعیت واقعی نمونه را نشان می‌دهد؛ مصرف واقعی مدل، هزینه و quota «متصل نیست» باقی مانده‌اند.

## ۷. الگوهای قراردادی ارزشمند که نباید در ساده‌سازی گم شوند

- همهٔ اشیای مهم به `projectId` محدودند.
- ثبت موفق بر پایهٔ write-before-state است؛ UI پیش از ذخیرهٔ واقعی موفقیت نشان نمی‌دهد.
- read-error با «هیچ داده‌ای نیست» یکی نیست.
- no-op نسخه را بالا نمی‌برد.
- revision تاریخی بازنویسی یا بی‌صدا به نسخهٔ تازه rebind نمی‌شود.
- snapshot و fingerprint وابستگی‌ها دقیق و تغییرناپذیرند.
- خطای یک مخزن نباید رکورد سالم upstream را پنهان یا خراب کند.
- هر اقدام بیرونی آینده باید مقصد، محتوا و نسخهٔ دقیق را پیش‌نمایش و تأیید انسانی مستقل بگیرد.
- ساده‌سازی UI به معنی حذف provenance، نسخه، lineage، fail-close یا audit نیست؛ فقط افشای آن‌ها progressive است.

## ۸. چیزهایی که ساخته نشده‌اند و نباید از UI فعلی استنباط شوند

- backend، حساب کاربری واقعی، sync یا cloud storage پروژه؛
- ارسال/دریافت واقعی، RFQ، شبکهٔ تأمین‌کنندگان، matching، ranking یا احراز تأمین‌کننده؛
- مدل محلی یا ابری، پاسخ واقعی Chat، حافظهٔ مصرف‌شده توسط مدل یا tool calling؛
- هیچ Harness/Orchestrator، `ContextManifest`، `ModelGateway`، registry ابزار، run-state/queue یا eval runtime پیاده نشده است؛ جریان‌های فعلی فقط handler و storeهای قطعی prototype هستند؛
- پیام مشترک و shared case واقعی میان سازنده و تأمین‌کننده؛
- upload، backup، parser، OCR، extraction، content search، citation و `SourceRecord` واقعی؛
- پیوست واقعی Composer، voice handler و ورودی چندرسانه‌ای کامل؛
- نصب واقعی Build، کد آزاد، lifecycle کامل ابزار یا marketplace؛
- worker دائمی، push notification یا پایش background واقعی؛
- Report ماندگار، «امروز پروژه» واقعی یا مالی سبک؛
- production readiness، desktop design یا supplier experience.

تأمین‌کننده‌های فعلی فقط رکورد خصوصی و دستی همان پروژه‌اند. عبارت «مرتبط» یا «ارسال‌شده» دربارهٔ آن‌ها واقعیت محصول نیست.

Approval برنامهٔ ارسال همیشه simulation محلی است و باید این invariantها را حفظ کند:

- `simulationOnly=true`
- `externalEffect=none`
- `sendAuthorized=false`
- `externalActionAttempted=false`

پرداخت، قرارداد، تضمین، حمل، تحویل و حل اختلاف خارج از تعریف فعلی محصول‌اند.

## ۹. تأییدهای تجربه که هنوز بازند

نسخه‌ها منتشر شده‌اند، اما تأیید صریح تجربهٔ این برش‌ها فقط باید توسط ماهیار، با استفاده از نظر و بازبینی Sol، بسته شود:

- T8-A5b: diff پیشنهاد خدمت؛
- T8-UX1: ساده‌سازی تجربهٔ پیشنهاد؛
- T8-UX2: خلوت‌سازی عکس/فایل/حافظه و مسیر سریع خرید؛
- T8-UX3: پولیش خانه، Quick Action و نشان CHIDA.

انتشار فنی به معنی تأیید تجربه یا پایان فاز نیست.

## ۱۰. مسیر باقی‌ماندهٔ سمت سازنده در backlog فعلی

### ۱۰.۱. T8-A — ادامهٔ مذاکره

- پاسخ واقعی یا مشترک طرف دوم؛
- اثر عددی/فرمول‌دار تغییر شرایط فقط پس از تعریف فرمول، واحد و منبع؛
- amendment رسمی/shared یا پیشنهاد تازهٔ احرازشده؛
- موعد پاسخ، بی‌پاسخی و follow-up.

### ۱۰.۲. T8-B — رضایت تبادل تماس و بستن پرونده

- پیش‌نمایش اطلاعات تماس و رضایت متناسب هر طرف؛
- پایان‌های معتبر پرونده و مرز مسئولیت CHIDA؛
- تا فاز تأمین‌کننده، رضایت طرف دوم فقط در Demo مستقل و آشکار Mock قابل‌نمایش است.

### ۱۰.۳. T9-A — عمق مدیریت پروژه

- مرحله و نقطهٔ عطف؛
- تصمیم و دلیل؛
- instruction نسخه‌دار پروژه؛
- قابلیت‌های فعال پروژه؛
- اولویت، وابستگی سبک و تاریخچهٔ عمیق‌تر task؛
- افراد/مسئول ساده؛
- بایگانی امن و سیاست حذف پروژه.

### ۱۰.۴. T9-B — پایش و تکمیل مرکز کارها

- موعد پاسخ، انقضای پیشنهاد و درخواست بی‌پاسخ؛
- دلیل پایش، آخرین اجرا، اجرای بعدی، خاموش‌کردن، شکست و retry؛
- ادعای صادقانهٔ «بررسی هنگام بازشدن/بازبودن مرورگر» تا قبل از worker؛
- اتصال Brief به دادهٔ واقعی محلی.

### ۱۰.۵. T9-C — دامنه‌های حافظه

- حافظهٔ شخصی سازنده؛
- حافظهٔ پروندهٔ خصوصی/مشترک با `caseId`؛
- حافظهٔ رویه‌ای به شکل instruction/workflow نسخه‌دار؛
- منع نشت میان پروژه، پرونده و نقش.

### ۱۰.۶. T10 تا T13

- T10: Report ماندگار و «امروز پروژه» بر پایهٔ دادهٔ واقعی؛
- T11: پیوست واقعی Composer، lifecycle فایل و `SourceRecord`؛
- T12: مالی سبک در افق دوم، نه حسابداری؛
- T13: جداسازی کامل Chat، پاسخ دیررس، draft آفلاین، lifecycle Build، export/delete/migration/quota، PWA، حذف affordanceهای مرده، README، QA و E2E نهایی.

فقط پس از تأیید صریح پایان T13، فاز M1 مدل مجاز می‌شود.

## ۱۱. نظر معماری من دربارهٔ ترتیب ادامه

### ۱۱.۱. پیشنهاد اول: shared negotiation را زودتر از طرف دوم نسازیم

بخش‌های باقی‌ماندهٔ T8 که ذاتاً به پاسخ، هویت، رضایت یا amendment طرف دوم وابسته‌اند، بدون مسیر تأمین‌کننده یا backend به‌راحتی به نمایش ساختگی تبدیل می‌شوند. پیشنهاد من:

- بخش‌های local/manual مفید T8 که اکنون ساخته شده‌اند حفظ شوند؛
- پیگیری موعد محلی را می‌توان همراه T9-B ساخت؛
- پاسخ واقعی/shared، amendment رسمی و رضایت واقعی طرف دوم تا فاز تأمین‌کننده defer شوند؛
- اگر برای طراحی تجربه نیاز به دیدن آن‌هاست، فقط Demo جدا، read-only و آشکار Mock ساخته شود.

### ۱۱.۲. پیشنهاد دوم: برش بعدی بعد از تأیید Sol، T9-A کوچک باشد

در صورت موافقت ماهیار، برش مناسب بعدی یک vertical slice کوچک از «سیستم عامل پروژه» است، نه افزودن صفحه‌های بیشتر به مذاکره. نمونهٔ برش:

> یک نقطهٔ عطف پروژه + یک تصمیم با دلیل + اتصال یک task به آن، همگی project-scoped، نسخه‌دار و قابل‌بازیابی از خانه.

این برش ارزش اصلی برای سازنده را بیشتر از ادامهٔ شبیه‌سازی طرف دوم نشان می‌دهد و پایهٔ Brief، Report، Memory و Harness آینده می‌شود.

### ۱۱.۳. پیشنهاد سوم: Build را از ظاهر ابزار به lifecycle کنترل‌شده برسانیم

Build امروز شبیه‌سازی صادقانه است، اما ارزش بالقوهٔ آن زیاد است. نسخهٔ کامل‌تر سازنده باید حداقل این قرارداد را داشته باشد:

- catalog بستهٔ اجزای امن؛
- manifest مجوز و دادهٔ مصرفی؛
- پیش‌نمایش محل نصب و اثر؛
- نسخه، فعال/غیرفعال، rollback و حذف؛
- تست پذیرش هر قابلیت؛
- بدون اجرای کد آزاد و بدون marketplace عمومی در نسخهٔ پایه.

### ۱۱.۴. پیشنهاد چهارم: UI روزمره ساده بماند، قرارداد زیرین قوی بماند

بازخورد اخیر ماهیار درست نشان داد که نمایش دائمی منشأ، revision، fingerprint و توضیح‌های طولانی سازنده را فراری می‌دهد. راه درست حذف contract نیست؛ راه درست دو سطح است:

- سطح روزمره: «چیست، چه وضعی دارد، قدم بعدی چیست»؛
- سطح جزئیات/حسابرسی: source، نسخه، تاریخچه، fingerprint، حریم و خطا.

## ۱۲. نظر معماری من دربارهٔ Memory

### ۱۲.۱. اصل پایه

مدل نباید «حافظه داشته باشد». حافظه باید متعلق به محصول و تحت کنترل Harness/Data Layer باشد. مدل فقط context محدود و مجاز هر درخواست را دریافت می‌کند و با تعویض مدل، حافظهٔ CHIDA باقی می‌ماند.

### ۱۲.۲. سلسله‌مراتب منبع حقیقت پیشنهادی

1. اشیای ساختاریافته و نسخه‌دار دامنه: Project، Task، Decision، File، Request، Proposal، Approval، Report؛
2. فایل و `SourceRecord` ثبت‌شده با provenance؛
3. حافظهٔ صریح یا تأییدشدهٔ کاربر با منبع؛
4. chat history فقط برای continuity کوتاه‌مدت؛ نه منبع حقیقت؛
5. برداشت مدل صرفاً `MemoryCandidate` و هرگز واقعیت تأییدشده.

### ۱۲.۳. دامنه‌های لازم

- `personal`: ترجیح‌های شخصی سازنده؛
- `project`: واقعیت‌ها، تصمیم‌ها و رویه‌های همان پروژه؛
- `case_private`: یادداشت خصوصی یک درخواست/مذاکره؛
- `case_shared`: فقط دادهٔ واقعاً مشترک همان پرونده؛
- `procedural`: instruction یا workflow نسخه‌دار؛
- دامنهٔ سازمانی در فاز تیمی بعدی.

### ۱۲.۴. چه چیزی چگونه ذخیره شود

- تغییر صریح وضعیت محصول مستقیماً در شیء دامنه ذخیره شود، نه دوباره به‌عنوان حافظهٔ آزاد.
- فرمان روشن کاربر مانند «این را به خاطر بسپار» می‌تواند رکورد حافظه بسازد.
- ترجیح یا قاعدهٔ استنباط‌شده توسط مدل فقط به‌صورت candidate با تأیید یک‌ضرب کاربر پیشنهاد شود.
- دادهٔ حساس، اختلافی یا استنباطی هرگز بی‌صدا پایدار نشود.
- مکالمهٔ عادی و جمله‌های گذرا ephemeral بمانند.

### ۱۲.۵. قرارداد پیشنهادی `MemoryRecord`

حداقل فیلدهای مفهومی:

- id، version و scope؛
- owner و project/case linkage؛
- content type و normalized content؛
- source reference و source date؛
- created/updated time؛
- direct-user / owner-confirmed / externally-verified / model-inferred؛
- current / superseded / disputed / deleted؛
- sensitivity و permission؛
- freshness/expiry؛
- `useInContext` به‌عنوان ترجیح، نه مجوز مطلق؛
- retention، tombstone یا hard-delete policy؛
- audit history.

### ۱۲.۶. retrieval و `ContextManifest`

Harness ابتدا باید hard filter اجرا کند:

1. tenant/account؛
2. role؛
3. project/case؛
4. ACL و sensitivity؛
5. وضعیت current و منع استفاده.

سپس relevance، freshness، source quality و conflict را محاسبه کند. خروجی باید یک `ContextManifest` bounded باشد که برای هر item دلیل انتخاب، منبع، scope، نسخه و وضعیت مجوز را ثبت می‌کند. `useInContext=true` به‌تنهایی مجوز ارسال همهٔ حافظه‌ها به مدل نیست.

### ۱۲.۷. رفتار مورد انتظار کاربر

کاربر باید بتواند ببیند:

- CHIDA چه می‌داند؛
- این داده از کجا آمده؛
- کجا استفاده می‌شود؛
- چه چیزی وارد درخواست حاضر شده؛
- چگونه اصلاح، غیرفعال، حذف یا export شود.

نمای روزمره می‌تواند کوتاه باشد؛ audit باید در disclosure باقی بماند.

## ۱۳. نظر معماری من دربارهٔ Harness

تصویر «Harness = tools + memory + agent loop» جهت‌گیری درستی دارد، اما برای CHIDA بهتر است به پنج مسئولیت تفکیک شود:

1. Data/Memory Layer؛
2. Harness Orchestrator؛
3. provider-neutral Model Gateway؛
4. محیط ابزار کنترل‌شده و permissioned؛
5. لایهٔ اعتماد، audit و eval که همه را قطع می‌کند.

### ۱۳.۱. Harness چه چیزهایی باید داشته باشد

- تشخیص intent، space، role و نوع خروجی؛
- انتخاب پاسخ، task، object، approval یا monitor؛
- ساخت `ContextManifest`؛
- routing مدل بر اساس کیفیت/هزینه/سرعت و fallback؛
- registry محدود ابزار/skill/capability؛
- permission envelope برای داده و اثر؛
- state machine اجرای کار؛
- validation schema و قواعد دامنه؛
- preview و approval متناسب با ریسک؛
- idempotency برای جلوگیری از تکرار اقدام بیرونی؛
- cancel، timeout، retry، recovery و failure union؛
- durable run state و queue در فاز backend؛
- بودجهٔ context، output، زمان، ابزار و concurrency؛
- audit نتیجه بدون ذخیره یا نمایش reasoning خام؛
- evalهای کیفیت، source honesty، نشت، prompt injection و هزینه.

### ۱۳.۲. مدل چه اختیاری نباید داشته باشد

مدل نباید مستقیماً:

- پروژه، حافظه، فایل، درخواست، پیشنهاد یا تصمیم را mutate کند؛
- پیام یا اطلاعات تماس ارسال کند؛
- منبع یا هویت طرف مقابل را جعل کند؛
- پرداخت، قرارداد، تضمین، حمل یا تحویل را اجرا کند؛
- دادهٔ پروژهٔ دیگر را ببیند؛
- output بدون schema را به واقعیت محصول تبدیل کند.

مدل پیشنهاد یا خروجی غیرقابل‌اعتماد می‌دهد؛ Harness آن را validate، محدود و در نقطهٔ لازم به کاربر ارائه می‌کند.

### ۱۳.۳. vertical slice پیشنهادی پیش از گسترش مدل

بعد از پایان رسمی فاز سازنده، نخستین آزمون معماری Memory/Harness بهتر است فقط این سناریو باشد:

1. کاربر در پروژهٔ A صریحاً یک مورد را به خاطر می‌سپارد؛
2. در درخواست بعدی همان پروژه، Harness آن را با دلیل در `ContextManifest` انتخاب می‌کند؛
3. کاربر می‌بیند چه چیزی وارد context می‌شود؛
4. غیرفعال یا حذف‌کردن، استفادهٔ بعدی را واقعاً متوقف می‌کند؛
5. پروژهٔ B هیچ دسترسی به آن ندارد؛
6. تعارض دو رکورد پنهان نمی‌شود؛
7. تعویض provider/model هیچ تغییری در حافظه ایجاد نمی‌کند.

این vertical slice، معماری را بسیار بهتر از یک چت طولانی یا RAG نمایشی اثبات می‌کند.

## ۱۴. ارزش افزوده و moat پیشنهادی CHIDA

مزیت پایدار CHIDA خود مدل، prompt یا یک UI چت نیست. moat احتمالی ترکیب این لایه‌هاست:

- ontology و schema واقعی پروژه و خرید ساخت ایران؛
- workflowها و state machineهای پروژه، درخواست، پیشنهاد، مذاکره و تأیید؛
- حافظه و context graph منبع‌دار و دامنه‌دار؛
- ابزارهای امن با preview، approval، recovery و audit؛
- evalهای تخصصی فارسی و ساخت‌وساز؛
- دادهٔ ساختاریافتهٔ outcome و اصلاحات واقعی کاربران؛
- در آینده، شبکهٔ تأمین‌کننده با سه قلمرو حریم و رضایت دوطرفه.

مدل قابل‌تعویض است؛ این قراردادها چیزی هستند که مدل عمومی را به CHIDA تبدیل می‌کنند.

## ۱۵. تصمیم‌هایی که از Sol خواسته می‌شود

Sol لطفاً به‌جای شروع کدنویسی، این تصمیم‌ها را بررسی و پیشنهاد روشن بدهد:

1. آیا تعریف فعلی «پایان فاز سازنده» و gate T13 درست است یا باید کوتاه‌تر شود؟
2. کدام بخش‌های باقی‌ماندهٔ T8 واقعاً باید پیش از supplier ساخته شوند و کدام‌ها defer شوند؟
3. آیا T9-A کوچک، بهترین برش بعدی است؟ اگر نه، برش جایگزین دقیق چیست؟
4. آیا سلسله‌مراتب منبع حقیقت و دامنه‌های Memory بالا مناسب‌اند؟
5. قرارداد حداقلی `MemoryRecord` و `ContextManifest` چه اصلاحی نیاز دارد؟
6. Harness v1 دقیقاً چه interface، state machine و failure unionی داشته باشد؟
7. Build در پایان فاز سازنده تا چه سطحی باید lifecycle واقعی داشته باشد؟
8. کدام بدهی‌ها شرط gate هستند و کدام‌ها می‌توانند به افق دوم منتقل شوند؟
9. آیا اصلاح سند مادر همین حالا لازم است یا بعد از بسته‌شدن prototype سازنده انجام شود؟
10. یک ترتیب اجرایی کوچک و قابل‌تأیید برای پنج برش بعدی پیشنهاد شود.

## ۱۶. دستور شروع برای Sol

پیش از هر پیشنهاد یا تغییر:

1. `AGENTS.md` و `prototype/AGENTS.md` را کامل بخوان.
2. `CHIDA-Product-Definition-FA.md`، `BUILDER-FEATURE-BACKLOG-FA.md`، `CHIDA-PRODUCT-LEARNINGS-FA.md` و `CHIDA-CONTINUATION-HANDOFF-FA.md` را با این فایل تطبیق بده.
3. Git، release و prototype tree را read-only بررسی کن.
4. وضعیت را «منتشرشده اما builder-incomplete» در نظر بگیر.
5. ابتدا فقط تحلیل مسیر و پیشنهاد یک برش کوچک بده.
6. بدون انتخاب صریح ماهیار هیچ فایل، قابلیت، تست artifact‌ساز، commit، push، deploy، مدل محلی، backend، ارسال واقعی یا مسیر تأمین‌کننده را آغاز نکن.

## ۱۷. لینک‌های مشاهده

- Cloudflare canonical: <https://chida-prototype.pages.dev>
- Cloudflare immutable snapshot: <https://f665580f.chida-prototype.pages.dev>
- ChatGPT Sites owner-only: <https://chida-prototype.mahyarkl.chatgpt.site>

---

**وضعیت پایان این هنداف:** انتشار انجام شده، فایل هنداف تهیه شده و پروژه برای بازبینی Sol متوقف است.
