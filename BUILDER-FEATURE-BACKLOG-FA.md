# بک‌لاگ قابلیت‌های سمت سازندهٔ CHIDA

مبنای این فهرست فقط `CHIDA-Product-Definition-FA.md` است. این سند جای سند مادر را نمی‌گیرد؛ آن را به ترتیب ساخت قابل‌اجرا تبدیل می‌کند.

راهنمای سطح:

- **پایه**: الزام صریح نسخهٔ پایه/افق اول.
- **افق ۲**: قابلیت صریح محصول، پس از اثبات جریان پایه.
- **استنباط**: نام صفحه در سند نیامده و صورت اجرایی زیر پیشنهاد محافظه‌کارانه است.

## اولویت پیشنهادی ساخت

1. ایجاد پروژهٔ واقعی و فضای پروژه از داده‌های راه‌اندازی.
2. فایل‌ها، حافظه و جست‌وجوی منبع‌دار.
3. مرکز کارها، تأییدها، نسخه و تاریخچه.
4. درخواست خرید و استعلام؛ جریان قهرمان سازنده.
5. تطبیق تأمین‌کننده، پیشنهادها و مقایسهٔ نرمال‌شده.
6. مذاکرهٔ کنترل‌شده و تبادل رضایتمندانهٔ اطلاعات تماس.
7. اعلان، یادآوری و پایش.
8. گزارش‌ها و «امروز پروژه».
9. مالی سبک و گزارش کارگاه در افق دوم.

## قابلیت‌های باقی‌مانده

| حوزه | خروجی مورد انتظار | سطح | مرز مهم |
| --- | --- | --- | --- |
| مدیریت پروژه و فضا | مشخصات، مرحله، نقاط عطف، افراد، وظایف، تصمیم‌ها، فایل‌ها، درخواست‌ها، بودجهٔ سبک، گزارش، هشدار و قابلیت‌های فعال | پایه | مدیریت کامل منابع و زمان‌بندی خارج از نسخهٔ پایه است. |
| مرکز کارها | کارهای جاری، منتظر تأیید، تمام‌شده، ناموفق و پایش‌ها؛ وضعیت، گام، ورودی، نتیجه، خطا، توقف و تلاش دوباره | پایه | کار نباید در تاریخچهٔ چت دفن شود. |
| اسناد و فایل‌ها | آپلود PDF، عکس، پیش‌فاکتور، فاکتور، قرارداد و صفحه‌گسترده؛ نسخه، منشأ، اتصال به صفحهٔ مبدأ، جست‌وجو و استناد | پایه | فایل بیرونی دادهٔ غیرقابل‌اعتماد است، نه دستور عامل. |
| حافظه | حافظهٔ خصوصی شخصی، پروژه‌ای و پرونده‌ای؛ دامنه، منبع، تاریخ، مشاهده، اصلاح، حذف و «چیدا چه می‌داند» | پایه | MC-1 فقط شخصی/پروژه‌ای است و `case_private` با آزمون‌های ACL/closure در Gate بعدی می‌آید؛ رویهٔ اجرایی `Instruction/WorkflowDefinition` و پروندهٔ مشترک `ShareEvent/SharedCaseProjection` جدا از Memory هستند. |
| دستورالعمل‌های پروژه | ترجیحات و رویه‌های نسخه‌دار و پروژه‌محور | استنباط | سند صفحه‌ای با نام Instructions ندارد؛ system prompt آزاد و سراسری نسازید. |
| درخواست خرید و استعلام | ورودی متن/صدا/عکس/فایل، استخراج قلم/مقدار/واحد، کمبود و اطمینان، سؤال حداقلی، پیش‌نویس و تأیید | پایه | اولین جریان دامنه‌ای کامل بعد از زیرساخت پروژه است. |
| تطبیق تأمین‌کننده | انتخاب فروشندهٔ موجود یا پیشنهاد شبکه بر پایهٔ دسته، محدوده و توان پاسخ همراه با دلیل تطبیق | پایه | اولویت پولی پنهان و ادعای «بهترین» بدون شاهد ممنوع است. |
| پیشنهاد و مقایسه | حفظ اصل پیشنهاد، استخراج و تعدیل جدا، نرمال‌سازی واحد/مالیات/حمل/حداقل سفارش/موعد/اعتبار/پرداخت و توصیهٔ دلیل‌دار | پایه | اصل سند و لایهٔ نرمال‌شده همیشه جدا بمانند. |
| مذاکره | پیش‌نویس سؤال و پاسخ، کشف تناقض، اثر تغییر شرایط، مقایسهٔ نسخه‌ها و پیگیری موعد | پایه | ارسال فقط با تأیید کاربر؛ مذاکرهٔ کاملاً خودکار خارج از پایه است. |
| اعلان و پایش | قیمت، مقررات، موعد پاسخ، انقضای پیشنهاد، کار بی‌پاسخ و آستانهٔ هزینه/زمان با دلیل و اقدام پیشنهادی | پایه | کاربر باید بتواند پایش را خاموش و آخرین اجرا را ببیند. |
| تأییدها و حسابرسی | پیش‌نمایش اقدام مهم/بیرونی، مقصد، محتوا، نسخه، تأیید صریح، ثبت نتیجه و جلوگیری از ارسال دوباره | پایه | پرداخت، قرارداد، تعهد خرید، تضمین، حمل و تحویل اجرا نمی‌شود. |
| گزارش‌ها | شیء Report ماندگار و متصل به پروژه، فایل و مرحله؛ گزارش پس‌زمینه و «امروز پروژه» | پایه / افق ۲ | گزارش کارگاه تخصصی عمدتاً افق دوم است. |
| مالی سبک | بودجه، هزینهٔ ثبت‌شده، تعهد اعلامی، سررسید، بودجه/واقعیت، جریان نقدی و هشدار انحراف | افق ۲ | حسابداری رسمی، بانک و پرداخت نیست؛ فرمول و واحد باید دیده شوند. |
| تحقیق فنی و وب | جست‌وجوی منبع‌دار، تاریخ و دامنهٔ اعتبار، تعارض منابع، تفکیک فایل خصوصی/دانش تأییدشده/وب باز | پایه / افق ۲ | Deep Research حالت تکمیلی است، نه الزام برای هر پرسش. |
| ورودی چندرسانه‌ای | متن، صدا، دوربین، عکس، چند فایل و دست‌نوشته با حفظ مدرک مبدأ و تأیید دادهٔ کم‌اطمینان | پایه | دکمه‌های Composer باید به آپلود، استخراج و پیش‌نمایش واقعی وصل شوند. |
| اعتماد و امنیت | جداسازی کاربر/پروژه/پرونده، حداقل مجوز، منشأ، فرض، دادهٔ خصوصی مصرف‌شده، نسخه، بازگشت، حذف و خروج داده | پایه | این حوزه غیرقابل‌مصالحه است. |

### وضعیت اجرایی معماری سازنده پس از H2

- **PB-1 — منتشرشده؛ تأیید نهایی تجربه باز:** «برنامهٔ پروژه» یک Milestone، یک Decision با دلیل اجباری و یک Task متصل را private/project-scoped و در envelope اتمیک نسخه‌دار با revision/fingerprint/history نگه می‌دارد. stale overwrite نمی‌کند، no-op نسخه نمی‌سازد، rollback revision تازه است، Task در مرکز کارها بدون duplication دیده می‌شود و فرم create/edit مستقل از portal واقعاً RTL است. یازده regression متمرکز، app برابر ۱۷۸/۱۷۸، runtime برابر ۱۸۶/۱۸۶، Sites برابر ۴/۴، build/TypeScript و integrity هر ۲۸ فایل پاس شدند؛ commit قابلیت `6d71e42a89441b3499a4fe335e7bc5874feb6e36` و source release `bcb22465c0f6e1f04252c40a65f6249a5084ff6b` در GitHub، Cloudflare Pages و ChatGPT Sites نسخهٔ ۲۷ منتشر شدند.
- **UX-R1 — منتشرشده:** مخزن ناخوانای درخواست خرید مسیر recovery دوگامی و backup-first دارد. فقط primary request store پس از snapshot و backup بایت‌به‌بایت تأییدشده، intent ماندگار، source recheck و empty verification قابل reset است؛ هر شکست fail-close می‌ماند و storeهای Approval، گیرنده، DispatchDraft، برنامهٔ ارسال، پیشنهاد و سایر داده‌ها ثابت می‌مانند. نه regression متمرکز و اجرای واقعی تأییدشده، بازشدن دوبارهٔ «درخواست جدید» را بدون ساخت درخواست به‌جای کاربر پوشش دادند.
- **MC-1 — منتشرشده و تأییدشده:** دو نمای «این پروژه» و «شخصی»، Direct Remember، رکورد و candidate جدا، کنترل‌های مستقل، lifecycle/conflict/supersede/delete، envelope نسخه‌دار، مهاجرت محافظه‌کارانه، optimistic concurrency و fail-close ساخته شده‌اند. focused برابر ۳۵/۳۵، app برابر ۲۱۰/۲۱۰، runtime برابر ۲۱۸/۲۱۸ و Sites برابر ۴/۴ پاس شدند و ممیزی مستقل finding باز P0/P1/P2 ندارد. commit قابلیت `6169035074b4262b7576df638fa4a657f04fdee7` با prototype tree `a831c16ffb049bbbacd79aee533935eea33aa078` در GitHub، Cloudflare deployment `6d59d990-f82b-4f76-b5e8-7ad46c09ecda` و ChatGPT Sites نسخهٔ ۲۹ منتشر شد. هیچ producer مدل، retrieval خودکار، share، backend، sync یا اثر بیرونی فعال نیست.
- **SI-1 — منتشرشده و تجربه تأییدشده:** ماهیار Source/Composer Intake را با پیام جداگانه مجاز کرد. Composer متن دقیق و حداکثر یک عکس یا سند پشتیبانی‌شدهٔ همان draft را به‌صورت Source پروژه‌محور محلی ثبت می‌کند؛ preview/remove، بازکردن اصل، جداسازی پروژه، hash محتوای Source، recovery چندمخزنی و fail-close Source/file پوشش داده شده‌اند. focused برابر ۲۳/۲۳ و QA واقعی `390 × 844` پاس‌اند. ماهیار تجربه را تأیید کرد و snapshot ترکیبی SI-1/TM-1 با commit قابلیت `525f1bb1577b495416a4e5944341862f0ae5cfbf` و prototype tree `ea1af0b50b2e131ffc139a6f092cf5aa81c82875` در هر سه مقصد same-source منتشر شد.
- **TM-1 — منتشرشده؛ gate release PASS:** یک Monitor موعد فقط برای Task متصل «برنامهٔ پروژه» ساخته می‌شود؛ زمان تهران به UTC canonical می‌رود و Monitor/Run دقیقاً به id/version/revision/fingerprint Task bind هستند. history و Runها append-only، cadence پس از موعد ۲۴ساعته، lock order و rollback fail-close هستند و parser رشته یا شناسهٔ non-canonical را normalize نمی‌کند. بررسی خودکار فقط هنگام mounted و visible بودن مرکز «کارها»، در mount/focus/visibility و هر ۶۰ ثانیه انجام می‌شود؛ درخواست queued پس از گرفتن هر دو lock دوباره همین authority را می‌سنجد. Run دستی، disable/re-enable و retry نیز وجود دارند. focused برابر ۲۰/۲۰، app برابر ۲۵۳/۲۵۳، runtime برابر ۲۶۱/۲۶۱، Sites برابر ۴/۴، build/TypeScript/integrity/diff و QA واقعی `390 × 844` پاس‌اند و بازبینی مستقل پس از بستن دو P2 finding باز P0/P1/P2 ندارد. Cloudflare deployment `d54faf50-c7e7-43d1-974f-2925c9bdfe9a` و ChatGPT Sites نسخهٔ ۳۱/deployment `appgdep_6a95754e5e3481918c0a6679b0ab6016` از source دقیق همین commit در وضعیت نهایی موفق‌اند و Sites owner-only/custom مانده است. worker، تب بسته/پنهان، Notification API، backend، وب/قیمت/مقررات، مدل، شبکه و اثر بیرونی خارج‌اند.
- **baseline یکپارچه تا BG-F5 — منتشرشده و هم‌تراز:** snapshot شامل `REL-1/BL-1`، `BG-F1` تا `BG-F5`، `BUX-1` تا `BUX-3` و اصلاح focus گیت با SHA دقیق `93c41a8f728ff973a3ca9db29581b1e4968fe52b` روی local/GitHub `main`، Cloudflare Pages و ChatGPT Sites same-source منتشر شد. گیت تازهٔ همین release روی bytes منجمد با Playwright برابر ۴۱۰/۴۱۰ و Sites برابر ۴/۴ پاس شد؛ دسترسی Sites owner-only ماند. این سطر وضعیت‌های میان‌راهی «candidate/local/unpublished» ردیف‌های تفصیلی زیر را supersede می‌کند، اما نتیجهٔ تاریخی `BG-GATE-1` را تغییر نمی‌دهد و رسیدهای پویای deployment/version فقط در پیام تحویل terminal نگه‌داری می‌شوند.
- **REL-1/BL-1 — پیاده‌سازی و آزمون‌شده؛ تجربهٔ BL-1 تأیید و داخل snapshot مجازِ انتشار:** ماهیار اصلاح کندی انتشار و اجرای Build Lifecycle Minimum را صریحاً خواست. Fast Publish suite تکراری و receipt-only deployment را حذف می‌کند و candidate/artifact دقیق را به یک گیت و یک push bind می‌کند. Build اکنون یک `BuiltArtifact` امن، declarative، project-scoped و نسخه‌دار با preview/approval دقیق، فعال/غیرفعال/blocked، revision/rollback، tombstone و journal hash‌شدهٔ local/session با lock order ثابت دارد؛ ۱۹/۱۹ regression متمرکز، QA `390 × 844` و بازبینی مستقل پاس‌اند و هیچ plugin/skill واقعی، کد آزاد، شبکه، backend، مدل یا نصب خارجی نیست. مجوز اولیهٔ عبور از BL-1 فقط Gate را باز کرد؛ ماهیار بعداً انتشار snapshot ترکیبی جاری را صریحاً مجاز کرد و receiptهای terminal آن بیرون repo ثبت می‌شوند.
- **REL-2 — baseline محلی/GitHub؛ وضعیت میزبانی ناممیزی‌شده:** مجموعهٔ ۴۶۳تایی بدون حذف، skip، retry، افزایش timeout یا کاهش assertion حفظ شد. ۴۵۵ تست اپ با `fullyParallel` و سقف ثابت دو worker اجرا می‌شوند و ۸ تست runtime زمان‌حساس سریال می‌مانند؛ scheduler قدیمی با `npm run test:all:compat` در دسترس است. هفت oracle قدیمی که پس از کلیک async پیش از commit واقعی storage را می‌خواندند، فقط پس از بازتولید قرمز با انتظار شرطیِ همان وضعیت نهایی UI همگام شدند؛ کد محصول تغییر نکرد. دو پایلوت کامل روی bytes یکسان به‌ترتیب ۴۶۳/۴۶۳ در `15:53.42` و `15:43.72` پاس شدند؛ در برابر مبنای `30.3m` کاهش زمان حدود ۴۷٫۶٪ و ۴۸٫۱٪ است. REL-2 اکنون روی local/GitHub `main@5df0ef6e34713d999143cc313438fbb9e4996613` قرار دارد؛ وضعیت Cloudflare Pages و ChatGPT Sites برای همین SHA در ادامهٔ BG-F7 دوباره ممیزی نشده و از Git استنباط نمی‌شود. رسید گیت قدیمی فقط تاریخی است و هیچ مجوز یا receipt آن به candidate BG-F7 منتقل نمی‌شود.
- **BG-GATE-1 — ارزیابی تاریخی، نتیجهٔ دودویی همچنان FAIL:** inventory هنجاری و ماتریس ۱۲ ردیفی در `CHIDA-BUILDER-PROTOTYPE-ARCHITECTURE-GATE-FA.md` ثبت شد. BG-F1 تا BG-F5 اکنون در baseline منتشرشده‌اند و BUX-1 تا BUX-3 affordanceهای بی‌پشتوانهٔ شناخته‌شده را بسته‌اند؛ بااین‌حال ماتریس کامل rerun نشده و `case_private`، Proposal/Comparison/Negotiation، File/Photo و پنج تصمیم Gate-or-defer بازند. اجرای release gate با rerun این ماتریس یکی نیست؛ نتیجهٔ تاریخی `FAIL` و M1a نامجاز است.
- **BG-F1 — پیاده‌سازی و آزمون‌شده؛ داخل snapshot مجازِ انتشار:** fixture exact و نسخه‌دار Identity/Policy شامل AccountIdentity، Membership، RoleAssignment و AuthorizationContext template ساخته شد و context دقیق هر پروژه از آن resolve می‌شود. Project و ProjectProfile در envelope canonical v3 جدا و private/project-scoped هستند؛ activeProjectId، revision/history/SHA-256 fingerprint و idempotency receipt همان commit اتمیک را دارند. migration از v2/legacy با source precedence صریح، marker pending/committed، preimage/readback/rollback و بدون fallback از نسل خراب fail-close است. create/select/update/rollback زیر Web Lock با commit-time reread، expectedVersion، شناسهٔ deterministic از idempotency key، write-before-state و rollback بایت‌های خود انجام می‌شوند. failure/no-op/rollback/tamper/cutover-race و رقابت قطعی دو تب پوشش مستقیم دارند. انتشار این snapshot به معنی PASS شدن Gate تاریخی یا مجوز M1a نیست.
- **BG-F2 — پیاده‌سازی و آزمون‌شده؛ داخل candidate مجاز انتشار:** Task دستی به envelope canonical v2 و mutation seam exact با Web Lock، commit-time reread، expectedVersion/idempotency، migration سه‌مرحله‌ای و regressionهای crash/tamper/دو تب منتقل شد. UI روزمره همان مرکز «کارها» مانده و فقط draft پروژه‌محور، stale conflict و حالت loading/read-error دقیق‌تر شده‌اند. این برش فقط بخش Task دستی از P1-03 تاریخی را remediation می‌کند؛ Request/Approval/Dispatch، `case_private`، Gate PASS، مدل، backend، شبکه، مسیر تأمین‌کننده و انتخاب remediation بعدی پیام‌های جدا می‌خواهند. ماهیار در ۲۰۲۶/۰۹/۰۱ انتشار exact candidate ترکیبی جاری را صریحاً مجاز کرد.
- **BUX-1 — پیاده‌سازی، آزمون و تأیید تجربه؛ داخل candidate مجاز انتشار:** کنترل‌های بی‌اثر «گفتگوی تازه»، شمارندهٔ ساختگی «پین‌شده‌ها» و جست‌وجوی بی‌عمل گفتگوهای اخیر از Drawer حذف شدند؛ «امکانات چیدا» اکنون Tools را باز می‌کند و «دستیار فنی» تا ساخت مسیر واقعی صریحاً disabled/«به‌زودی» است. Settings دسترسی فعلی «نسخهٔ آزمایشی سازنده»، وضعیت جداگانهٔ مصرف مدل و صورتحساب به‌صورت «متصل نیست»، شمارش دادهٔ محلی و نمای داخلی «طرح و ارتقا» را دارد؛ این نما هیچ نام پلن پولی، قیمت، سهمیه، checkout یا موفقیت خرید نمی‌سازد. ماهیار با «بزن بریم» عبور از نقطهٔ مشاهده و در ۲۰۲۶/۰۹/۰۱ انتشار candidate ترکیبی جاری را صریحاً تأیید کرد.
- **BUX-2 — پیاده‌سازی، آزمون و تأیید تجربه؛ داخل candidate مجاز انتشار:** Build اکنون جریان صریح نیاز → تنها قالب امن `project-followup-view` → پیش‌نمایش زندهٔ برنامه/کارهای همان پروژه → مرور داده و مجوز → تأیید/فعال‌سازی → استفاده را دارد. کارت خلاصه، شمارش، فیلترهای واقعی و فهرست وضعیت از دادهٔ project-filtered رندر می‌شوند و اقدام مجاز «بازکردن مرکز کارها» فقط در `effectiveStatus=active` ظاهر می‌شود؛ disabled/blocked/read-error حق استفاده ندارند. schema، manifest، parser، writer و journal تغییر نکرده‌اند و هیچ کد آزاد، plugin/skill واقعی، مدل، شبکه، نصب یا اثر بیرونی افزوده نشده است. ۲۲/۲۲ regression دامنهٔ BuiltArtifact/BUX-2، build/TypeScript/runtime integrity، diff-check و QA واقعی `390 × 844` با overflow و console warning/error صفر پاس‌اند. ماهیار با «خوبه بریم بعدی» عبور از نقطهٔ مشاهده و در ۲۰۲۶/۰۹/۰۱ انتشار candidate ترکیبی جاری را صریحاً تأیید کرد.
- **BUX-3 — پیاده‌سازی، آزمون و تأیید تجربه؛ داخل candidate مجاز انتشار:** affordance صوتی بی‌عمل disabled/«به‌زودی» شد؛ Drawer به dialog واقعی با trap/Escape/بازگشت focus تبدیل شد؛ Build، جست‌وجو، نمونهٔ منبع‌دار و اسناد به مبدأ دقیق خود برمی‌گردند؛ starter صورت‌جلسه draft موجود را پاک نمی‌کند؛ مرز مدل و Brief محلی صریح است؛ Build در هر مرحله heading/focus/scroll/pending درست دارد و navigation هنگام mutation قفل می‌شود؛ Settings loading حافظه/Task را از read-error جدا می‌کند؛ و empty/error و اصطلاحات فنی در Tools/Build ساده شده‌اند. ۷/۷ regression اختصاصی و بستهٔ مرتبط ۱۹/۱۹، build/TypeScript/Vite/Sites prepare، integrity هر ۲۸ فایل، `git diff --check` و QA واقعی `390 × 844` با overflow و console warning/error تازهٔ صفر پاس‌اند. یک finding مستقل P2 برای Memory loading با state و regression جدا بسته شد و بازبینی نهایی finding باز P0/P1/P2 ندارد. ماهیار با «بریم تسک بعدی» عبور از نقطهٔ مشاهده و در ۲۰۲۶/۰۹/۰۱ انتشار candidate ترکیبی جاری را صریحاً تأیید کرد.
- **BG-F3 — پیاده‌سازی، آزمون و تأیید انتشار؛ داخل candidate مجاز انتشار:** writerهای create/edit/ready/return خود Request، recovery، تأیید ترکیبی Request+Approval و consumerهای مستقیم ساخت/تصمیم Approval زیر Web Lock مشترک با commit-time reread، expectedVersion، idempotency receipt، readback دقیق و rollback اثبات‌شده اجرا می‌شوند؛ stale overwrite رد و draft باز کاربر حفظ می‌شود. parser ترتیب/شناسه/authority رسیدها و وابستگی دوطرفهٔ confirm receipt↔Approval را fail-close می‌سنجد و حالت Draft دلیل قفل Approval ناخوانا را صریح نشان می‌دهد. ۱۷/۱۷ regression اختصاصی، ۴۱/۴۱ مسیر Request/Approval، ۱۸/۱۸ مسیر سازگاری service/T6-C/T6-D، build/runtime integrity، diff-check و QA واقعی `390 × 844` پاس‌اند و بازبینی مستقل finding باز P0/P1/P2 ندارد. legacy v2 بدون receipt فعلاً فقط برای سازگاری پذیرفته می‌شود؛ envelope/migration/idempotency کامل Approval و writerهای Contact/Dispatch باز می‌مانند. ماهیار در ۲۰۲۶/۰۹/۰۱ با پیام «همه جا منتشر کن» اجرای گیت کامل، commit و انتشار exact candidate ترکیبی جاری را در GitHub، Cloudflare Pages و ChatGPT Sites مجاز کرد؛ این مجوز Gate تاریخی را PASS نمی‌کند و به مدل، backend، شبکه، مسیر تأمین‌کننده یا تسک بعدی گسترش ندارد.
- **BG-F4 — پیاده‌سازی و آزمون‌شده؛ نقطهٔ مشاهده عبور کرده و داخل candidate محلی:** Content Approval فقط در envelope canonical `chida-prototype-project-approvals:v2` با record/revision/history/fingerprint و receiptهای command-bound نگه‌داری می‌شود؛ v1 فقط ورودی migration سه‌مرحله‌ای pending/verified/committed است و canonical/marker خراب fallback ندارد. مسیر compound Request+Approval یک intent ماندگار با سه حالت تولیدپذیر `prepared / request-written / committed` دارد؛ phase یا cross-link نامعتبر fail-close می‌ماند. confirmation نیمه‌کارهٔ تاریخی BG-F3 فقط وقتی دقیقاً tail یگانه و قابل‌اثبات باشد به Draft قبلی rollback می‌شود و evidence کامل آن در migration report می‌ماند؛ Approval گمشده جعل نمی‌شود. migration confirmation واقعی با timestampهای برابر، schema آیندهٔ ناشناخته، concurrency create/decision، نبود Web Locks، tamper دوطرفه، resume/reload/same-tab و reconfirm پوشش مستقیم دارند. ۱۸/۱۸ regression BG-F4، بستهٔ سازگاری BG-F3/BG-F4 برابر ۲۰/۲۰، TypeScript/build/runtime/diff و QA واقعی `390 × 844` پاس‌اند؛ `gate:release` همان candidate با ۳۷۳/۳۷۳ سناریوی Playwright و Sites برابر ۴/۴ پاس شد و بازبینی مستقل finding باز نداشت. ماهیار با پیام تازهٔ «تسک بعدی رو شروع کن» از نقطهٔ مشاهدهٔ BG-F4 عبور کرد و فقط BG-F5 را مجاز کرد. BG-F4 همچنان همراه candidate جاری local/uncommitted/unpublished است و هیچ commit/push/deploy از این پیام مجاز نشده است.
- **BG-F5 — پیاده‌سازی و آزمون‌شده؛ candidate مجاز انتشار:** `SupplierContact`، `DispatchDraft` و `DispatchPlanApproval` اکنون سه authority مستقل در `chida-prototype-project-supplier-contacts:v2`، `chida-prototype-project-dispatch-drafts:v2` و `chida-prototype-project-dispatch-plan-approvals:v2` هستند؛ هرکدام envelope دقیق، owner/scope/authority، revision/history، SHA-256 fingerprint، receiptهای command-bound، migration report و cutover سه‌مرحله‌ای دارند و v1 فقط migration input است. Draft نسخه/revision/fingerprint دقیق Contact را pin و Plan همان referenceهای pin‌شده را از Draft کپی می‌کند. هر mutation Draft/Plan با checkpoint علّی اتمیک در هر دو ledger Request و Content Approval به command/target/authorization و جایگاه دقیق prefix وصل می‌شود و از replay واقعی receiptها اعتبار می‌گیرد، نه timestamp. فرمان Draft→«کارها» افزون بر intent بایتی crash-safe، receiptهای aggregate دوطرفهٔ Draft/Plan دارد: Draft مستقیمِ بدون queue receipt از Plan مستقل است، اما Draft صف‌شده فقط با Plan متناظرِ committed معتبر می‌ماند و orphan/half-commit گمشده یا خراب fail-close می‌شود. UI قابلیت تازه‌ای نگرفته و همان مسیر محلی T6-C/T6-D را با pending/fail-close و focus دقیق‌تر حفظ می‌کند. regressionهای اختصاصی BG-F5 برابر ۳۷/۳۷، بستهٔ مرتبط T6-C/T6-D/T7-A/T7-B/خرید سریع برابر ۳۵/۳۵، build/runtime integrity و QA واقعی `390 × 844` با overflow و console warning/error صفر پاس‌اند؛ بازبینی مستقل finding باز P0/P1 ندارد. ماهیار انتشار exact candidate تا BG-F5 را پیش از شروع تسک بعدی صریحاً مجاز کرد؛ `gate:release` نهایی فقط پس از freeze اسناد اجرا و receiptهای terminal در پیام تحویل گزارش می‌شوند. تا آن زمان candidate local/uncommitted/unpublished است. فقط پس از موفقیت همان SHA در هر سه مقصد، شروع محلی و تست‌محور BG-F6 مجاز است؛ BG-F6 داخل این release نیست و هیچ مدل، backend، شبکه، ارسال، اثر بیرونی یا مسیر تأمین‌کننده ساخته نشده است.
- **اصلاح blocker انتشار BG-F5 — بسته و منتشرشده:** نخستین release-gate با build/integrity موفق و Playwright برابر ۴۰۹/۴۱۰ روی race focus پس از ذخیرهٔ Request متوقف شد؛ heading وجود داشت اما one-shot rAF گاهی پیش از attach ref اجرا و بی‌اثر می‌شد. انتقال focus به passive effect پس از commit/editor-close، بدون شل‌کردن test یا timeout، همان مسیر را از ۷/۱۰ به ۱۰/۱۰ رساند؛ چهار regression مجاور ۴/۴، build، integrity و review مستقل نیز پاس شدند. گیت شکست‌خورده receipt انتشار نبود؛ گیت کامل تازه روی candidate نهایی ۴۱۰/۴۱۰ و Sites برابر ۴/۴ پاس شد و همان bytes با SHA `93c41a8f728ff973a3ca9db29581b1e4968fe52b` منتشر شدند.
- **BG-F6 — تکمیل و آزموده؛ baseline کد local/GitHub:** Proposal و ProposalRevision ثبت‌شدهٔ سازنده از آرایهٔ v1 به authority canonical v2 با SHA-256، owner/scope، migration/cutover سه‌مرحله‌ای، Web Lock مشترک procurement، commit-time reread، expectedVersion، idempotency، readback و rollback candidate-owned منتقل شدند. receipt exact `commandPins` را برای replay payload نگه می‌دارد؛ UI نیز `proposalId/idempotencyKey/normalizedPayloadHash/pins` همان save attempt را روی failure مبهم حفظ می‌کند تا retry بدون‌تغییر فقط receipt را بازیابی و duplicate نسازد. lineage قدیمی FNV برای downstream موجود فقط از evidence هش‌شده خوانده می‌شود و storeهای Comparison/Decision/Negotiation بازنویسی نمی‌شوند. suite نهایی BG-F6 برابر ۵۳/۵۳ و بستهٔ نمایندهٔ downstream برابر ۹/۹ پاس شد؛ runtime integrity هر ۲۸ فایل، build/TypeScript/Vite/Sites prepare، diff-check، QA واقعی `390 × 844` با overflow افقی و console warning/error تازهٔ صفر و review مستقل نهایی نیز پاس‌اند. کد نهایی BG-F6 اکنون روی local/GitHub `main@7cab540ffad3e501e76a75c08a0e6462e36b7cea` هم‌تراز است؛ وضعیت Cloudflare Pages و ChatGPT Sites این SHA در REL-2 دوباره ممیزی نشده و از Git استنباط نمی‌شود. Gate تاریخی rerun نشده و مدل، backend، شبکه/ارسال، `case_private` و مسیر تأمین‌کننده خارج از دامنه‌اند.
- **اصلاح blocker گیت BG-F6 — بسته در baseline کد `7cab540`:** نخستین گیت مجاز پس از build/integrity/TypeScript با ۶ شکست Playwright از ۴۶۳ متوقف شد و receipt نداشت. یک regression واقعی سقف ۱۶۰نویسه‌ای عدد Proposal با قرارداد ۲۰۰رقمی T7 تعارض داشت؛ پنج failure دیگر و چند oracle بی‌صدای null-to-null از خواندن قدیمی v1 می‌آمدند. حد canonical به ۲۰۰ هم‌تراز و همهٔ oracleهای غیرمهاجرتی Proposal به raw v2 منتقل شدند؛ reproduction برابر ۶/۶ و بستهٔ عمیق T7/T8/BG-F6 برابر ۹۱/۹۱ پاس شد، درحالی‌که raw guard و رد oversized پابرجاست. اصلاح نهایی داخل baseline local/GitHub بالا قرار دارد؛ گیت شکست‌خوردهٔ تاریخی receipt انتشار نیست.
- **BG-F7 — Comparison Authority & Concurrency Foundation؛ candidate محلی در اصلاح گیت انتشار:** سطر پیشین «پس از BG-F6 — انتخاب‌نشده» وضعیت توقف پیش از مجوز جداگانهٔ BG-F7 بود و برای chronology با این رسید supersede می‌شود. Product و Service Comparison اکنون دو authority canonical v2 مستقل با topology هشت‌کلیدی legacy/canonical/marker/rollback-incident، SHA-256، migration/cutover سه‌مرحله‌ای، receipt/idempotency، Web Lock مشترک procurement، commit-time reread، readback و rollback candidate-owned دارند؛ خرابی یک خانواده دیگری را از خواندن/نوشتن سالم ساقط نمی‌کند. Decision و Negotiation مهاجرت یا بازنویسی نشده‌اند؛ target تاریخی FNV فقط با evidence سازگاری پذیرفته می‌شود و descendant تازه SHA canonical را pin می‌کند. بازبینی مستقل همهٔ findingهای درون scope را با RED/GREEN بست: uniqueness سراسری revision/event، drift پروژه، مرز scalar داخلی در برابر UTF-16 authority بالادستی، decimal نهایی، stale/no-receipt replay، receipt واقعاً committed، serializer مستقیم برای کلیدهای integer-index، شناسهٔ پروژهٔ رزروشده و رد بازگشتی ورودی غیر-JSON. روی bytes پیش از گیت، BG-F7 `42/42` در ۷٫۳ دقیقه، T7 `9/9` در ۱٫۲ دقیقه و downstream سریال `11/11` در ۱٫۸ دقیقه، همگی بدون retry/skip پاس شدند؛ TypeScript، integrity هر ۲۸ فایل، build/Sites preparation و diff-check tracked/untracked نیز پاس‌اند و review نهایی finding باز Critical/Important/Minor ندارد. نخستین `gate:release` تازه پس از build روی regression قدیمی Mock در ۱۰۳/۴۹۷ متوقف شد: test پیش از پایان cutoverهای پس‌زمینهٔ Comparison snapshot می‌گرفت، نه اینکه خود صفحهٔ Mock داده‌ای بنویسد. همان failure در اجرای تک‌تستی سریال بازتولید شد؛ oracle اکنون پیش از snapshot منتظر committed شدن هر دو marker می‌ماند و مسیر ۱/۱ و تکرار موازی ۱۰/۱۰ پاس است. این attempt receipt انتشار نساخت و candidate اصلاح‌شده یک full gate تازه می‌خواهد. QA گستردهٔ واقعی `390 × 844` پیش از آخرین سخت‌گیری‌های hash/replay، ساخت/بازگشایی/ویرایش Product، ماتریس ده‌معیارهٔ Service بدون score/rank، تعارض stale، fault معکوس دو ledger، reload/project isolation، disclosure بسته، overflow و console صفر و صفر request خارجی را تأیید کرد؛ همهٔ runtime pathهای تغییرکرده پس از آن نیز در Playwright واقعی همان viewport با byte-stability کامل دوباره سبز شدند. پایهٔ design در commit محلی `ef951657c3a9c41ba0a6269596de1340f715a2a8` است و amendment آن همراه implementation، plan و سه سند در working tree قرار دارد؛ کل BG-F7 unpublished است و نتیجهٔ تاریخی `BG-GATE-1` همچنان `FAIL` است. مجوز فعلی فقط full gate و انتشار همین exact candidate اصلاح‌شده را پوشش می‌دهد؛ مدل، backend، شبکه/ارسال، `case_private`، مسیر تأمین‌کننده و تسک محصولی بعدی داخل این release نیستند.
- **بدهی جدا پس از BG-F7 — Decision/Negotiation currentness و reader coupling:** writer فعلی Product Decision باید در برشی مستقل، currentness مقایسه را هنگام commit هم‌سطح مسیر Service دوباره بسنجد. همچنین reader فعلی Negotiation هر دو Comparison family را با هم می‌خواند؛ بنابراین خرابی یک family می‌تواند نمای Negotiation را موقتاً unavailable کند، هرچند ledger سالم Comparison همچنان مستقل خواندنی/نوشتنی است. این دو مورد عمداً داخل scope یا ادعای completion BG-F7 پنهان نشده‌اند و طراحی/آزمون/مجوز جدا می‌خواهند؛ authority و concurrency خود Decision/Negotiation نیز همچنان بدهی مستقل Gate هستند.

### وضعیت اجرایی «پیشنهاد و مقایسه»

- T7-A، ثبت دستی و صندوق خصوصی پیشنهاد، منتشر و تأیید شده است.
- T7-B1، هم‌سطح‌سازی و مقایسهٔ پیشنهادهای محصول و تصمیم مستقل سازنده، تأیید و با receipt نهایی SHA `92dcc14b911ff7840c3816ce5345c472604bff98` در هر سه مقصد same-source منتشر و بسته شده است: Cloudflare Pages deployment `c5c067b2-a623-4476-a024-6f6c81be8c4a` موفق بود و ChatGPT Sites نسخهٔ ۱۱ با version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_791562f862008191889941b682ef13b5` و deployment `appgdep_6a926a6591608191995a04aaeab7da6` در وضعیت `succeeded` قرار گرفت.
- T7-B2، قرارداد و مقایسهٔ مستقل پیشنهادهای خدمت، پس از تأیید ماهیار با receipt نهایی SHA `ef21c022ae28a772bbd710bc09c8f61a18db74ad` در GitHub، Cloudflare Pages deployment `4396280e-95e7-4ceb-bdf2-da720de51170` و ChatGPT Sites نسخهٔ ۱۳/deployment `appgdep_6a927e3aaec881918dc255bde7e18ba4` same-source منتشر و بسته شد. این برش، مقایسهٔ کیفیِ معیارمحور و تصمیم انسانی جداست؛ واحد/فرمول محصول، امتیازدهی، رتبه‌بندی، انتخاب «بهترین»، نرمال‌سازی، شبکه و اثر بیرونی به آن وارد نشده‌اند.

### وضعیت اجرایی «مذاکره»

- T8-A1 با receipt same-source نهایی SHA `2d72f5bbe86eab6c975a7beb2bd72ffc38c7e9ba`، Cloudflare deployment `806f7378-facb-411a-b9fa-f8febdaac62b` و ChatGPT Sites نسخهٔ ۱۵/deployment `appgdep_6a9295dd273c819195e5bde8cf2886bc` بسته شد. شش regression متمرکز، اپ ۱۱۸/۱۱۸، runtime ۱۲۶/۱۲۶، Sites ۴/۴، build/TypeScript و QA موبایل `390 × 844` پاس شدند و بازبینی مستقل نهایی P0/P1/P2 نداشت.
- T8-A2 پیاده‌سازی، آزموده و در `390 × 844` بازبینی شد: رونویسی خصوصی و دستی یک پاسخ مرتبط توسط سازنده، pin‌شده به revision دقیق سؤال T8-A1. هفت regression متمرکز، اپ ۱۲۵/۱۲۵، runtime ۱۳۳/۱۳۳، Sites ۴/۴، build/TypeScript و بازبینی مستقل بدون finding باز P0/P1/P2 پاس شدند. رکورد ارسال سؤال، دریافت پاسخ، هویت/اصالت تأمین‌کننده، شبکه، AI یا اثر بیرونی ادعا نمی‌کند؛ هر revision سؤال رکورد مستقل دارد، revision تاریخی فقط‌خواندنی می‌ماند و تغییر سؤال rebind خاموش نمی‌سازد. ماهیار آن را تأیید کرد؛ release کد با SHA `f5d68b6e757c0763a6770a46c5c3a211825713d3` منتشر شد و receipt same-source نهایی با SHA `5357fc503df57f51bd4659401399f1b3b76a0caa`، Cloudflare deployment `4ae8e8ee-bc2d-4b44-b542-7bffe7332181` و ChatGPT Sites نسخهٔ ۱۷/deployment `appgdep_6a92b60a7f848191bc8de2ac805a2934` در وضعیت `succeeded` بسته شد. prototype tree دو SHA یکسان ماند و access Sites همچنان owner-only/custom است.
- **T8-A3 — تأییدشده، منتشرشده و بسته‌شده:** سازنده می‌تواند روی revision/fingerprint دقیق رونویسی پاسخ T8-A2 یکی از سه قضاوت «پاسخ به پرسش می‌پردازد / نیازمند روشن‌سازی / تعارض احتمالی» را همراه دلیل اجباری و نسخه‌دار ثبت کند؛ lineage دقیق سؤال T8-A1 از snapshot تغییرناپذیر داخل همان پاسخ به ارث می‌رسد. هفت regression متمرکز، اپ ۱۳۲/۱۳۲، runtime ۱۴۰/۱۴۰، Sites ۴/۴، build/TypeScript، integrity ۲۸ فایل، QA مرورگر داخلی `390 × 844` و دو بازبینی مستقل بدون finding باز P0/P1/P2 پاس شدند. receipt نهایی SHA `0b8810683dd5c35819397cea95c7ff01e3ce9c95`، Cloudflare deployment `e2fb901e-bc10-49b7-b041-5d25606af0ed` و ChatGPT Sites نسخهٔ ۱۹/deployment `appgdep_6a92cb1191ac8191b655a8b73774b039` same-source منتشر شدند. AI، تشخیص خودکار، شبکه، پیام مشترک، احراز طرف مقابل، اصلاح خودکار پیشنهاد و اثر بیرونی در این برش وجود ندارد.
- **T8-A4 — تأییدشده و منتشرشده در snapshot یکپارچه:** سازنده می‌تواند برای revision/fingerprint دقیق پاسخ T8-A2 خلاصهٔ تغییر، حوزهٔ اثر، جهت اثر و دلیل را به‌صورت کیفی، خصوصی و نسخه‌دار ثبت کند. lineage از snapshot تغییرناپذیر پاسخ به ارث می‌رسد؛ تغییر review خواهر T8-A3 رکورد را stale نمی‌کند و اصلاح پاسخ یا dependency بالادستی رکورد قبلی را تاریخی و بدون rebind نگه می‌دارد. هفت regression متمرکز، اپ ۱۳۹/۱۳۹، runtime ۱۴۷/۱۴۷، Sites ۴/۴، build/TypeScript، integrity ۲۸ فایل و QA `390 × 844` پاس شدند. `automatedCalculationUsed=false`، `automatedDetectionUsed=false`، AI/شبکه false، mutation پیشنهاد/مقایسه false و `externalEffect=none` ثابت‌اند. ماهیار نسخهٔ لوکال را تأیید کرد و snapshot یکپارچه با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` در GitHub، Cloudflare Pages و ChatGPT Sites منتشر شد.
- **T8-A5a — تأییدشده و منتشرشده در snapshot یکپارچه:** مقایسهٔ مشتق‌شده و فقط‌خواندنی دو revision دقیق از همان پیشنهاد محصول، با pin نمایشی id/version/fingerprint و delta قطعی همهٔ فیلدهای رونویسی‌شده پیاده‌سازی و در `390 × 844` QA شده است. جفت نامعتبر fail-close می‌شود، null از literal کاربر جداست و نما store یا history تازه نمی‌سازد، اعداد را محاسبه یا رتبه‌بندی نمی‌کند و revisionهای ثبت‌شده توسط سازنده را amendment یا پیشنهاد تازهٔ احرازشدهٔ تأمین‌کننده نمی‌نامد. focused ۷/۷ و app ۱۴۶/۱۴۶ پاس‌اند؛ شکست قبلی runtime مربوط به زمان‌بندی Carousel زیر بار CPU بیرونی با اجرای isolated بسته شد، بدون تغییر runtime. ماهیار نسخهٔ لوکال را تأیید کرد و snapshot یکپارچه با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` منتشر شد؛ خدمت، محاسبهٔ عددی اثر، AI، شبکه، mutation و اثر بیرونی در T8-A5a نیستند.
- **T8-A5b — منتشرشده برای مشاهده؛ تأیید تجربه هنوز باز:** همان قرارداد diff مشتق‌شده و فقط‌خواندنی برای proposal خدمت، با یک line دقیق دارای `requestItemId=null` و `serviceSpecId` هم‌تراز با snapshot درخواست، پیاده‌سازی و در `390 × 844` QA شده است. سه فیلد revision و سیزده فیلد line خدمت دقیقاً به‌صورت رشته‌ای مقایسه می‌شوند؛ هویت line جداگانه pin می‌شود و لایهٔ ده‌معیارهٔ T7-B2 وارد proposal diff نمی‌شود. focused ۷/۷، app ۱۵۳/۱۵۳، runtime ۱۶۱/۱۶۱ و Sites ۴/۴ پاس‌اند و بازبینی مستقل finding باز P0/P1/P2 ندارد؛ storage/history تازه، arithmetic، AI، شبکه، mutation، amendment رسمی یا اثر بیرونی وجود ندارد. snapshot یکپارچه با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` برای مشاهده از خانه منتشر شد؛ این به معنی تأیید خود T8-A5b نیست.
- **T8-UX1 — منتشرشده برای مشاهده؛ تأیید تجربه هنوز باز:** کل تجربهٔ پیشنهاد سازنده بدون تغییر قرارداد داده بازآرایی شد: صندوق با یک CTA اصلی، فرم ساده/پیشرفته با حفظ مقادیر، خلاصهٔ تصمیم‌ساز پیش از جزئیات، diff با changed-first و جریان مقایسه/مذاکره با زبان انسانی و افشای تدریجی. رسیدهای فنی، تاریخچه، lineage، fail-close و focus بازگشت حفظ شده‌اند؛ دو regression تازه، app ۱۵۵/۱۵۵، runtime ۱۶۳/۱۶۳، Sites ۴/۴، build/TypeScript، integrity ۲۸ فایل، QA واقعی `390 × 844` و بازبینی مستقل بدون finding باز P0/P1/P2 پاس‌اند. snapshot آزموده با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` و receipt نهایی `d6224bbc392fda4b07910b49c110ada4859505f0` در همهٔ مقصدها منتشر شد؛ تأیید نهایی UX پس از مشاهده باز است.
- **T8-UX2 — منتشرشده برای مشاهده؛ تأیید تجربه باز:** ماهیار جداسازی عکس از «فایل‌ها»، بازشدن خود سند، حذف metadata و توضیح‌های تکراری از نمای عکس/فایل/حافظه، درشت‌ترشدن متن جزئیات و کوتاه‌شدن خرید به «ثبت نیاز → انتخاب تأمین‌کننده‌های ثبت‌شده → تأیید نهایی در کارها» را خواست. قراردادهای storage، version/lineage، fail-close و project isolation حفظ و جزئیات فنی از مسیر عادی پنهان شده‌اند؛ فایل تازه پس از تطبیق پسوند/MIME با Blob canonical در IndexedDB همان مرورگر باز می‌شود و فایل قدیمی `metadata-only` نیاز به انتخاب فایل پشتیبانی‌شده‌ای با نام و اندازهٔ ثبت‌شده دارد، بدون ادعای اصالت محتوا. این برش matching، حساب/مسیر تأمین‌کننده، شبکه، ارسال یا اثر بیرونی نمی‌سازد. build/TypeScript، integrity هر ۲۸ فایل، focused امنیت فایل ۲/۲، focused T8-UX2 برابر ۶/۶، رسانه/فایل/حافظه برابر ۲۰/۲۰، app برابر ۱۵۸/۱۵۸، runtime برابر ۱۶۶/۱۶۶ و Sites برابر ۴/۴ پاس‌اند؛ QA واقعی `390 × 844` و بازبینی مستقل بدون finding باز P0/P1/P2 پاس شدند. commit کد `a08c93edefad6cc79bc7bb1f83a023730a2ef06f` و receipt نهایی `8c7e5d9a46f2da1acb02456b6b55010e6037db94` در GitHub، Cloudflare deployment نهایی `e24010be-d6bd-427d-95f5-77f2c05ec9a5` و ChatGPT Sites نسخهٔ ۲۳/deployment `appgdep_6a93f27f77c48191adc895a833c41695` same-source منتشر شدند؛ تأیید نهایی تجربه پس از مشاهده باز است.
- **T8-UX3 — منتشرشده برای مشاهده؛ تأیید تجربه باز:** خانهٔ سازنده فقط نام پروژه را در کنترل بالا و پایین نشان می‌دهد، کنترل پایین affordance روشن دارد، نشان عمومی Sparkles با دارایی تصویری اختصاصی CHIDA جایگزین شده و متن‌های خانه scoped درشت‌تر شده‌اند. Quick Action از grid دو‌ستونه به rail افقی dragپذیر تغییر کرده و ده اقدام صادقانه دارد: هشت مقصد واقعیِ درخواست قیمت، پیشنهادها، کارها، فایل، عکس، حافظه، جست‌وجو و Build؛ دو شروع‌کنندهٔ صریح Composer برای صورت‌جلسه و برنامه خرید. قراردادهای مسیرهای موجود، project isolation، fail-close و runtime محافظت‌شده تغییر نکرده‌اند و مدل/شبکه/ارسال یا مسیر تأمین‌کننده ساخته نشده است. focused نهایی ۵/۵، app برابر ۱۵۹/۱۵۹، runtime برابر ۱۶۷/۱۶۷، Sites برابر ۴/۴، build/TypeScript و integrity ۲۸ فایل پاس‌اند؛ QA واقعی `390 × 844` با overflow صفحه و console error صفر و بازبینی مستقل بدون finding باز P0/P1/P2 بسته شد. commit کد `3e4b35fe08b02918e4be814d5d796da14bfa54ca` و receipt مستندی نهایی `3bbd870ca021339d89abc09528f57fc690deec50` prototype tree یکسان دارند. receipt نهایی در GitHub، Cloudflare deployment `f665580f-26c9-4363-9793-d4baaae5e644` و ChatGPT Sites نسخهٔ ۲۵/deployment `appgdep_6a941bdda88881918951b8508e2c2eec` same-source منتشر شد؛ تأیید تجربه پس از مشاهده باز است.
- کل T8-A هنوز تکمیل نیست. پاسخ واقعی/مشترک طرف دوم، محاسبهٔ عددی/فرمول‌دار اثر در صورت تعریف قرارداد آن، amendment رسمی/shared یا پیشنهاد تازهٔ احرازشده و پیگیری موعد در برش‌های بعدی باقی مانده‌اند. diff محلی revisionهای رونویسی‌شدهٔ محصول و خدمت با T8-A5a/T8-A5b پوشش داده شده است؛ هر مسیر مشترک/ارسالی آینده به snapshot مقصد/محتوا/نسخه و تأیید مستقل نیاز دارد.

### وضعیت اجرایی «پایش و امروز پروژه»

- **T9-B1 — پیاده‌سازی و آزموده؛ انتشار exact مجاز:** بریف از فهرست fixture به projection فقط‌خواندنیِ پروژهٔ فعال تبدیل شد و سه بخش واقعی «تصمیم‌های منتظر شما»، «کارهای باز و موعدها» و «درخواست‌های خرید باز» را با شمار، خلاصه و CTA به مقصد اصلی نشان می‌دهد. تاریخ آزاد Task دستی فقط برچسب ثبت‌شده است و فقط `dueAt` canonical برنامه به عقب‌افتاده/آینده طبقه‌بندی می‌شود. هر بخش loading/read-error مستقل دارد و بازکردن بریف هیچ storage write، Report، backend، مدل، شبکه یا worker ایجاد نمی‌کند. سه regression اختصاصی ۳/۳، regressionهای Brief برابر ۶/۶، build/integrity و QA `390 × 844` با overflow و console صفر پاس‌اند؛ بازبینی مستقل finding باز P0/P1/P2 ندارد. ماهیار با پیام «منتشر کن» انتشار exact همین candidate را مجاز کرد؛ candidate تا PASS شدن گیت و رسید سه مقصد همچنان uncommitted/unpublished است.
- **باقی‌ماندهٔ جدا:** «اسناد/ورودی‌های تعیین‌تکلیف‌نشده» و «تغییرات از آخرین مراجعه» تا ساخت authority قابل‌اثبات خودشان deferred هستند. Report ماندگار، اعلان واقعی و اجرای پس‌زمینه نیز در T9-B1 ساخته نشده‌اند و برش/مجوز جدا می‌خواهند.

## خارج از نسخهٔ پایه

- بازار عمومی پلاگین شخص ثالث و اجرای کد آزاد؛
- کانکتورهای گسترده؛
- مدیریت پیشرفتهٔ تیم و سازمان؛
- حسابداری رسمی و ERP؛
- مدیریت کامل زمان‌بندی و منابع؛
- BIM پیچیده و نقشهٔ اجرایی؛
- تشخیص قطعی نقص یا ایمنی از تصویر؛
- کنترل رایانه یا مرورگر؛
- مذاکرهٔ کاملاً خودکار؛
- قیمت یا تقاضای بازار بدون پوشش دادهٔ کافی.

پرداخت، تضمین، قرارداد، حمل، تحویل و حل اختلاف از تعریف فعلی محصول خارج‌اند.

## مراجع سند مادر

- معماری اطلاعات و پروژه: بخش‌های ۶ و ۱۳.
- Build و پلاگین: بخش‌های ۱۰ و ۱۱.
- خرید و مذاکره: بخش‌های ۱۵ و ۱۶.
- اعتماد و قراردادهای محصول: بخش‌های ۲۰ و ۲۱.
- نسخهٔ پایه و افق‌ها: بخش‌های ۲۴ و ۲۵.
