# بک‌لاگ قابلیت‌های سمت سازندهٔ CHIDA

مبنای این فهرست فقط `CHIDA-Product-Definition-FA.md` است. این سند جای سند مادر را نمی‌گیرد؛ آن را به ترتیب ساخت قابل‌اجرا تبدیل می‌کند.

راهنمای سطح:

- **پایه**: الزام صریح نسخهٔ پایه/افق اول.
- **افق ۲**: قابلیت صریح محصول، پس از اثبات جریان پایه.
- **استنباط**: نام صفحه در سند نیامده و صورت اجرایی زیر پیشنهاد محافظه‌کارانه است.

## اولویت پیشنهادی ساخت

1. ایجاد پروژهٔ واقعی و فضای پروژه از داده‌های راه‌اندازی.
2. فایل‌ها، حافظه و جست‌وجوی منبع‌دار.
3. مرکز کارها، تأییدها، نسخه و تاریخچه.
4. درخواست خرید و استعلام؛ جریان قهرمان سازنده.
5. تطبیق تأمین‌کننده، پیشنهادها و مقایسهٔ نرمال‌شده.
6. مذاکرهٔ کنترل‌شده و تبادل رضایتمندانهٔ اطلاعات تماس.
7. اعلان، یادآوری و پایش.
8. گزارش‌ها و «امروز پروژه».
9. مالی سبک و گزارش کارگاه در افق دوم.

## قابلیت‌های باقی‌مانده

| حوزه | خروجی مورد انتظار | سطح | مرز مهم |
| --- | --- | --- | --- |
| مدیریت پروژه و فضا | مشخصات، مرحله، نقاط عطف، افراد، وظایف، تصمیم‌ها، فایل‌ها، درخواست‌ها، بودجهٔ سبک، گزارش، هشدار و قابلیت‌های فعال | پایه | مدیریت کامل منابع و زمان‌بندی خارج از نسخهٔ پایه است. |
| مرکز کارها | کارهای جاری، منتظر تأیید، تمام‌شده، ناموفق و پایش‌ها؛ وضعیت، گام، ورودی، نتیجه، خطا، توقف و تلاش دوباره | پایه | کار نباید در تاریخچهٔ چت دفن شود. |
| اسناد و فایل‌ها | آپلود PDF، عکس، پیش‌فاکتور، فاکتور، قرارداد و صفحه‌گسترده؛ نسخه، منشأ، اتصال به صفحهٔ مبدأ، جست‌وجو و استناد | پایه | فایل بیرونی دادهٔ غیرقابل‌اعتماد است، نه دستور عامل. |
| حافظه | حافظهٔ شخصی، پروژه، پرونده و رویه‌ای؛ دامنه، منبع، تاریخ، مشاهده، اصلاح، حذف و «چیدا چه می‌داند» | پایه | دادهٔ یک پروژه یا طرف نباید به دیگری نشت کند. |
| دستورالعمل‌های پروژه | ترجیحات و رویه‌های نسخه‌دار و پروژه‌محور | استنباط | سند صفحه‌ای با نام Instructions ندارد؛ system prompt آزاد و سراسری نسازید. |
| درخواست خرید و استعلام | ورودی متن/صدا/عکس/فایل، استخراج قلم/مقدار/واحد، کمبود و اطمینان، سؤال حداقلی، پیش‌نویس و تأیید | پایه | اولین جریان دامنه‌ای کامل بعد از زیرساخت پروژه است. |
| تطبیق تأمین‌کننده | انتخاب فروشندهٔ موجود یا پیشنهاد شبکه بر پایهٔ دسته، محدوده و توان پاسخ همراه با دلیل تطبیق | پایه | اولویت پولی پنهان و ادعای «بهترین» بدون شاهد ممنوع است. |
| پیشنهاد و مقایسه | حفظ اصل پیشنهاد، استخراج و تعدیل جدا، نرمال‌سازی واحد/مالیات/حمل/حداقل سفارش/موعد/اعتبار/پرداخت و توصیهٔ دلیل‌دار | پایه | اصل سند و لایهٔ نرمال‌شده همیشه جدا بمانند. |
| مذاکره | پیش‌نویس سؤال و پاسخ، کشف تناقض، اثر تغییر شرایط، مقایسهٔ نسخه‌ها و پیگیری موعد | پایه | ارسال فقط با تأیید کاربر؛ مذاکرهٔ کاملاً خودکار خارج از پایه است. |
| اعلان و پایش | قیمت، مقررات، موعد پاسخ، انقضای پیشنهاد، کار بی‌پاسخ و آستانهٔ هزینه/زمان با دلیل و اقدام پیشنهادی | پایه | کاربر باید بتواند پایش را خاموش و آخرین اجرا را ببیند. |
| تأییدها و حسابرسی | پیش‌نمایش اقدام مهم/بیرونی، مقصد، محتوا، نسخه، تأیید صریح، ثبت نتیجه و جلوگیری از ارسال دوباره | پایه | پرداخت، قرارداد، تعهد خرید، تضمین، حمل و تحویل اجرا نمی‌شود. |
| گزارش‌ها | شیء Report ماندگار و متصل به پروژه، فایل و مرحله؛ گزارش پس‌زمینه و «امروز پروژه» | پایه / افق ۲ | گزارش کارگاه تخصصی عمدتاً افق دوم است. |
| مالی سبک | بودجه، هزینهٔ ثبت‌شده، تعهد اعلامی، سررسید، بودجه/واقعیت، جریان نقدی و هشدار انحراف | افق ۲ | حسابداری رسمی، بانک و پرداخت نیست؛ فرمول و واحد باید دیده شوند. |
| تحقیق فنی و وب | جست‌وجوی منبع‌دار، تاریخ و دامنهٔ اعتبار، تعارض منابع، تفکیک فایل خصوصی/دانش تأییدشده/وب باز | پایه / افق ۲ | Deep Research حالت تکمیلی است، نه الزام برای هر پرسش. |
| ورودی چندرسانه‌ای | متن، صدا، دوربین، عکس، چند فایل و دست‌نوشته با حفظ مدرک مبدأ و تأیید دادهٔ کم‌اطمینان | پایه | دکمه‌های Composer باید به آپلود، استخراج و پیش‌نمایش واقعی وصل شوند. |
| اعتماد و امنیت | جداسازی کاربر/پروژه/پرونده، حداقل مجوز، منشأ، فرض، دادهٔ خصوصی مصرف‌شده، نسخه، بازگشت، حذف و خروج داده | پایه | این حوزه غیرقابل‌مصالحه است. |

### وضعیت اجرایی معماری سازنده پس از H2

- **PB-1 — منتشرشده؛ تأیید نهایی تجربه باز:** «برنامهٔ پروژه» یک Milestone، یک Decision با دلیل اجباری و یک Task متصل را private/project-scoped و در envelope اتمیک نسخه‌دار با revision/fingerprint/history نگه می‌دارد. stale overwrite نمی‌کند، no-op نسخه نمی‌سازد، rollback revision تازه است، Task در مرکز کارها بدون duplication دیده می‌شود و فرم create/edit مستقل از portal واقعاً RTL است. یازده regression متمرکز، app برابر ۱۷۸/۱۷۸، runtime برابر ۱۸۶/۱۸۶، Sites برابر ۴/۴، build/TypeScript و integrity هر ۲۸ فایل پاس شدند؛ commit قابلیت `6d71e42a89441b3499a4fe335e7bc5874feb6e36` و source release `bcb22465c0f6e1f04252c40a65f6249a5084ff6b` در GitHub، Cloudflare Pages و ChatGPT Sites نسخهٔ ۲۷ منتشر شدند.
- **UX-R1 — منتشرشده:** مخزن ناخوانای درخواست خرید مسیر recovery دوگامی و backup-first دارد. فقط primary request store پس از snapshot و backup بایت‌به‌بایت تأییدشده، intent ماندگار، source recheck و empty verification قابل reset است؛ هر شکست fail-close می‌ماند و storeهای Approval، گیرنده، DispatchDraft، برنامهٔ ارسال، پیشنهاد و سایر داده‌ها ثابت می‌مانند. نه regression متمرکز و اجرای واقعی تأییدشده، بازشدن دوبارهٔ «درخواست جدید» را بدون ساخت درخواست به‌جای کاربر پوشش دادند.
- **برش بعدی — بدون مجوز اجرا:** طبق بستهٔ معماری تأییدشده، نامزد بعدی Memory Core و سپس Source/Composer Intake، Task/Monitor Core و Build Lifecycle Minimum است. انتشار PB-1/UX-R1 هیچ‌یک را خودکار آغاز نمی‌کند؛ هر برش مشاهده، تصمیم و پیام اجرایی جداگانهٔ ماهیار می‌خواهد. مدل، backend، شبکه و مسیر تأمین‌کننده همچنان خارج از این gate هستند.

### وضعیت اجرایی «پیشنهاد و مقایسه»

- T7-A، ثبت دستی و صندوق خصوصی پیشنهاد، منتشر و تأیید شده است.
- T7-B1، هم‌سطح‌سازی و مقایسهٔ پیشنهادهای محصول و تصمیم مستقل سازنده، تأیید و با receipt نهایی SHA `92dcc14b911ff7840c3816ce5345c472604bff98` در هر سه مقصد same-source منتشر و بسته شده است: Cloudflare Pages deployment `c5c067b2-a623-4476-a024-6f6c81be8c4a` موفق بود و ChatGPT Sites نسخهٔ ۱۱ با version ID برابر `appgprj_6a90313e390c81918572fc1b45269dac~appgver_791562f862008191889941b682ef13b5` و deployment `appgdep_6a926a6591608191995a04aaeab7da6` در وضعیت `succeeded` قرار گرفت.
- T7-B2، قرارداد و مقایسهٔ مستقل پیشنهادهای خدمت، پس از تأیید ماهیار با receipt نهایی SHA `ef21c022ae28a772bbd710bc09c8f61a18db74ad` در GitHub، Cloudflare Pages deployment `4396280e-95e7-4ceb-bdf2-da720de51170` و ChatGPT Sites نسخهٔ ۱۳/deployment `appgdep_6a927e3aaec881918dc255bde7e18ba4` same-source منتشر و بسته شد. این برش، مقایسهٔ کیفیِ معیارمحور و تصمیم انسانی جداست؛ واحد/فرمول محصول، امتیازدهی، رتبه‌بندی، انتخاب «بهترین»، نرمال‌سازی، شبکه و اثر بیرونی به آن وارد نشده‌اند.

### وضعیت اجرایی «مذاکره»

- T8-A1 با receipt same-source نهایی SHA `2d72f5bbe86eab6c975a7beb2bd72ffc38c7e9ba`، Cloudflare deployment `806f7378-facb-411a-b9fa-f8febdaac62b` و ChatGPT Sites نسخهٔ ۱۵/deployment `appgdep_6a9295dd273c819195e5bde8cf2886bc` بسته شد. شش regression متمرکز، اپ ۱۱۸/۱۱۸، runtime ۱۲۶/۱۲۶، Sites ۴/۴، build/TypeScript و QA موبایل `390 × 844` پاس شدند و بازبینی مستقل نهایی P0/P1/P2 نداشت.
- T8-A2 پیاده‌سازی، آزموده و در `390 × 844` بازبینی شد: رونویسی خصوصی و دستی یک پاسخ مرتبط توسط سازنده، pin‌شده به revision دقیق سؤال T8-A1. هفت regression متمرکز، اپ ۱۲۵/۱۲۵، runtime ۱۳۳/۱۳۳، Sites ۴/۴، build/TypeScript و بازبینی مستقل بدون finding باز P0/P1/P2 پاس شدند. رکورد ارسال سؤال، دریافت پاسخ، هویت/اصالت تأمین‌کننده، شبکه، AI یا اثر بیرونی ادعا نمی‌کند؛ هر revision سؤال رکورد مستقل دارد، revision تاریخی فقط‌خواندنی می‌ماند و تغییر سؤال rebind خاموش نمی‌سازد. ماهیار آن را تأیید کرد؛ release کد با SHA `f5d68b6e757c0763a6770a46c5c3a211825713d3` منتشر شد و receipt same-source نهایی با SHA `5357fc503df57f51bd4659401399f1b3b76a0caa`، Cloudflare deployment `4ae8e8ee-bc2d-4b44-b542-7bffe7332181` و ChatGPT Sites نسخهٔ ۱۷/deployment `appgdep_6a92b60a7f848191bc8de2ac805a2934` در وضعیت `succeeded` بسته شد. prototype tree دو SHA یکسان ماند و access Sites همچنان owner-only/custom است.
- **T8-A3 — تأییدشده، منتشرشده و بسته‌شده:** سازنده می‌تواند روی revision/fingerprint دقیق رونویسی پاسخ T8-A2 یکی از سه قضاوت «پاسخ به پرسش می‌پردازد / نیازمند روشن‌سازی / تعارض احتمالی» را همراه دلیل اجباری و نسخه‌دار ثبت کند؛ lineage دقیق سؤال T8-A1 از snapshot تغییرناپذیر داخل همان پاسخ به ارث می‌رسد. هفت regression متمرکز، اپ ۱۳۲/۱۳۲، runtime ۱۴۰/۱۴۰، Sites ۴/۴، build/TypeScript، integrity ۲۸ فایل، QA مرورگر داخلی `390 × 844` و دو بازبینی مستقل بدون finding باز P0/P1/P2 پاس شدند. receipt نهایی SHA `0b8810683dd5c35819397cea95c7ff01e3ce9c95`، Cloudflare deployment `e2fb901e-bc10-49b7-b041-5d25606af0ed` و ChatGPT Sites نسخهٔ ۱۹/deployment `appgdep_6a92cb1191ac8191b655a8b73774b039` same-source منتشر شدند. AI، تشخیص خودکار، شبکه، پیام مشترک، احراز طرف مقابل، اصلاح خودکار پیشنهاد و اثر بیرونی در این برش وجود ندارد.
- **T8-A4 — تأییدشده و منتشرشده در snapshot یکپارچه:** سازنده می‌تواند برای revision/fingerprint دقیق پاسخ T8-A2 خلاصهٔ تغییر، حوزهٔ اثر، جهت اثر و دلیل را به‌صورت کیفی، خصوصی و نسخه‌دار ثبت کند. lineage از snapshot تغییرناپذیر پاسخ به ارث می‌رسد؛ تغییر review خواهر T8-A3 رکورد را stale نمی‌کند و اصلاح پاسخ یا dependency بالادستی رکورد قبلی را تاریخی و بدون rebind نگه می‌دارد. هفت regression متمرکز، اپ ۱۳۹/۱۳۹، runtime ۱۴۷/۱۴۷، Sites ۴/۴، build/TypeScript، integrity ۲۸ فایل و QA `390 × 844` پاس شدند. `automatedCalculationUsed=false`، `automatedDetectionUsed=false`، AI/شبکه false، mutation پیشنهاد/مقایسه false و `externalEffect=none` ثابت‌اند. ماهیار نسخهٔ لوکال را تأیید کرد و snapshot یکپارچه با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` در GitHub، Cloudflare Pages و ChatGPT Sites منتشر شد.
- **T8-A5a — تأییدشده و منتشرشده در snapshot یکپارچه:** مقایسهٔ مشتق‌شده و فقط‌خواندنی دو revision دقیق از همان پیشنهاد محصول، با pin نمایشی id/version/fingerprint و delta قطعی همهٔ فیلدهای رونویسی‌شده پیاده‌سازی و در `390 × 844` QA شده است. جفت نامعتبر fail-close می‌شود، null از literal کاربر جداست و نما store یا history تازه نمی‌سازد، اعداد را محاسبه یا رتبه‌بندی نمی‌کند و revisionهای ثبت‌شده توسط سازنده را amendment یا پیشنهاد تازهٔ احرازشدهٔ تأمین‌کننده نمی‌نامد. focused ۷/۷ و app ۱۴۶/۱۴۶ پاس‌اند؛ شکست قبلی runtime مربوط به زمان‌بندی Carousel زیر بار CPU بیرونی با اجرای isolated بسته شد، بدون تغییر runtime. ماهیار نسخهٔ لوکال را تأیید کرد و snapshot یکپارچه با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` منتشر شد؛ خدمت، محاسبهٔ عددی اثر، AI، شبکه، mutation و اثر بیرونی در T8-A5a نیستند.
- **T8-A5b — منتشرشده برای مشاهده؛ تأیید تجربه هنوز باز:** همان قرارداد diff مشتق‌شده و فقط‌خواندنی برای proposal خدمت، با یک line دقیق دارای `requestItemId=null` و `serviceSpecId` هم‌تراز با snapshot درخواست، پیاده‌سازی و در `390 × 844` QA شده است. سه فیلد revision و سیزده فیلد line خدمت دقیقاً به‌صورت رشته‌ای مقایسه می‌شوند؛ هویت line جداگانه pin می‌شود و لایهٔ ده‌معیارهٔ T7-B2 وارد proposal diff نمی‌شود. focused ۷/۷، app ۱۵۳/۱۵۳، runtime ۱۶۱/۱۶۱ و Sites ۴/۴ پاس‌اند و بازبینی مستقل finding باز P0/P1/P2 ندارد؛ storage/history تازه، arithmetic، AI، شبکه، mutation، amendment رسمی یا اثر بیرونی وجود ندارد. snapshot یکپارچه با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` برای مشاهده از خانه منتشر شد؛ این به معنی تأیید خود T8-A5b نیست.
- **T8-UX1 — منتشرشده برای مشاهده؛ تأیید تجربه هنوز باز:** کل تجربهٔ پیشنهاد سازنده بدون تغییر قرارداد داده بازآرایی شد: صندوق با یک CTA اصلی، فرم ساده/پیشرفته با حفظ مقادیر، خلاصهٔ تصمیم‌ساز پیش از جزئیات، diff با changed-first و جریان مقایسه/مذاکره با زبان انسانی و افشای تدریجی. رسیدهای فنی، تاریخچه، lineage، fail-close و focus بازگشت حفظ شده‌اند؛ دو regression تازه، app ۱۵۵/۱۵۵، runtime ۱۶۳/۱۶۳، Sites ۴/۴، build/TypeScript، integrity ۲۸ فایل، QA واقعی `390 × 844` و بازبینی مستقل بدون finding باز P0/P1/P2 پاس‌اند. snapshot آزموده با commit کد `00a97a116a0115f6ffdae06579ded8fa64eb8c60` و receipt نهایی `d6224bbc392fda4b07910b49c110ada4859505f0` در همهٔ مقصدها منتشر شد؛ تأیید نهایی UX پس از مشاهده باز است.
- **T8-UX2 — منتشرشده برای مشاهده؛ تأیید تجربه باز:** ماهیار جداسازی عکس از «فایل‌ها»، بازشدن خود سند، حذف metadata و توضیح‌های تکراری از نمای عکس/فایل/حافظه، درشت‌ترشدن متن جزئیات و کوتاه‌شدن خرید به «ثبت نیاز → انتخاب تأمین‌کننده‌های ثبت‌شده → تأیید نهایی در کارها» را خواست. قراردادهای storage، version/lineage، fail-close و project isolation حفظ و جزئیات فنی از مسیر عادی پنهان شده‌اند؛ فایل تازه پس از تطبیق پسوند/MIME با Blob canonical در IndexedDB همان مرورگر باز می‌شود و فایل قدیمی `metadata-only` نیاز به انتخاب فایل پشتیبانی‌شده‌ای با نام و اندازهٔ ثبت‌شده دارد، بدون ادعای اصالت محتوا. این برش matching، حساب/مسیر تأمین‌کننده، شبکه، ارسال یا اثر بیرونی نمی‌سازد. build/TypeScript، integrity هر ۲۸ فایل، focused امنیت فایل ۲/۲، focused T8-UX2 برابر ۶/۶، رسانه/فایل/حافظه برابر ۲۰/۲۰، app برابر ۱۵۸/۱۵۸، runtime برابر ۱۶۶/۱۶۶ و Sites برابر ۴/۴ پاس‌اند؛ QA واقعی `390 × 844` و بازبینی مستقل بدون finding باز P0/P1/P2 پاس شدند. commit کد `a08c93edefad6cc79bc7bb1f83a023730a2ef06f` و receipt نهایی `8c7e5d9a46f2da1acb02456b6b55010e6037db94` در GitHub، Cloudflare deployment نهایی `e24010be-d6bd-427d-95f5-77f2c05ec9a5` و ChatGPT Sites نسخهٔ ۲۳/deployment `appgdep_6a93f27f77c48191adc895a833c41695` same-source منتشر شدند؛ تأیید نهایی تجربه پس از مشاهده باز است.
- **T8-UX3 — منتشرشده برای مشاهده؛ تأیید تجربه باز:** خانهٔ سازنده فقط نام پروژه را در کنترل بالا و پایین نشان می‌دهد، کنترل پایین affordance روشن دارد، نشان عمومی Sparkles با دارایی تصویری اختصاصی CHIDA جایگزین شده و متن‌های خانه scoped درشت‌تر شده‌اند. Quick Action از grid دو‌ستونه به rail افقی dragپذیر تغییر کرده و ده اقدام صادقانه دارد: هشت مقصد واقعیِ درخواست قیمت، پیشنهادها، کارها، فایل، عکس، حافظه، جست‌وجو و Build؛ دو شروع‌کنندهٔ صریح Composer برای صورت‌جلسه و برنامه خرید. قراردادهای مسیرهای موجود، project isolation، fail-close و runtime محافظت‌شده تغییر نکرده‌اند و مدل/شبکه/ارسال یا مسیر تأمین‌کننده ساخته نشده است. focused نهایی ۵/۵، app برابر ۱۵۹/۱۵۹، runtime برابر ۱۶۷/۱۶۷، Sites برابر ۴/۴، build/TypeScript و integrity ۲۸ فایل پاس‌اند؛ QA واقعی `390 × 844` با overflow صفحه و console error صفر و بازبینی مستقل بدون finding باز P0/P1/P2 بسته شد. commit کد `3e4b35fe08b02918e4be814d5d796da14bfa54ca` و receipt مستندی نهایی `3bbd870ca021339d89abc09528f57fc690deec50` prototype tree یکسان دارند. receipt نهایی در GitHub، Cloudflare deployment `f665580f-26c9-4363-9793-d4baaae5e644` و ChatGPT Sites نسخهٔ ۲۵/deployment `appgdep_6a941bdda88881918951b8508e2c2eec` same-source منتشر شد؛ تأیید تجربه پس از مشاهده باز است.
- کل T8-A هنوز تکمیل نیست. پاسخ واقعی/مشترک طرف دوم، محاسبهٔ عددی/فرمول‌دار اثر در صورت تعریف قرارداد آن، amendment رسمی/shared یا پیشنهاد تازهٔ احرازشده و پیگیری موعد در برش‌های بعدی باقی مانده‌اند. diff محلی revisionهای رونویسی‌شدهٔ محصول و خدمت با T8-A5a/T8-A5b پوشش داده شده است؛ هر مسیر مشترک/ارسالی آینده به snapshot مقصد/محتوا/نسخه و تأیید مستقل نیاز دارد.

## خارج از نسخهٔ پایه

- بازار عمومی پلاگین شخص ثالث و اجرای کد آزاد؛
- کانکتورهای گسترده؛
- مدیریت پیشرفتهٔ تیم و سازمان؛
- حسابداری رسمی و ERP؛
- مدیریت کامل زمان‌بندی و منابع؛
- BIM پیچیده و نقشهٔ اجرایی؛
- تشخیص قطعی نقص یا ایمنی از تصویر؛
- کنترل رایانه یا مرورگر؛
- مذاکرهٔ کاملاً خودکار؛
- قیمت یا تقاضای بازار بدون پوشش دادهٔ کافی.

پرداخت، تضمین، قرارداد، حمل، تحویل و حل اختلاف از تعریف فعلی محصول خارج‌اند.

## مراجع سند مادر

- معماری اطلاعات و پروژه: بخش‌های ۶ و ۱۳.
- Build و پلاگین: بخش‌های ۱۰ و ۱۱.
- خرید و مذاکره: بخش‌های ۱۵ و ۱۶.
- اعتماد و قراردادهای محصول: بخش‌های ۲۰ و ۲۱.
- نسخهٔ پایه و افق‌ها: بخش‌های ۲۴ و ۲۵.
