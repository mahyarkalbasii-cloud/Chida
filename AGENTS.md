# راهنمای عامل‌های پروژهٔ CHIDA

## منبع حقیقت

- سند مادر و تنها مرجع تعریف محصول، `CHIDA-Product-Definition-FA.md` است.
- `CHIDA-PRODUCT-LEARNINGS-FA.md` دفتر کاریِ یادگیری‌های ساخت، شکاف‌های سند مادر و تصمیم‌های پروتوتایپ است؛ این فایل جای سند مادر را نمی‌گیرد و فقط ورودی بازنگری بعدی آن است.
- پیش از تغییر دامنه، جریان، نقش، داده یا اقدام تجاری، بخش مرتبط سند مادر خوانده شود.
- اگر نمونهٔ نمایشی با سند مادر یا تصمیم صریح جدید کاربر تعارض داشت، تصمیم صریح جدید ثبت و تعارض شفاف شود؛ از روی UI دامنهٔ محصول حدس زده نشود.
- پیش از تحویل هر تسک پروتوتایپ و دوباره پس از بازخورد یا تأیید کاربر، دفتر یادگیری به‌روزرسانی شود: تجربهٔ مشاهده‌شده، شکاف یا ابهام سند مادر، تصمیم و وضعیت آن، و پیشنهاد اصلاح سند مادر باید از هم جدا ثبت شوند.

## تصمیم‌های ثابت فعلی

- چیدا یک محصول فارسی، RTL، واکنش‌گرا و Mobile-first است.
- موبایل و دسکتاپ دو حالت نمایش مستقل‌اند؛ دامنهٔ طراحی فعلی فقط موبایل است و نمای موبایل نباید به‌عنوان یک ستون کوچک داخل بوم دسکتاپ ارزیابی یا به‌جای طراحی دسکتاپ کش داده شود.
- نسخهٔ فعلی فقط Dark است و باید مثل یک اپ تمام‌صفحه اجرا شود؛ قاب نمایشی موبایل جزو محصول نیست.
- هر حساب دقیقاً یک نقش تغییرناپذیر دارد. تا وقتی کاربر شروع فاز سوم را صریحاً تأیید نکرده، مسیر سازنده فعال و مسیر تأمین‌کننده فقط قابل انتخاب و «به‌زودی» است؛ پس از آن نیز تأمین‌کننده مسیر مستقل خودش را می‌گیرد و role switch ساخته نمی‌شود.
- خانهٔ سازنده Chat-first است: منوی جمع‌شونده از راست، نام پروژهٔ جاری، اقدام‌های سریع، Composer و نوار چسبیدهٔ پروژه/ابزارها. کنترل پروژه در header و نوار پایین فقط نام پروژه را نشان می‌دهد و با ظاهر دکمه‌ای همچنان آشکارا قابل‌کلیک است. اقدام‌های سریع یک rail افقی RTL با چیپ آیکون‌دار و drag واقعی‌اند؛ rail فقط مسیرهای واقعاً ساخته‌شده و starterهای گفت‌وگوییِ صریح را نشان می‌دهد، overflow آن داخل خود Carousel می‌ماند و صفحه در `390 × 844` overflow افقی ندارد.
- «کارها» تنها مقصد روزمرهٔ سطح اول برای برنامهٔ فعلی، کارها، تأییدها و پایش‌هاست. Quick Action، Drawer و فضای پروژه همگی همین مرکز را باز می‌کنند؛ «برنامه/تصمیم‌ها» مقصد مستقل نیست و فقط جزئیات برنامهٔ فعلی از داخل کارها باز می‌شود. Milestone، Decision، Task متصل، Task دستی، Approval و Monitor در مدل داده مستقل می‌مانند و برای ساده‌سازی UI در یک store ادغام یا تکثیر نمی‌شوند.
- ساخت پروژهٔ تازه پس از onboarding فقط از داخل مجموعهٔ «پروژه‌ها» در دسترس است؛ این مجموعه از project switcher و ردیف «پروژه‌ها» در منوی راست باز می‌شود و دکمهٔ افزودن داخل همان مجموعه جریان واحد ساخت را با حداقل نام، محدودهٔ تهران و مرحلهٔ ساخت باز می‌کند. اقدام مستقیم افزودن در header، نوار پایین و خود drawer حذف شده است. پس از ذخیره، پروژهٔ تازه زمینهٔ فعال می‌شود.
- سه دادهٔ نام، محدودهٔ تهران و مرحلهٔ ساخت حداقل راه‌اندازی پروژه‌اند؛ شناسنامهٔ فضای پروژه به‌صورت تدریجی با نوع کاربری، مساحت زمین، زیربنای کل، طبقات روی زمین، طبقات منفی و تعداد واحدها تکمیل می‌شود و نباید فرم ورود را طولانی کند.
- رده‌بندی معتبر مرحلهٔ ساخت، به همین ترتیب، شامل «طراحی و اخذ مجوز»، «تخریب و گودبرداری»، «فونداسیون»، «اسکلت بندی»، «دیوارچینی و سفت کاری»، «گچ و خاک و تاسیسات»، «نازک کاری و نما»، «ظریف کاری و نصبیات» و «پایان کار» است. برچسب‌های قدیمیِ شناخته‌شده فقط برای سازگاری دادهٔ محلی به نزدیک‌ترین عنوان جدید مهاجرت می‌کنند.
- foundation پروژه در `chida-prototype-builder-projects:v3` یک envelope دقیق و اتمیک است: Project و ProjectProfile جدا، activeProjectId داخل همان envelope و revision/history/fingerprint/idempotency نسخه‌دارند. Identity/Policy mirror فقط وقتی معتبر است که دقیقاً با fixture کامپایل‌شدهٔ AccountIdentity، Membership، RoleAssignment و AuthorizationContext template برابر باشد؛ AccountSide از آن می‌آید و از MembershipRole جداست. v2/legacy و pointer قدیمی فقط ورودی migration یک‌باره‌اند؛ cutover سه‌مرحله‌ای pending/verified/committed است و پس از committed فقط v3 authority می‌ماند. canonical یا نسل جدید خراب، marker/identity نامعتبر و preimage تغییرکرده پیش از commit بدون fallback و overwrite fail-close می‌شوند. همهٔ create/select/update/rollbackها زیر Web Lock با reread، expectedVersion، validator exact پیش از idempotency، write-before-state، readback و rollback بایت‌های candidate انجام می‌شوند؛ parser باید receipt/command/snapshot، شناسه و chronology، ترتیب Project/Profile و active-project replay را به state قابل‌تولید writer محدود کند. پروژهٔ active ناقص، از جمله پس از rollback یا storage event، همیشه به فرم تکمیل برمی‌گردد.
- فایل پروژه یک شیء مستقل و پروژه‌محور است، نه پیوست گم‌شده در چت. عکس فقط در گالری و سند غیرتصویری فقط در «فایل‌ها» نمایش داده می‌شود. Blob عکس و سندی که کاربر مستقیم انتخاب کرده فقط در IndexedDB نسخه‌دار همان مرورگر نگه‌داری می‌شود تا پیش‌نمایش یا بازشدن فایل بعد از reload ممکن باشد؛ `localStorage` فقط شناسنامه را نگه می‌دارد و هرگز Blob، base64 یا محتوای فایل نمی‌گیرد. سند فقط وقتی پذیرفته می‌شود که پسوند پشتیبانی‌شده و MIME سازگار داشته باشد و Blob هنگام ذخیره و خواندن با MIME امن همان پسوند بازسازی شود. رکورد قدیمی `metadata-only` فقط پس از انتخاب یک فایل پشتیبانی‌شده با نام و اندازهٔ ثبت‌شده قابل‌بازشدن است؛ اصالت یا برابری محتوای فایل قدیمی قابل‌تأیید نیست. هیچ آپلود ابری، استخراج، جست‌وجوی محتوایی یا ورود به حافظه ادعا نشود. شکست هر بخش ذخیره‌سازی باید صریح نمایش داده و ثبت نیمه‌کاره rollback شود. پیوست Composer اکنون عکس یا یک سند پشتیبانی‌شده را واقعاً به همان draft متصل، پیش از ارسال قابل‌حذف و پس از ثبت به‌صورت یک `SourceRecord` پروژه‌محور قابل‌بازشدن می‌کند؛ کتابخانهٔ فایل جای پیوست نیست. همهٔ writerهای Source و metadata فایل یک Web Lock مشترک و reread هنگام commit دارند. فقط intake چندمخزنی Composer از intent ماندگار و rollback/resume استفاده می‌کند؛ پیوندهای Source به فایل و hash SHA-256 Blob همان Source پیش و پس از mutation سنجیده می‌شوند. writer مستقل فایل Blob تازه را پیش از commit شناسنامه می‌نویسد و همان bytes را بازخوانی و hash می‌کند، اما رکورد فایل مستقل هنوز hash ماندگار ندارد. هر خواندن یا بازیابی نامطمئن mutation هر دو مخزن را fail-close می‌کند.
- Memory Core فعلی فقط دو دامنهٔ خصوصی دارد: `account_private` با عنوان کاربری «شخصی» و `project_private` با عنوان «این پروژه». مسیر عادی ساخت فقط ثبت صریح سازنده است؛ پیام، فایل و خروجی مدل خودکار حافظه نمی‌شوند. `MemoryCandidate` شیئی جداست و فقط تأیید صریح همان متن، دامنه، نسخه و hash می‌تواند آن را به رکورد تازه تبدیل کند؛ در نسخهٔ فعلی هیچ producer مدل یا جریان ساخت candidate وجود ندارد. visibility، جست‌وجوی دستی، retrieval خودکار، model eligibility، shareability و ترجیح استفاده در context مستقل‌اند؛ retrieval/model/share فعلاً خاموش‌اند و `useInContextPreference=true` به‌تنهایی مجوز هیچ مصرفی نیست. منبع canonical فقط envelope دقیق v2 با SHA-256 است؛ نسل‌های صریح v1/legacy محافظه‌کارانه مهاجرت می‌شوند، v2 خراب fallback ندارد و cutover/hard-delete با marker کمینه و bridge موقت crash-safe می‌ماند. رکوردها با revision/fingerprint/history، conflict/supersede، rollback، tombstone حذف و Web Lock/expectedVersion نگه‌داری می‌شوند؛ خطای خواندن، مهاجرت یا نوشتن fail-close است. نمای روزمره content-first می‌ماند و جزئیات فنی پشت افشای اختیاری است.
- وظیفهٔ دستی پروژه پس از ثبت قابل‌ویرایش است و canonical آن فقط envelope دقیق `chida-prototype-project-tasks:v2` با owner/scope/custodian، projection مستقیم، revision/history/fingerprint، migration report و receiptهای idempotency است؛ آرایهٔ v1 فقط ورودی migration یک‌باره می‌ماند و canonical یا marker خراب هرگز به آن fallback نمی‌کند. cutover سه‌مرحله‌ای pending/verified/committed است: pending همراه candidate دقیق قابل-resume است، آخرین بازسنجی exact منبع/authority/candidate نقطهٔ انتقال authority و marker committed فقط انتشار ماندگار آن است؛ نوشتن v1 پس از این validation غیرمرجع است. همهٔ create/update/statusها زیر Web Lock با commit-time reread، expectedVersion، validator پیش از idempotency، receipt-command replay، write-before-state و rollback بایت‌های candidate انجام می‌شوند؛ no-op نسخه یا bytes را بالا نمی‌برد. پیش‌نویس editor به projectId زمان بازشدن bind است، stale conflict یا شکست قفل/نوشتن آن را حفظ می‌کند و هنگام pending یا loading/read-error قابل‌تغییر/ارسال نیست. عنوان، گام بعدی و موعد اتمام اختیاری در همان Task نسخه‌دار تغییر می‌کنند؛ موعد این نوع وظیفه فقط برچسب محلی است و به Monitor یا اجرای پس‌زمینه وصل نیست.
- بستهٔ چهار سند معماری توسط ماهیار تأیید شده و برش اول آن با نام کاربری «برنامهٔ پروژه» ساخته و در GitHub، Cloudflare Pages و ChatGPT Sites منتشر شده است: یک Milestone، یک Decision با دلیل اجباری و یک Task متصل، همگی private/project-scoped و در envelope اتمیک نسخه‌دار با revision/fingerprint/history نگه‌داری می‌شوند. read-error با empty یکی نیست، mutation روی state ناخوانده fail-close است، stale expectedVersion overwrite نمی‌کند، no-op نسخه نمی‌سازد و rollback محتوای قدیمی را به‌عنوان revision تازه ثبت می‌کند. Task متصل در مرکز کارها دیده می‌شود اما در store Task دستی تکثیر نمی‌شود. اصلاح RTL فرم create/edit و مسیر بازیابی امن مخزن ناخوانای درخواست خرید نیز در همین snapshot منتشر شده‌اند. برش دوم Memory Core نیز پس از مجوز، تکمیل و تأیید ماهیار با commit قابلیت `6169035074b4262b7576df638fa4a657f04fdee7` در GitHub، Cloudflare Pages و ChatGPT Sites نسخهٔ ۲۹ منتشر شد. برش‌های سوم Source/Composer Intake و چهارم Task/Monitor Core نیز با commit قابلیت `525f1bb1577b495416a4e5944341862f0ae5cfbf` و prototype tree `ea1af0b50b2e131ffc139a6f092cf5aa81c82875` در GitHub، Cloudflare Pages deployment `d54faf50-c7e7-43d1-974f-2925c9bdfe9a` و ChatGPT Sites نسخهٔ ۳۱/deployment `appgdep_6a95754e5e3481918c0a6679b0ab6016` same-source منتشر شدند. تجربهٔ SI-1 توسط ماهیار تأیید شده است؛ مجوز انتشار TM-1 به معنی تأیید UX تفصیلی آن نیست. ماهیار سپس اصلاح فرایند انتشار و اجرای Build Lifecycle Minimum را صریحاً مجاز کرد؛ `REL-1`، `BL-1`، `BG-GATE-1`، `BG-F1` و دو اصلاح UX اکنون در baseline هم‌تراز local/GitHub `main` با SHA `8196357339bf80c6532f8443a1baa957b6904af3` هستند؛ رسیدهای بیرونی terminal همچنان فقط در پیام تحویل مبنا هستند. `BG-GATE-1` نتیجهٔ تاریخی `FAIL` دارد و rerun کامل نشده است. `BG-F2`، `BUX-1`، `BUX-2`، `BUX-3` و `BG-F3` در candidate ترکیبی checkout اصلی ساخته، آزموده و بازبینی شده‌اند؛ ماهیار در ۲۰۲۶/۰۹/۰۱ با پیام «همه جا منتشر کن» اجرای گیت کامل، commit و انتشار exact همین candidate در GitHub، Cloudflare Pages و ChatGPT Sites را صریحاً مجاز کرد. این مجوز Builder Gate تاریخی را PASS نمی‌کند، remediation یا تسک بعدی را آغاز نمی‌کند و به مدل، backend، شبکه یا مسیر تأمین‌کننده گسترش ندارد؛ رسیدهای terminal انتشار فقط در پیام تحویل ثبت می‌شوند.
- Task/Monitor Core فعلی فقط یک پایش موعد برای Task متصلِ «برنامهٔ پروژه» می‌سازد؛ وظیفهٔ دستی قدیمی producer آن نیست. ورودی موعد `datetime-local` با زمان تهران است و در snapshot Task به ISO UTC canonical تبدیل می‌شود. `Monitor` و `Run` در envelope جدا، project-scoped، نسخه‌دار، append-only و دقیقاً به id/version/revision/fingerprint همان Task bind هستند؛ parser رشته‌ها و شناسه‌های non-canonical را normalize نمی‌کند و fail-close می‌شود. همهٔ mutationها Monitor→Backbone را زیر Web Lock مشترک، expected preimage، reread پیش و پس از commit و rollback بایت‌های خود اجرا می‌کنند. بررسی خودکار فقط وقتی مرکز «کارها» در همین مرورگر باز و قابل‌دیدن است، هنگام mount/focus/visibility و هر ۶۰ ثانیه انجام می‌شود؛ درخواست خودکاری که پشت lock منتظر مانده باید پس از گرفتن هر دو lock و پیش از هر read/mutation، mounted/visible بودن را دوباره بسنجد. worker، اجرای تب بسته یا پنهان، Notification API، backend، مدل، شبکه و اثر بیرونی وجود ندارند. تغییر revision Task و حذف موعد شکست قابل‌تلاش‌مجدد می‌سازند؛ Task حذف‌شده یا مخزن ناخوانا بدون tombstone معتبر fail-close است و recoverable ادعا نمی‌شود. full Task status transitions، چند پایش/تکرار دلخواه، cancellation/rollback پایش و cadence غیر از بررسی ۲۴ساعتهٔ ثابت پس از رسیدن موعد deferred هستند.
- جست‌وجوی محلی پروژه فقط تطبیق قطعی روی عنوان، متن و نوع حافظهٔ دستی و نام، دسته، فرمت و مرحلهٔ شناسنامهٔ اسناد همان پروژه است. فیلدهای فنی پنهان مانند منشأ، دسترسی، نسخه و وضعیت نباید نتیجه‌ای بسازند که دلیل تطبیق آن برای کاربر دیده نمی‌شود. هر نتیجه رکورد اصلی را باز می‌کند و فقط نوع، تاریخ و اطلاعات روزمرهٔ قابل‌فهم را نشان می‌دهد. محتوای فایل، OCR، embedding، رتبه‌بندی هوشمند، وب، بازار، citation، پاسخ AI، پروژهٔ دیگر، sync و backend هنوز جست‌وجو نمی‌شوند و پاسخ منبع‌دار واقعی تا اتصال منابع واقعی «به‌زودی» باقی می‌ماند. یک تجربهٔ مستقل و فقط‌خواندنی می‌تواند قرارداد این پاسخ را با دادهٔ ثابت `Mock` نشان دهد، مشروط به اینکه در ورودی، بنر، پاسخ، منبع و جزئیات به‌وضوح «نمونه/ساختگی» باشد، هیچ لینک یا بازیابی واقعی نداشته باشد، در پروژه/چت/storage ثبت نشود و با T4-B ادغام نشود. خطای خواندن دادهٔ محلی باید از empty state جدا نمایش داده شود و تا بارگذاری موفق بعدی، نوشتن در همان مخزن فایل یا حافظه مسدود بماند تا دادهٔ ناخوانده با آرایهٔ ناقص بازنویسی نشود.
- Build فعلی فقط lifecycle محلی یک `BuiltArtifact` امن، declarative و project-scoped از catalog بسته است. preview باید داده، مجوز، اجزای امن، action محدود، محل فعال‌سازی و رابطهٔ metadata با capability/skill را نشان دهد؛ فعال‌سازی، disable/reactivate، revision، rollback تازه و حذف tombstone نسخه‌دارند. خطای خواندن empty نیست و mutation را قفل می‌کند. ناخواناشدن برنامه/کارها باید پیش از انتظار برای قفل مخزن اصلی در journal مستقل، exact و hash‌شده ثبت شود؛ journal در `localStorage` و `sessionStorage` همان تب mirror می‌شود و همهٔ writerهای BuiltArtifact ترتیب قفل ثابت journal سپس مخزن اصلی دارند. `active@N + intent@N` حتی پس از repair، reload یا تعویض پروژه مؤثرانه blocked می‌ماند و intent فقط پس از اثبات `blocked@N+1` مستقیم پاک می‌شود. خطای خواندن هر mirror fail-close است و cleanup شکست‌خورده بدون hot-loop فقط با trigger تازه یا تلاش صریح تکرار می‌شود. هیچ plugin/skill واقعی، اجرای کد آزاد، شبکه یا نصب خارجی وجود ندارد.
- Brief خلاصهٔ اقدام‌محور روزانه یا هفتگی پروژه است؛ زمان‌بندی فعلی فقط داخل مرورگر شبیه‌سازی می‌شود. ذخیرهٔ موفق sheet را می‌بندد و خلاصهٔ drawer را به‌روز می‌کند؛ شکست ذخیره sheet را با خطای صریح باز نگه می‌دارد.
- تنظیمات فعلی باید فقط وضعیت‌های واقعی نمونه را نشان دهد: نقش ثابت، حالت پاسخ، برنامهٔ Brief، Dark ثابت، حریم و شمارش رکوردهای محلی. مصرف واقعی مدل، هزینه یا quota تا اتصال منبع معتبر «متصل نیست» نمایش داده می‌شود و عدد ساختگی ندارد. تصویر پروفایل تولیدشده fixture نمایشی است و نباید هویت تأییدشده القا کند.
- بازخورد صریح ۲۰۲۶/۰۹/۰۱ تکمیل قابل‌دیدن سازنده را پیش از ادامهٔ remediationهای صرفاً معماری قرار می‌دهد. هیچ کنترل enabled در Drawer، Tools یا Settings نباید بی‌اثر باشد؛ هر کنترل یا مقصد واقعی و قابل‌آزمون دارد یا صریحاً disabled/«به‌زودی» است. Settings باید سطح روشن «دسترسی فعلی، مصرف و ارتقا» داشته باشد: دسترسی فعلی فقط «نسخهٔ آزمایشی سازنده» است، مصرف مدل/هزینه/quota و صورتحساب تا اتصال منبع معتبر «متصل نیست» می‌مانند و مسیر ارتقا بدون جعل نام پلن پولی، قیمت، سهمیه، checkout یا موفقیت خرید فقط مرز و وضعیت واقعی را توضیح می‌دهد. گسترش Build/«برایم بساز» و sweep ریزه‌کاری‌ها برش‌های قابل‌دیدن جدا هستند و هرکدام پیش از تسک بعدی مشاهده و تأیید می‌شوند.
- هر ارسال بیرونی، قیمت، درخواست خرید، مذاکره یا تبادل اطلاعات تماس به تأیید صریح انسان نیاز دارد. پرداخت، قرارداد، تضمین، حمل، تحویل و حل اختلاف خارج از محصول‌اند.
- فرم درخواست محصول/خدمت باید با حالت «ساده» آغاز شود و فقط فیلدهای اثرگذار روزمره را نشان دهد؛ «پیشرفته» همان شیء و schema را با جزئیات بیشتر باز می‌کند، نه اینکه نوع درخواست دیگری بسازد. جابه‌جایی حالت نباید دادهٔ واردشده را پاک کند و فیلدهای تکمیل‌نشده باید طبق قرارداد unknown/clarification موجود باقی بمانند. فیلد مشترک «توضیح بیشتر (اختیاری)» در هر دو نوع محصول/خدمت و هر دو حالت ساده/پیشرفته، پس از همهٔ فیلدهای اختصاصی و بلافاصله بالای اقدام اصلی «ادامه» یا «ذخیرهٔ تغییرات» قرار می‌گیرد و تغییر حالت نباید مقدار آن را پاک کند.
- مسیر روزمرهٔ درخواست خرید باید فقط سه گام روشن داشته باشد: ثبت نیاز، انتخاب یک یا چند تأمین‌کنندهٔ ثبت‌شدهٔ همان پروژه و تأیید نهایی برنامه در «کارها». Approval محتوا، snapshot دقیق، DispatchDraft، نسخه/lineage و fail-close پشت همین مسیر حفظ می‌شوند و فقط در جزئیات فنی اختیاری دیده می‌شوند. عنوان «تأمین‌کنندهٔ مرتبط» تا وجود شبکه و قرارداد تطبیق واقعی ادعا نشود؛ تأیید نهایی فعلاً شبیه‌سازی محلیِ بدون ارسال، شبکه یا اثر بیرونی است.
- مخزن ناخوانای درخواست خرید باید fail-close بماند اما کاربر را در بن‌بست دائمی نگذارد. بازیابی فقط با تأیید دوگامی، snapshot دقیق، backup محلیِ بایت‌به‌بایت تأییدشده، intent ماندگار، بررسی دوبارهٔ منبع و reset فقط همان کلید اصلی مجاز است؛ هر شکست mutation را بسته نگه می‌دارد و پس از remove باید rollback یا resume امن ممکن باشد. Approval، گیرنده، DispatchDraft، برنامهٔ ارسال، پیشنهاد و سایر storeها هرگز با این recovery پاک یا بازنویسی نمی‌شوند.
- تجربهٔ پیشنهاد برای سازنده باید آرام، کارمحور و دارای افشای تدریجی باشد: صندوق در حالت عادی یک اقدام اصلی برای ثبت پیشنهاد دارد، کارت و جزئیات ابتدا فقط وضعیت و شرط‌های تصمیم‌ساز را نشان می‌دهند و مقایسه، پیگیری، تاریخچه، شناسه‌ها، fingerprintها، flagهای فنی و نام برش‌های داخلی فقط در سطح بعدی باز می‌شوند. خطای خواندن، وضعیت تاریخی/نیازمند بازبینی و مرز «ثبت دستی و بدون اثر بیرونی» همچنان باید کوتاه و آشکار بمانند؛ ساده‌سازی نباید schema، نسخه، lineage، fail-close یا جداسازی پروژه را تضعیف کند.
- تأیید محلی برنامهٔ ارسال از تأیید محتوای درخواست و از مجوز/اقدام بیرونی آینده مستقل است. این رکورد فقط snapshot دقیق revision جاری Draft، نسخهٔ تماس گیرنده‌ها، payload و حریم بازبینی‌شده را با چرخهٔ `pending / withdrawn / reopened / approved` نگه می‌دارد؛ تغییر هر وابستگی اعتبار مؤثر نسخهٔ قبلی را بدون بازنویسی تاریخچه از بین می‌برد. این شبیه‌سازی همیشه `simulationOnly=true`، `externalEffect=none`، `sendAuthorized=false` و `externalActionAttempted=false` می‌ماند و هیچ دکمه، وضعیت یا رسید ارسال نمی‌سازد.
- مدل پایه باید قابل‌تعویض بماند. ادعاهای حساس به منبع، تاریخ و بیان عدم‌قطعیت نیاز دارند.
- ترتیب فعلی توسعه قطعی است: نخست تجربه و قراردادهای قابل‌آزمون سمت سازنده در پروتوتایپ موبایل کامل و به‌صورت صریح توسط کاربر تأیید می‌شود؛ سپس مدل محلی فقط برای ارزیابی به چیدای سازنده وصل می‌شود؛ پس از آن مسیر و فضای تأمین‌کننده ساخته می‌شود. هر مرحله همچنان به تسک‌های کوچک تقسیم می‌شود و عبور به تسک یا فاز بعد به تأیید کاربر وابسته است.
- معیار کاری پیشنهادی هنداف برای «تکمیل سمت سازنده» کامل‌شدن پروتوتایپ کلیک‌پذیر، مدل داده، حالت‌های شکست و قراردادهای قابل‌آزمون آن است؛ نه backend، شبکهٔ واقعی، ارسال واقعی، وب واقعی یا محصول production. این تعریف تا تأیید مستقیم کاربر تصمیم کاری می‌ماند. مسیر ماندگارِ وضعیت‌های وابسته به طرف دوم تا فاز تأمین‌کننده فقط از ثبت دستی کاربر استفاده می‌کند؛ `Mock` باید تجربه‌ای جدا، فقط‌خواندنی، آشکار و بدون نوشتن در پروژه/چت/storage باشد.
- پس از تأیید پایان فاز سازنده، اتصال آزمایشی مدل باید از درگاه provider-neutral و adapter محلیِ opt-in روی loopback انجام شود. شروع آن فقط متن صریح همان پیام، بدون حافظه، فایل، وب، ابزار، mutation پروژه یا اقدام بیرونی است؛ پاسخ «مدل محلی آزمایشی» و وضعیت منبع را صریح نشان می‌دهد، fallback امن دارد و در buildهای میزبانی‌شده خاموش می‌ماند. این adapter استثنای توسعه و ارزیابی است، نه جایگزین محیط ابری تعریف‌شده در سند مادر.

## ساختار مخزن

- `CHIDA-Product-Definition-FA.md`: سند مادر محصول.
- `BUILDER-FEATURE-BACKLOG-FA.md`: فهرست اجرایی قابلیت‌های باقی‌ماندهٔ سازنده با مرز نسخه‌ها.
- `CHIDA-PRODUCT-LEARNINGS-FA.md`: دفتر غیرمرجعِ یادگیری‌ها، شکاف‌ها و تصمیم‌ها برای تکمیل بعدی سند مادر.
- `CHIDA-CONTINUATION-HANDOFF-FA.md`: هنداف زندهٔ ادامهٔ پروتوتایپ، ترتیب فازها، وضعیت نسخه‌ها و نقطهٔ شروع گفت‌وگوی بعدی.
- `design-system/`: خروجی و دارایی‌های سیستم طراحی.
- `prototype/`: پروتوتایپ React/Vite، تست‌ها و بستهٔ میزبانی.
- دستورهای اختصاصی runtime در `prototype/AGENTS.md` نیز لازم‌الاجرا هستند.

## کنترل کیفیت و انتشار

کنترل کیفیت سه سطح دارد و نباید بعد از هر بازخورد کوچک گیت کامل تکرار شود:

- **تغییر کوچک UX** مثل متن، ترتیب، فاصله و ورودی ناوبری، تا وقتی schema/storage/runtime را عوض نکرده است: فقط regressionهای مرتبط با `npm run test:app -- --grep "..."`، سپس `npm run check:runtime`، QA واقعی `390 × 844` برای مسیر تغییرکرده، console/overflow و `git diff --check`. اگر TSX تغییر کرده، `npm run build` یک بار پس از جمع‌شدن همان بستهٔ بازخورد اجرا شود، نه بعد از هر ریزاصلاح.
- **برش عادی قابلیت**: regressionهای متمرکز همان دامنه، `npm run build` و QA موبایل. `npm run test:sites` فقط وقتی worker، hosting contract یا بستهٔ `dist` در دامنهٔ تغییر است.
- **گیت کامل**: `npm run gate:release` دقیقاً یک بار روی candidate منجمدِ انتشار صریح یا در پایان یک برش واقعاً پرریسک معماری شامل schema/migration/concurrency/recovery/security/protected runtime/release tooling. این فرمان ابزار توسعهٔ روزمره نیست.

- `gate:release` build/integrity/TypeScript، تمام آزمون‌های Playwright دقیقاً یک بار، آزمون‌های Sites و `git diff --check` را اجرا می‌کند و fingerprint کل candidate و بستهٔ `dist` را در `.git` ثبت می‌کند. fingerprint مسیر، mode/type، bytes یا مقصد symlink را می‌سنجد و source و artifact را پیش و پس از گیت ثابت می‌خواهد. `test:app` و `test:runtime` برای توسعه و اجرای متمرکز باقی می‌مانند؛ `test:runtime` فقط هشت تست runtime را اجرا می‌کند و `test:all` مجموعهٔ کامل را.
- هنگام پیام صریح انتشار، اسناد وضعیت باید پیش از candidate نهایی شوند. پس از commit همان bytes، `npm run gate:publish` باید cleanliness و fingerprint را تأیید کند؛ هر تغییر در candidate، گیت قبلی را باطل می‌کند. همان SHA فقط یک بار push و مبنای Cloudflare Pages و ChatGPT Sites می‌شود.
- شناسه‌های پویای deployment/version و رسید terminal فقط در پیام تحویل ثبت می‌شوند؛ برای ثبت receipt، commit یا push دوم و در نتیجه deployment دوباره ساخته نمی‌شود.

- تغییر رابط باید در حالت‌های فعال دامنه، بدون اسکرول افقی و با مسیرهای کلیک‌پذیر بررسی شود؛ دامنهٔ فعلی فقط موبایل در viewport واقعی `390 × 844` است و دسکتاپ پس از شروع طراحی مستقل خودش بررسی می‌شود.
- انتشار فقط از commit تست‌شده انجام شود و نسخهٔ GitHub، Cloudflare Pages و ChatGPT Sites باید به همان منبع متصل باشند.
- کلید، توکن، OTP واقعی یا دادهٔ حساس در مخزن، لاگ و پروتوتایپ ذخیره نشود.
