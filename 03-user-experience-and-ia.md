# تجربهٔ کاربر و معماری فضاهای چیدا

**پیش‌نویس ۰.۱ · ۱۴۰۵/۰۵/۲۲**

این سند جای کاربر، مسیرها و محل بازگشت به نتیجهٔ هر گفت‌وگو را روشن می‌کند. مخاطب آن تیم محصول و طراحی است؛ نه مجری جزئیات فنی یا اندازه‌های رابط.

دامنهٔ قابلیت‌های نسخهٔ یک در سند ۰۲، «محصول نسخهٔ یک»، آمده است. قواعد دسترسی و تعامل دو سمت بازار متعلق به سند ۰۴، «بازار دوطرفه و قواعد تعامل» است و نحوهٔ حرف‌زدن، پیشنهاد‌دادن، تأییدگرفتن و اجرای کار را سند ۰۵، «قرارداد کاری، رفتار و اعتماد»، تعیین می‌کند.

## ۱. شاکلهٔ تجربه

چیدا باید به‌اندازهٔ یک پیام‌رسان ساده و به‌اندازهٔ یک ابزار کاری مرتب باشد. کاربر خواسته‌اش را با متن یا ویس‌نوت می‌گوید و در صورت نیاز عکس یا فایل می‌فرستد؛ اما نتیجه در رشته‌ای از پیام‌ها گم نمی‌شود و در پروژه، مالی، اسناد، استعلام یا فروشگاه در جای خودش می‌نشیند.

> **چت، راه انجام کار است؛ ورک‌اسپیس (فضای کاری)، جای نگهداری و پیگیری نتیجه.**

چتِ تنها اطلاعات را گم می‌کند و فرمِ تنها اصطکاک می‌سازد. چیدا میان این دو پل می‌زند: گفت‌وگو برای پیش‌برد کار؛ ساختار برای نگهداری و بازگشت.

هرگاه گفت‌وگو به داده یا اقدامی قابل ثبت برسد، چیدا یک **کارت پیشنهادی ساختاریافته (آرتیفکت)** می‌سازد. کاربر آن را اصلاح، رد یا تأیید می‌کند و نتیجه در بخش درست می‌نشیند. آرتیفکت مرز روشن میان «صحبت» و «ثبت یا ارسال» است؛ جزئیات چرخه در سند ۰۵ آمده است.

چند اصل در سراسر تجربه ثابت می‌ماند:

- کاربر همیشه بداند در فضای عمومی است یا در کدام پروژه و کدام بخش قرار دارد.
- در نسخهٔ یک فقط خود چیدا دیده شود؛ نه انتخاب مدل، متخصص، تیم یا سطح تلاش.
- آنبورد فعال و راهنما باشد، اما پس از آن چیدا آرام‌تر و درخواستی شود.
- پیچیدگی به‌تدریج باز شود؛ صفحهٔ اول وضعیت و راه ادامه را نشان دهد، نه همهٔ امکانات را.
- هر نتیجه هم از بخش مربوط پیدا شود و هم ارتباطش با گفت‌وگوی سازندهٔ آن حفظ شود.

## ۲. ورود تا اولین نتیجه

ترتیب شروع برای هر دو نقش ثابت است:

**انتخاب نقش ← ورود ← خوشامد و معرفی ← آنبورد چت‌محور ← ورود به فضای کاری نقش**

### انتخاب نقش و ورود

کاربر در آغاز نقش «سازنده» یا «تأمین‌کننده» را انتخاب می‌کند. این انتخاب معماری فضای او را تعیین می‌کند؛ شرح حدود هر نقش در سند ۰۲ است.

این مسیر هنوز تعیین نمی‌کند که یک فرد می‌تواند هر دو نقش را در یک حساب داشته باشد یا چگونه میان آن‌ها جابه‌جا شود. تا بسته‌شدن `ب-۰۴۱`، پروتوتایپ یا پیاده‌سازی نباید حافظه، اینباکس، بریف، اشتراک یا دادهٔ خصوصی دو سمت را با هم ادغام کند.

ورود با شمارهٔ همراه و کد ورود انجام می‌شود و سازنده در عرضهٔ کنترل‌شده کد دعوت نیز دارد. در نمونهٔ نمایشی مقدارها می‌توانند از پیش پر باشند، اما تایپ و اصلاح باید واقعی باشد؛ خطا کنار همان ورودی دیده شود و مقدارهای درست پاک نشوند.

### خوشامد و معرفی

خوشامد فقط روشن می‌کند چیدا چه کمکی می‌کند، اطلاعات کجا مرتب می‌ماند و کار مهم بدون تأیید انجام نمی‌شود. اینجا محل تیم خیالی، تور منوها یا فهرست قابلیت‌ها نیست.

### آنبورد سازنده

در آنبورد فقط گفت‌وگو، ورودی پیام و آرتیفکت در حال ساخت دیده می‌شود. سایدبار، داشبورد و منوهای پروژه تا پایان این مرحله پنهان‌اند. چیدا نام اولین پروژه، موقعیت، مرحلهٔ فعلی و چند مشخصهٔ ضروری را با سؤال‌های کوتاه می‌گیرد. پاسخ می‌تواند متن یا ویس‌نوت باشد و کاربر می‌تواند عکس یا سند اولیه نیز اضافه کند.

وقتی اطلاعات کافی شد، چیدا آرتیفکت «پیشنهاد ساخت پروژه» را نشان می‌دهد. مقصد صریح پس از تأیید، **فهرست پروژه‌ها** است؛ نه خانهٔ پروژه یا صفحهٔ چت. پروژهٔ تازه بالای فهرست دیده می‌شود تا مدل ذهنی «فضای کاری شامل چند پروژه است» شکل بگیرد. با انتخاب آن، خانهٔ پروژه باز می‌شود.

### آنبورد تأمین‌کننده

چیدا با متن یا ویس‌نوت، نام فروشگاه، حوزهٔ فعالیت و نام چند محصول یا خدمت را می‌گیرد و آرتیفکت «پیشنهاد ساخت فروشگاه» را با دو بخش جداگانهٔ «محصولات» و «خدمات» می‌سازد. عکس و فایل ورودی اختیاری‌اند و پایان آنبورد به استخراج کامل کاتالوگ از آن‌ها وابسته نیست. سطح استخراج خودکار از این ورودی‌ها طبق «دفتر تصمیم‌ها» هنوز باز است.

پس از تأیید، تأمین‌کننده وارد خانهٔ فروشگاه می‌شود. تکمیل کاتالوگ شرط پایان آنبورد نیست؛ یک فروشگاه قابل تشخیص و قدم بعدی روشن کافی است.

## ۳. اجزای ثابت فضای کاری

هر دو نقش چهار جزء ثابت دارند:

1. **سربرگ زمینه:** نام فضای جاری را نشان می‌دهد؛ برای سازنده نام پروژه و بخش، و برای تأمین‌کننده نام فروشگاه یا بخش فروش.
2. **سایدبار:** مسیر اصلی جابه‌جایی میان خانه، فضاها، گفت‌وگوهای اخیر و تنظیمات است. ناوبری اصلی پایین صفحه نداریم.
3. **صفحهٔ محتوا:** فهرست، خلاصه، رکوردها یا آرتیفکت‌های ذخیره‌شده را متناسب با جای فعلی نمایش می‌دهد.
4. **ورودی چیدا:** در پایین صفحه می‌ماند، اما جای ناوبری را نمی‌گیرد. متن راهنمای آن زمینه را بازتاب می‌دهد؛ برای مثال «دربارهٔ مالی پروژه ونک از چیدا بپرسید».

داخل گفت‌وگو، منوهای بخش‌ها نباید صفحه را اشغال کنند؛ سربرگ زمینه و سایدبار برای خروج یا جابه‌جایی کافی‌اند. بیرون چت، صفحهٔ بخش دوباره وضعیت و محتوا را نشان می‌دهد.

## ۴. معماری فضای سازنده

فضای سازنده دو سطح دارد: سطح عمومی و سطح پروژه.

### سطح عمومی

- **امروز:** موارد نیازمند توجه از همهٔ پروژه‌ها، به‌ترتیب فوریت.
- **فضای عمومی:** گفت‌وگو و فایل‌هایی که هنوز به پروژه‌ای تعلق ندارند.
- **پروژه‌ها:** فهرست همهٔ پروژه‌ها و راه ساخت پروژهٔ جدید.
- **بازار:** جست‌وجوی فروشگاه‌ها، محصولات و خدمات.
- **اینباکس:** پیام‌ها و رویدادهای بازار، از جمله پاسخ‌های استعلام و پیام عمومی.

«امروز» جای انجام تمام کارها نیست. نقش آن این است که کاربر بفهمد کجا باید برود. هر مورد با لمس، بخش و رکورد اصلی خود را باز می‌کند.

### سطح پروژه

هر پروژه مرز اطلاعاتی مستقل دارد و شامل این بخش‌هاست:

- خانهٔ پروژه
- کارها و یادآورها
- مالی
- اسناد
- استعلام‌ها
- گفت‌وگوها
- اطلاعات و حافظهٔ پروژه

خانهٔ پروژه یک نمای جهت‌یاب است، نه داشبورد شلوغ مدیریتی. نام و مرحلهٔ پروژه، خلاصهٔ کوتاه وضعیت، موارد نیازمند توجه و راه ورود به بخش‌ها را نشان می‌دهد. کاربر از همین‌جا باید بفهمد امروز کدام قسمت کار دارد.

## ۵. فهرست و تعویض پروژه

هر کارت در فهرست پروژه‌ها نام، موقعیت یا شناسه، مرحله، آخرین فعالیت و موارد نیازمند توجه را نشان می‌دهد. پروژهٔ بدون هشدار نیز باید سالم دیده شود، نه خالی یا خراب.

پس از بازکردن پروژه، نام آن در بالای صفحه به‌صورت یک انتخاب‌گر کشویی دیده می‌شود. کاربر می‌تواند بدون بازگشت به فهرست، پروژه را عوض کند. تعویض پروژه تا حد امکان همان بخش را حفظ می‌کند: اگر در مالی پروژهٔ ونک است و پروژهٔ لواسان را انتخاب کند، مالی لواسان باز می‌شود. اگر آن بخش یا رکورد معادل وجود نداشت، خانهٔ پروژه با توضیح کوتاه نمایش داده می‌شود.

در سایدبار، پروژهٔ جاری باز و بخش‌هایش نمایان است؛ پروژه‌های دیگر جمع‌شده‌اند. «همهٔ پروژه‌ها» همیشه در دسترس می‌ماند. این ترکیب هم جای فعلی را روشن می‌کند و هم از تبدیل سایدبار به فهرستی طولانی جلوگیری می‌کند.

## ۶. الگوی بخش‌های پروژه

هر بخش سه لایه دارد: خلاصهٔ وضعیت، فهرست یا رکوردها، و گفت‌وگوی زمینه‌دار. لازم نیست برای هر بخش یک داشبورد مستقل طراحی شود. یک الگوی ثابت، یادگیری را کم می‌کند:

| بخش | آنچه در صفحه دیده می‌شود | کار معمول با چیدا |
|---|---|---|
| کارها | کارهای باز، سررسیدها، انجام‌شده‌ها | ساخت کار، یادآوری، مرتب‌کردن اولویت‌ها |
| مالی | فاکتورها، چک‌ها، پرداخت‌ها، جمع‌بندی ساده | خواندن فاکتور، ثبت پیش‌نویس، گزارش وضعیت |
| اسناد | فایل‌ها بر اساس نوع و تاریخ، همراه با وضعیت بررسی | بارگذاری، خلاصه‌سازی، پیدا‌کردن بند یا اطلاعات |
| استعلام‌ها | پیش‌نویس، ارسال‌شده، پاسخ‌دار و بسته‌شده | ساخت استعلام، مقایسهٔ پاسخ‌ها، ادامهٔ گفت‌وگو |
| گفت‌وگوها | گفت‌وگوهای مرتبط با همین پروژه | بازگشت به تصمیم‌ها و ادامهٔ کار قبلی |
| اطلاعات پروژه | مشخصات تأییدشده، ترجیحات و حافظهٔ همین پروژه | اصلاح مشخصات و ثبت زمینهٔ ماندگار |

در «مالی»، کاربر خلاصه و موارد نزدیک را می‌بیند و با فیلترهای سبک میان فاکتورها، چک‌ها و پرداخت‌ها می‌رود. جزئیات فاکتور شامل اصل فایل، داده‌های استخراج‌شده، وضعیت ثبت و گفت‌وگوی مرتبط است. گزارش نیز از همین داده‌ها ساخته می‌شود.

در «اسناد»، فایل اصل است و صفحهٔ جزئیات، اطلاعات پایه، خلاصه و گفت‌وگوی مرتبط را کنار هم نگه می‌دارد. در «کارها»، توضیح، سررسید و گفت‌وگو به رکورد کار متصل‌اند. مرز قابلیت‌ها در سند ۰۲ آمده است.

## ۷. گفت‌وگوی زمینه‌دار و آرتیفکت

هر گفت‌وگو یک زمینهٔ آشکار دارد: عمومی، پروژه یا یک رکورد مشخص. این زمینه باید بالای گفت‌وگو و در متن راهنمای ورودی معلوم باشد. اگر کاربر از داخل مالی چت را شروع کند، چیدا می‌فهمد موضوع مالی همان پروژه است؛ بااین‌حال داده‌ای را بدون نشانه از پروژهٔ دیگری وارد نمی‌کند.

گفت‌وگو ممکن است به یک آرتیفکت مانند «ثبت فاکتور»، «ساخت کار»، «پیش‌نویس استعلام» یا «محصول فروشگاه» برسد. آرتیفکت باید سه ویژگی داشته باشد: مقصد ثبت روشن، موارد نیازمند تکمیل مشخص و اقدام‌های ویرایش، رد و تأیید قابل فهم.

بعد از تأیید، آرتیفکت از بین نمی‌رود. نتیجه در صفحهٔ اصلی بخش ظاهر می‌شود و از آنجا پیوندی به گفت‌وگوی سازندهٔ آن دارد. خود گفت‌وگو نیز در فهرست گفت‌وگوهای پروژه باقی می‌ماند. به این ترتیب کاربر هم از مسیر «می‌دانم چه رکوردی می‌خواهم» و هم از مسیر «یادم هست درباره‌اش حرف زدیم» به نتیجه می‌رسد.

## ۸. خلاصه‌ها، توجه و اینباکس

چیدا به‌جای ساختن خلاصه‌ای جدا برای هر منو، سه سطح دارد:

1. **خلاصهٔ روزانه (بریف):** مهم‌ترین موارد همهٔ فضاها که در «امروز» دیده می‌شود.
2. **خلاصهٔ پروژه:** وضعیت و موارد مهم همان پروژه در خانهٔ پروژه.
3. **خلاصهٔ بخش:** جمع‌بندی کوتاه مالی، اسناد، استعلام یا هر بخش در ابتدای همان صفحه.

در نام‌گذاری محصول فقط مورد اول «بریف» است. خلاصهٔ پروژه و خلاصهٔ بخش جزئی از همان صفحه‌اند؛ صندوق، اعلان یا بریف جداگانه نمی‌سازند.

یک مورد نباید در چند جا به شکل اعلان تکراری ظاهر شود. خلاصه فقط راه رسیدن به منبع است؛ با انتخاب هر مورد، رکورد یا گفت‌وگوی اصلی باز می‌شود. سررسید چک، کار عقب‌افتاده و پاسخ تازهٔ استعلام می‌توانند نیازمند توجه باشند، اما جزئیات دقیق دامنه در سند ۰۲ تعیین می‌شود.

اینباکس محل رویدادهای ارتباطی است، نه انبار همهٔ هشدارهای محصول. پاسخ استعلام، ادامهٔ گفت‌وگوی بازار و پیام عمومی ابتدا بدون فیلتر پنهان‌کننده وارد اینباکس می‌شوند. چیدا آن‌ها را در خلاصهٔ روزانه جمع‌بندی می‌کند و برای موارد واقعاً نیازمند اقدام، انتقال به «امروز» یا فهرست کارها را پیشنهاد می‌دهد؛ ساخت کار با تأیید کاربر انجام می‌شود. قواعد مقصد، حریم دو سمت بازار و پیام عمومی در سند ۰۴ است.

## ۹. معماری فضای تأمین‌کننده

فضای تأمین‌کننده حول فروشگاه و فرصت فروش ساخته می‌شود، نه پروژه‌های سازنده. سطح عمومی آن شامل این مسیرهاست:

- **امروز:** فرصت‌های تازه، پاسخ‌های نیازمند پیگیری و کارهای ناقص فروشگاه.
- **فروشگاه:** نمای عمومی و وضعیت تکمیل فروشگاه.
- **محصولات:** فهرست، ساخت و ویرایش محصولات.
- **خدمات:** فهرست، ساخت و ویرایش خدمات؛ این بخش باید از محصولات جدا و هم‌ارزش باشد.
- **فرصت‌های فروش:** استعلام‌هایی که برای این تأمین‌کننده قابل پاسخ‌اند.
- **اینباکس:** گفت‌وگوها، وضعیت پاسخ‌ها و پیام‌های مرتبط.
- **پیام عمومی:** ساخت و پیگیری پیام برای سازندگان در محدودهٔ مجاز.

خانهٔ فروشگاه در یک نگاه نشان می‌دهد فروشگاه برای سازنده چگونه دیده می‌شود، کدام محصول یا خدمت ناقص است و چه فرصت تازه‌ای وجود دارد. شمارنده‌ها باید به کار قابل انجام وصل باشند، نه صرفاً عدد تزئینی.

محصول و خدمت هرکدام صفحهٔ جزئیات و گفت‌وگوی مرتبط دارند. چیدا می‌تواند از توضیح کاربر و، در صورت ارسال، عکس یا فایل، پیش‌نویس اطلاعات و هشتگ‌ها را بسازد؛ اما کاربر قبل از انتشار آن را می‌بیند. تأمین‌کننده از صفحهٔ فرصت فروش وارد جزئیات استعلام می‌شود و پاسخ را با چیدا آماده می‌کند. پیشنهاد فعلی این است که ادامهٔ ارتباط پس از پاسخ در همان رشتهٔ استعلام دیده شود؛ قطعی‌شدن این مسیر به تصمیم باز مربوط در «دفتر تصمیم‌ها» و قواعد سند ۰۴ وابسته است.

## ۱۰. سایدبار، پروفایل و تنظیمات

سایدبار کامل است، اما همه‌چیز را هم‌سطح نشان نمی‌دهد: بالای آن جست‌وجو و گفت‌وگوی تازه، میانه مسیرهای نقش و گفت‌وگوهای اخیر، و پایین آن حساب کاربر قرار دارد.

صفحهٔ حساب این موارد را در گروه‌های روشن نگه می‌دارد:

- پروفایل و اطلاعات تماس
- دستورالعمل‌های عمومی و حافظهٔ عمومی
- پلن، اشتراک، مصرف و محدودیت‌ها
- اعلان‌ها و خلاصهٔ روزانه
- ظاهر، انتخاب تم و دسترس‌پذیری
- حریم خصوصی، خروج از حساب و پشتیبانی آموزشی

تنظیمات عمومی بر همهٔ فضاها اثر می‌گذارند؛ اطلاعات پروژه فقط داخل همان پروژه می‌ماند. تفاوت حافظهٔ عمومی و حافظهٔ پروژه باید در نام، توضیح و مسیر دسترسی روشن باشد. قواعد ثبت و استفاده از حافظه در سند ۰۵ است.

## ۱۱. ورودی و کنترل کاربر

ورودی چیدا چهار نوع محتوا می‌پذیرد: متن، ویس‌نوت، تصویر و فایل. دکمهٔ «+» تصویر و فایل را در یک منوی کوتاه ارائه می‌کند؛ آیکون مستقل عکس کنار آن لازم نیست. در نسخهٔ یک تماس صوتی زنده، انتخاب مدل، انتخاب عامل یا کنترل سطح تلاش وجود ندارد.

کاربر پیش از ارسال باید زمینهٔ پیام را ببیند و در صورت نیاز عوض کند. اگر پیام خارج از پروژه آغاز شده ولی به پروژه‌ای مربوط است، چیدا مقصد را پیشنهاد می‌دهد؛ جابه‌جایی فقط با انتخاب کاربر انجام می‌شود. برای رفتار چیدا در برابر ابهام، منبع و تأیید، سند ۰۵ مرجع است.

## ۱۲. حالت‌های ضروری رابط

هر صفحه فقط حالت پُر و ایده‌آل ندارد. این حالت‌ها باید در طراحی دیده شوند:

- **خالی:** توضیح کوتاه دربارهٔ فایدهٔ بخش و یک اقدام روشن؛ نه صفحهٔ سفید.
- **در حال پردازش:** فایل یا ویس‌نوت در حال خواندن، همراه با امکان ترک صفحه و بازگشت.
- **نیازمند بازبینی:** آرتیفکت با موارد ناقص و دکمه‌های ویرایش، رد و تأیید.
- **موفق:** نتیجهٔ ثبت یا ارسال، مقصد آن و راه بازکردنش معلوم باشد.
- **خطا:** علت به زبان ساده، حفظ ورودی کاربر و امکان تلاش دوباره.
- **مغایرت یا تکرار:** چیدا دو گزینه را نشان دهد و انتخاب را به کاربر بسپارد.

بازگشت باید قابل پیش‌بینی باشد: بستن جزئیات به فهرست همان بخش و بستن چت به صفحهٔ زمینه برمی‌گردد؛ تعویض پروژه نیز پیش‌نویس تأییدنشده را بی‌هشدار دور نمی‌ریزد.

## ۱۳. موبایل، راست‌به‌چپ و تم

چیدا ابتدا برای استفادهٔ واقعی روی گوشی طراحی می‌شود: تمام‌صفحه، با کیبورد طبیعی دستگاه، اهداف لمسی مناسب، اسکرول روشن و راست‌به‌چپ واقعی. قاب نمایشی گوشی و کیبورد شبیه‌سازی‌شده جزو رابط محصول نیستند.

تم روشن و تیره یک ساختار و اولویت دارند و فقط ارائه‌شان عوض می‌شود. نارنجی رنگ تأکید است، نه همهٔ کارت‌ها؛ وضعیت نیز علاوه بر رنگ، متن یا نشانه دارد. آواتار چیدا نباید تزئین تکراری هر کارت شود.

## ۱۴. سفر اصلی سازنده

1. نقش سازنده را انتخاب می‌کند، شماره، کد ورود و کد دعوت را وارد می‌کند.
2. معرفی کوتاه را می‌بیند و در آنبورد با ویس‌نوت پروژهٔ اول را شرح می‌دهد.
3. آرتیفکت پروژه را اصلاح و تأیید می‌کند و وارد **فهرست پروژه‌ها** می‌شود.
4. پروژهٔ تازه را باز می‌کند؛ خانهٔ پروژه وضعیت و بخش‌ها را نشان می‌دهد.
5. به مالی می‌رود، عکس فاکتور را با «+» می‌فرستد و پیش‌نویس ثبت را تأیید می‌کند.
6. فاکتور در مالی ذخیره می‌شود و گفت‌وگوی مرتبط روی آن می‌ماند.
7. روز بعد، سررسید مهم را در خلاصهٔ روزانه می‌بیند و مستقیم به همان فاکتور می‌رود.
8. از استعلام‌ها یک درخواست می‌سازد و پاسخ‌ها را از اینباکس و رشتهٔ همان استعلام دنبال می‌کند.

## ۱۵. سفر اصلی تأمین‌کننده

1. نقش تأمین‌کننده را انتخاب و وارد می‌شود.
2. نام فروشگاه و چند محصول یا خدمت را با متن یا ویس‌نوت معرفی می‌کند؛ در صورت تمایل عکس هم می‌فرستد.
3. آرتیفکت فروشگاه را با تفکیک محصول و خدمت بازبینی و تأیید می‌کند.
4. وارد خانهٔ فروشگاه می‌شود و یک محصول را با عکس، قیمت، توضیح و هشتگ کامل می‌کند.
5. در «امروز» فرصت مرتبطی می‌بیند، جزئیات آن را باز می‌کند و پاسخ پیشنهادی را با چیدا می‌سازد.
6. پاسخ را ویرایش و تأیید می‌کند؛ در پیشنهاد فعلی، ادامه در رشتهٔ همان استعلام دیده می‌شود.
7. در صورت نیاز از «پیام عمومی» پیش‌نویس می‌سازد، مقصد را می‌بیند و پس از تأیید ارسال می‌کند.

## ۱۶. معیار آزمون تجربه

پروتوتایپ زمانی موفق است که کاربر، بدون آموزش بیرونی، بتواند پاسخ این پرسش‌ها را بدهد:

- اکنون در فضای عمومی است یا در کدام پروژه و بخش؟
- پیام بعدی در چه زمینه‌ای فرستاده می‌شود؟
- نتیجهٔ تأییدشده بعداً در کجا پیدا می‌شود؟
- فرق چت، آرتیفکت و رکورد ذخیره‌شده چیست؟
- برای دیدن کارهای مهم امروز، جزئیات پروژه و سابقهٔ گفت‌وگو از کجا باید برود؟
- تأمین‌کننده فرق محصول، خدمت، فرصت فروش، اینباکس و پیام عمومی را می‌فهمد؟

معیار موفقیت، دیده‌شدن همهٔ دکمه‌ها نیست. تجربه زمانی درست است که کاربر جای خودش را گم نکند، با کمترین رفت‌وآمد کار را پیش ببرد و مطمئن باشد نتیجه در جای قابل بازگشت نشسته است.
